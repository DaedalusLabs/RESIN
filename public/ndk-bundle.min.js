"use strict";
(() => {
   var Ul = Object.create;
   var Wn = Object.defineProperty;
   var Pl = Object.getOwnPropertyDescriptor;
   var Ol = Object.getOwnPropertyNames;
   var Ml = Object.getPrototypeOf,
      Nl = Object.prototype.hasOwnProperty;
   var Hl = (t, e, n) =>
      e in t
         ? Wn(t, e, {
              enumerable: !0,
              configurable: !0,
              writable: !0,
              value: n,
           })
         : (t[e] = n);
   var pe = (t, e) => () => (
         e || t((e = { exports: {} }).exports, e), e.exports
      ),
      go = (t, e) => {
         for (var n in e) Wn(t, n, { get: e[n], enumerable: !0 });
      },
      ql = (t, e, n, r) => {
         if ((e && typeof e == "object") || typeof e == "function")
            for (let s of Ol(e))
               !Nl.call(t, s) &&
                  s !== n &&
                  Wn(t, s, {
                     get: () => e[s],
                     enumerable: !(r = Pl(e, s)) || r.enumerable,
                  });
         return t;
      };
   var W = (t, e, n) => (
      (n = t != null ? Ul(Ml(t)) : {}),
      ql(
         e || !t || !t.__esModule
            ? Wn(n, "default", { value: t, enumerable: !0 })
            : n,
         t,
      )
   );
   var m = (t, e, n) => Hl(t, typeof e != "symbol" ? e + "" : e, n);
   var bo = pe((yo) => {
      "use strict";
      Object.defineProperty(yo, "__esModule", { value: !0 });
   });
   var ls = pe((Gn) => {
      "use strict";
      Object.defineProperty(Gn, "__esModule", { value: !0 });
      Gn._fast_remove_single = void 0;
      function $l(t, e) {
         e !== -1 &&
            (e === 0
               ? t.shift()
               : e === t.length - 1
                 ? (t.length = t.length - 1)
                 : t.splice(e, 1));
      }
      Gn._fast_remove_single = $l;
   });
   var mo = pe((exports, module) => {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: !0 });
      exports.bakeCollectionVariadic =
         exports.bakeCollectionAwait =
         exports.bakeCollection =
         exports.BAKED_EMPTY_FUNC =
            void 0;
      exports.BAKED_EMPTY_FUNC = function () {};
      var FORLOOP_FALLBACK = 1500;
      function generateArgsDefCode(t) {
         var e = "";
         if (t === 0) return e;
         for (var n = 0; n < t - 1; ++n) e += "arg" + String(n) + ", ";
         return (e += "arg" + String(t - 1)), e;
      }
      function generateBodyPartsCode(t, e) {
         for (var n = "", r = "", s = 0; s < e; ++s)
            (n += "var f".concat(s, " = collection[").concat(
               s,
               `];
`,
            )),
               (r += "f".concat(s, "(").concat(
                  t,
                  `)
`,
               ));
         return { funcDefCode: n, funcCallCode: r };
      }
      function generateBodyPartsVariadicCode(t) {
         for (var e = "", n = "", r = 0; r < t; ++r)
            (e += "var f".concat(r, " = collection[").concat(
               r,
               `];
`,
            )),
               (n += "f".concat(
                  r,
                  `.apply(undefined, arguments)
`,
               ));
         return { funcDefCode: e, funcCallCode: n };
      }
      function bakeCollection(collection, fixedArgsNum) {
         if (collection.length === 0) return exports.BAKED_EMPTY_FUNC;
         if (collection.length === 1) return collection[0];
         var funcFactoryCode;
         if (collection.length < FORLOOP_FALLBACK) {
            var argsDefCode = generateArgsDefCode(fixedArgsNum),
               _a = generateBodyPartsCode(argsDefCode, collection.length),
               funcDefCode = _a.funcDefCode,
               funcCallCode = _a.funcCallCode;
            funcFactoryCode = `(function(collection) {
            `
               .concat(
                  funcDefCode,
                  `
            collection = undefined;
            return (function(`,
               )
               .concat(
                  argsDefCode,
                  `) {
                `,
               )
               .concat(
                  funcCallCode,
                  `
            });
        })`,
               );
         } else {
            var argsDefCode = generateArgsDefCode(fixedArgsNum);
            collection.length % 10 === 0
               ? (funcFactoryCode = `(function(collection) {
                return (function(`
                    .concat(
                       argsDefCode,
                       `) {
                    for (var i = 0; i < collection.length; i += 10) {
                        collection[i](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+1](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+2](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+3](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+4](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+5](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+6](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+7](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+8](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                        collection[i+9](`,
                    )
                    .concat(
                       argsDefCode,
                       `);
                    }
                });
            })`,
                    ))
               : collection.length % 4 === 0
                 ? (funcFactoryCode = `(function(collection) {
                return (function(`
                      .concat(
                         argsDefCode,
                         `) {
                    for (var i = 0; i < collection.length; i += 4) {
                        collection[i](`,
                      )
                      .concat(
                         argsDefCode,
                         `);
                        collection[i+1](`,
                      )
                      .concat(
                         argsDefCode,
                         `);
                        collection[i+2](`,
                      )
                      .concat(
                         argsDefCode,
                         `);
                        collection[i+3](`,
                      )
                      .concat(
                         argsDefCode,
                         `);
                    }
                });
            })`,
                      ))
                 : collection.length % 3 === 0
                   ? (funcFactoryCode = `(function(collection) {
                return (function(`
                        .concat(
                           argsDefCode,
                           `) {
                    for (var i = 0; i < collection.length; i += 3) {
                        collection[i](`,
                        )
                        .concat(
                           argsDefCode,
                           `);
                        collection[i+1](`,
                        )
                        .concat(
                           argsDefCode,
                           `);
                        collection[i+2](`,
                        )
                        .concat(
                           argsDefCode,
                           `);
                    }
                });
            })`,
                        ))
                   : (funcFactoryCode = `(function(collection) {
                return (function(`
                        .concat(
                           argsDefCode,
                           `) {
                    for (var i = 0; i < collection.length; ++i) {
                        collection[i](`,
                        )
                        .concat(
                           argsDefCode,
                           `);
                    }
                });
            })`,
                        ));
         }
         {
            var bakeCollection_1 = void 0,
               fixedArgsNum_1 = void 0,
               bakeCollectionVariadic_1 = void 0,
               bakeCollectionAwait_1 = void 0,
               funcFactory = eval(funcFactoryCode);
            return funcFactory(collection);
         }
      }
      exports.bakeCollection = bakeCollection;
      function bakeCollectionAwait(collection, fixedArgsNum) {
         if (collection.length === 0) return exports.BAKED_EMPTY_FUNC;
         if (collection.length === 1) return collection[0];
         var funcFactoryCode;
         if (collection.length < FORLOOP_FALLBACK) {
            var argsDefCode = generateArgsDefCode(fixedArgsNum),
               _a = generateBodyPartsCode(argsDefCode, collection.length),
               funcDefCode = _a.funcDefCode,
               funcCallCode = _a.funcCallCode;
            funcFactoryCode = `(function(collection) {
            `
               .concat(
                  funcDefCode,
                  `
            collection = undefined;
            return (function(`,
               )
               .concat(
                  argsDefCode,
                  `) {
                return Promise.all([ `,
               )
               .concat(
                  funcCallCode,
                  ` ]);
            });
        })`,
               );
         } else {
            var argsDefCode = generateArgsDefCode(fixedArgsNum);
            funcFactoryCode = `(function(collection) {
            return (function(`
               .concat(
                  argsDefCode,
                  `) {
                var promises = Array(collection.length);
                for (var i = 0; i < collection.length; ++i) {
                    promises[i] = collection[i](`,
               )
               .concat(
                  argsDefCode,
                  `);
                }
                return Promise.all(promises);
            });
        })`,
               );
         }
         {
            var bakeCollection_2 = void 0,
               fixedArgsNum_2 = void 0,
               bakeCollectionVariadic_2 = void 0,
               bakeCollectionAwait_2 = void 0,
               funcFactory = eval(funcFactoryCode);
            return funcFactory(collection);
         }
      }
      exports.bakeCollectionAwait = bakeCollectionAwait;
      function bakeCollectionVariadic(collection) {
         if (collection.length === 0) return exports.BAKED_EMPTY_FUNC;
         if (collection.length === 1) return collection[0];
         var funcFactoryCode;
         if (collection.length < FORLOOP_FALLBACK) {
            var _a = generateBodyPartsVariadicCode(collection.length),
               funcDefCode = _a.funcDefCode,
               funcCallCode = _a.funcCallCode;
            funcFactoryCode = `(function(collection) {
            `
               .concat(
                  funcDefCode,
                  `
            collection = undefined;
            return (function() {
                `,
               )
               .concat(
                  funcCallCode,
                  `
            });
        })`,
               );
         } else
            funcFactoryCode = `(function(collection) {
            return (function() {
                for (var i = 0; i < collection.length; ++i) {
                    collection[i].apply(undefined, arguments);
                }
            });
        })`;
         {
            var bakeCollection_3 = void 0,
               fixedArgsNum = void 0,
               bakeCollectionVariadic_3 = void 0,
               bakeCollectionAwait_3 = void 0,
               funcFactory = eval(funcFactoryCode);
            return funcFactory(collection);
         }
      }
      exports.bakeCollectionVariadic = bakeCollectionVariadic;
   });
   var xo = pe((sn) => {
      "use strict";
      var wo =
         (sn && sn.__spreadArray) ||
         function (t, e, n) {
            if (n || arguments.length === 2)
               for (var r = 0, s = e.length, i; r < s; r++)
                  (i || !(r in e)) &&
                     (i || (i = Array.prototype.slice.call(e, 0, r)),
                     (i[r] = e[r]));
            return t.concat(i || Array.prototype.slice.call(e));
         };
      Object.defineProperty(sn, "__esModule", { value: !0 });
      sn.TaskCollection = void 0;
      var vo = ls(),
         Qe = mo();
      function Vl(t, e) {
         var n = this.length;
         if (n > 1)
            if (e) {
               var r;
               (r = this._tasks).push.apply(r, arguments),
                  (this.length += arguments.length);
            } else this._tasks.push(t), this.length++;
         else if (e) {
            if (n === 1) {
               var s = Array(1 + arguments.length);
               s.push(s), s.push.apply(s, arguments), (this._tasks = s);
            } else {
               var s = Array(arguments.length);
               s.push.apply(s, arguments), (this._tasks = s);
            }
            this.length += arguments.length;
         } else
            n === 1 ? (this._tasks = [this._tasks, t]) : (this._tasks = t),
               this.length++;
      }
      function zl(t, e) {
         var n = this.length;
         if (n > 1)
            if (e) {
               var r;
               (r = this._tasks).push.apply(r, arguments),
                  (this.length += arguments.length);
            } else this._tasks.push(t), this.length++;
         else if (e) {
            if (n === 1) {
               var s = Array(1 + arguments.length);
               s.push(s), s.push.apply(s, arguments), (this._tasks = s);
            } else {
               var s = Array(arguments.length);
               s.push.apply(s, arguments), (this._tasks = s);
            }
            this.length += arguments.length;
         } else
            n === 1 ? (this._tasks = [this._tasks, t]) : (this._tasks = t),
               this.length++;
         this.firstEmitBuildStrategy ? (this.call = Rn) : this.rebuild();
      }
      function jl(t) {
         this.length !== 0 &&
            (this.length === 1
               ? this._tasks === t && (this.length = 0)
               : ((0, vo._fast_remove_single)(
                    this._tasks,
                    this._tasks.lastIndexOf(t),
                 ),
                 this._tasks.length === 1
                    ? ((this._tasks = this._tasks[0]), (this.length = 1))
                    : (this.length = this._tasks.length)));
      }
      function Fl(t) {
         if (this.length !== 0) {
            if (this.length === 1)
               if (
                  (this._tasks === t && (this.length = 0),
                  this.firstEmitBuildStrategy)
               ) {
                  this.call = Qe.BAKED_EMPTY_FUNC;
                  return;
               } else {
                  this.rebuild();
                  return;
               }
            else
               (0, vo._fast_remove_single)(
                  this._tasks,
                  this._tasks.lastIndexOf(t),
               ),
                  this._tasks.length === 1
                     ? ((this._tasks = this._tasks[0]), (this.length = 1))
                     : (this.length = this._tasks.length);
            this.firstEmitBuildStrategy ? (this.call = Rn) : this.rebuild();
         }
      }
      function Dl(t) {
         for (var e, n = [], r = 1; r < arguments.length; r++)
            n[r - 1] = arguments[r];
         this.length === 0
            ? ((this._tasks = n), (this.length = 1))
            : this.length === 1
              ? (n.unshift(this._tasks),
                (this._tasks = n),
                (this.length = this._tasks.length))
              : ((e = this._tasks).splice.apply(e, wo([t, 0], n, !1)),
                (this.length = this._tasks.length));
      }
      function Wl(t) {
         for (var e, n = [], r = 1; r < arguments.length; r++)
            n[r - 1] = arguments[r];
         this.length === 0
            ? ((this._tasks = n), (this.length = 1))
            : this.length === 1
              ? (n.unshift(this._tasks),
                (this._tasks = n),
                (this.length = this._tasks.length))
              : ((e = this._tasks).splice.apply(e, wo([t, 0], n, !1)),
                (this.length = this._tasks.length)),
            this.firstEmitBuildStrategy ? (this.call = Rn) : this.rebuild();
      }
      function Gl() {
         this.length === 0
            ? (this.call = Qe.BAKED_EMPTY_FUNC)
            : this.length === 1
              ? (this.call = this._tasks)
              : (this.call = (0, Qe.bakeCollection)(this._tasks, this.argsNum));
      }
      function Zl() {
         this.length === 0
            ? (this.call = Qe.BAKED_EMPTY_FUNC)
            : this.length === 1
              ? (this.call = this._tasks)
              : (this.call = (0, Qe.bakeCollectionAwait)(
                   this._tasks,
                   this.argsNum,
                ));
      }
      function Rn() {
         this.rebuild(), this.call.apply(void 0, arguments);
      }
      var Ht = (function () {
         function t(e, n, r, s) {
            n === void 0 && (n = !0),
               r === void 0 && (r = null),
               s === void 0 && (s = !1),
               (this.awaitTasks = s),
               (this.call = Qe.BAKED_EMPTY_FUNC),
               (this.argsNum = e),
               (this.firstEmitBuildStrategy = !0),
               s
                  ? (this.rebuild = Zl.bind(this))
                  : (this.rebuild = Gl.bind(this)),
               this.setAutoRebuild(n),
               r
                  ? typeof r == "function"
                     ? ((this._tasks = r), (this.length = 1))
                     : ((this._tasks = r), (this.length = r.length))
                  : ((this._tasks = null), (this.length = 0)),
               n && this.rebuild();
         }
         return t;
      })();
      sn.TaskCollection = Ht;
      function Yl() {
         (this._tasks = null),
            (this.length = 0),
            (this.call = Qe.BAKED_EMPTY_FUNC);
      }
      function Jl() {
         (this._tasks = null),
            (this.length = 0),
            (this.call = Qe.BAKED_EMPTY_FUNC);
      }
      function Ql(t) {
         this.argsNum < t &&
            ((this.argsNum = t),
            this.firstEmitBuildStrategy ? (this.call = Rn) : this.rebuild());
      }
      function Xl(t) {
         t
            ? ((this.push = zl.bind(this)),
              (this.insert = Wl.bind(this)),
              (this.removeLast = Fl.bind(this)))
            : ((this.push = Vl.bind(this)),
              (this.insert = Dl.bind(this)),
              (this.removeLast = jl.bind(this)));
      }
      function Kl() {
         return this.length === 0
            ? []
            : this.length === 1
              ? [this._tasks]
              : this._tasks;
      }
      function eh(t) {
         t.length === 0
            ? ((this.length = 0), (this.call = Qe.BAKED_EMPTY_FUNC))
            : t.length === 1
              ? ((this.length = 1), (this.call = t[0]), (this._tasks = t[0]))
              : ((this.length = t.length),
                (this._tasks = t),
                this.firstEmitBuildStrategy
                   ? (this.call = Rn)
                   : this.rebuild());
      }
      Ht.prototype.fastClear = Yl;
      Ht.prototype.clear = Jl;
      Ht.prototype.growArgsNum = Ql;
      Ht.prototype.setAutoRebuild = Xl;
      Ht.prototype.tasksAsArray = Kl;
      Ht.prototype.setTasks = eh;
   });
   var Eo = pe((qt) => {
      "use strict";
      var th =
            (qt && qt.__createBinding) ||
            (Object.create
               ? function (t, e, n, r) {
                    r === void 0 && (r = n);
                    var s = Object.getOwnPropertyDescriptor(e, n);
                    (!s ||
                       ("get" in s
                          ? !e.__esModule
                          : s.writable || s.configurable)) &&
                       (s = {
                          enumerable: !0,
                          get: function () {
                             return e[n];
                          },
                       }),
                       Object.defineProperty(t, r, s);
                 }
               : function (t, e, n, r) {
                    r === void 0 && (r = n), (t[r] = e[n]);
                 }),
         nh =
            (qt && qt.__exportStar) ||
            function (t, e) {
               for (var n in t)
                  n !== "default" &&
                     !Object.prototype.hasOwnProperty.call(e, n) &&
                     th(e, t, n);
            };
      Object.defineProperty(qt, "__esModule", { value: !0 });
      nh(xo(), qt);
   });
   var ko = pe((Zn) => {
      "use strict";
      Object.defineProperty(Zn, "__esModule", { value: !0 });
      Zn.nullObj = void 0;
      function rh() {
         var t = {};
         return (t.__proto__ = null), t;
      }
      Zn.nullObj = rh;
   });
   var So = pe((on) => {
      "use strict";
      var $t =
         (on && on.__spreadArray) ||
         function (t, e, n) {
            if (n || arguments.length === 2)
               for (var r = 0, s = e.length, i; r < s; r++)
                  (i || !(r in e)) &&
                     (i || (i = Array.prototype.slice.call(e, 0, r)),
                     (i[r] = e[r]));
            return t.concat(i || Array.prototype.slice.call(e));
         };
      Object.defineProperty(on, "__esModule", { value: !0 });
      on.EventEmitter = void 0;
      var hs = Eo(),
         sh = ls(),
         Yn = ko();
      function an(t, e, n, r, s, i) {
         var o = this.events[t];
         if (o) {
            if (o.length === 0) return !1;
            if (o.argsNum < 6) o.call(e, n, r, s, i);
            else {
               for (
                  var a = new Array(o.argsNum), c = 0, l = a.length;
                  c < l;
                  ++c
               )
                  a[c] = arguments[c + 1];
               o.call.apply(void 0, a);
            }
            return !0;
         }
         return !1;
      }
      function Ao(t, e, n, r, s, i) {
         var o = this.events[t],
            a;
         if (o !== void 0) {
            if (o.length === 0) return !1;
            if (o.argsNum < 6) o.call(e, n, r, s, i);
            else {
               a = new Array(o.argsNum);
               for (var c = 0, l = a.length; c < l; ++c)
                  a[c] = arguments[c + 1];
               o.call.apply(void 0, a);
            }
         }
         var u = this.onceEvents[t];
         if (u) {
            if (typeof u == "function")
               if (((this.onceEvents[t] = void 0), arguments.length < 6))
                  u(e, n, r, s, i);
               else {
                  if (a === void 0) {
                     a = new Array(arguments.length - 1);
                     for (var c = 0, l = a.length; c < l; ++c)
                        a[c] = arguments[c + 1];
                  }
                  u.apply(void 0, a);
               }
            else {
               var h = u;
               if (((this.onceEvents[t] = void 0), arguments.length < 6))
                  for (var c = 0; c < h.length; ++c) h[c](e, n, r, s, i);
               else {
                  if (a === void 0) {
                     a = new Array(arguments.length - 1);
                     for (var c = 0, l = a.length; c < l; ++c)
                        a[c] = arguments[c + 1];
                  }
                  for (var c = 0; c < h.length; ++c) h[c].apply(void 0, a);
               }
            }
            return !0;
         }
         return o !== void 0;
      }
      var ue = (function () {
         function t() {
            (this.events = (0, Yn.nullObj)()),
               (this.onceEvents = (0, Yn.nullObj)()),
               (this._symbolKeys = new Set()),
               (this.maxListeners = 1 / 0);
         }
         return (
            Object.defineProperty(t.prototype, "_eventsCount", {
               get: function () {
                  return this.eventNames().length;
               },
               enumerable: !1,
               configurable: !0,
            }),
            t
         );
      })();
      on.EventEmitter = ue;
      function ih(t, e) {
         switch (
            (this.emit === an && (this.emit = Ao), typeof this.onceEvents[t])
         ) {
            case "undefined":
               (this.onceEvents[t] = e),
                  typeof t == "symbol" && this._symbolKeys.add(t);
               break;
            case "function":
               this.onceEvents[t] = [this.onceEvents[t], e];
               break;
            case "object":
               this.onceEvents[t].push(e);
         }
         return this;
      }
      function To(t, e, n) {
         if ((n === void 0 && (n = e.length), typeof e != "function"))
            throw new TypeError("The listener must be a function");
         var r = this.events[t];
         return (
            r
               ? (r.push(e),
                 r.growArgsNum(n),
                 this.maxListeners !== 1 / 0 &&
                    this.maxListeners <= r.length &&
                    console.warn(
                       'Maximum event listeners for "'.concat(
                          String(t),
                          '" event!',
                       ),
                    ))
               : ((this.events[t] = new hs.TaskCollection(n, !0, e, !1)),
                 typeof t == "symbol" && this._symbolKeys.add(t)),
            this
         );
      }
      function _o(t, e) {
         var n = this.events[t];
         n && n.removeLast(e);
         var r = this.onceEvents[t];
         return (
            r &&
               (typeof r == "function"
                  ? (this.onceEvents[t] = void 0)
                  : typeof r == "object" &&
                    (r.length === 1 && r[0] === e
                       ? (this.onceEvents[t] = void 0)
                       : (0, sh._fast_remove_single)(r, r.lastIndexOf(e)))),
            this
         );
      }
      function oh(t, e, n, r) {
         n === void 0 && (n = this),
            r === void 0 && (r = e.length),
            this.boundFuncs || (this.boundFuncs = new Map());
         var s = e.bind(n);
         return this.boundFuncs.set(e, s), this.addListener(t, s, r);
      }
      function ah(t, e) {
         var n,
            r,
            s =
               (n = this.boundFuncs) === null || n === void 0
                  ? void 0
                  : n.get(e);
         return (
            (r = this.boundFuncs) === null || r === void 0 || r.delete(e),
            this.removeListener(t, s)
         );
      }
      function ch(t) {
         return this.events[t] && !!this.events[t].length;
      }
      function uh(t, e, n) {
         if ((n === void 0 && (n = e.length), typeof e != "function"))
            throw new TypeError("The listener must be a function");
         var r = this.events[t];
         return (
            !r || !(r instanceof hs.TaskCollection)
               ? ((r = this.events[t] = new hs.TaskCollection(n, !0, e, !1)),
                 typeof t == "symbol" && this._symbolKeys.add(t))
               : (r.insert(0, e),
                 r.growArgsNum(n),
                 this.maxListeners !== 1 / 0 &&
                    this.maxListeners <= r.length &&
                    console.warn(
                       'Maximum event listeners for "'.concat(
                          String(t),
                          '" event!',
                       ),
                    )),
            this
         );
      }
      function lh(t, e) {
         this.emit === an && (this.emit = Ao);
         var n = this.onceEvents[t];
         return (
            n
               ? typeof n != "object"
                  ? ((this.onceEvents[t] = [e, n]),
                    typeof t == "symbol" && this._symbolKeys.add(t))
                  : (n.unshift(e),
                    this.maxListeners !== 1 / 0 &&
                       this.maxListeners <= n.length &&
                       console.warn(
                          'Maximum event listeners for "'.concat(
                             String(t),
                             '" once event!',
                          ),
                       ))
               : ((this.onceEvents[t] = [e]),
                 typeof t == "symbol" && this._symbolKeys.add(t)),
            this
         );
      }
      function hh(t) {
         return (
            t === void 0
               ? ((this.events = (0, Yn.nullObj)()),
                 (this.onceEvents = (0, Yn.nullObj)()),
                 (this._symbolKeys = new Set()))
               : ((this.events[t] = void 0),
                 (this.onceEvents[t] = void 0),
                 typeof t == "symbol" && this._symbolKeys.delete(t)),
            this
         );
      }
      function fh(t) {
         return (this.maxListeners = t), this;
      }
      function dh() {
         return this.maxListeners;
      }
      function ph(t) {
         return this.emit === an
            ? this.events[t]
               ? this.events[t].tasksAsArray().slice()
               : []
            : this.events[t] && this.onceEvents[t]
              ? $t(
                   $t([], this.events[t].tasksAsArray(), !0),
                   typeof this.onceEvents[t] == "function"
                      ? [this.onceEvents[t]]
                      : this.onceEvents[t],
                   !0,
                )
              : this.events[t]
                ? this.events[t].tasksAsArray()
                : this.onceEvents[t]
                  ? typeof this.onceEvents[t] == "function"
                     ? [this.onceEvents[t]]
                     : this.onceEvents[t]
                  : [];
      }
      function gh() {
         var t = this;
         if (this.emit === an) {
            var e = Object.keys(this.events);
            return $t($t([], e, !0), Array.from(this._symbolKeys), !0).filter(
               function (r) {
                  return r in t.events && t.events[r] && t.events[r].length;
               },
            );
         } else {
            var e = Object.keys(this.events).filter(function (s) {
                  return t.events[s] && t.events[s].length;
               }),
               n = Object.keys(this.onceEvents).filter(function (s) {
                  return t.onceEvents[s] && t.onceEvents[s].length;
               });
            return $t(
               $t($t([], e, !0), n, !0),
               Array.from(this._symbolKeys).filter(function (s) {
                  return (
                     (s in t.events && t.events[s] && t.events[s].length) ||
                     (s in t.onceEvents &&
                        t.onceEvents[s] &&
                        t.onceEvents[s].length)
                  );
               }),
               !0,
            );
         }
      }
      function yh(t) {
         return this.emit === an
            ? (this.events[t] && this.events[t].length) || 0
            : ((this.events[t] && this.events[t].length) || 0) +
                 ((this.onceEvents[t] && this.onceEvents[t].length) || 0);
      }
      ue.prototype.emit = an;
      ue.prototype.on = To;
      ue.prototype.once = ih;
      ue.prototype.addListener = To;
      ue.prototype.removeListener = _o;
      ue.prototype.addListenerBound = oh;
      ue.prototype.removeListenerBound = ah;
      ue.prototype.hasListeners = ch;
      ue.prototype.prependListener = uh;
      ue.prototype.prependOnceListener = lh;
      ue.prototype.off = _o;
      ue.prototype.removeAllListeners = hh;
      ue.prototype.setMaxListeners = fh;
      ue.prototype.getMaxListeners = dh;
      ue.prototype.listeners = ph;
      ue.prototype.eventNames = gh;
      ue.prototype.listenerCount = yh;
   });
   var qe = pe((dt) => {
      "use strict";
      var bh =
            (dt && dt.__createBinding) ||
            (Object.create
               ? function (t, e, n, r) {
                    r === void 0 && (r = n);
                    var s = Object.getOwnPropertyDescriptor(e, n);
                    (!s ||
                       ("get" in s
                          ? !e.__esModule
                          : s.writable || s.configurable)) &&
                       (s = {
                          enumerable: !0,
                          get: function () {
                             return e[n];
                          },
                       }),
                       Object.defineProperty(t, r, s);
                 }
               : function (t, e, n, r) {
                    r === void 0 && (r = n), (t[r] = e[n]);
                 }),
         Ro =
            (dt && dt.__exportStar) ||
            function (t, e) {
               for (var n in t)
                  n !== "default" &&
                     !Object.prototype.hasOwnProperty.call(e, n) &&
                     bh(e, t, n);
            };
      Object.defineProperty(dt, "__esModule", { value: !0 });
      Ro(bo(), dt);
      Ro(So(), dt);
   });
   var Bo = pe((qb, Co) => {
      var cn = 1e3,
         un = cn * 60,
         ln = un * 60,
         Vt = ln * 24,
         mh = Vt * 7,
         wh = Vt * 365.25;
      Co.exports = function (t, e) {
         e = e || {};
         var n = typeof t;
         if (n === "string" && t.length > 0) return vh(t);
         if (n === "number" && isFinite(t)) return e.long ? Eh(t) : xh(t);
         throw new Error(
            "val is not a non-empty string or a valid number. val=" +
               JSON.stringify(t),
         );
      };
      function vh(t) {
         if (((t = String(t)), !(t.length > 100))) {
            var e =
               /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(
                  t,
               );
            if (e) {
               var n = parseFloat(e[1]),
                  r = (e[2] || "ms").toLowerCase();
               switch (r) {
                  case "years":
                  case "year":
                  case "yrs":
                  case "yr":
                  case "y":
                     return n * wh;
                  case "weeks":
                  case "week":
                  case "w":
                     return n * mh;
                  case "days":
                  case "day":
                  case "d":
                     return n * Vt;
                  case "hours":
                  case "hour":
                  case "hrs":
                  case "hr":
                  case "h":
                     return n * ln;
                  case "minutes":
                  case "minute":
                  case "mins":
                  case "min":
                  case "m":
                     return n * un;
                  case "seconds":
                  case "second":
                  case "secs":
                  case "sec":
                  case "s":
                     return n * cn;
                  case "milliseconds":
                  case "millisecond":
                  case "msecs":
                  case "msec":
                  case "ms":
                     return n;
                  default:
                     return;
               }
            }
         }
      }
      function xh(t) {
         var e = Math.abs(t);
         return e >= Vt
            ? Math.round(t / Vt) + "d"
            : e >= ln
              ? Math.round(t / ln) + "h"
              : e >= un
                ? Math.round(t / un) + "m"
                : e >= cn
                  ? Math.round(t / cn) + "s"
                  : t + "ms";
      }
      function Eh(t) {
         var e = Math.abs(t);
         return e >= Vt
            ? Jn(t, e, Vt, "day")
            : e >= ln
              ? Jn(t, e, ln, "hour")
              : e >= un
                ? Jn(t, e, un, "minute")
                : e >= cn
                  ? Jn(t, e, cn, "second")
                  : t + " ms";
      }
      function Jn(t, e, n, r) {
         var s = e >= n * 1.5;
         return Math.round(t / n) + " " + r + (s ? "s" : "");
      }
   });
   var Lo = pe(($b, Io) => {
      function kh(t) {
         (n.debug = n),
            (n.default = n),
            (n.coerce = c),
            (n.disable = o),
            (n.enable = s),
            (n.enabled = a),
            (n.humanize = Bo()),
            (n.destroy = l),
            Object.keys(t).forEach((u) => {
               n[u] = t[u];
            }),
            (n.names = []),
            (n.skips = []),
            (n.formatters = {});
         function e(u) {
            let h = 0;
            for (let f = 0; f < u.length; f++)
               (h = (h << 5) - h + u.charCodeAt(f)), (h |= 0);
            return n.colors[Math.abs(h) % n.colors.length];
         }
         n.selectColor = e;
         function n(u) {
            let h,
               f = null,
               g,
               y;
            function d(...p) {
               if (!d.enabled) return;
               let b = d,
                  w = Number(new Date()),
                  E = w - (h || w);
               (b.diff = E),
                  (b.prev = h),
                  (b.curr = w),
                  (h = w),
                  (p[0] = n.coerce(p[0])),
                  typeof p[0] != "string" && p.unshift("%O");
               let k = 0;
               (p[0] = p[0].replace(/%([a-zA-Z%])/g, (A, T) => {
                  if (A === "%%") return "%";
                  k++;
                  let _ = n.formatters[T];
                  if (typeof _ == "function") {
                     let I = p[k];
                     (A = _.call(b, I)), p.splice(k, 1), k--;
                  }
                  return A;
               })),
                  n.formatArgs.call(b, p),
                  (b.log || n.log).apply(b, p);
            }
            return (
               (d.namespace = u),
               (d.useColors = n.useColors()),
               (d.color = n.selectColor(u)),
               (d.extend = r),
               (d.destroy = n.destroy),
               Object.defineProperty(d, "enabled", {
                  enumerable: !0,
                  configurable: !1,
                  get: () =>
                     f !== null
                        ? f
                        : (g !== n.namespaces &&
                             ((g = n.namespaces), (y = n.enabled(u))),
                          y),
                  set: (p) => {
                     f = p;
                  },
               }),
               typeof n.init == "function" && n.init(d),
               d
            );
         }
         function r(u, h) {
            let f = n(this.namespace + (typeof h > "u" ? ":" : h) + u);
            return (f.log = this.log), f;
         }
         function s(u) {
            n.save(u), (n.namespaces = u), (n.names = []), (n.skips = []);
            let h = (typeof u == "string" ? u : "")
               .trim()
               .replace(" ", ",")
               .split(",")
               .filter(Boolean);
            for (let f of h)
               f[0] === "-" ? n.skips.push(f.slice(1)) : n.names.push(f);
         }
         function i(u, h) {
            let f = 0,
               g = 0,
               y = -1,
               d = 0;
            for (; f < u.length; )
               if (g < h.length && (h[g] === u[f] || h[g] === "*"))
                  h[g] === "*" ? ((y = g), (d = f), g++) : (f++, g++);
               else if (y !== -1) (g = y + 1), d++, (f = d);
               else return !1;
            for (; g < h.length && h[g] === "*"; ) g++;
            return g === h.length;
         }
         function o() {
            let u = [...n.names, ...n.skips.map((h) => "-" + h)].join(",");
            return n.enable(""), u;
         }
         function a(u) {
            for (let h of n.skips) if (i(u, h)) return !1;
            for (let h of n.names) if (i(u, h)) return !0;
            return !1;
         }
         function c(u) {
            return u instanceof Error ? u.stack || u.message : u;
         }
         function l() {
            console.warn(
               "Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.",
            );
         }
         return n.enable(n.load()), n;
      }
      Io.exports = kh;
   });
   var Ee = pe((be, Qn) => {
      be.formatArgs = Th;
      be.save = _h;
      be.load = Sh;
      be.useColors = Ah;
      be.storage = Rh();
      be.destroy = (() => {
         let t = !1;
         return () => {
            t ||
               ((t = !0),
               console.warn(
                  "Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.",
               ));
         };
      })();
      be.colors = [
         "#0000CC",
         "#0000FF",
         "#0033CC",
         "#0033FF",
         "#0066CC",
         "#0066FF",
         "#0099CC",
         "#0099FF",
         "#00CC00",
         "#00CC33",
         "#00CC66",
         "#00CC99",
         "#00CCCC",
         "#00CCFF",
         "#3300CC",
         "#3300FF",
         "#3333CC",
         "#3333FF",
         "#3366CC",
         "#3366FF",
         "#3399CC",
         "#3399FF",
         "#33CC00",
         "#33CC33",
         "#33CC66",
         "#33CC99",
         "#33CCCC",
         "#33CCFF",
         "#6600CC",
         "#6600FF",
         "#6633CC",
         "#6633FF",
         "#66CC00",
         "#66CC33",
         "#9900CC",
         "#9900FF",
         "#9933CC",
         "#9933FF",
         "#99CC00",
         "#99CC33",
         "#CC0000",
         "#CC0033",
         "#CC0066",
         "#CC0099",
         "#CC00CC",
         "#CC00FF",
         "#CC3300",
         "#CC3333",
         "#CC3366",
         "#CC3399",
         "#CC33CC",
         "#CC33FF",
         "#CC6600",
         "#CC6633",
         "#CC9900",
         "#CC9933",
         "#CCCC00",
         "#CCCC33",
         "#FF0000",
         "#FF0033",
         "#FF0066",
         "#FF0099",
         "#FF00CC",
         "#FF00FF",
         "#FF3300",
         "#FF3333",
         "#FF3366",
         "#FF3399",
         "#FF33CC",
         "#FF33FF",
         "#FF6600",
         "#FF6633",
         "#FF9900",
         "#FF9933",
         "#FFCC00",
         "#FFCC33",
      ];
      function Ah() {
         if (
            typeof window < "u" &&
            window.process &&
            (window.process.type === "renderer" || window.process.__nwjs)
         )
            return !0;
         if (
            typeof navigator < "u" &&
            navigator.userAgent &&
            navigator.userAgent.toLowerCase().match(/(edge|trident)\/(\d+)/)
         )
            return !1;
         let t;
         return (
            (typeof document < "u" &&
               document.documentElement &&
               document.documentElement.style &&
               document.documentElement.style.WebkitAppearance) ||
            (typeof window < "u" &&
               window.console &&
               (window.console.firebug ||
                  (window.console.exception && window.console.table))) ||
            (typeof navigator < "u" &&
               navigator.userAgent &&
               (t = navigator.userAgent
                  .toLowerCase()
                  .match(/firefox\/(\d+)/)) &&
               parseInt(t[1], 10) >= 31) ||
            (typeof navigator < "u" &&
               navigator.userAgent &&
               navigator.userAgent.toLowerCase().match(/applewebkit\/(\d+)/))
         );
      }
      function Th(t) {
         if (
            ((t[0] =
               (this.useColors ? "%c" : "") +
               this.namespace +
               (this.useColors ? " %c" : " ") +
               t[0] +
               (this.useColors ? "%c " : " ") +
               "+" +
               Qn.exports.humanize(this.diff)),
            !this.useColors)
         )
            return;
         let e = "color: " + this.color;
         t.splice(1, 0, e, "color: inherit");
         let n = 0,
            r = 0;
         t[0].replace(/%[a-zA-Z%]/g, (s) => {
            s !== "%%" && (n++, s === "%c" && (r = n));
         }),
            t.splice(r, 0, e);
      }
      be.log = console.debug || console.log || (() => {});
      function _h(t) {
         try {
            t ? be.storage.setItem("debug", t) : be.storage.removeItem("debug");
         } catch {}
      }
      function Sh() {
         let t;
         try {
            t = be.storage.getItem("debug");
         } catch {}
         return (
            !t &&
               typeof process < "u" &&
               "env" in process &&
               (t = process.env.DEBUG),
            t
         );
      }
      function Rh() {
         try {
            return localStorage;
         } catch {}
      }
      Qn.exports = Lo()(be);
      var { formatters: Ch } = Qn.exports;
      Ch.j = function (t) {
         try {
            return JSON.stringify(t);
         } catch (e) {
            return "[UnexpectedJSONParseError]: " + e.message;
         }
      };
   });
   var du = pe((ns) => {
      "use strict";
      Object.defineProperty(ns, "__esModule", { value: !0 });
      ns.LRUCacheNode = void 0;
      var Yi = class {
         constructor(e, n, r) {
            let {
               entryExpirationTimeInMS: s = null,
               next: i = null,
               prev: o = null,
               onEntryEvicted: a,
               onEntryMarkedAsMostRecentlyUsed: c,
               clone: l,
               cloneFn: u,
            } = r ?? {};
            if (typeof s == "number" && (s <= 0 || Number.isNaN(s)))
               throw new Error(
                  "entryExpirationTimeInMS must either be null (no expiry) or greater than 0",
               );
            (this.clone = l ?? !1),
               (this.cloneFn = u ?? this.defaultClone),
               (this.key = e),
               (this.internalValue = this.clone ? this.cloneFn(n) : n),
               (this.created = Date.now()),
               (this.entryExpirationTimeInMS = s),
               (this.next = i),
               (this.prev = o),
               (this.onEntryEvicted = a),
               (this.onEntryMarkedAsMostRecentlyUsed = c);
         }
         get value() {
            return this.clone
               ? this.cloneFn(this.internalValue)
               : this.internalValue;
         }
         get isExpired() {
            return (
               typeof this.entryExpirationTimeInMS == "number" &&
               Date.now() - this.created > this.entryExpirationTimeInMS
            );
         }
         invokeOnEvicted() {
            if (this.onEntryEvicted) {
               let { key: e, value: n, isExpired: r } = this;
               this.onEntryEvicted({ key: e, value: n, isExpired: r });
            }
         }
         invokeOnEntryMarkedAsMostRecentlyUsed() {
            if (this.onEntryMarkedAsMostRecentlyUsed) {
               let { key: e, value: n } = this;
               this.onEntryMarkedAsMostRecentlyUsed({ key: e, value: n });
            }
         }
         defaultClone(e) {
            return typeof e == "boolean" ||
               typeof e == "string" ||
               typeof e == "number"
               ? e
               : JSON.parse(JSON.stringify(e));
         }
      };
      ns.LRUCacheNode = Yi;
   });
   var pu = pe((rs) => {
      "use strict";
      Object.defineProperty(rs, "__esModule", { value: !0 });
      rs.LRUCache = void 0;
      var U0 = du(),
         Ji = class {
            constructor(e) {
               (this.lookupTable = new Map()),
                  (this.head = null),
                  (this.tail = null);
               let {
                  maxSize: n = 25,
                  entryExpirationTimeInMS: r = null,
                  onEntryEvicted: s,
                  onEntryMarkedAsMostRecentlyUsed: i,
                  cloneFn: o,
                  clone: a,
               } = e ?? {};
               if (Number.isNaN(n) || n <= 0)
                  throw new Error("maxSize must be greater than 0.");
               if (typeof r == "number" && (r <= 0 || Number.isNaN(r)))
                  throw new Error(
                     "entryExpirationTimeInMS must either be null (no expiry) or greater than 0",
                  );
               (this.maxSizeInternal = n),
                  (this.entryExpirationTimeInMS = r),
                  (this.onEntryEvicted = s),
                  (this.onEntryMarkedAsMostRecentlyUsed = i),
                  (this.clone = a),
                  (this.cloneFn = o);
            }
            get size() {
               return this.cleanCache(), this.lookupTable.size;
            }
            get remainingSize() {
               return this.maxSizeInternal - this.size;
            }
            get newest() {
               return this.head
                  ? this.head.isExpired
                     ? (this.removeNodeFromListAndLookupTable(this.head),
                       this.newest)
                     : this.mapNodeToEntry(this.head)
                  : null;
            }
            get oldest() {
               return this.tail
                  ? this.tail.isExpired
                     ? (this.removeNodeFromListAndLookupTable(this.tail),
                       this.oldest)
                     : this.mapNodeToEntry(this.tail)
                  : null;
            }
            get maxSize() {
               return this.maxSizeInternal;
            }
            set maxSize(e) {
               if (Number.isNaN(e) || e <= 0)
                  throw new Error("maxSize must be greater than 0.");
               (this.maxSizeInternal = e), this.enforceSizeLimit();
            }
            set(e, n, r) {
               let s = this.lookupTable.get(e);
               s && this.removeNodeFromListAndLookupTable(s);
               let i = new U0.LRUCacheNode(e, n, {
                  entryExpirationTimeInMS: this.entryExpirationTimeInMS,
                  onEntryEvicted: this.onEntryEvicted,
                  onEntryMarkedAsMostRecentlyUsed:
                     this.onEntryMarkedAsMostRecentlyUsed,
                  clone: this.clone,
                  cloneFn: this.cloneFn,
                  ...r,
               });
               return (
                  this.setNodeAsHead(i),
                  this.lookupTable.set(e, i),
                  this.enforceSizeLimit(),
                  this
               );
            }
            get(e) {
               let n = this.lookupTable.get(e);
               return n
                  ? n.isExpired
                     ? (this.removeNodeFromListAndLookupTable(n), null)
                     : (this.setNodeAsHead(n), n.value)
                  : null;
            }
            peek(e) {
               let n = this.lookupTable.get(e);
               return n
                  ? n.isExpired
                     ? (this.removeNodeFromListAndLookupTable(n), null)
                     : n.value
                  : null;
            }
            delete(e) {
               let n = this.lookupTable.get(e);
               return n ? this.removeNodeFromListAndLookupTable(n) : !1;
            }
            has(e) {
               let n = this.lookupTable.get(e);
               return n
                  ? n.isExpired
                     ? (this.removeNodeFromListAndLookupTable(n), !1)
                     : !0
                  : !1;
            }
            clear() {
               (this.head = null), (this.tail = null), this.lookupTable.clear();
            }
            find(e) {
               let n = this.head;
               for (; n; ) {
                  if (n.isExpired) {
                     let s = n.next;
                     this.removeNodeFromListAndLookupTable(n), (n = s);
                     continue;
                  }
                  let r = this.mapNodeToEntry(n);
                  if (e(r)) return this.setNodeAsHead(n), r;
                  n = n.next;
               }
               return null;
            }
            forEach(e) {
               let n = this.head,
                  r = 0;
               for (; n; ) {
                  if (n.isExpired) {
                     let s = n.next;
                     this.removeNodeFromListAndLookupTable(n), (n = s);
                     continue;
                  }
                  e(n.value, n.key, r), (n = n.next), r++;
               }
            }
            *values() {
               let e = this.head;
               for (; e; ) {
                  if (e.isExpired) {
                     let n = e.next;
                     this.removeNodeFromListAndLookupTable(e), (e = n);
                     continue;
                  }
                  yield e.value, (e = e.next);
               }
            }
            *keys() {
               let e = this.head;
               for (; e; ) {
                  if (e.isExpired) {
                     let n = e.next;
                     this.removeNodeFromListAndLookupTable(e), (e = n);
                     continue;
                  }
                  yield e.key, (e = e.next);
               }
            }
            *entries() {
               let e = this.head;
               for (; e; ) {
                  if (e.isExpired) {
                     let n = e.next;
                     this.removeNodeFromListAndLookupTable(e), (e = n);
                     continue;
                  }
                  yield this.mapNodeToEntry(e), (e = e.next);
               }
            }
            *[Symbol.iterator]() {
               let e = this.head;
               for (; e; ) {
                  if (e.isExpired) {
                     let n = e.next;
                     this.removeNodeFromListAndLookupTable(e), (e = n);
                     continue;
                  }
                  yield this.mapNodeToEntry(e), (e = e.next);
               }
            }
            enforceSizeLimit() {
               let e = this.tail;
               for (; e !== null && this.size > this.maxSizeInternal; ) {
                  let n = e.prev;
                  this.removeNodeFromListAndLookupTable(e), (e = n);
               }
            }
            mapNodeToEntry({ key: e, value: n }) {
               return { key: e, value: n };
            }
            setNodeAsHead(e) {
               this.removeNodeFromList(e),
                  this.head
                     ? ((e.next = this.head),
                       (this.head.prev = e),
                       (this.head = e))
                     : ((this.head = e), (this.tail = e)),
                  e.invokeOnEntryMarkedAsMostRecentlyUsed();
            }
            removeNodeFromList(e) {
               e.prev !== null && (e.prev.next = e.next),
                  e.next !== null && (e.next.prev = e.prev),
                  this.head === e && (this.head = e.next),
                  this.tail === e && (this.tail = e.prev),
                  (e.next = null),
                  (e.prev = null);
            }
            removeNodeFromListAndLookupTable(e) {
               return (
                  e.invokeOnEvicted(),
                  this.removeNodeFromList(e),
                  this.lookupTable.delete(e.key)
               );
            }
            cleanCache() {
               if (!this.entryExpirationTimeInMS) return;
               let e = [];
               for (let n of this.lookupTable.values())
                  n.isExpired && e.push(n);
               e.forEach((n) => this.removeNodeFromListAndLookupTable(n));
            }
         };
      rs.LRUCache = Ji;
   });
   var Qi = pe((tn) => {
      "use strict";
      var P0 =
            (tn && tn.__createBinding) ||
            (Object.create
               ? function (t, e, n, r) {
                    r === void 0 && (r = n);
                    var s = Object.getOwnPropertyDescriptor(e, n);
                    (!s ||
                       ("get" in s
                          ? !e.__esModule
                          : s.writable || s.configurable)) &&
                       (s = {
                          enumerable: !0,
                          get: function () {
                             return e[n];
                          },
                       }),
                       Object.defineProperty(t, r, s);
                 }
               : function (t, e, n, r) {
                    r === void 0 && (r = n), (t[r] = e[n]);
                 }),
         O0 =
            (tn && tn.__exportStar) ||
            function (t, e) {
               for (var n in t)
                  n !== "default" &&
                     !Object.prototype.hasOwnProperty.call(e, n) &&
                     P0(e, t, n);
            };
      Object.defineProperty(tn, "__esModule", { value: !0 });
      O0(pu(), tn);
   });
   var Ou = pe((N) => {
      "use strict";
      Object.defineProperty(N, "__esModule", { value: !0 });
      N.bytes =
         N.stringToBytes =
         N.str =
         N.bytesToString =
         N.hex =
         N.utf8 =
         N.bech32m =
         N.bech32 =
         N.base58check =
         N.base58xmr =
         N.base58xrp =
         N.base58flickr =
         N.base58 =
         N.base64url =
         N.base64 =
         N.base32crockford =
         N.base32hex =
         N.base32 =
         N.base16 =
         N.utils =
         N.assertNumber =
            void 0;
      function Pt(t) {
         if (!Number.isSafeInteger(t)) throw new Error(`Wrong integer: ${t}`);
      }
      N.assertNumber = Pt;
      function Me(...t) {
         let e = (s, i) => (o) => s(i(o)),
            n = Array.from(t)
               .reverse()
               .reduce((s, i) => (s ? e(s, i.encode) : i.encode), void 0),
            r = t.reduce((s, i) => (s ? e(s, i.decode) : i.decode), void 0);
         return { encode: n, decode: r };
      }
      function Ge(t) {
         return {
            encode: (e) => {
               if (!Array.isArray(e) || (e.length && typeof e[0] != "number"))
                  throw new Error(
                     "alphabet.encode input should be an array of numbers",
                  );
               return e.map((n) => {
                  if ((Pt(n), n < 0 || n >= t.length))
                     throw new Error(
                        `Digit index outside alphabet: ${n} (alphabet: ${t.length})`,
                     );
                  return t[n];
               });
            },
            decode: (e) => {
               if (!Array.isArray(e) || (e.length && typeof e[0] != "string"))
                  throw new Error(
                     "alphabet.decode input should be array of strings",
                  );
               return e.map((n) => {
                  if (typeof n != "string")
                     throw new Error(
                        `alphabet.decode: not string element=${n}`,
                     );
                  let r = t.indexOf(n);
                  if (r === -1)
                     throw new Error(`Unknown letter: "${n}". Allowed: ${t}`);
                  return r;
               });
            },
         };
      }
      function Ze(t = "") {
         if (typeof t != "string")
            throw new Error("join separator should be string");
         return {
            encode: (e) => {
               if (!Array.isArray(e) || (e.length && typeof e[0] != "string"))
                  throw new Error(
                     "join.encode input should be array of strings",
                  );
               for (let n of e)
                  if (typeof n != "string")
                     throw new Error(`join.encode: non-string input=${n}`);
               return e.join(t);
            },
            decode: (e) => {
               if (typeof e != "string")
                  throw new Error("join.decode input should be string");
               return e.split(t);
            },
         };
      }
      function Fn(t, e = "=") {
         if ((Pt(t), typeof e != "string"))
            throw new Error("padding chr should be string");
         return {
            encode(n) {
               if (!Array.isArray(n) || (n.length && typeof n[0] != "string"))
                  throw new Error(
                     "padding.encode input should be array of strings",
                  );
               for (let r of n)
                  if (typeof r != "string")
                     throw new Error(`padding.encode: non-string input=${r}`);
               for (; (n.length * t) % 8; ) n.push(e);
               return n;
            },
            decode(n) {
               if (!Array.isArray(n) || (n.length && typeof n[0] != "string"))
                  throw new Error(
                     "padding.encode input should be array of strings",
                  );
               for (let s of n)
                  if (typeof s != "string")
                     throw new Error(`padding.decode: non-string input=${s}`);
               let r = n.length;
               if ((r * t) % 8)
                  throw new Error(
                     "Invalid padding: string should have whole number of bytes",
                  );
               for (; r > 0 && n[r - 1] === e; r--)
                  if (!(((r - 1) * t) % 8))
                     throw new Error(
                        "Invalid padding: string has too much padding",
                     );
               return n.slice(0, r);
            },
         };
      }
      function Cu(t) {
         if (typeof t != "function")
            throw new Error("normalize fn should be function");
         return { encode: (e) => e, decode: (e) => t(e) };
      }
      function Au(t, e, n) {
         if (e < 2)
            throw new Error(
               `convertRadix: wrong from=${e}, base cannot be less than 2`,
            );
         if (n < 2)
            throw new Error(
               `convertRadix: wrong to=${n}, base cannot be less than 2`,
            );
         if (!Array.isArray(t))
            throw new Error("convertRadix: data should be array");
         if (!t.length) return [];
         let r = 0,
            s = [],
            i = Array.from(t);
         for (
            i.forEach((o) => {
               if ((Pt(o), o < 0 || o >= e))
                  throw new Error(`Wrong integer: ${o}`);
            });
            ;

         ) {
            let o = 0,
               a = !0;
            for (let c = r; c < i.length; c++) {
               let l = i[c],
                  u = e * o + l;
               if (
                  !Number.isSafeInteger(u) ||
                  (e * o) / e !== o ||
                  u - l !== e * o
               )
                  throw new Error("convertRadix: carry overflow");
               if (
                  ((o = u % n),
                  (i[c] = Math.floor(u / n)),
                  !Number.isSafeInteger(i[c]) || i[c] * n + o !== u)
               )
                  throw new Error("convertRadix: carry overflow");
               if (a) i[c] ? (a = !1) : (r = c);
               else continue;
            }
            if ((s.push(o), a)) break;
         }
         for (let o = 0; o < t.length - 1 && t[o] === 0; o++) s.push(0);
         return s.reverse();
      }
      var Bu = (t, e) => (e ? Bu(e, t % e) : t),
         os = (t, e) => t + (e - Bu(t, e));
      function no(t, e, n, r) {
         if (!Array.isArray(t))
            throw new Error("convertRadix2: data should be array");
         if (e <= 0 || e > 32)
            throw new Error(`convertRadix2: wrong from=${e}`);
         if (n <= 0 || n > 32) throw new Error(`convertRadix2: wrong to=${n}`);
         if (os(e, n) > 32)
            throw new Error(
               `convertRadix2: carry overflow from=${e} to=${n} carryBits=${os(e, n)}`,
            );
         let s = 0,
            i = 0,
            o = 2 ** n - 1,
            a = [];
         for (let c of t) {
            if ((Pt(c), c >= 2 ** e))
               throw new Error(
                  `convertRadix2: invalid data word=${c} from=${e}`,
               );
            if (((s = (s << e) | c), i + e > 32))
               throw new Error(
                  `convertRadix2: carry overflow pos=${i} from=${e}`,
               );
            for (i += e; i >= n; i -= n) a.push(((s >> (i - n)) & o) >>> 0);
            s &= 2 ** i - 1;
         }
         if (((s = (s << (n - i)) & o), !r && i >= e))
            throw new Error("Excess padding");
         if (!r && s) throw new Error(`Non-zero padding: ${s}`);
         return r && i > 0 && a.push(s >>> 0), a;
      }
      function Iu(t) {
         return (
            Pt(t),
            {
               encode: (e) => {
                  if (!(e instanceof Uint8Array))
                     throw new Error("radix.encode input should be Uint8Array");
                  return Au(Array.from(e), 2 ** 8, t);
               },
               decode: (e) => {
                  if (
                     !Array.isArray(e) ||
                     (e.length && typeof e[0] != "number")
                  )
                     throw new Error(
                        "radix.decode input should be array of strings",
                     );
                  return Uint8Array.from(Au(e, t, 2 ** 8));
               },
            }
         );
      }
      function ut(t, e = !1) {
         if ((Pt(t), t <= 0 || t > 32))
            throw new Error("radix2: bits should be in (0..32]");
         if (os(8, t) > 32 || os(t, 8) > 32)
            throw new Error("radix2: carry overflow");
         return {
            encode: (n) => {
               if (!(n instanceof Uint8Array))
                  throw new Error("radix2.encode input should be Uint8Array");
               return no(Array.from(n), 8, t, !e);
            },
            decode: (n) => {
               if (!Array.isArray(n) || (n.length && typeof n[0] != "number"))
                  throw new Error(
                     "radix2.decode input should be array of strings",
                  );
               return Uint8Array.from(no(n, t, 8, e));
            },
         };
      }
      function Tu(t) {
         if (typeof t != "function")
            throw new Error("unsafeWrapper fn should be function");
         return function (...e) {
            try {
               return t.apply(null, e);
            } catch {}
         };
      }
      function Lu(t, e) {
         if ((Pt(t), typeof e != "function"))
            throw new Error("checksum fn should be function");
         return {
            encode(n) {
               if (!(n instanceof Uint8Array))
                  throw new Error(
                     "checksum.encode: input should be Uint8Array",
                  );
               let r = e(n).slice(0, t),
                  s = new Uint8Array(n.length + t);
               return s.set(n), s.set(r, n.length), s;
            },
            decode(n) {
               if (!(n instanceof Uint8Array))
                  throw new Error(
                     "checksum.decode: input should be Uint8Array",
                  );
               let r = n.slice(0, -t),
                  s = e(r).slice(0, t),
                  i = n.slice(-t);
               for (let o = 0; o < t; o++)
                  if (s[o] !== i[o]) throw new Error("Invalid checksum");
               return r;
            },
         };
      }
      N.utils = {
         alphabet: Ge,
         chain: Me,
         checksum: Lu,
         radix: Iu,
         radix2: ut,
         join: Ze,
         padding: Fn,
      };
      N.base16 = Me(ut(4), Ge("0123456789ABCDEF"), Ze(""));
      N.base32 = Me(
         ut(5),
         Ge("ABCDEFGHIJKLMNOPQRSTUVWXYZ234567"),
         Fn(5),
         Ze(""),
      );
      N.base32hex = Me(
         ut(5),
         Ge("0123456789ABCDEFGHIJKLMNOPQRSTUV"),
         Fn(5),
         Ze(""),
      );
      N.base32crockford = Me(
         ut(5),
         Ge("0123456789ABCDEFGHJKMNPQRSTVWXYZ"),
         Ze(""),
         Cu((t) => t.toUpperCase().replace(/O/g, "0").replace(/[IL]/g, "1")),
      );
      N.base64 = Me(
         ut(6),
         Ge("ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/"),
         Fn(6),
         Ze(""),
      );
      N.base64url = Me(
         ut(6),
         Ge("ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_"),
         Fn(6),
         Ze(""),
      );
      var so = (t) => Me(Iu(58), Ge(t), Ze(""));
      N.base58 = so(
         "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz",
      );
      N.base58flickr = so(
         "123456789abcdefghijkmnopqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ",
      );
      N.base58xrp = so(
         "rpshnaf39wBUDNEGHJKLM4PQRST7VWXYZ2bcdeCg65jkm8oFqi1tuvAxyz",
      );
      var _u = [0, 2, 3, 5, 6, 7, 9, 10, 11];
      N.base58xmr = {
         encode(t) {
            let e = "";
            for (let n = 0; n < t.length; n += 8) {
               let r = t.subarray(n, n + 8);
               e += N.base58.encode(r).padStart(_u[r.length], "1");
            }
            return e;
         },
         decode(t) {
            let e = [];
            for (let n = 0; n < t.length; n += 11) {
               let r = t.slice(n, n + 11),
                  s = _u.indexOf(r.length),
                  i = N.base58.decode(r);
               for (let o = 0; o < i.length - s; o++)
                  if (i[o] !== 0) throw new Error("base58xmr: wrong padding");
               e = e.concat(Array.from(i.slice(i.length - s)));
            }
            return Uint8Array.from(e);
         },
      };
      var z0 = (t) =>
         Me(
            Lu(4, (e) => t(t(e))),
            N.base58,
         );
      N.base58check = z0;
      var ro = Me(Ge("qpzry9x8gf2tvdw0s3jn54khce6mua7l"), Ze("")),
         Su = [996825010, 642813549, 513874426, 1027748829, 705979059];
      function zn(t) {
         let e = t >> 25,
            n = (t & 33554431) << 5;
         for (let r = 0; r < Su.length; r++)
            ((e >> r) & 1) === 1 && (n ^= Su[r]);
         return n;
      }
      function Ru(t, e, n = 1) {
         let r = t.length,
            s = 1;
         for (let i = 0; i < r; i++) {
            let o = t.charCodeAt(i);
            if (o < 33 || o > 126) throw new Error(`Invalid prefix (${t})`);
            s = zn(s) ^ (o >> 5);
         }
         s = zn(s);
         for (let i = 0; i < r; i++) s = zn(s) ^ (t.charCodeAt(i) & 31);
         for (let i of e) s = zn(s) ^ i;
         for (let i = 0; i < 6; i++) s = zn(s);
         return (s ^= n), ro.encode(no([s % 2 ** 30], 30, 5, !1));
      }
      function Uu(t) {
         let e = t === "bech32" ? 1 : 734539939,
            n = ut(5),
            r = n.decode,
            s = n.encode,
            i = Tu(r);
         function o(u, h, f = 90) {
            if (typeof u != "string")
               throw new Error(
                  `bech32.encode prefix should be string, not ${typeof u}`,
               );
            if (!Array.isArray(h) || (h.length && typeof h[0] != "number"))
               throw new Error(
                  `bech32.encode words should be array of numbers, not ${typeof h}`,
               );
            let g = u.length + 7 + h.length;
            if (f !== !1 && g > f)
               throw new TypeError(`Length ${g} exceeds limit ${f}`);
            return (u = u.toLowerCase()), `${u}1${ro.encode(h)}${Ru(u, h, e)}`;
         }
         function a(u, h = 90) {
            if (typeof u != "string")
               throw new Error(
                  `bech32.decode input should be string, not ${typeof u}`,
               );
            if (u.length < 8 || (h !== !1 && u.length > h))
               throw new TypeError(
                  `Wrong string length: ${u.length} (${u}). Expected (8..${h})`,
               );
            let f = u.toLowerCase();
            if (u !== f && u !== u.toUpperCase())
               throw new Error("String must be lowercase or uppercase");
            u = f;
            let g = u.lastIndexOf("1");
            if (g === 0 || g === -1)
               throw new Error(
                  'Letter "1" must be present between prefix and data only',
               );
            let y = u.slice(0, g),
               d = u.slice(g + 1);
            if (d.length < 6)
               throw new Error("Data must be at least 6 characters long");
            let p = ro.decode(d).slice(0, -6),
               b = Ru(y, p, e);
            if (!d.endsWith(b))
               throw new Error(`Invalid checksum in ${u}: expected "${b}"`);
            return { prefix: y, words: p };
         }
         let c = Tu(a);
         function l(u) {
            let { prefix: h, words: f } = a(u, !1);
            return { prefix: h, words: f, bytes: r(f) };
         }
         return {
            encode: o,
            decode: a,
            decodeToBytes: l,
            decodeUnsafe: c,
            fromWords: r,
            fromWordsUnsafe: i,
            toWords: s,
         };
      }
      N.bech32 = Uu("bech32");
      N.bech32m = Uu("bech32m");
      N.utf8 = {
         encode: (t) => new TextDecoder().decode(t),
         decode: (t) => new TextEncoder().encode(t),
      };
      N.hex = Me(
         ut(4),
         Ge("0123456789abcdef"),
         Ze(""),
         Cu((t) => {
            if (typeof t != "string" || t.length % 2)
               throw new TypeError(
                  `hex.decode: expected string, got ${typeof t} with length ${t.length}`,
               );
            return t.toLowerCase();
         }),
      );
      var jn = {
            utf8: N.utf8,
            hex: N.hex,
            base16: N.base16,
            base32: N.base32,
            base64: N.base64,
            base64url: N.base64url,
            base58: N.base58,
            base58xmr: N.base58xmr,
         },
         Pu = `Invalid encoding type. Available types: ${Object.keys(jn).join(", ")}`,
         j0 = (t, e) => {
            if (typeof t != "string" || !jn.hasOwnProperty(t))
               throw new TypeError(Pu);
            if (!(e instanceof Uint8Array))
               throw new TypeError("bytesToString() expects Uint8Array");
            return jn[t].encode(e);
         };
      N.bytesToString = j0;
      N.str = N.bytesToString;
      var F0 = (t, e) => {
         if (!jn.hasOwnProperty(t)) throw new TypeError(Pu);
         if (typeof e != "string")
            throw new TypeError("stringToBytes() expects string");
         return jn[t].decode(e);
      };
      N.stringToBytes = F0;
      N.bytes = N.stringToBytes;
   });
   var Du = pe((jv, Fu) => {
      var { bech32: Ye, hex: Ne, utf8: D0 } = Ou(),
         Mu = {
            bech32: "bc",
            pubKeyHash: 0,
            scriptHash: 5,
            validWitnessVersions: [0],
         },
         Nu = {
            bech32: "tb",
            pubKeyHash: 111,
            scriptHash: 196,
            validWitnessVersions: [0],
         },
         Hu = {
            bech32: "tbs",
            pubKeyHash: 111,
            scriptHash: 196,
            validWitnessVersions: [0],
         },
         qu = {
            bech32: "bcrt",
            pubKeyHash: 111,
            scriptHash: 196,
            validWitnessVersions: [0],
         },
         $u = {
            bech32: "sb",
            pubKeyHash: 63,
            scriptHash: 123,
            validWitnessVersions: [0],
         },
         as = [
            "option_data_loss_protect",
            "initial_routing_sync",
            "option_upfront_shutdown_script",
            "gossip_queries",
            "var_onion_optin",
            "gossip_queries_ex",
            "option_static_remotekey",
            "payment_secret",
            "basic_mpp",
            "option_support_large_channel",
         ],
         W0 = {
            m: BigInt(1e3),
            u: BigInt(1e6),
            n: BigInt(1e9),
            p: BigInt(1e12),
         },
         G0 = BigInt("2100000000000000000"),
         Vu = BigInt(1e11),
         io = {
            payment_hash: 1,
            payment_secret: 16,
            description: 13,
            payee: 19,
            description_hash: 23,
            expiry: 6,
            min_final_cltv_expiry: 24,
            fallback_address: 9,
            route_hint: 3,
            feature_bits: 5,
            metadata: 27,
         },
         zu = {};
      for (let t = 0, e = Object.keys(io); t < e.length; t++) {
         let n = e[t],
            r = io[e[t]].toString();
         zu[r] = n;
      }
      var Z0 = {
         1: (t) => Ne.encode(Ye.fromWordsUnsafe(t)),
         16: (t) => Ne.encode(Ye.fromWordsUnsafe(t)),
         13: (t) => D0.encode(Ye.fromWordsUnsafe(t)),
         19: (t) => Ne.encode(Ye.fromWordsUnsafe(t)),
         23: (t) => Ne.encode(Ye.fromWordsUnsafe(t)),
         27: (t) => Ne.encode(Ye.fromWordsUnsafe(t)),
         6: cs,
         24: cs,
         3: J0,
         5: Q0,
      };
      function Y0(t) {
         return (e) => ({
            tagCode: parseInt(t),
            words: Ye.encode("unknown", e, Number.MAX_SAFE_INTEGER),
         });
      }
      function cs(t) {
         return t.reverse().reduce((e, n, r) => e + n * Math.pow(32, r), 0);
      }
      function J0(t) {
         let e = [],
            n,
            r,
            s,
            i,
            o,
            a = Ye.fromWordsUnsafe(t);
         for (; a.length > 0; )
            (n = Ne.encode(a.slice(0, 33))),
               (r = Ne.encode(a.slice(33, 41))),
               (s = parseInt(Ne.encode(a.slice(41, 45)), 16)),
               (i = parseInt(Ne.encode(a.slice(45, 49)), 16)),
               (o = parseInt(Ne.encode(a.slice(49, 51)), 16)),
               (a = a.slice(51)),
               e.push({
                  pubkey: n,
                  short_channel_id: r,
                  fee_base_msat: s,
                  fee_proportional_millionths: i,
                  cltv_expiry_delta: o,
               });
         return e;
      }
      function Q0(t) {
         let e = t
            .slice()
            .reverse()
            .map((s) => [
               !!(s & 1),
               !!(s & 2),
               !!(s & 4),
               !!(s & 8),
               !!(s & 16),
            ])
            .reduce((s, i) => s.concat(i), []);
         for (; e.length < as.length * 2; ) e.push(!1);
         let n = {};
         as.forEach((s, i) => {
            let o;
            e[i * 2]
               ? (o = "required")
               : e[i * 2 + 1]
                 ? (o = "supported")
                 : (o = "unsupported"),
               (n[s] = o);
         });
         let r = e.slice(as.length * 2);
         return (
            (n.extra_bits = {
               start_bit: as.length * 2,
               bits: r,
               has_required: r.reduce(
                  (s, i, o) => (o % 2 !== 0 ? s || !1 : s || i),
                  !1,
               ),
            }),
            n
         );
      }
      function ju(t, e) {
         let n, r;
         if (t.slice(-1).match(/^[munp]$/))
            (n = t.slice(-1)), (r = t.slice(0, -1));
         else {
            if (t.slice(-1).match(/^[^munp0-9]$/))
               throw new Error("Not a valid multiplier for the amount");
            r = t;
         }
         if (!r.match(/^\d+$/))
            throw new Error("Not a valid human readable amount");
         let s = BigInt(r),
            i = n ? (s * Vu) / W0[n] : s * Vu;
         if ((n === "p" && s % BigInt(10) !== BigInt(0)) || i > G0)
            throw new Error("Amount is outside of valid range");
         return e ? i.toString() : i;
      }
      function X0(t, e) {
         if (typeof t != "string")
            throw new Error("Lightning Payment Request must be string");
         if (t.slice(0, 2).toLowerCase() !== "ln")
            throw new Error("Not a proper lightning payment request");
         let n = [],
            r = Ye.decode(t, Number.MAX_SAFE_INTEGER);
         t = t.toLowerCase();
         let s = r.prefix,
            i = r.words,
            o = t.slice(s.length + 1),
            a = i.slice(-104);
         i = i.slice(0, -104);
         let c = s.match(/^ln(\S+?)(\d*)([a-zA-Z]?)$/);
         if ((c && !c[2] && (c = s.match(/^ln(\S+)$/)), !c))
            throw new Error("Not a proper lightning payment request");
         n.push({ name: "lightning_network", letters: "ln" });
         let l = c[1],
            u;
         if (e) {
            if (
               e.bech32 === void 0 ||
               e.pubKeyHash === void 0 ||
               e.scriptHash === void 0 ||
               !Array.isArray(e.validWitnessVersions)
            )
               throw new Error("Invalid network");
            u = e;
         } else
            switch (l) {
               case Mu.bech32:
                  u = Mu;
                  break;
               case Nu.bech32:
                  u = Nu;
                  break;
               case Hu.bech32:
                  u = Hu;
                  break;
               case qu.bech32:
                  u = qu;
                  break;
               case $u.bech32:
                  u = $u;
                  break;
            }
         if (!u || u.bech32 !== l)
            throw new Error("Unknown coin bech32 prefix");
         n.push({ name: "coin_network", letters: l, value: u });
         let h = c[2],
            f;
         if (h) {
            let k = c[3];
            (f = ju(h + k, !0)),
               n.push({ name: "amount", letters: c[2] + c[3], value: f });
         } else f = null;
         n.push({ name: "separator", letters: "1" });
         let g = cs(i.slice(0, 7));
         (i = i.slice(7)),
            n.push({ name: "timestamp", letters: o.slice(0, 7), value: g }),
            (o = o.slice(7));
         let y, d, p, b;
         for (; i.length > 0; ) {
            let k = i[0].toString();
            (y = zu[k] || "unknown_tag"),
               (d = Z0[k] || Y0(k)),
               (i = i.slice(1)),
               (p = cs(i.slice(0, 2))),
               (i = i.slice(2)),
               (b = i.slice(0, p)),
               (i = i.slice(p)),
               n.push({
                  name: y,
                  tag: o[0],
                  letters: o.slice(0, 3 + p),
                  value: d(b),
               }),
               (o = o.slice(3 + p));
         }
         n.push({
            name: "signature",
            letters: o.slice(0, 104),
            value: Ne.encode(Ye.fromWordsUnsafe(a)),
         }),
            (o = o.slice(104)),
            n.push({ name: "checksum", letters: o });
         let w = {
            paymentRequest: t,
            sections: n,
            get expiry() {
               let k = n.find((B) => B.name === "expiry");
               if (k) return E("timestamp") + k.value;
            },
            get route_hints() {
               return n
                  .filter((k) => k.name === "route_hint")
                  .map((k) => k.value);
            },
         };
         for (let k in io)
            k !== "route_hint" &&
               Object.defineProperty(w, k, {
                  get() {
                     return E(k);
                  },
               });
         return w;
         function E(k) {
            let B = n.find((A) => A.name === k);
            return B ? B.value : void 0;
         }
      }
      Fu.exports = { decode: X0, hrpToMillisat: ju };
   });
   var al = W(qe(), 1),
      cl = W(Ee(), 1),
      ul = W(qe(), 1),
      ll = W(qe(), 1),
      dl = W(Ee(), 1);
   function Uo(t) {
      if (!Number.isSafeInteger(t) || t < 0)
         throw new Error(`Wrong positive integer: ${t}`);
   }
   function fs(t, ...e) {
      if (!(t instanceof Uint8Array)) throw new Error("Expected Uint8Array");
      if (e.length > 0 && !e.includes(t.length))
         throw new Error(
            `Expected Uint8Array of length ${e}, not of length=${t.length}`,
         );
   }
   function Po(t) {
      if (typeof t != "function" || typeof t.create != "function")
         throw new Error("Hash should be wrapped by utils.wrapConstructor");
      Uo(t.outputLen), Uo(t.blockLen);
   }
   function hn(t, e = !0) {
      if (t.destroyed) throw new Error("Hash instance has been destroyed");
      if (e && t.finished)
         throw new Error("Hash#digest() has already been called");
   }
   function Oo(t, e) {
      fs(t);
      let n = e.outputLen;
      if (t.length < n)
         throw new Error(
            `digestInto() expects output buffer of length at least ${n}`,
         );
   }
   var Xn =
      typeof globalThis == "object" && "crypto" in globalThis
         ? globalThis.crypto
         : void 0;
   var Mo = (t) => t instanceof Uint8Array;
   var Kn = (t) => new DataView(t.buffer, t.byteOffset, t.byteLength),
      Ce = (t, e) => (t << (32 - e)) | (t >>> e),
      Bh = new Uint8Array(new Uint32Array([287454020]).buffer)[0] === 68;
   if (!Bh) throw new Error("Non little-endian hardware is not supported");
   function Ih(t) {
      if (typeof t != "string")
         throw new Error(`utf8ToBytes expected string, got ${typeof t}`);
      return new Uint8Array(new TextEncoder().encode(t));
   }
   function Cn(t) {
      if ((typeof t == "string" && (t = Ih(t)), !Mo(t)))
         throw new Error(`expected Uint8Array, got ${typeof t}`);
      return t;
   }
   function No(...t) {
      let e = new Uint8Array(t.reduce((r, s) => r + s.length, 0)),
         n = 0;
      return (
         t.forEach((r) => {
            if (!Mo(r)) throw new Error("Uint8Array expected");
            e.set(r, n), (n += r.length);
         }),
         e
      );
   }
   var fn = class {
         clone() {
            return this._cloneInto();
         }
      },
      Fb = {}.toString;
   function Ho(t) {
      let e = (r) => t().update(Cn(r)).digest(),
         n = t();
      return (
         (e.outputLen = n.outputLen),
         (e.blockLen = n.blockLen),
         (e.create = () => t()),
         e
      );
   }
   function er(t = 32) {
      if (Xn && typeof Xn.getRandomValues == "function")
         return Xn.getRandomValues(new Uint8Array(t));
      throw new Error("crypto.getRandomValues must be defined");
   }
   function Lh(t, e, n, r) {
      if (typeof t.setBigUint64 == "function") return t.setBigUint64(e, n, r);
      let s = BigInt(32),
         i = BigInt(4294967295),
         o = Number((n >> s) & i),
         a = Number(n & i),
         c = r ? 4 : 0,
         l = r ? 0 : 4;
      t.setUint32(e + c, o, r), t.setUint32(e + l, a, r);
   }
   var tr = class extends fn {
      constructor(e, n, r, s) {
         super(),
            (this.blockLen = e),
            (this.outputLen = n),
            (this.padOffset = r),
            (this.isLE = s),
            (this.finished = !1),
            (this.length = 0),
            (this.pos = 0),
            (this.destroyed = !1),
            (this.buffer = new Uint8Array(e)),
            (this.view = Kn(this.buffer));
      }
      update(e) {
         hn(this);
         let { view: n, buffer: r, blockLen: s } = this;
         e = Cn(e);
         let i = e.length;
         for (let o = 0; o < i; ) {
            let a = Math.min(s - this.pos, i - o);
            if (a === s) {
               let c = Kn(e);
               for (; s <= i - o; o += s) this.process(c, o);
               continue;
            }
            r.set(e.subarray(o, o + a), this.pos),
               (this.pos += a),
               (o += a),
               this.pos === s && (this.process(n, 0), (this.pos = 0));
         }
         return (this.length += e.length), this.roundClean(), this;
      }
      digestInto(e) {
         hn(this), Oo(e, this), (this.finished = !0);
         let { buffer: n, view: r, blockLen: s, isLE: i } = this,
            { pos: o } = this;
         (n[o++] = 128),
            this.buffer.subarray(o).fill(0),
            this.padOffset > s - o && (this.process(r, 0), (o = 0));
         for (let h = o; h < s; h++) n[h] = 0;
         Lh(r, s - 8, BigInt(this.length * 8), i), this.process(r, 0);
         let a = Kn(e),
            c = this.outputLen;
         if (c % 4)
            throw new Error("_sha2: outputLen should be aligned to 32bit");
         let l = c / 4,
            u = this.get();
         if (l > u.length)
            throw new Error("_sha2: outputLen bigger than state");
         for (let h = 0; h < l; h++) a.setUint32(4 * h, u[h], i);
      }
      digest() {
         let { buffer: e, outputLen: n } = this;
         this.digestInto(e);
         let r = e.slice(0, n);
         return this.destroy(), r;
      }
      _cloneInto(e) {
         e || (e = new this.constructor()), e.set(...this.get());
         let {
            blockLen: n,
            buffer: r,
            length: s,
            finished: i,
            destroyed: o,
            pos: a,
         } = this;
         return (
            (e.length = s),
            (e.pos = a),
            (e.finished = i),
            (e.destroyed = o),
            s % n && e.buffer.set(r),
            e
         );
      }
   };
   var Uh = (t, e, n) => (t & e) ^ (~t & n),
      Ph = (t, e, n) => (t & e) ^ (t & n) ^ (e & n),
      Oh = new Uint32Array([
         1116352408, 1899447441, 3049323471, 3921009573, 961987163, 1508970993,
         2453635748, 2870763221, 3624381080, 310598401, 607225278, 1426881987,
         1925078388, 2162078206, 2614888103, 3248222580, 3835390401, 4022224774,
         264347078, 604807628, 770255983, 1249150122, 1555081692, 1996064986,
         2554220882, 2821834349, 2952996808, 3210313671, 3336571891, 3584528711,
         113926993, 338241895, 666307205, 773529912, 1294757372, 1396182291,
         1695183700, 1986661051, 2177026350, 2456956037, 2730485921, 2820302411,
         3259730800, 3345764771, 3516065817, 3600352804, 4094571909, 275423344,
         430227734, 506948616, 659060556, 883997877, 958139571, 1322822218,
         1537002063, 1747873779, 1955562222, 2024104815, 2227730452, 2361852424,
         2428436474, 2756734187, 3204031479, 3329325298,
      ]),
      pt = new Uint32Array([
         1779033703, 3144134277, 1013904242, 2773480762, 1359893119, 2600822924,
         528734635, 1541459225,
      ]),
      gt = new Uint32Array(64),
      ds = class extends tr {
         constructor() {
            super(64, 32, 8, !1),
               (this.A = pt[0] | 0),
               (this.B = pt[1] | 0),
               (this.C = pt[2] | 0),
               (this.D = pt[3] | 0),
               (this.E = pt[4] | 0),
               (this.F = pt[5] | 0),
               (this.G = pt[6] | 0),
               (this.H = pt[7] | 0);
         }
         get() {
            let { A: e, B: n, C: r, D: s, E: i, F: o, G: a, H: c } = this;
            return [e, n, r, s, i, o, a, c];
         }
         set(e, n, r, s, i, o, a, c) {
            (this.A = e | 0),
               (this.B = n | 0),
               (this.C = r | 0),
               (this.D = s | 0),
               (this.E = i | 0),
               (this.F = o | 0),
               (this.G = a | 0),
               (this.H = c | 0);
         }
         process(e, n) {
            for (let h = 0; h < 16; h++, n += 4) gt[h] = e.getUint32(n, !1);
            for (let h = 16; h < 64; h++) {
               let f = gt[h - 15],
                  g = gt[h - 2],
                  y = Ce(f, 7) ^ Ce(f, 18) ^ (f >>> 3),
                  d = Ce(g, 17) ^ Ce(g, 19) ^ (g >>> 10);
               gt[h] = (d + gt[h - 7] + y + gt[h - 16]) | 0;
            }
            let { A: r, B: s, C: i, D: o, E: a, F: c, G: l, H: u } = this;
            for (let h = 0; h < 64; h++) {
               let f = Ce(a, 6) ^ Ce(a, 11) ^ Ce(a, 25),
                  g = (u + f + Uh(a, c, l) + Oh[h] + gt[h]) | 0,
                  d = ((Ce(r, 2) ^ Ce(r, 13) ^ Ce(r, 22)) + Ph(r, s, i)) | 0;
               (u = l),
                  (l = c),
                  (c = a),
                  (a = (o + g) | 0),
                  (o = i),
                  (i = s),
                  (s = r),
                  (r = (g + d) | 0);
            }
            (r = (r + this.A) | 0),
               (s = (s + this.B) | 0),
               (i = (i + this.C) | 0),
               (o = (o + this.D) | 0),
               (a = (a + this.E) | 0),
               (c = (c + this.F) | 0),
               (l = (l + this.G) | 0),
               (u = (u + this.H) | 0),
               this.set(r, s, i, o, a, c, l, u);
         }
         roundClean() {
            gt.fill(0);
         }
         destroy() {
            this.set(0, 0, 0, 0, 0, 0, 0, 0), this.buffer.fill(0);
         }
      };
   var nr = Ho(() => new ds());
   var bs = {};
   go(bs, {
      bitGet: () => zh,
      bitLen: () => Vh,
      bitMask: () => Bn,
      bitSet: () => jh,
      bytesToHex: () => zt,
      bytesToNumberBE: () => fe,
      bytesToNumberLE: () => ir,
      concatBytes: () => Xe,
      createHmacDrbg: () => ys,
      ensureBytes: () => te,
      equalBytes: () => qh,
      hexToBytes: () => jt,
      hexToNumber: () => gs,
      numberToBytesBE: () => Be,
      numberToBytesLE: () => or,
      numberToHexUnpadded: () => Vo,
      numberToVarBytesBE: () => Hh,
      utf8ToBytes: () => $h,
      validateObject: () => yt,
   });
   var $o = BigInt(0),
      rr = BigInt(1),
      Mh = BigInt(2),
      sr = (t) => t instanceof Uint8Array,
      Nh = Array.from({ length: 256 }, (t, e) =>
         e.toString(16).padStart(2, "0"),
      );
   function zt(t) {
      if (!sr(t)) throw new Error("Uint8Array expected");
      let e = "";
      for (let n = 0; n < t.length; n++) e += Nh[t[n]];
      return e;
   }
   function Vo(t) {
      let e = t.toString(16);
      return e.length & 1 ? `0${e}` : e;
   }
   function gs(t) {
      if (typeof t != "string")
         throw new Error("hex string expected, got " + typeof t);
      return BigInt(t === "" ? "0" : `0x${t}`);
   }
   function jt(t) {
      if (typeof t != "string")
         throw new Error("hex string expected, got " + typeof t);
      let e = t.length;
      if (e % 2)
         throw new Error(
            "padded hex string expected, got unpadded hex of length " + e,
         );
      let n = new Uint8Array(e / 2);
      for (let r = 0; r < n.length; r++) {
         let s = r * 2,
            i = t.slice(s, s + 2),
            o = Number.parseInt(i, 16);
         if (Number.isNaN(o) || o < 0) throw new Error("Invalid byte sequence");
         n[r] = o;
      }
      return n;
   }
   function fe(t) {
      return gs(zt(t));
   }
   function ir(t) {
      if (!sr(t)) throw new Error("Uint8Array expected");
      return gs(zt(Uint8Array.from(t).reverse()));
   }
   function Be(t, e) {
      return jt(t.toString(16).padStart(e * 2, "0"));
   }
   function or(t, e) {
      return Be(t, e).reverse();
   }
   function Hh(t) {
      return jt(Vo(t));
   }
   function te(t, e, n) {
      let r;
      if (typeof e == "string")
         try {
            r = jt(e);
         } catch (i) {
            throw new Error(
               `${t} must be valid hex string, got "${e}". Cause: ${i}`,
            );
         }
      else if (sr(e)) r = Uint8Array.from(e);
      else throw new Error(`${t} must be hex string or Uint8Array`);
      let s = r.length;
      if (typeof n == "number" && s !== n)
         throw new Error(`${t} expected ${n} bytes, got ${s}`);
      return r;
   }
   function Xe(...t) {
      let e = new Uint8Array(t.reduce((r, s) => r + s.length, 0)),
         n = 0;
      return (
         t.forEach((r) => {
            if (!sr(r)) throw new Error("Uint8Array expected");
            e.set(r, n), (n += r.length);
         }),
         e
      );
   }
   function qh(t, e) {
      if (t.length !== e.length) return !1;
      for (let n = 0; n < t.length; n++) if (t[n] !== e[n]) return !1;
      return !0;
   }
   function $h(t) {
      if (typeof t != "string")
         throw new Error(`utf8ToBytes expected string, got ${typeof t}`);
      return new Uint8Array(new TextEncoder().encode(t));
   }
   function Vh(t) {
      let e;
      for (e = 0; t > $o; t >>= rr, e += 1);
      return e;
   }
   function zh(t, e) {
      return (t >> BigInt(e)) & rr;
   }
   var jh = (t, e, n) => t | ((n ? rr : $o) << BigInt(e)),
      Bn = (t) => (Mh << BigInt(t - 1)) - rr,
      ps = (t) => new Uint8Array(t),
      qo = (t) => Uint8Array.from(t);
   function ys(t, e, n) {
      if (typeof t != "number" || t < 2)
         throw new Error("hashLen must be a number");
      if (typeof e != "number" || e < 2)
         throw new Error("qByteLen must be a number");
      if (typeof n != "function") throw new Error("hmacFn must be a function");
      let r = ps(t),
         s = ps(t),
         i = 0,
         o = () => {
            r.fill(1), s.fill(0), (i = 0);
         },
         a = (...h) => n(s, r, ...h),
         c = (h = ps()) => {
            (s = a(qo([0]), h)),
               (r = a()),
               h.length !== 0 && ((s = a(qo([1]), h)), (r = a()));
         },
         l = () => {
            if (i++ >= 1e3) throw new Error("drbg: tried 1000 values");
            let h = 0,
               f = [];
            for (; h < e; ) {
               r = a();
               let g = r.slice();
               f.push(g), (h += r.length);
            }
            return Xe(...f);
         };
      return (h, f) => {
         o(), c(h);
         let g;
         for (; !(g = f(l())); ) c();
         return o(), g;
      };
   }
   var Fh = {
      bigint: (t) => typeof t == "bigint",
      function: (t) => typeof t == "function",
      boolean: (t) => typeof t == "boolean",
      string: (t) => typeof t == "string",
      stringOrUint8Array: (t) =>
         typeof t == "string" || t instanceof Uint8Array,
      isSafeInteger: (t) => Number.isSafeInteger(t),
      array: (t) => Array.isArray(t),
      field: (t, e) => e.Fp.isValid(t),
      hash: (t) => typeof t == "function" && Number.isSafeInteger(t.outputLen),
   };
   function yt(t, e, n = {}) {
      let r = (s, i, o) => {
         let a = Fh[i];
         if (typeof a != "function")
            throw new Error(`Invalid validator "${i}", expected function`);
         let c = t[s];
         if (!(o && c === void 0) && !a(c, t))
            throw new Error(
               `Invalid param ${String(s)}=${c} (${typeof c}), expected ${i}`,
            );
      };
      for (let [s, i] of Object.entries(e)) r(s, i, !1);
      for (let [s, i] of Object.entries(n)) r(s, i, !0);
      return t;
   }
   var re = BigInt(0),
      G = BigInt(1),
      Ft = BigInt(2),
      Dh = BigInt(3),
      ms = BigInt(4),
      zo = BigInt(5),
      jo = BigInt(8),
      Wh = BigInt(9),
      Gh = BigInt(16);
   function ne(t, e) {
      let n = t % e;
      return n >= re ? n : e + n;
   }
   function Zh(t, e, n) {
      if (n <= re || e < re) throw new Error("Expected power/modulo > 0");
      if (n === G) return re;
      let r = G;
      for (; e > re; ) e & G && (r = (r * t) % n), (t = (t * t) % n), (e >>= G);
      return r;
   }
   function me(t, e, n) {
      let r = t;
      for (; e-- > re; ) (r *= r), (r %= n);
      return r;
   }
   function ar(t, e) {
      if (t === re || e <= re)
         throw new Error(
            `invert: expected positive integers, got n=${t} mod=${e}`,
         );
      let n = ne(t, e),
         r = e,
         s = re,
         i = G,
         o = G,
         a = re;
      for (; n !== re; ) {
         let l = r / n,
            u = r % n,
            h = s - o * l,
            f = i - a * l;
         (r = n), (n = u), (s = o), (i = a), (o = h), (a = f);
      }
      if (r !== G) throw new Error("invert: does not exist");
      return ne(s, e);
   }
   function Yh(t) {
      let e = (t - G) / Ft,
         n,
         r,
         s;
      for (n = t - G, r = 0; n % Ft === re; n /= Ft, r++);
      for (s = Ft; s < t && Zh(s, e, t) !== t - G; s++);
      if (r === 1) {
         let o = (t + G) / ms;
         return function (c, l) {
            let u = c.pow(l, o);
            if (!c.eql(c.sqr(u), l)) throw new Error("Cannot find square root");
            return u;
         };
      }
      let i = (n + G) / Ft;
      return function (a, c) {
         if (a.pow(c, e) === a.neg(a.ONE))
            throw new Error("Cannot find square root");
         let l = r,
            u = a.pow(a.mul(a.ONE, s), n),
            h = a.pow(c, i),
            f = a.pow(c, n);
         for (; !a.eql(f, a.ONE); ) {
            if (a.eql(f, a.ZERO)) return a.ZERO;
            let g = 1;
            for (let d = a.sqr(f); g < l && !a.eql(d, a.ONE); g++) d = a.sqr(d);
            let y = a.pow(u, G << BigInt(l - g - 1));
            (u = a.sqr(y)), (h = a.mul(h, y)), (f = a.mul(f, u)), (l = g);
         }
         return h;
      };
   }
   function Jh(t) {
      if (t % ms === Dh) {
         let e = (t + G) / ms;
         return function (r, s) {
            let i = r.pow(s, e);
            if (!r.eql(r.sqr(i), s)) throw new Error("Cannot find square root");
            return i;
         };
      }
      if (t % jo === zo) {
         let e = (t - zo) / jo;
         return function (r, s) {
            let i = r.mul(s, Ft),
               o = r.pow(i, e),
               a = r.mul(s, o),
               c = r.mul(r.mul(a, Ft), o),
               l = r.mul(a, r.sub(c, r.ONE));
            if (!r.eql(r.sqr(l), s)) throw new Error("Cannot find square root");
            return l;
         };
      }
      return t % Gh, Yh(t);
   }
   var Qh = [
      "create",
      "isValid",
      "is0",
      "neg",
      "inv",
      "sqrt",
      "sqr",
      "eql",
      "add",
      "sub",
      "mul",
      "pow",
      "div",
      "addN",
      "subN",
      "mulN",
      "sqrN",
   ];
   function ws(t) {
      let e = {
            ORDER: "bigint",
            MASK: "bigint",
            BYTES: "isSafeInteger",
            BITS: "isSafeInteger",
         },
         n = Qh.reduce((r, s) => ((r[s] = "function"), r), e);
      return yt(t, n);
   }
   function Xh(t, e, n) {
      if (n < re) throw new Error("Expected power > 0");
      if (n === re) return t.ONE;
      if (n === G) return e;
      let r = t.ONE,
         s = e;
      for (; n > re; ) n & G && (r = t.mul(r, s)), (s = t.sqr(s)), (n >>= G);
      return r;
   }
   function Kh(t, e) {
      let n = new Array(e.length),
         r = e.reduce(
            (i, o, a) => (t.is0(o) ? i : ((n[a] = i), t.mul(i, o))),
            t.ONE,
         ),
         s = t.inv(r);
      return (
         e.reduceRight(
            (i, o, a) =>
               t.is0(o) ? i : ((n[a] = t.mul(i, n[a])), t.mul(i, o)),
            s,
         ),
         n
      );
   }
   function vs(t, e) {
      let n = e !== void 0 ? e : t.toString(2).length,
         r = Math.ceil(n / 8);
      return { nBitLength: n, nByteLength: r };
   }
   function Fo(t, e, n = !1, r = {}) {
      if (t <= re) throw new Error(`Expected Field ORDER > 0, got ${t}`);
      let { nBitLength: s, nByteLength: i } = vs(t, e);
      if (i > 2048)
         throw new Error("Field lengths over 2048 bytes are not supported");
      let o = Jh(t),
         a = Object.freeze({
            ORDER: t,
            BITS: s,
            BYTES: i,
            MASK: Bn(s),
            ZERO: re,
            ONE: G,
            create: (c) => ne(c, t),
            isValid: (c) => {
               if (typeof c != "bigint")
                  throw new Error(
                     `Invalid field element: expected bigint, got ${typeof c}`,
                  );
               return re <= c && c < t;
            },
            is0: (c) => c === re,
            isOdd: (c) => (c & G) === G,
            neg: (c) => ne(-c, t),
            eql: (c, l) => c === l,
            sqr: (c) => ne(c * c, t),
            add: (c, l) => ne(c + l, t),
            sub: (c, l) => ne(c - l, t),
            mul: (c, l) => ne(c * l, t),
            pow: (c, l) => Xh(a, c, l),
            div: (c, l) => ne(c * ar(l, t), t),
            sqrN: (c) => c * c,
            addN: (c, l) => c + l,
            subN: (c, l) => c - l,
            mulN: (c, l) => c * l,
            inv: (c) => ar(c, t),
            sqrt: r.sqrt || ((c) => o(a, c)),
            invertBatch: (c) => Kh(a, c),
            cmov: (c, l, u) => (u ? l : c),
            toBytes: (c) => (n ? or(c, i) : Be(c, i)),
            fromBytes: (c) => {
               if (c.length !== i)
                  throw new Error(
                     `Fp.fromBytes: expected ${i}, got ${c.length}`,
                  );
               return n ? ir(c) : fe(c);
            },
         });
      return Object.freeze(a);
   }
   function Do(t) {
      if (typeof t != "bigint") throw new Error("field order must be bigint");
      let e = t.toString(2).length;
      return Math.ceil(e / 8);
   }
   function xs(t) {
      let e = Do(t);
      return e + Math.ceil(e / 2);
   }
   function Wo(t, e, n = !1) {
      let r = t.length,
         s = Do(e),
         i = xs(e);
      if (r < 16 || r < i || r > 1024)
         throw new Error(`expected ${i}-1024 bytes of input, got ${r}`);
      let o = n ? fe(t) : ir(t),
         a = ne(o, e - G) + G;
      return n ? or(a, s) : Be(a, s);
   }
   var tf = BigInt(0),
      Es = BigInt(1);
   function Go(t, e) {
      let n = (s, i) => {
            let o = i.negate();
            return s ? o : i;
         },
         r = (s) => {
            let i = Math.ceil(e / s) + 1,
               o = 2 ** (s - 1);
            return { windows: i, windowSize: o };
         };
      return {
         constTimeNegate: n,
         unsafeLadder(s, i) {
            let o = t.ZERO,
               a = s;
            for (; i > tf; )
               i & Es && (o = o.add(a)), (a = a.double()), (i >>= Es);
            return o;
         },
         precomputeWindow(s, i) {
            let { windows: o, windowSize: a } = r(i),
               c = [],
               l = s,
               u = l;
            for (let h = 0; h < o; h++) {
               (u = l), c.push(u);
               for (let f = 1; f < a; f++) (u = u.add(l)), c.push(u);
               l = u.double();
            }
            return c;
         },
         wNAF(s, i, o) {
            let { windows: a, windowSize: c } = r(s),
               l = t.ZERO,
               u = t.BASE,
               h = BigInt(2 ** s - 1),
               f = 2 ** s,
               g = BigInt(s);
            for (let y = 0; y < a; y++) {
               let d = y * c,
                  p = Number(o & h);
               (o >>= g), p > c && ((p -= f), (o += Es));
               let b = d,
                  w = d + Math.abs(p) - 1,
                  E = y % 2 !== 0,
                  k = p < 0;
               p === 0 ? (u = u.add(n(E, i[b]))) : (l = l.add(n(k, i[w])));
            }
            return { p: l, f: u };
         },
         wNAFCached(s, i, o, a) {
            let c = s._WINDOW_SIZE || 1,
               l = i.get(s);
            return (
               l ||
                  ((l = this.precomputeWindow(s, c)),
                  c !== 1 && i.set(s, a(l))),
               this.wNAF(c, l, o)
            );
         },
      };
   }
   function ks(t) {
      return (
         ws(t.Fp),
         yt(
            t,
            { n: "bigint", h: "bigint", Gx: "field", Gy: "field" },
            { nBitLength: "isSafeInteger", nByteLength: "isSafeInteger" },
         ),
         Object.freeze({ ...vs(t.n, t.nBitLength), ...t, p: t.Fp.ORDER })
      );
   }
   function nf(t) {
      let e = ks(t);
      yt(
         e,
         { a: "field", b: "field" },
         {
            allowedPrivateKeyLengths: "array",
            wrapPrivateKey: "boolean",
            isTorsionFree: "function",
            clearCofactor: "function",
            allowInfinityPoint: "boolean",
            fromBytes: "function",
            toBytes: "function",
         },
      );
      let { endo: n, Fp: r, a: s } = e;
      if (n) {
         if (!r.eql(s, r.ZERO))
            throw new Error(
               "Endomorphism can only be defined for Koblitz curves that have a=0",
            );
         if (
            typeof n != "object" ||
            typeof n.beta != "bigint" ||
            typeof n.splitScalar != "function"
         )
            throw new Error(
               "Expected endomorphism with beta: bigint and splitScalar: function",
            );
      }
      return Object.freeze({ ...e });
   }
   var { bytesToNumberBE: rf, hexToBytes: sf } = bs,
      Dt = {
         Err: class extends Error {
            constructor(e = "") {
               super(e);
            }
         },
         _parseInt(t) {
            let { Err: e } = Dt;
            if (t.length < 2 || t[0] !== 2)
               throw new e("Invalid signature integer tag");
            let n = t[1],
               r = t.subarray(2, n + 2);
            if (!n || r.length !== n)
               throw new e("Invalid signature integer: wrong length");
            if (r[0] & 128) throw new e("Invalid signature integer: negative");
            if (r[0] === 0 && !(r[1] & 128))
               throw new e(
                  "Invalid signature integer: unnecessary leading zero",
               );
            return { d: rf(r), l: t.subarray(n + 2) };
         },
         toSig(t) {
            let { Err: e } = Dt,
               n = typeof t == "string" ? sf(t) : t;
            if (!(n instanceof Uint8Array)) throw new Error("ui8a expected");
            let r = n.length;
            if (r < 2 || n[0] != 48) throw new e("Invalid signature tag");
            if (n[1] !== r - 2)
               throw new e("Invalid signature: incorrect length");
            let { d: s, l: i } = Dt._parseInt(n.subarray(2)),
               { d: o, l: a } = Dt._parseInt(i);
            if (a.length)
               throw new e("Invalid signature: left bytes after parsing");
            return { r: s, s: o };
         },
         hexFromSig(t) {
            let e = (l) => (Number.parseInt(l[0], 16) & 8 ? "00" + l : l),
               n = (l) => {
                  let u = l.toString(16);
                  return u.length & 1 ? `0${u}` : u;
               },
               r = e(n(t.s)),
               s = e(n(t.r)),
               i = r.length / 2,
               o = s.length / 2,
               a = n(i),
               c = n(o);
            return `30${n(o + i + 4)}02${c}${s}02${a}${r}`;
         },
      },
      Ke = BigInt(0),
      ke = BigInt(1),
      im = BigInt(2),
      Zo = BigInt(3),
      om = BigInt(4);
   function of(t) {
      let e = nf(t),
         { Fp: n } = e,
         r =
            e.toBytes ||
            ((y, d, p) => {
               let b = d.toAffine();
               return Xe(Uint8Array.from([4]), n.toBytes(b.x), n.toBytes(b.y));
            }),
         s =
            e.fromBytes ||
            ((y) => {
               let d = y.subarray(1),
                  p = n.fromBytes(d.subarray(0, n.BYTES)),
                  b = n.fromBytes(d.subarray(n.BYTES, 2 * n.BYTES));
               return { x: p, y: b };
            });
      function i(y) {
         let { a: d, b: p } = e,
            b = n.sqr(y),
            w = n.mul(b, y);
         return n.add(n.add(w, n.mul(y, d)), p);
      }
      if (!n.eql(n.sqr(e.Gy), i(e.Gx)))
         throw new Error("bad generator point: equation left != right");
      function o(y) {
         return typeof y == "bigint" && Ke < y && y < e.n;
      }
      function a(y) {
         if (!o(y))
            throw new Error("Expected valid bigint: 0 < bigint < curve.n");
      }
      function c(y) {
         let {
            allowedPrivateKeyLengths: d,
            nByteLength: p,
            wrapPrivateKey: b,
            n: w,
         } = e;
         if (d && typeof y != "bigint") {
            if (
               (y instanceof Uint8Array && (y = zt(y)),
               typeof y != "string" || !d.includes(y.length))
            )
               throw new Error("Invalid key");
            y = y.padStart(p * 2, "0");
         }
         let E;
         try {
            E = typeof y == "bigint" ? y : fe(te("private key", y, p));
         } catch {
            throw new Error(
               `private key must be ${p} bytes, hex or bigint, not ${typeof y}`,
            );
         }
         return b && (E = ne(E, w)), a(E), E;
      }
      let l = new Map();
      function u(y) {
         if (!(y instanceof h)) throw new Error("ProjectivePoint expected");
      }
      class h {
         constructor(d, p, b) {
            if (
               ((this.px = d),
               (this.py = p),
               (this.pz = b),
               d == null || !n.isValid(d))
            )
               throw new Error("x required");
            if (p == null || !n.isValid(p)) throw new Error("y required");
            if (b == null || !n.isValid(b)) throw new Error("z required");
         }
         static fromAffine(d) {
            let { x: p, y: b } = d || {};
            if (!d || !n.isValid(p) || !n.isValid(b))
               throw new Error("invalid affine point");
            if (d instanceof h) throw new Error("projective point not allowed");
            let w = (E) => n.eql(E, n.ZERO);
            return w(p) && w(b) ? h.ZERO : new h(p, b, n.ONE);
         }
         get x() {
            return this.toAffine().x;
         }
         get y() {
            return this.toAffine().y;
         }
         static normalizeZ(d) {
            let p = n.invertBatch(d.map((b) => b.pz));
            return d.map((b, w) => b.toAffine(p[w])).map(h.fromAffine);
         }
         static fromHex(d) {
            let p = h.fromAffine(s(te("pointHex", d)));
            return p.assertValidity(), p;
         }
         static fromPrivateKey(d) {
            return h.BASE.multiply(c(d));
         }
         _setWindowSize(d) {
            (this._WINDOW_SIZE = d), l.delete(this);
         }
         assertValidity() {
            if (this.is0()) {
               if (e.allowInfinityPoint && !n.is0(this.py)) return;
               throw new Error("bad point: ZERO");
            }
            let { x: d, y: p } = this.toAffine();
            if (!n.isValid(d) || !n.isValid(p))
               throw new Error("bad point: x or y not FE");
            let b = n.sqr(p),
               w = i(d);
            if (!n.eql(b, w))
               throw new Error("bad point: equation left != right");
            if (!this.isTorsionFree())
               throw new Error("bad point: not in prime-order subgroup");
         }
         hasEvenY() {
            let { y: d } = this.toAffine();
            if (n.isOdd) return !n.isOdd(d);
            throw new Error("Field doesn't support isOdd");
         }
         equals(d) {
            u(d);
            let { px: p, py: b, pz: w } = this,
               { px: E, py: k, pz: B } = d,
               A = n.eql(n.mul(p, B), n.mul(E, w)),
               T = n.eql(n.mul(b, B), n.mul(k, w));
            return A && T;
         }
         negate() {
            return new h(this.px, n.neg(this.py), this.pz);
         }
         double() {
            let { a: d, b: p } = e,
               b = n.mul(p, Zo),
               { px: w, py: E, pz: k } = this,
               B = n.ZERO,
               A = n.ZERO,
               T = n.ZERO,
               _ = n.mul(w, w),
               I = n.mul(E, E),
               P = n.mul(k, k),
               U = n.mul(w, E);
            return (
               (U = n.add(U, U)),
               (T = n.mul(w, k)),
               (T = n.add(T, T)),
               (B = n.mul(d, T)),
               (A = n.mul(b, P)),
               (A = n.add(B, A)),
               (B = n.sub(I, A)),
               (A = n.add(I, A)),
               (A = n.mul(B, A)),
               (B = n.mul(U, B)),
               (T = n.mul(b, T)),
               (P = n.mul(d, P)),
               (U = n.sub(_, P)),
               (U = n.mul(d, U)),
               (U = n.add(U, T)),
               (T = n.add(_, _)),
               (_ = n.add(T, _)),
               (_ = n.add(_, P)),
               (_ = n.mul(_, U)),
               (A = n.add(A, _)),
               (P = n.mul(E, k)),
               (P = n.add(P, P)),
               (_ = n.mul(P, U)),
               (B = n.sub(B, _)),
               (T = n.mul(P, I)),
               (T = n.add(T, T)),
               (T = n.add(T, T)),
               new h(B, A, T)
            );
         }
         add(d) {
            u(d);
            let { px: p, py: b, pz: w } = this,
               { px: E, py: k, pz: B } = d,
               A = n.ZERO,
               T = n.ZERO,
               _ = n.ZERO,
               I = e.a,
               P = n.mul(e.b, Zo),
               U = n.mul(p, E),
               O = n.mul(b, k),
               q = n.mul(w, B),
               C = n.add(p, b),
               v = n.add(E, k);
            (C = n.mul(C, v)),
               (v = n.add(U, O)),
               (C = n.sub(C, v)),
               (v = n.add(p, w));
            let x = n.add(E, B);
            return (
               (v = n.mul(v, x)),
               (x = n.add(U, q)),
               (v = n.sub(v, x)),
               (x = n.add(b, w)),
               (A = n.add(k, B)),
               (x = n.mul(x, A)),
               (A = n.add(O, q)),
               (x = n.sub(x, A)),
               (_ = n.mul(I, v)),
               (A = n.mul(P, q)),
               (_ = n.add(A, _)),
               (A = n.sub(O, _)),
               (_ = n.add(O, _)),
               (T = n.mul(A, _)),
               (O = n.add(U, U)),
               (O = n.add(O, U)),
               (q = n.mul(I, q)),
               (v = n.mul(P, v)),
               (O = n.add(O, q)),
               (q = n.sub(U, q)),
               (q = n.mul(I, q)),
               (v = n.add(v, q)),
               (U = n.mul(O, v)),
               (T = n.add(T, U)),
               (U = n.mul(x, v)),
               (A = n.mul(C, A)),
               (A = n.sub(A, U)),
               (U = n.mul(C, O)),
               (_ = n.mul(x, _)),
               (_ = n.add(_, U)),
               new h(A, T, _)
            );
         }
         subtract(d) {
            return this.add(d.negate());
         }
         is0() {
            return this.equals(h.ZERO);
         }
         wNAF(d) {
            return g.wNAFCached(this, l, d, (p) => {
               let b = n.invertBatch(p.map((w) => w.pz));
               return p.map((w, E) => w.toAffine(b[E])).map(h.fromAffine);
            });
         }
         multiplyUnsafe(d) {
            let p = h.ZERO;
            if (d === Ke) return p;
            if ((a(d), d === ke)) return this;
            let { endo: b } = e;
            if (!b) return g.unsafeLadder(this, d);
            let { k1neg: w, k1: E, k2neg: k, k2: B } = b.splitScalar(d),
               A = p,
               T = p,
               _ = this;
            for (; E > Ke || B > Ke; )
               E & ke && (A = A.add(_)),
                  B & ke && (T = T.add(_)),
                  (_ = _.double()),
                  (E >>= ke),
                  (B >>= ke);
            return (
               w && (A = A.negate()),
               k && (T = T.negate()),
               (T = new h(n.mul(T.px, b.beta), T.py, T.pz)),
               A.add(T)
            );
         }
         multiply(d) {
            a(d);
            let p = d,
               b,
               w,
               { endo: E } = e;
            if (E) {
               let { k1neg: k, k1: B, k2neg: A, k2: T } = E.splitScalar(p),
                  { p: _, f: I } = this.wNAF(B),
                  { p: P, f: U } = this.wNAF(T);
               (_ = g.constTimeNegate(k, _)),
                  (P = g.constTimeNegate(A, P)),
                  (P = new h(n.mul(P.px, E.beta), P.py, P.pz)),
                  (b = _.add(P)),
                  (w = I.add(U));
            } else {
               let { p: k, f: B } = this.wNAF(p);
               (b = k), (w = B);
            }
            return h.normalizeZ([b, w])[0];
         }
         multiplyAndAddUnsafe(d, p, b) {
            let w = h.BASE,
               E = (B, A) =>
                  A === Ke || A === ke || !B.equals(w)
                     ? B.multiplyUnsafe(A)
                     : B.multiply(A),
               k = E(this, p).add(E(d, b));
            return k.is0() ? void 0 : k;
         }
         toAffine(d) {
            let { px: p, py: b, pz: w } = this,
               E = this.is0();
            d == null && (d = E ? n.ONE : n.inv(w));
            let k = n.mul(p, d),
               B = n.mul(b, d),
               A = n.mul(w, d);
            if (E) return { x: n.ZERO, y: n.ZERO };
            if (!n.eql(A, n.ONE)) throw new Error("invZ was invalid");
            return { x: k, y: B };
         }
         isTorsionFree() {
            let { h: d, isTorsionFree: p } = e;
            if (d === ke) return !0;
            if (p) return p(h, this);
            throw new Error(
               "isTorsionFree() has not been declared for the elliptic curve",
            );
         }
         clearCofactor() {
            let { h: d, clearCofactor: p } = e;
            return d === ke ? this : p ? p(h, this) : this.multiplyUnsafe(e.h);
         }
         toRawBytes(d = !0) {
            return this.assertValidity(), r(h, this, d);
         }
         toHex(d = !0) {
            return zt(this.toRawBytes(d));
         }
      }
      (h.BASE = new h(e.Gx, e.Gy, n.ONE)),
         (h.ZERO = new h(n.ZERO, n.ONE, n.ZERO));
      let f = e.nBitLength,
         g = Go(h, e.endo ? Math.ceil(f / 2) : f);
      return {
         CURVE: e,
         ProjectivePoint: h,
         normPrivateKeyToScalar: c,
         weierstrassEquation: i,
         isWithinCurveOrder: o,
      };
   }
   function af(t) {
      let e = ks(t);
      return (
         yt(
            e,
            { hash: "hash", hmac: "function", randomBytes: "function" },
            {
               bits2int: "function",
               bits2int_modN: "function",
               lowS: "boolean",
            },
         ),
         Object.freeze({ lowS: !0, ...e })
      );
   }
   function Yo(t) {
      let e = af(t),
         { Fp: n, n: r } = e,
         s = n.BYTES + 1,
         i = 2 * n.BYTES + 1;
      function o(v) {
         return Ke < v && v < n.ORDER;
      }
      function a(v) {
         return ne(v, r);
      }
      function c(v) {
         return ar(v, r);
      }
      let {
            ProjectivePoint: l,
            normPrivateKeyToScalar: u,
            weierstrassEquation: h,
            isWithinCurveOrder: f,
         } = of({
            ...e,
            toBytes(v, x, R) {
               let L = x.toAffine(),
                  S = n.toBytes(L.x),
                  H = Xe;
               return R
                  ? H(Uint8Array.from([x.hasEvenY() ? 2 : 3]), S)
                  : H(Uint8Array.from([4]), S, n.toBytes(L.y));
            },
            fromBytes(v) {
               let x = v.length,
                  R = v[0],
                  L = v.subarray(1);
               if (x === s && (R === 2 || R === 3)) {
                  let S = fe(L);
                  if (!o(S)) throw new Error("Point is not on curve");
                  let H = h(S),
                     $ = n.sqrt(H),
                     V = ($ & ke) === ke;
                  return (
                     ((R & 1) === 1) !== V && ($ = n.neg($)), { x: S, y: $ }
                  );
               } else if (x === i && R === 4) {
                  let S = n.fromBytes(L.subarray(0, n.BYTES)),
                     H = n.fromBytes(L.subarray(n.BYTES, 2 * n.BYTES));
                  return { x: S, y: H };
               } else
                  throw new Error(
                     `Point of length ${x} was invalid. Expected ${s} compressed bytes or ${i} uncompressed bytes`,
                  );
            },
         }),
         g = (v) => zt(Be(v, e.nByteLength));
      function y(v) {
         let x = r >> ke;
         return v > x;
      }
      function d(v) {
         return y(v) ? a(-v) : v;
      }
      let p = (v, x, R) => fe(v.slice(x, R));
      class b {
         constructor(x, R, L) {
            (this.r = x),
               (this.s = R),
               (this.recovery = L),
               this.assertValidity();
         }
         static fromCompact(x) {
            let R = e.nByteLength;
            return (
               (x = te("compactSignature", x, R * 2)),
               new b(p(x, 0, R), p(x, R, 2 * R))
            );
         }
         static fromDER(x) {
            let { r: R, s: L } = Dt.toSig(te("DER", x));
            return new b(R, L);
         }
         assertValidity() {
            if (!f(this.r)) throw new Error("r must be 0 < r < CURVE.n");
            if (!f(this.s)) throw new Error("s must be 0 < s < CURVE.n");
         }
         addRecoveryBit(x) {
            return new b(this.r, this.s, x);
         }
         recoverPublicKey(x) {
            let { r: R, s: L, recovery: S } = this,
               H = T(te("msgHash", x));
            if (S == null || ![0, 1, 2, 3].includes(S))
               throw new Error("recovery id invalid");
            let $ = S === 2 || S === 3 ? R + e.n : R;
            if ($ >= n.ORDER) throw new Error("recovery id 2 or 3 invalid");
            let V = S & 1 ? "03" : "02",
               F = l.fromHex(V + g($)),
               D = c($),
               X = a(-H * D),
               J = a(L * D),
               ee = l.BASE.multiplyAndAddUnsafe(F, X, J);
            if (!ee) throw new Error("point at infinify");
            return ee.assertValidity(), ee;
         }
         hasHighS() {
            return y(this.s);
         }
         normalizeS() {
            return this.hasHighS()
               ? new b(this.r, a(-this.s), this.recovery)
               : this;
         }
         toDERRawBytes() {
            return jt(this.toDERHex());
         }
         toDERHex() {
            return Dt.hexFromSig({ r: this.r, s: this.s });
         }
         toCompactRawBytes() {
            return jt(this.toCompactHex());
         }
         toCompactHex() {
            return g(this.r) + g(this.s);
         }
      }
      let w = {
         isValidPrivateKey(v) {
            try {
               return u(v), !0;
            } catch {
               return !1;
            }
         },
         normPrivateKeyToScalar: u,
         randomPrivateKey: () => {
            let v = xs(e.n);
            return Wo(e.randomBytes(v), e.n);
         },
         precompute(v = 8, x = l.BASE) {
            return x._setWindowSize(v), x.multiply(BigInt(3)), x;
         },
      };
      function E(v, x = !0) {
         return l.fromPrivateKey(v).toRawBytes(x);
      }
      function k(v) {
         let x = v instanceof Uint8Array,
            R = typeof v == "string",
            L = (x || R) && v.length;
         return x
            ? L === s || L === i
            : R
              ? L === 2 * s || L === 2 * i
              : v instanceof l;
      }
      function B(v, x, R = !0) {
         if (k(v)) throw new Error("first arg must be private key");
         if (!k(x)) throw new Error("second arg must be public key");
         return l.fromHex(x).multiply(u(v)).toRawBytes(R);
      }
      let A =
            e.bits2int ||
            function (v) {
               let x = fe(v),
                  R = v.length * 8 - e.nBitLength;
               return R > 0 ? x >> BigInt(R) : x;
            },
         T =
            e.bits2int_modN ||
            function (v) {
               return a(A(v));
            },
         _ = Bn(e.nBitLength);
      function I(v) {
         if (typeof v != "bigint") throw new Error("bigint expected");
         if (!(Ke <= v && v < _))
            throw new Error(`bigint expected < 2^${e.nBitLength}`);
         return Be(v, e.nByteLength);
      }
      function P(v, x, R = U) {
         if (["recovered", "canonical"].some((Q) => Q in R))
            throw new Error("sign() legacy options not supported");
         let { hash: L, randomBytes: S } = e,
            { lowS: H, prehash: $, extraEntropy: V } = R;
         H == null && (H = !0),
            (v = te("msgHash", v)),
            $ && (v = te("prehashed msgHash", L(v)));
         let F = T(v),
            D = u(x),
            X = [I(D), I(F)];
         if (V != null) {
            let Q = V === !0 ? S(n.BYTES) : V;
            X.push(te("extraEntropy", Q));
         }
         let J = Xe(...X),
            ee = F;
         function ae(Q) {
            let He = A(Q);
            if (!f(He)) return;
            let ft = c(He),
               ce = l.BASE.multiply(He).toAffine(),
               xe = a(ce.x);
            if (xe === Ke) return;
            let Re = a(ft * a(ee + xe * D));
            if (Re === Ke) return;
            let Sn = (ce.x === xe ? 0 : 2) | Number(ce.y & ke),
               po = Re;
            return H && y(Re) && ((po = d(Re)), (Sn ^= 1)), new b(xe, po, Sn);
         }
         return { seed: J, k2sig: ae };
      }
      let U = { lowS: e.lowS, prehash: !1 },
         O = { lowS: e.lowS, prehash: !1 };
      function q(v, x, R = U) {
         let { seed: L, k2sig: S } = P(v, x, R),
            H = e;
         return ys(H.hash.outputLen, H.nByteLength, H.hmac)(L, S);
      }
      l.BASE._setWindowSize(8);
      function C(v, x, R, L = O) {
         let S = v;
         if (((x = te("msgHash", x)), (R = te("publicKey", R)), "strict" in L))
            throw new Error("options.strict was renamed to lowS");
         let { lowS: H, prehash: $ } = L,
            V,
            F;
         try {
            if (typeof S == "string" || S instanceof Uint8Array)
               try {
                  V = b.fromDER(S);
               } catch (ce) {
                  if (!(ce instanceof Dt.Err)) throw ce;
                  V = b.fromCompact(S);
               }
            else if (
               typeof S == "object" &&
               typeof S.r == "bigint" &&
               typeof S.s == "bigint"
            ) {
               let { r: ce, s: xe } = S;
               V = new b(ce, xe);
            } else throw new Error("PARSE");
            F = l.fromHex(R);
         } catch (ce) {
            if (ce.message === "PARSE")
               throw new Error(
                  "signature must be Signature instance, Uint8Array or hex string",
               );
            return !1;
         }
         if (H && V.hasHighS()) return !1;
         $ && (x = e.hash(x));
         let { r: D, s: X } = V,
            J = T(x),
            ee = c(X),
            ae = a(J * ee),
            Q = a(D * ee),
            He = l.BASE.multiplyAndAddUnsafe(F, ae, Q)?.toAffine();
         return He ? a(He.x) === D : !1;
      }
      return {
         CURVE: e,
         getPublicKey: E,
         getSharedSecret: B,
         sign: q,
         verify: C,
         ProjectivePoint: l,
         Signature: b,
         utils: w,
      };
   }
   var cr = class extends fn {
         constructor(e, n) {
            super(), (this.finished = !1), (this.destroyed = !1), Po(e);
            let r = Cn(n);
            if (
               ((this.iHash = e.create()),
               typeof this.iHash.update != "function")
            )
               throw new Error(
                  "Expected instance of class which extends utils.Hash",
               );
            (this.blockLen = this.iHash.blockLen),
               (this.outputLen = this.iHash.outputLen);
            let s = this.blockLen,
               i = new Uint8Array(s);
            i.set(r.length > s ? e.create().update(r).digest() : r);
            for (let o = 0; o < i.length; o++) i[o] ^= 54;
            this.iHash.update(i), (this.oHash = e.create());
            for (let o = 0; o < i.length; o++) i[o] ^= 106;
            this.oHash.update(i), i.fill(0);
         }
         update(e) {
            return hn(this), this.iHash.update(e), this;
         }
         digestInto(e) {
            hn(this),
               fs(e, this.outputLen),
               (this.finished = !0),
               this.iHash.digestInto(e),
               this.oHash.update(e),
               this.oHash.digestInto(e),
               this.destroy();
         }
         digest() {
            let e = new Uint8Array(this.oHash.outputLen);
            return this.digestInto(e), e;
         }
         _cloneInto(e) {
            e || (e = Object.create(Object.getPrototypeOf(this), {}));
            let {
               oHash: n,
               iHash: r,
               finished: s,
               destroyed: i,
               blockLen: o,
               outputLen: a,
            } = this;
            return (
               (e = e),
               (e.finished = s),
               (e.destroyed = i),
               (e.blockLen = o),
               (e.outputLen = a),
               (e.oHash = n._cloneInto(e.oHash)),
               (e.iHash = r._cloneInto(e.iHash)),
               e
            );
         }
         destroy() {
            (this.destroyed = !0), this.oHash.destroy(), this.iHash.destroy();
         }
      },
      As = (t, e, n) => new cr(t, e).update(n).digest();
   As.create = (t, e) => new cr(t, e);
   function cf(t) {
      return {
         hash: t,
         hmac: (e, ...n) => As(t, e, No(...n)),
         randomBytes: er,
      };
   }
   function Jo(t, e) {
      let n = (r) => Yo({ ...t, ...cf(r) });
      return Object.freeze({ ...n(e), create: n });
   }
   var fr = BigInt(
         "0xfffffffffffffffffffffffffffffffffffffffffffffffffffffffefffffc2f",
      ),
      ur = BigInt(
         "0xfffffffffffffffffffffffffffffffebaaedce6af48a03bbfd25e8cd0364141",
      ),
      Ko = BigInt(1),
      lr = BigInt(2),
      Qo = (t, e) => (t + e / lr) / e;
   function ea(t) {
      let e = fr,
         n = BigInt(3),
         r = BigInt(6),
         s = BigInt(11),
         i = BigInt(22),
         o = BigInt(23),
         a = BigInt(44),
         c = BigInt(88),
         l = (t * t * t) % e,
         u = (l * l * t) % e,
         h = (me(u, n, e) * u) % e,
         f = (me(h, n, e) * u) % e,
         g = (me(f, lr, e) * l) % e,
         y = (me(g, s, e) * g) % e,
         d = (me(y, i, e) * y) % e,
         p = (me(d, a, e) * d) % e,
         b = (me(p, c, e) * p) % e,
         w = (me(b, a, e) * d) % e,
         E = (me(w, n, e) * u) % e,
         k = (me(E, o, e) * y) % e,
         B = (me(k, r, e) * l) % e,
         A = me(B, lr, e);
      if (!_s.eql(_s.sqr(A), t)) throw new Error("Cannot find square root");
      return A;
   }
   var _s = Fo(fr, void 0, void 0, { sqrt: ea }),
      bt = Jo(
         {
            a: BigInt(0),
            b: BigInt(7),
            Fp: _s,
            n: ur,
            Gx: BigInt(
               "55066263022277343669578718895168534326250603453777594175500187360389116729240",
            ),
            Gy: BigInt(
               "32670510020758816978083085130507043184471273380659243275938904335757337482424",
            ),
            h: BigInt(1),
            lowS: !0,
            endo: {
               beta: BigInt(
                  "0x7ae96a2b657c07106e64479eac3434e99cf0497512f58995c1396c28719501ee",
               ),
               splitScalar: (t) => {
                  let e = ur,
                     n = BigInt("0x3086d221a7d46bcde86c90e49284eb15"),
                     r = -Ko * BigInt("0xe4437ed6010e88286f547fa90abfe4c3"),
                     s = BigInt("0x114ca50f7a8e2f3f657c1108d9d44cfd8"),
                     i = n,
                     o = BigInt("0x100000000000000000000000000000000"),
                     a = Qo(i * t, e),
                     c = Qo(-r * t, e),
                     l = ne(t - a * n - c * s, e),
                     u = ne(-a * r - c * i, e),
                     h = l > o,
                     f = u > o;
                  if ((h && (l = e - l), f && (u = e - u), l > o || u > o))
                     throw new Error(
                        "splitScalar: Endomorphism failed, k=" + t,
                     );
                  return { k1neg: h, k1: l, k2neg: f, k2: u };
               },
            },
         },
         nr,
      ),
      dr = BigInt(0),
      ta = (t) => typeof t == "bigint" && dr < t && t < fr,
      uf = (t) => typeof t == "bigint" && dr < t && t < ur,
      Xo = {};
   function hr(t, ...e) {
      let n = Xo[t];
      if (n === void 0) {
         let r = nr(Uint8Array.from(t, (s) => s.charCodeAt(0)));
         (n = Xe(r, r)), (Xo[t] = n);
      }
      return nr(Xe(n, ...e));
   }
   var Cs = (t) => t.toRawBytes(!0).slice(1),
      Ss = (t) => Be(t, 32),
      Ts = (t) => ne(t, fr),
      In = (t) => ne(t, ur),
      Bs = bt.ProjectivePoint,
      lf = (t, e, n) => Bs.BASE.multiplyAndAddUnsafe(t, e, n);
   function Rs(t) {
      let e = bt.utils.normPrivateKeyToScalar(t),
         n = Bs.fromPrivateKey(e);
      return { scalar: n.hasEvenY() ? e : In(-e), bytes: Cs(n) };
   }
   function na(t) {
      if (!ta(t)) throw new Error("bad x: need 0 < x < p");
      let e = Ts(t * t),
         n = Ts(e * t + BigInt(7)),
         r = ea(n);
      r % lr !== dr && (r = Ts(-r));
      let s = new Bs(t, r, Ko);
      return s.assertValidity(), s;
   }
   function ra(...t) {
      return In(fe(hr("BIP0340/challenge", ...t)));
   }
   function hf(t) {
      return Rs(t).bytes;
   }
   function ff(t, e, n = er(32)) {
      let r = te("message", t),
         { bytes: s, scalar: i } = Rs(e),
         o = te("auxRand", n, 32),
         a = Ss(i ^ fe(hr("BIP0340/aux", o))),
         c = hr("BIP0340/nonce", a, s, r),
         l = In(fe(c));
      if (l === dr) throw new Error("sign failed: k is zero");
      let { bytes: u, scalar: h } = Rs(l),
         f = ra(u, s, r),
         g = new Uint8Array(64);
      if ((g.set(u, 0), g.set(Ss(In(h + f * i)), 32), !sa(g, r, s)))
         throw new Error("sign: Invalid signature produced");
      return g;
   }
   function sa(t, e, n) {
      let r = te("signature", t, 64),
         s = te("message", e),
         i = te("publicKey", n, 32);
      try {
         let o = na(fe(i)),
            a = fe(r.subarray(0, 32));
         if (!ta(a)) return !1;
         let c = fe(r.subarray(32, 64));
         if (!uf(c)) return !1;
         let l = ra(Ss(a), Cs(o), s),
            u = lf(o, c, In(-l));
         return !(!u || !u.hasEvenY() || u.toAffine().x !== a);
      } catch {
         return !1;
      }
   }
   var dn = {
      getPublicKey: hf,
      sign: ff,
      verify: sa,
      utils: {
         randomPrivateKey: bt.utils.randomPrivateKey,
         lift_x: na,
         pointToBytes: Cs,
         numberToBytesBE: Be,
         bytesToNumberBE: fe,
         taggedHash: hr,
         mod: ne,
      },
   };
   var pr =
      typeof globalThis == "object" && "crypto" in globalThis
         ? globalThis.crypto
         : void 0;
   var Is = (t) => t instanceof Uint8Array;
   var gr = (t) => new DataView(t.buffer, t.byteOffset, t.byteLength),
      Ie = (t, e) => (t << (32 - e)) | (t >>> e),
      df = new Uint8Array(new Uint32Array([287454020]).buffer)[0] === 68;
   if (!df) throw new Error("Non little-endian hardware is not supported");
   var pf = Array.from({ length: 256 }, (t, e) =>
      e.toString(16).padStart(2, "0"),
   );
   function K(t) {
      if (!Is(t)) throw new Error("Uint8Array expected");
      let e = "";
      for (let n = 0; n < t.length; n++) e += pf[t[n]];
      return e;
   }
   function Wt(t) {
      if (typeof t != "string")
         throw new Error("hex string expected, got " + typeof t);
      let e = t.length;
      if (e % 2)
         throw new Error(
            "padded hex string expected, got unpadded hex of length " + e,
         );
      let n = new Uint8Array(e / 2);
      for (let r = 0; r < n.length; r++) {
         let s = r * 2,
            i = t.slice(s, s + 2),
            o = Number.parseInt(i, 16);
         if (Number.isNaN(o) || o < 0) throw new Error("Invalid byte sequence");
         n[r] = o;
      }
      return n;
   }
   function gf(t) {
      if (typeof t != "string")
         throw new Error(`utf8ToBytes expected string, got ${typeof t}`);
      return new Uint8Array(new TextEncoder().encode(t));
   }
   function mt(t) {
      if ((typeof t == "string" && (t = gf(t)), !Is(t)))
         throw new Error(`expected Uint8Array, got ${typeof t}`);
      return t;
   }
   function gn(...t) {
      let e = new Uint8Array(t.reduce((r, s) => r + s.length, 0)),
         n = 0;
      return (
         t.forEach((r) => {
            if (!Is(r)) throw new Error("Uint8Array expected");
            e.set(r, n), (n += r.length);
         }),
         e
      );
   }
   var pn = class {
      clone() {
         return this._cloneInto();
      }
   };
   function Ls(t) {
      let e = (r) => t().update(mt(r)).digest(),
         n = t();
      return (
         (e.outputLen = n.outputLen),
         (e.blockLen = n.blockLen),
         (e.create = () => t()),
         e
      );
   }
   function yr(t = 32) {
      if (pr && typeof pr.getRandomValues == "function")
         return pr.getRandomValues(new Uint8Array(t));
      throw new Error("crypto.getRandomValues must be defined");
   }
   function Us(t) {
      if (!Number.isSafeInteger(t) || t < 0)
         throw new Error(`Wrong positive integer: ${t}`);
   }
   function yf(t) {
      if (typeof t != "boolean") throw new Error(`Expected boolean, not ${t}`);
   }
   function ia(t, ...e) {
      if (!(t instanceof Uint8Array)) throw new Error("Expected Uint8Array");
      if (e.length > 0 && !e.includes(t.length))
         throw new Error(
            `Expected Uint8Array of length ${e}, not of length=${t.length}`,
         );
   }
   function bf(t) {
      if (typeof t != "function" || typeof t.create != "function")
         throw new Error("Hash should be wrapped by utils.wrapConstructor");
      Us(t.outputLen), Us(t.blockLen);
   }
   function mf(t, e = !0) {
      if (t.destroyed) throw new Error("Hash instance has been destroyed");
      if (e && t.finished)
         throw new Error("Hash#digest() has already been called");
   }
   function wf(t, e) {
      ia(t);
      let n = e.outputLen;
      if (t.length < n)
         throw new Error(
            `digestInto() expects output buffer of length at least ${n}`,
         );
   }
   var vf = {
         number: Us,
         bool: yf,
         bytes: ia,
         hash: bf,
         exists: mf,
         output: wf,
      },
      we = vf;
   function xf(t, e, n, r) {
      if (typeof t.setBigUint64 == "function") return t.setBigUint64(e, n, r);
      let s = BigInt(32),
         i = BigInt(4294967295),
         o = Number((n >> s) & i),
         a = Number(n & i),
         c = r ? 4 : 0,
         l = r ? 0 : 4;
      t.setUint32(e + c, o, r), t.setUint32(e + l, a, r);
   }
   var br = class extends pn {
      constructor(e, n, r, s) {
         super(),
            (this.blockLen = e),
            (this.outputLen = n),
            (this.padOffset = r),
            (this.isLE = s),
            (this.finished = !1),
            (this.length = 0),
            (this.pos = 0),
            (this.destroyed = !1),
            (this.buffer = new Uint8Array(e)),
            (this.view = gr(this.buffer));
      }
      update(e) {
         we.exists(this);
         let { view: n, buffer: r, blockLen: s } = this;
         e = mt(e);
         let i = e.length;
         for (let o = 0; o < i; ) {
            let a = Math.min(s - this.pos, i - o);
            if (a === s) {
               let c = gr(e);
               for (; s <= i - o; o += s) this.process(c, o);
               continue;
            }
            r.set(e.subarray(o, o + a), this.pos),
               (this.pos += a),
               (o += a),
               this.pos === s && (this.process(n, 0), (this.pos = 0));
         }
         return (this.length += e.length), this.roundClean(), this;
      }
      digestInto(e) {
         we.exists(this), we.output(e, this), (this.finished = !0);
         let { buffer: n, view: r, blockLen: s, isLE: i } = this,
            { pos: o } = this;
         (n[o++] = 128),
            this.buffer.subarray(o).fill(0),
            this.padOffset > s - o && (this.process(r, 0), (o = 0));
         for (let h = o; h < s; h++) n[h] = 0;
         xf(r, s - 8, BigInt(this.length * 8), i), this.process(r, 0);
         let a = gr(e),
            c = this.outputLen;
         if (c % 4)
            throw new Error("_sha2: outputLen should be aligned to 32bit");
         let l = c / 4,
            u = this.get();
         if (l > u.length)
            throw new Error("_sha2: outputLen bigger than state");
         for (let h = 0; h < l; h++) a.setUint32(4 * h, u[h], i);
      }
      digest() {
         let { buffer: e, outputLen: n } = this;
         this.digestInto(e);
         let r = e.slice(0, n);
         return this.destroy(), r;
      }
      _cloneInto(e) {
         e || (e = new this.constructor()), e.set(...this.get());
         let {
            blockLen: n,
            buffer: r,
            length: s,
            finished: i,
            destroyed: o,
            pos: a,
         } = this;
         return (
            (e.length = s),
            (e.pos = a),
            (e.finished = i),
            (e.destroyed = o),
            s % n && e.buffer.set(r),
            e
         );
      }
   };
   var Ef = (t, e, n) => (t & e) ^ (~t & n),
      kf = (t, e, n) => (t & e) ^ (t & n) ^ (e & n),
      Af = new Uint32Array([
         1116352408, 1899447441, 3049323471, 3921009573, 961987163, 1508970993,
         2453635748, 2870763221, 3624381080, 310598401, 607225278, 1426881987,
         1925078388, 2162078206, 2614888103, 3248222580, 3835390401, 4022224774,
         264347078, 604807628, 770255983, 1249150122, 1555081692, 1996064986,
         2554220882, 2821834349, 2952996808, 3210313671, 3336571891, 3584528711,
         113926993, 338241895, 666307205, 773529912, 1294757372, 1396182291,
         1695183700, 1986661051, 2177026350, 2456956037, 2730485921, 2820302411,
         3259730800, 3345764771, 3516065817, 3600352804, 4094571909, 275423344,
         430227734, 506948616, 659060556, 883997877, 958139571, 1322822218,
         1537002063, 1747873779, 1955562222, 2024104815, 2227730452, 2361852424,
         2428436474, 2756734187, 3204031479, 3329325298,
      ]),
      wt = new Uint32Array([
         1779033703, 3144134277, 1013904242, 2773480762, 1359893119, 2600822924,
         528734635, 1541459225,
      ]),
      vt = new Uint32Array(64),
      mr = class extends br {
         constructor() {
            super(64, 32, 8, !1),
               (this.A = wt[0] | 0),
               (this.B = wt[1] | 0),
               (this.C = wt[2] | 0),
               (this.D = wt[3] | 0),
               (this.E = wt[4] | 0),
               (this.F = wt[5] | 0),
               (this.G = wt[6] | 0),
               (this.H = wt[7] | 0);
         }
         get() {
            let { A: e, B: n, C: r, D: s, E: i, F: o, G: a, H: c } = this;
            return [e, n, r, s, i, o, a, c];
         }
         set(e, n, r, s, i, o, a, c) {
            (this.A = e | 0),
               (this.B = n | 0),
               (this.C = r | 0),
               (this.D = s | 0),
               (this.E = i | 0),
               (this.F = o | 0),
               (this.G = a | 0),
               (this.H = c | 0);
         }
         process(e, n) {
            for (let h = 0; h < 16; h++, n += 4) vt[h] = e.getUint32(n, !1);
            for (let h = 16; h < 64; h++) {
               let f = vt[h - 15],
                  g = vt[h - 2],
                  y = Ie(f, 7) ^ Ie(f, 18) ^ (f >>> 3),
                  d = Ie(g, 17) ^ Ie(g, 19) ^ (g >>> 10);
               vt[h] = (d + vt[h - 7] + y + vt[h - 16]) | 0;
            }
            let { A: r, B: s, C: i, D: o, E: a, F: c, G: l, H: u } = this;
            for (let h = 0; h < 64; h++) {
               let f = Ie(a, 6) ^ Ie(a, 11) ^ Ie(a, 25),
                  g = (u + f + Ef(a, c, l) + Af[h] + vt[h]) | 0,
                  d = ((Ie(r, 2) ^ Ie(r, 13) ^ Ie(r, 22)) + kf(r, s, i)) | 0;
               (u = l),
                  (l = c),
                  (c = a),
                  (a = (o + g) | 0),
                  (o = i),
                  (i = s),
                  (s = r),
                  (r = (g + d) | 0);
            }
            (r = (r + this.A) | 0),
               (s = (s + this.B) | 0),
               (i = (i + this.C) | 0),
               (o = (o + this.D) | 0),
               (a = (a + this.E) | 0),
               (c = (c + this.F) | 0),
               (l = (l + this.G) | 0),
               (u = (u + this.H) | 0),
               this.set(r, s, i, o, a, c, l, u);
         }
         roundClean() {
            vt.fill(0);
         }
         destroy() {
            this.set(0, 0, 0, 0, 0, 0, 0, 0), this.buffer.fill(0);
         }
      },
      Ps = class extends mr {
         constructor() {
            super(),
               (this.A = -1056596264),
               (this.B = 914150663),
               (this.C = 812702999),
               (this.D = -150054599),
               (this.E = -4191439),
               (this.F = 1750603025),
               (this.G = 1694076839),
               (this.H = -1090891868),
               (this.outputLen = 28);
         }
      },
      $e = Ls(() => new mr()),
      Bm = Ls(() => new Ps());
   function yn(t) {
      if (!Number.isSafeInteger(t)) throw new Error(`Wrong integer: ${t}`);
   }
   function et(...t) {
      let e = (s, i) => (o) => s(i(o)),
         n = Array.from(t)
            .reverse()
            .reduce((s, i) => (s ? e(s, i.encode) : i.encode), void 0),
         r = t.reduce((s, i) => (s ? e(s, i.decode) : i.decode), void 0);
      return { encode: n, decode: r };
   }
   function tt(t) {
      return {
         encode: (e) => {
            if (!Array.isArray(e) || (e.length && typeof e[0] != "number"))
               throw new Error(
                  "alphabet.encode input should be an array of numbers",
               );
            return e.map((n) => {
               if ((yn(n), n < 0 || n >= t.length))
                  throw new Error(
                     `Digit index outside alphabet: ${n} (alphabet: ${t.length})`,
                  );
               return t[n];
            });
         },
         decode: (e) => {
            if (!Array.isArray(e) || (e.length && typeof e[0] != "string"))
               throw new Error(
                  "alphabet.decode input should be array of strings",
               );
            return e.map((n) => {
               if (typeof n != "string")
                  throw new Error(`alphabet.decode: not string element=${n}`);
               let r = t.indexOf(n);
               if (r === -1)
                  throw new Error(`Unknown letter: "${n}". Allowed: ${t}`);
               return r;
            });
         },
      };
   }
   function nt(t = "") {
      if (typeof t != "string")
         throw new Error("join separator should be string");
      return {
         encode: (e) => {
            if (!Array.isArray(e) || (e.length && typeof e[0] != "string"))
               throw new Error("join.encode input should be array of strings");
            for (let n of e)
               if (typeof n != "string")
                  throw new Error(`join.encode: non-string input=${n}`);
            return e.join(t);
         },
         decode: (e) => {
            if (typeof e != "string")
               throw new Error("join.decode input should be string");
            return e.split(t);
         },
      };
   }
   function vr(t, e = "=") {
      if ((yn(t), typeof e != "string"))
         throw new Error("padding chr should be string");
      return {
         encode(n) {
            if (!Array.isArray(n) || (n.length && typeof n[0] != "string"))
               throw new Error(
                  "padding.encode input should be array of strings",
               );
            for (let r of n)
               if (typeof r != "string")
                  throw new Error(`padding.encode: non-string input=${r}`);
            for (; (n.length * t) % 8; ) n.push(e);
            return n;
         },
         decode(n) {
            if (!Array.isArray(n) || (n.length && typeof n[0] != "string"))
               throw new Error(
                  "padding.encode input should be array of strings",
               );
            for (let s of n)
               if (typeof s != "string")
                  throw new Error(`padding.decode: non-string input=${s}`);
            let r = n.length;
            if ((r * t) % 8)
               throw new Error(
                  "Invalid padding: string should have whole number of bytes",
               );
            for (; r > 0 && n[r - 1] === e; r--)
               if (!(((r - 1) * t) % 8))
                  throw new Error(
                     "Invalid padding: string has too much padding",
                  );
            return n.slice(0, r);
         },
      };
   }
   function ha(t) {
      if (typeof t != "function")
         throw new Error("normalize fn should be function");
      return { encode: (e) => e, decode: (e) => t(e) };
   }
   function oa(t, e, n) {
      if (e < 2)
         throw new Error(
            `convertRadix: wrong from=${e}, base cannot be less than 2`,
         );
      if (n < 2)
         throw new Error(
            `convertRadix: wrong to=${n}, base cannot be less than 2`,
         );
      if (!Array.isArray(t))
         throw new Error("convertRadix: data should be array");
      if (!t.length) return [];
      let r = 0,
         s = [],
         i = Array.from(t);
      for (
         i.forEach((o) => {
            if ((yn(o), o < 0 || o >= e))
               throw new Error(`Wrong integer: ${o}`);
         });
         ;

      ) {
         let o = 0,
            a = !0;
         for (let c = r; c < i.length; c++) {
            let l = i[c],
               u = e * o + l;
            if (
               !Number.isSafeInteger(u) ||
               (e * o) / e !== o ||
               u - l !== e * o
            )
               throw new Error("convertRadix: carry overflow");
            if (
               ((o = u % n),
               (i[c] = Math.floor(u / n)),
               !Number.isSafeInteger(i[c]) || i[c] * n + o !== u)
            )
               throw new Error("convertRadix: carry overflow");
            if (a) i[c] ? (a = !1) : (r = c);
            else continue;
         }
         if ((s.push(o), a)) break;
      }
      for (let o = 0; o < t.length - 1 && t[o] === 0; o++) s.push(0);
      return s.reverse();
   }
   var fa = (t, e) => (e ? fa(e, t % e) : t),
      wr = (t, e) => t + (e - fa(t, e));
   function Os(t, e, n, r) {
      if (!Array.isArray(t))
         throw new Error("convertRadix2: data should be array");
      if (e <= 0 || e > 32) throw new Error(`convertRadix2: wrong from=${e}`);
      if (n <= 0 || n > 32) throw new Error(`convertRadix2: wrong to=${n}`);
      if (wr(e, n) > 32)
         throw new Error(
            `convertRadix2: carry overflow from=${e} to=${n} carryBits=${wr(e, n)}`,
         );
      let s = 0,
         i = 0,
         o = 2 ** n - 1,
         a = [];
      for (let c of t) {
         if ((yn(c), c >= 2 ** e))
            throw new Error(`convertRadix2: invalid data word=${c} from=${e}`);
         if (((s = (s << e) | c), i + e > 32))
            throw new Error(`convertRadix2: carry overflow pos=${i} from=${e}`);
         for (i += e; i >= n; i -= n) a.push(((s >> (i - n)) & o) >>> 0);
         s &= 2 ** i - 1;
      }
      if (((s = (s << (n - i)) & o), !r && i >= e))
         throw new Error("Excess padding");
      if (!r && s) throw new Error(`Non-zero padding: ${s}`);
      return r && i > 0 && a.push(s >>> 0), a;
   }
   function Tf(t) {
      return (
         yn(t),
         {
            encode: (e) => {
               if (!(e instanceof Uint8Array))
                  throw new Error("radix.encode input should be Uint8Array");
               return oa(Array.from(e), 2 ** 8, t);
            },
            decode: (e) => {
               if (!Array.isArray(e) || (e.length && typeof e[0] != "number"))
                  throw new Error(
                     "radix.decode input should be array of strings",
                  );
               return Uint8Array.from(oa(e, t, 2 ** 8));
            },
         }
      );
   }
   function xt(t, e = !1) {
      if ((yn(t), t <= 0 || t > 32))
         throw new Error("radix2: bits should be in (0..32]");
      if (wr(8, t) > 32 || wr(t, 8) > 32)
         throw new Error("radix2: carry overflow");
      return {
         encode: (n) => {
            if (!(n instanceof Uint8Array))
               throw new Error("radix2.encode input should be Uint8Array");
            return Os(Array.from(n), 8, t, !e);
         },
         decode: (n) => {
            if (!Array.isArray(n) || (n.length && typeof n[0] != "number"))
               throw new Error(
                  "radix2.decode input should be array of strings",
               );
            return Uint8Array.from(Os(n, t, 8, e));
         },
      };
   }
   function aa(t) {
      if (typeof t != "function")
         throw new Error("unsafeWrapper fn should be function");
      return function (...e) {
         try {
            return t.apply(null, e);
         } catch {}
      };
   }
   var _f = et(xt(4), tt("0123456789ABCDEF"), nt("")),
      Sf = et(xt(5), tt("ABCDEFGHIJKLMNOPQRSTUVWXYZ234567"), vr(5), nt("")),
      Lm = et(xt(5), tt("0123456789ABCDEFGHIJKLMNOPQRSTUV"), vr(5), nt("")),
      Um = et(
         xt(5),
         tt("0123456789ABCDEFGHJKMNPQRSTVWXYZ"),
         nt(""),
         ha((t) => t.toUpperCase().replace(/O/g, "0").replace(/[IL]/g, "1")),
      ),
      Ae = et(
         xt(6),
         tt("ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/"),
         vr(6),
         nt(""),
      ),
      Rf = et(
         xt(6),
         tt("ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_"),
         vr(6),
         nt(""),
      ),
      Hs = (t) => et(Tf(58), tt(t), nt("")),
      Ms = Hs("123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz"),
      Pm = Hs("123456789abcdefghijkmnopqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ"),
      Om = Hs("rpshnaf39wBUDNEGHJKLM4PQRST7VWXYZ2bcdeCg65jkm8oFqi1tuvAxyz"),
      ca = [0, 2, 3, 5, 6, 7, 9, 10, 11],
      Cf = {
         encode(t) {
            let e = "";
            for (let n = 0; n < t.length; n += 8) {
               let r = t.subarray(n, n + 8);
               e += Ms.encode(r).padStart(ca[r.length], "1");
            }
            return e;
         },
         decode(t) {
            let e = [];
            for (let n = 0; n < t.length; n += 11) {
               let r = t.slice(n, n + 11),
                  s = ca.indexOf(r.length),
                  i = Ms.decode(r);
               for (let o = 0; o < i.length - s; o++)
                  if (i[o] !== 0) throw new Error("base58xmr: wrong padding");
               e = e.concat(Array.from(i.slice(i.length - s)));
            }
            return Uint8Array.from(e);
         },
      };
   var Ns = et(tt("qpzry9x8gf2tvdw0s3jn54khce6mua7l"), nt("")),
      ua = [996825010, 642813549, 513874426, 1027748829, 705979059];
   function Ln(t) {
      let e = t >> 25,
         n = (t & 33554431) << 5;
      for (let r = 0; r < ua.length; r++) ((e >> r) & 1) === 1 && (n ^= ua[r]);
      return n;
   }
   function la(t, e, n = 1) {
      let r = t.length,
         s = 1;
      for (let i = 0; i < r; i++) {
         let o = t.charCodeAt(i);
         if (o < 33 || o > 126) throw new Error(`Invalid prefix (${t})`);
         s = Ln(s) ^ (o >> 5);
      }
      s = Ln(s);
      for (let i = 0; i < r; i++) s = Ln(s) ^ (t.charCodeAt(i) & 31);
      for (let i of e) s = Ln(s) ^ i;
      for (let i = 0; i < 6; i++) s = Ln(s);
      return (s ^= n), Ns.encode(Os([s % 2 ** 30], 30, 5, !1));
   }
   function da(t) {
      let e = t === "bech32" ? 1 : 734539939,
         n = xt(5),
         r = n.decode,
         s = n.encode,
         i = aa(r);
      function o(u, h, f = 90) {
         if (typeof u != "string")
            throw new Error(
               `bech32.encode prefix should be string, not ${typeof u}`,
            );
         if (!Array.isArray(h) || (h.length && typeof h[0] != "number"))
            throw new Error(
               `bech32.encode words should be array of numbers, not ${typeof h}`,
            );
         let g = u.length + 7 + h.length;
         if (f !== !1 && g > f)
            throw new TypeError(`Length ${g} exceeds limit ${f}`);
         return (u = u.toLowerCase()), `${u}1${Ns.encode(h)}${la(u, h, e)}`;
      }
      function a(u, h = 90) {
         if (typeof u != "string")
            throw new Error(
               `bech32.decode input should be string, not ${typeof u}`,
            );
         if (u.length < 8 || (h !== !1 && u.length > h))
            throw new TypeError(
               `Wrong string length: ${u.length} (${u}). Expected (8..${h})`,
            );
         let f = u.toLowerCase();
         if (u !== f && u !== u.toUpperCase())
            throw new Error("String must be lowercase or uppercase");
         u = f;
         let g = u.lastIndexOf("1");
         if (g === 0 || g === -1)
            throw new Error(
               'Letter "1" must be present between prefix and data only',
            );
         let y = u.slice(0, g),
            d = u.slice(g + 1);
         if (d.length < 6)
            throw new Error("Data must be at least 6 characters long");
         let p = Ns.decode(d).slice(0, -6),
            b = la(y, p, e);
         if (!d.endsWith(b))
            throw new Error(`Invalid checksum in ${u}: expected "${b}"`);
         return { prefix: y, words: p };
      }
      let c = aa(a);
      function l(u) {
         let { prefix: h, words: f } = a(u, !1);
         return { prefix: h, words: f, bytes: r(f) };
      }
      return {
         encode: o,
         decode: a,
         decodeToBytes: l,
         decodeUnsafe: c,
         fromWords: r,
         fromWordsUnsafe: i,
         toWords: s,
      };
   }
   var Et = da("bech32"),
      Mm = da("bech32m"),
      Bf = {
         encode: (t) => new TextDecoder().decode(t),
         decode: (t) => new TextEncoder().encode(t),
      },
      If = et(
         xt(4),
         tt("0123456789abcdef"),
         nt(""),
         ha((t) => {
            if (typeof t != "string" || t.length % 2)
               throw new TypeError(
                  `hex.decode: expected string, got ${typeof t} with length ${t.length}`,
               );
            return t.toLowerCase();
         }),
      ),
      Lf = {
         utf8: Bf,
         hex: If,
         base16: _f,
         base32: Sf,
         base64: Ae,
         base64url: Rf,
         base58: Ms,
         base58xmr: Cf,
      },
      Nm = `Invalid encoding type. Available types: ${Object.keys(Lf).join(", ")}`;
   function xr(t) {
      if (!Number.isSafeInteger(t) || t < 0)
         throw new Error(`positive integer expected, not ${t}`);
   }
   function qs(t) {
      if (typeof t != "boolean") throw new Error(`boolean expected, not ${t}`);
   }
   function $s(t) {
      return (
         t instanceof Uint8Array ||
         (t != null &&
            typeof t == "object" &&
            t.constructor.name === "Uint8Array")
      );
   }
   function z(t, ...e) {
      if (!$s(t)) throw new Error("Uint8Array expected");
      if (e.length > 0 && !e.includes(t.length))
         throw new Error(
            `Uint8Array expected of length ${e}, not of length=${t.length}`,
         );
   }
   function kt(t, e = !0) {
      if (t.destroyed) throw new Error("Hash instance has been destroyed");
      if (e && t.finished)
         throw new Error("Hash#digest() has already been called");
   }
   function Un(t, e) {
      z(t);
      let n = e.outputLen;
      if (t.length < n)
         throw new Error(
            `digestInto() expects output buffer of length at least ${n}`,
         );
   }
   var Er = (t) => new Uint8Array(t.buffer, t.byteOffset, t.byteLength);
   var j = (t) =>
         new Uint32Array(t.buffer, t.byteOffset, Math.floor(t.byteLength / 4)),
      At = (t) => new DataView(t.buffer, t.byteOffset, t.byteLength),
      Uf = new Uint8Array(new Uint32Array([287454020]).buffer)[0] === 68;
   if (!Uf) throw new Error("Non little-endian hardware is not supported");
   function Pf(t) {
      if (typeof t != "string")
         throw new Error(`string expected, got ${typeof t}`);
      return new Uint8Array(new TextEncoder().encode(t));
   }
   function Ve(t) {
      if (typeof t == "string") t = Pf(t);
      else if ($s(t)) t = t.slice();
      else throw new Error(`Uint8Array expected, got ${typeof t}`);
      return t;
   }
   function pa(t, e) {
      if (e == null || typeof e != "object")
         throw new Error("options must be defined");
      return Object.assign(t, e);
   }
   function Gt(t, e) {
      if (t.length !== e.length) return !1;
      let n = 0;
      for (let r = 0; r < t.length; r++) n |= t[r] ^ e[r];
      return n === 0;
   }
   var ze = (t, e) => (Object.assign(e, t), e);
   function Zt(t, e, n, r) {
      if (typeof t.setBigUint64 == "function") return t.setBigUint64(e, n, r);
      let s = BigInt(32),
         i = BigInt(4294967295),
         o = Number((n >> s) & i),
         a = Number(n & i),
         c = r ? 4 : 0,
         l = r ? 0 : 4;
      t.setUint32(e + c, o, r), t.setUint32(e + l, a, r);
   }
   var rt = 16,
      zs = new Uint8Array(16),
      je = j(zs),
      Of = 225,
      Mf = (t, e, n, r) => {
         let s = r & 1;
         return {
            s3: (n << 31) | (r >>> 1),
            s2: (e << 31) | (n >>> 1),
            s1: (t << 31) | (e >>> 1),
            s0: (t >>> 1) ^ ((Of << 24) & -(s & 1)),
         };
      },
      Te = (t) =>
         (((t >>> 0) & 255) << 24) |
         (((t >>> 8) & 255) << 16) |
         (((t >>> 16) & 255) << 8) |
         ((t >>> 24) & 255) |
         0;
   function Nf(t) {
      t.reverse();
      let e = t[15] & 1,
         n = 0;
      for (let r = 0; r < t.length; r++) {
         let s = t[r];
         (t[r] = (s >>> 1) | n), (n = (s & 1) << 7);
      }
      return (t[0] ^= -e & 225), t;
   }
   var Hf = (t) => (t > 64 * 1024 ? 8 : t > 1024 ? 4 : 2),
      kr = class {
         constructor(e, n) {
            (this.blockLen = rt),
               (this.outputLen = rt),
               (this.s0 = 0),
               (this.s1 = 0),
               (this.s2 = 0),
               (this.s3 = 0),
               (this.finished = !1),
               (e = Ve(e)),
               z(e, 16);
            let r = At(e),
               s = r.getUint32(0, !1),
               i = r.getUint32(4, !1),
               o = r.getUint32(8, !1),
               a = r.getUint32(12, !1),
               c = [];
            for (let y = 0; y < 128; y++)
               c.push({ s0: Te(s), s1: Te(i), s2: Te(o), s3: Te(a) }),
                  ({ s0: s, s1: i, s2: o, s3: a } = Mf(s, i, o, a));
            let l = Hf(n || 1024);
            if (![1, 2, 4, 8].includes(l))
               throw new Error(
                  `ghash: wrong window size=${l}, should be 2, 4 or 8`,
               );
            this.W = l;
            let h = 128 / l,
               f = (this.windowSize = 2 ** l),
               g = [];
            for (let y = 0; y < h; y++)
               for (let d = 0; d < f; d++) {
                  let p = 0,
                     b = 0,
                     w = 0,
                     E = 0;
                  for (let k = 0; k < l; k++) {
                     if (!((d >>> (l - k - 1)) & 1)) continue;
                     let { s0: A, s1: T, s2: _, s3: I } = c[l * y + k];
                     (p ^= A), (b ^= T), (w ^= _), (E ^= I);
                  }
                  g.push({ s0: p, s1: b, s2: w, s3: E });
               }
            this.t = g;
         }
         _updateBlock(e, n, r, s) {
            (e ^= this.s0), (n ^= this.s1), (r ^= this.s2), (s ^= this.s3);
            let { W: i, t: o, windowSize: a } = this,
               c = 0,
               l = 0,
               u = 0,
               h = 0,
               f = (1 << i) - 1,
               g = 0;
            for (let y of [e, n, r, s])
               for (let d = 0; d < 4; d++) {
                  let p = (y >>> (8 * d)) & 255;
                  for (let b = 8 / i - 1; b >= 0; b--) {
                     let w = (p >>> (i * b)) & f,
                        { s0: E, s1: k, s2: B, s3: A } = o[g * a + w];
                     (c ^= E), (l ^= k), (u ^= B), (h ^= A), (g += 1);
                  }
               }
            (this.s0 = c), (this.s1 = l), (this.s2 = u), (this.s3 = h);
         }
         update(e) {
            (e = Ve(e)), kt(this);
            let n = j(e),
               r = Math.floor(e.length / rt),
               s = e.length % rt;
            for (let i = 0; i < r; i++)
               this._updateBlock(
                  n[i * 4 + 0],
                  n[i * 4 + 1],
                  n[i * 4 + 2],
                  n[i * 4 + 3],
               );
            return (
               s &&
                  (zs.set(e.subarray(r * rt)),
                  this._updateBlock(je[0], je[1], je[2], je[3]),
                  je.fill(0)),
               this
            );
         }
         destroy() {
            let { t: e } = this;
            for (let n of e) (n.s0 = 0), (n.s1 = 0), (n.s2 = 0), (n.s3 = 0);
         }
         digestInto(e) {
            kt(this), Un(e, this), (this.finished = !0);
            let { s0: n, s1: r, s2: s, s3: i } = this,
               o = j(e);
            return (o[0] = n), (o[1] = r), (o[2] = s), (o[3] = i), e;
         }
         digest() {
            let e = new Uint8Array(rt);
            return this.digestInto(e), this.destroy(), e;
         }
      },
      Vs = class extends kr {
         constructor(e, n) {
            e = Ve(e);
            let r = Nf(e.slice());
            super(r, n), r.fill(0);
         }
         update(e) {
            (e = Ve(e)), kt(this);
            let n = j(e),
               r = e.length % rt,
               s = Math.floor(e.length / rt);
            for (let i = 0; i < s; i++)
               this._updateBlock(
                  Te(n[i * 4 + 3]),
                  Te(n[i * 4 + 2]),
                  Te(n[i * 4 + 1]),
                  Te(n[i * 4 + 0]),
               );
            return (
               r &&
                  (zs.set(e.subarray(s * rt)),
                  this._updateBlock(Te(je[3]), Te(je[2]), Te(je[1]), Te(je[0])),
                  je.fill(0)),
               this
            );
         }
         digestInto(e) {
            kt(this), Un(e, this), (this.finished = !0);
            let { s0: n, s1: r, s2: s, s3: i } = this,
               o = j(e);
            return (o[0] = n), (o[1] = r), (o[2] = s), (o[3] = i), e.reverse();
         }
      };
   function ga(t) {
      let e = (r, s) => t(s, r.length).update(Ve(r)).digest(),
         n = t(new Uint8Array(16), 0);
      return (
         (e.outputLen = n.outputLen),
         (e.blockLen = n.blockLen),
         (e.create = (r, s) => t(r, s)),
         e
      );
   }
   var js = ga((t, e) => new kr(t, e)),
      ya = ga((t, e) => new Vs(t, e));
   var ge = 16,
      Ws = 4,
      Ar = new Uint8Array(ge),
      qf = 283;
   function Gs(t) {
      return (t << 1) ^ (qf & -(t >> 7));
   }
   function bn(t, e) {
      let n = 0;
      for (; e > 0; e >>= 1) (n ^= t & -(e & 1)), (t = Gs(t));
      return n;
   }
   var Ds = (() => {
         let t = new Uint8Array(256);
         for (let n = 0, r = 1; n < 256; n++, r ^= Gs(r)) t[n] = r;
         let e = new Uint8Array(256);
         e[0] = 99;
         for (let n = 0; n < 255; n++) {
            let r = t[255 - n];
            (r |= r << 8),
               (e[t[n]] =
                  (r ^ (r >> 4) ^ (r >> 5) ^ (r >> 6) ^ (r >> 7) ^ 99) & 255);
         }
         return e;
      })(),
      $f = Ds.map((t, e) => Ds.indexOf(e)),
      Vf = (t) => (t << 24) | (t >>> 8),
      Fs = (t) => (t << 8) | (t >>> 24);
   function ba(t, e) {
      if (t.length !== 256) throw new Error("Wrong sbox length");
      let n = new Uint32Array(256).map((l, u) => e(t[u])),
         r = n.map(Fs),
         s = r.map(Fs),
         i = s.map(Fs),
         o = new Uint32Array(256 * 256),
         a = new Uint32Array(256 * 256),
         c = new Uint16Array(256 * 256);
      for (let l = 0; l < 256; l++)
         for (let u = 0; u < 256; u++) {
            let h = l * 256 + u;
            (o[h] = n[l] ^ r[u]),
               (a[h] = s[l] ^ i[u]),
               (c[h] = (t[l] << 8) | t[u]);
         }
      return { sbox: t, sbox2: c, T0: n, T1: r, T2: s, T3: i, T01: o, T23: a };
   }
   var Zs = ba(Ds, (t) => (bn(t, 3) << 24) | (t << 16) | (t << 8) | bn(t, 2)),
      ma = ba(
         $f,
         (t) =>
            (bn(t, 11) << 24) | (bn(t, 13) << 16) | (bn(t, 9) << 8) | bn(t, 14),
      ),
      zf = (() => {
         let t = new Uint8Array(16);
         for (let e = 0, n = 1; e < 16; e++, n = Gs(n)) t[e] = n;
         return t;
      })();
   function _t(t) {
      z(t);
      let e = t.length;
      if (![16, 24, 32].includes(e))
         throw new Error(
            `aes: wrong key size: should be 16, 24 or 32, got: ${e}`,
         );
      let { sbox2: n } = Zs,
         r = j(t),
         s = r.length,
         i = (a) => Fe(n, a, a, a, a),
         o = new Uint32Array(e + 28);
      o.set(r);
      for (let a = s; a < o.length; a++) {
         let c = o[a - 1];
         a % s === 0
            ? (c = i(Vf(c)) ^ zf[a / s - 1])
            : s > 6 && a % s === 4 && (c = i(c)),
            (o[a] = o[a - s] ^ c);
      }
      return o;
   }
   function wa(t) {
      let e = _t(t),
         n = e.slice(),
         r = e.length,
         { sbox2: s } = Zs,
         { T0: i, T1: o, T2: a, T3: c } = ma;
      for (let l = 0; l < r; l += 4)
         for (let u = 0; u < 4; u++) n[l + u] = e[r - l - 4 + u];
      e.fill(0);
      for (let l = 4; l < r - 4; l++) {
         let u = n[l],
            h = Fe(s, u, u, u, u);
         n[l] =
            i[h & 255] ^ o[(h >>> 8) & 255] ^ a[(h >>> 16) & 255] ^ c[h >>> 24];
      }
      return n;
   }
   function Tt(t, e, n, r, s, i) {
      return (
         t[((n << 8) & 65280) | ((r >>> 8) & 255)] ^
         e[((s >>> 8) & 65280) | ((i >>> 24) & 255)]
      );
   }
   function Fe(t, e, n, r, s) {
      return (
         t[(e & 255) | (n & 65280)] |
         (t[((r >>> 16) & 255) | ((s >>> 16) & 65280)] << 16)
      );
   }
   function _e(t, e, n, r, s) {
      let { sbox2: i, T01: o, T23: a } = Zs,
         c = 0;
      (e ^= t[c++]), (n ^= t[c++]), (r ^= t[c++]), (s ^= t[c++]);
      let l = t.length / 4 - 2;
      for (let y = 0; y < l; y++) {
         let d = t[c++] ^ Tt(o, a, e, n, r, s),
            p = t[c++] ^ Tt(o, a, n, r, s, e),
            b = t[c++] ^ Tt(o, a, r, s, e, n),
            w = t[c++] ^ Tt(o, a, s, e, n, r);
         (e = d), (n = p), (r = b), (s = w);
      }
      let u = t[c++] ^ Fe(i, e, n, r, s),
         h = t[c++] ^ Fe(i, n, r, s, e),
         f = t[c++] ^ Fe(i, r, s, e, n),
         g = t[c++] ^ Fe(i, s, e, n, r);
      return { s0: u, s1: h, s2: f, s3: g };
   }
   function va(t, e, n, r, s) {
      let { sbox2: i, T01: o, T23: a } = ma,
         c = 0;
      (e ^= t[c++]), (n ^= t[c++]), (r ^= t[c++]), (s ^= t[c++]);
      let l = t.length / 4 - 2;
      for (let y = 0; y < l; y++) {
         let d = t[c++] ^ Tt(o, a, e, s, r, n),
            p = t[c++] ^ Tt(o, a, n, e, s, r),
            b = t[c++] ^ Tt(o, a, r, n, e, s),
            w = t[c++] ^ Tt(o, a, s, r, n, e);
         (e = d), (n = p), (r = b), (s = w);
      }
      let u = t[c++] ^ Fe(i, e, s, r, n),
         h = t[c++] ^ Fe(i, n, e, s, r),
         f = t[c++] ^ Fe(i, r, n, e, s),
         g = t[c++] ^ Fe(i, s, r, n, e);
      return { s0: u, s1: h, s2: f, s3: g };
   }
   function mn(t, e) {
      if (!e) return new Uint8Array(t);
      if ((z(e), e.length < t))
         throw new Error(
            `aes: wrong destination length, expected at least ${t}, got: ${e.length}`,
         );
      return e;
   }
   function jf(t, e, n, r) {
      z(e, ge), z(n);
      let s = n.length;
      r = mn(s, r);
      let i = e,
         o = j(i),
         { s0: a, s1: c, s2: l, s3: u } = _e(t, o[0], o[1], o[2], o[3]),
         h = j(n),
         f = j(r);
      for (let y = 0; y + 4 <= h.length; y += 4) {
         (f[y + 0] = h[y + 0] ^ a),
            (f[y + 1] = h[y + 1] ^ c),
            (f[y + 2] = h[y + 2] ^ l),
            (f[y + 3] = h[y + 3] ^ u);
         let d = 1;
         for (let p = i.length - 1; p >= 0; p--)
            (d = (d + (i[p] & 255)) | 0), (i[p] = d & 255), (d >>>= 8);
         ({ s0: a, s1: c, s2: l, s3: u } = _e(t, o[0], o[1], o[2], o[3]));
      }
      let g = ge * Math.floor(h.length / Ws);
      if (g < s) {
         let y = new Uint32Array([a, c, l, u]),
            d = Er(y);
         for (let p = g, b = 0; p < s; p++, b++) r[p] = n[p] ^ d[b];
      }
      return r;
   }
   function Pn(t, e, n, r, s) {
      z(n, ge), z(r), (s = mn(r.length, s));
      let i = n,
         o = j(i),
         a = At(i),
         c = j(r),
         l = j(s),
         u = e ? 0 : 12,
         h = r.length,
         f = a.getUint32(u, e),
         { s0: g, s1: y, s2: d, s3: p } = _e(t, o[0], o[1], o[2], o[3]);
      for (let w = 0; w + 4 <= c.length; w += 4)
         (l[w + 0] = c[w + 0] ^ g),
            (l[w + 1] = c[w + 1] ^ y),
            (l[w + 2] = c[w + 2] ^ d),
            (l[w + 3] = c[w + 3] ^ p),
            (f = (f + 1) >>> 0),
            a.setUint32(u, f, e),
            ({ s0: g, s1: y, s2: d, s3: p } = _e(t, o[0], o[1], o[2], o[3]));
      let b = ge * Math.floor(c.length / Ws);
      if (b < h) {
         let w = new Uint32Array([g, y, d, p]),
            E = Er(w);
         for (let k = b, B = 0; k < h; k++, B++) s[k] = r[k] ^ E[B];
      }
      return s;
   }
   var Zm = ze({ blockSize: 16, nonceLength: 16 }, function (e, n) {
      z(e), z(n, ge);
      function r(s, i) {
         let o = _t(e),
            a = n.slice(),
            c = jf(o, a, s, i);
         return o.fill(0), a.fill(0), c;
      }
      return { encrypt: (s, i) => r(s, i), decrypt: (s, i) => r(s, i) };
   });
   function xa(t) {
      if ((z(t), t.length % ge !== 0))
         throw new Error(
            `aes/(cbc-ecb).decrypt ciphertext should consist of blocks with size ${ge}`,
         );
   }
   function Ea(t, e, n) {
      let r = t.length,
         s = r % ge;
      if (!e && s !== 0)
         throw new Error(
            "aec/(cbc-ecb): unpadded plaintext with disabled padding",
         );
      let i = j(t);
      if (e) {
         let c = ge - s;
         c || (c = ge), (r = r + c);
      }
      let o = mn(r, n),
         a = j(o);
      return { b: i, o: a, out: o };
   }
   function ka(t, e) {
      if (!e) return t;
      let n = t.length;
      if (!n) throw new Error("aes/pcks5: empty ciphertext not allowed");
      let r = t[n - 1];
      if (r <= 0 || r > 16)
         throw new Error(`aes/pcks5: wrong padding byte: ${r}`);
      let s = t.subarray(0, -r);
      for (let i = 0; i < r; i++)
         if (t[n - i - 1] !== r) throw new Error("aes/pcks5: wrong padding");
      return s;
   }
   function Aa(t) {
      let e = new Uint8Array(16),
         n = j(e);
      e.set(t);
      let r = ge - t.length;
      for (let s = ge - r; s < ge; s++) e[s] = r;
      return n;
   }
   var Ym = ze({ blockSize: 16 }, function (e, n = {}) {
         z(e);
         let r = !n.disablePadding;
         return {
            encrypt: (s, i) => {
               z(s);
               let { b: o, o: a, out: c } = Ea(s, r, i),
                  l = _t(e),
                  u = 0;
               for (; u + 4 <= o.length; ) {
                  let {
                     s0: h,
                     s1: f,
                     s2: g,
                     s3: y,
                  } = _e(l, o[u + 0], o[u + 1], o[u + 2], o[u + 3]);
                  (a[u++] = h), (a[u++] = f), (a[u++] = g), (a[u++] = y);
               }
               if (r) {
                  let h = Aa(s.subarray(u * 4)),
                     {
                        s0: f,
                        s1: g,
                        s2: y,
                        s3: d,
                     } = _e(l, h[0], h[1], h[2], h[3]);
                  (a[u++] = f), (a[u++] = g), (a[u++] = y), (a[u++] = d);
               }
               return l.fill(0), c;
            },
            decrypt: (s, i) => {
               xa(s);
               let o = wa(e),
                  a = mn(s.length, i),
                  c = j(s),
                  l = j(a);
               for (let u = 0; u + 4 <= c.length; ) {
                  let {
                     s0: h,
                     s1: f,
                     s2: g,
                     s3: y,
                  } = va(o, c[u + 0], c[u + 1], c[u + 2], c[u + 3]);
                  (l[u++] = h), (l[u++] = f), (l[u++] = g), (l[u++] = y);
               }
               return o.fill(0), ka(a, r);
            },
         };
      }),
      Ys = ze({ blockSize: 16, nonceLength: 16 }, function (e, n, r = {}) {
         z(e), z(n, 16);
         let s = !r.disablePadding;
         return {
            encrypt: (i, o) => {
               let a = _t(e),
                  { b: c, o: l, out: u } = Ea(i, s, o),
                  h = j(n),
                  f = h[0],
                  g = h[1],
                  y = h[2],
                  d = h[3],
                  p = 0;
               for (; p + 4 <= c.length; )
                  (f ^= c[p + 0]),
                     (g ^= c[p + 1]),
                     (y ^= c[p + 2]),
                     (d ^= c[p + 3]),
                     ({ s0: f, s1: g, s2: y, s3: d } = _e(a, f, g, y, d)),
                     (l[p++] = f),
                     (l[p++] = g),
                     (l[p++] = y),
                     (l[p++] = d);
               if (s) {
                  let b = Aa(i.subarray(p * 4));
                  (f ^= b[0]),
                     (g ^= b[1]),
                     (y ^= b[2]),
                     (d ^= b[3]),
                     ({ s0: f, s1: g, s2: y, s3: d } = _e(a, f, g, y, d)),
                     (l[p++] = f),
                     (l[p++] = g),
                     (l[p++] = y),
                     (l[p++] = d);
               }
               return a.fill(0), u;
            },
            decrypt: (i, o) => {
               xa(i);
               let a = wa(e),
                  c = j(n),
                  l = mn(i.length, o),
                  u = j(i),
                  h = j(l),
                  f = c[0],
                  g = c[1],
                  y = c[2],
                  d = c[3];
               for (let p = 0; p + 4 <= u.length; ) {
                  let b = f,
                     w = g,
                     E = y,
                     k = d;
                  (f = u[p + 0]),
                     (g = u[p + 1]),
                     (y = u[p + 2]),
                     (d = u[p + 3]);
                  let { s0: B, s1: A, s2: T, s3: _ } = va(a, f, g, y, d);
                  (h[p++] = B ^ b),
                     (h[p++] = A ^ w),
                     (h[p++] = T ^ E),
                     (h[p++] = _ ^ k);
               }
               return a.fill(0), ka(l, s);
            },
         };
      }),
      Jm = ze({ blockSize: 16, nonceLength: 16 }, function (e, n) {
         z(e), z(n, 16);
         function r(s, i, o) {
            let a = _t(e),
               c = s.length;
            o = mn(c, o);
            let l = j(s),
               u = j(o),
               h = i ? u : l,
               f = j(n),
               g = f[0],
               y = f[1],
               d = f[2],
               p = f[3];
            for (let w = 0; w + 4 <= l.length; ) {
               let { s0: E, s1: k, s2: B, s3: A } = _e(a, g, y, d, p);
               (u[w + 0] = l[w + 0] ^ E),
                  (u[w + 1] = l[w + 1] ^ k),
                  (u[w + 2] = l[w + 2] ^ B),
                  (u[w + 3] = l[w + 3] ^ A),
                  (g = h[w++]),
                  (y = h[w++]),
                  (d = h[w++]),
                  (p = h[w++]);
            }
            let b = ge * Math.floor(l.length / Ws);
            if (b < c) {
               ({ s0: g, s1: y, s2: d, s3: p } = _e(a, g, y, d, p));
               let w = Er(new Uint32Array([g, y, d, p]));
               for (let E = b, k = 0; E < c; E++, k++) o[E] = s[E] ^ w[k];
               w.fill(0);
            }
            return a.fill(0), o;
         }
         return {
            encrypt: (s, i) => r(s, !0, i),
            decrypt: (s, i) => r(s, !1, i),
         };
      });
   function Ta(t, e, n, r, s) {
      let i = t.create(n, r.length + (s?.length || 0));
      s && i.update(s), i.update(r);
      let o = new Uint8Array(16),
         a = At(o);
      return (
         s && Zt(a, 0, BigInt(s.length * 8), e),
         Zt(a, 8, BigInt(r.length * 8), e),
         i.update(o),
         i.digest()
      );
   }
   var Qm = ze(
         { blockSize: 16, nonceLength: 12, tagLength: 16 },
         function (e, n, r) {
            if ((z(n), n.length === 0)) throw new Error("aes/gcm: empty nonce");
            let s = 16;
            function i(a, c, l) {
               let u = Ta(js, !1, a, l, r);
               for (let h = 0; h < c.length; h++) u[h] ^= c[h];
               return u;
            }
            function o() {
               let a = _t(e),
                  c = Ar.slice(),
                  l = Ar.slice();
               if ((Pn(a, !1, l, l, c), n.length === 12)) l.set(n);
               else {
                  let h = Ar.slice(),
                     f = At(h);
                  Zt(f, 8, BigInt(n.length * 8), !1),
                     js.create(c).update(n).update(h).digestInto(l);
               }
               let u = Pn(a, !1, l, Ar);
               return { xk: a, authKey: c, counter: l, tagMask: u };
            }
            return {
               encrypt: (a) => {
                  z(a);
                  let { xk: c, authKey: l, counter: u, tagMask: h } = o(),
                     f = new Uint8Array(a.length + s);
                  Pn(c, !1, u, a, f);
                  let g = i(l, h, f.subarray(0, f.length - s));
                  return f.set(g, a.length), c.fill(0), f;
               },
               decrypt: (a) => {
                  if ((z(a), a.length < s))
                     throw new Error(
                        `aes/gcm: ciphertext less than tagLen (${s})`,
                     );
                  let { xk: c, authKey: l, counter: u, tagMask: h } = o(),
                     f = a.subarray(0, -s),
                     g = a.subarray(-s),
                     y = i(l, h, f);
                  if (!Gt(y, g)) throw new Error("aes/gcm: invalid ghash tag");
                  let d = Pn(c, !1, u, f);
                  return l.fill(0), h.fill(0), c.fill(0), d;
               },
            };
         },
      ),
      Tr = (t, e, n) => (r) => {
         if (!Number.isSafeInteger(r) || e > r || r > n)
            throw new Error(`${t}: invalid value=${r}, must be [${e}..${n}]`);
      },
      Xm = ze(
         { blockSize: 16, nonceLength: 12, tagLength: 16 },
         function (e, n, r) {
            let i = Tr("AAD", 0, 68719476736),
               o = Tr("plaintext", 0, 2 ** 36),
               a = Tr("nonce", 12, 12),
               c = Tr("ciphertext", 16, 2 ** 36 + 16);
            z(n), a(n.length), r && (z(r), i(r.length));
            function l() {
               let f = e.length;
               if (f !== 16 && f !== 24 && f !== 32)
                  throw new Error(
                     `key length must be 16, 24 or 32 bytes, got: ${f} bytes`,
                  );
               let g = _t(e),
                  y = new Uint8Array(f),
                  d = new Uint8Array(16),
                  p = j(n),
                  b = 0,
                  w = p[0],
                  E = p[1],
                  k = p[2],
                  B = 0;
               for (let A of [d, y].map(j)) {
                  let T = j(A);
                  for (let _ = 0; _ < T.length; _ += 2) {
                     let { s0: I, s1: P } = _e(g, b, w, E, k);
                     (T[_ + 0] = I), (T[_ + 1] = P), (b = ++B);
                  }
               }
               return g.fill(0), { authKey: d, encKey: _t(y) };
            }
            function u(f, g, y) {
               let d = Ta(ya, !0, g, y, r);
               for (let B = 0; B < 12; B++) d[B] ^= n[B];
               d[15] &= 127;
               let p = j(d),
                  b = p[0],
                  w = p[1],
                  E = p[2],
                  k = p[3];
               return (
                  ({ s0: b, s1: w, s2: E, s3: k } = _e(f, b, w, E, k)),
                  (p[0] = b),
                  (p[1] = w),
                  (p[2] = E),
                  (p[3] = k),
                  d
               );
            }
            function h(f, g, y) {
               let d = g.slice();
               return (d[15] |= 128), Pn(f, !0, d, y);
            }
            return {
               encrypt: (f) => {
                  z(f), o(f.length);
                  let { encKey: g, authKey: y } = l(),
                     d = u(g, y, f),
                     p = new Uint8Array(f.length + 16);
                  return (
                     p.set(d, f.length),
                     p.set(h(g, d, f)),
                     g.fill(0),
                     y.fill(0),
                     p
                  );
               },
               decrypt: (f) => {
                  z(f), c(f.length);
                  let g = f.subarray(-16),
                     { encKey: y, authKey: d } = l(),
                     p = h(y, g, f.subarray(0, -16)),
                     b = u(y, d, p);
                  if ((y.fill(0), d.fill(0), !Gt(g, b)))
                     throw new Error("invalid polyval tag");
                  return p;
               },
            };
         },
      );
   var de = (t, e) => (t[e++] & 255) | ((t[e++] & 255) << 8),
      Js = class {
         constructor(e) {
            (this.blockLen = 16),
               (this.outputLen = 16),
               (this.buffer = new Uint8Array(16)),
               (this.r = new Uint16Array(10)),
               (this.h = new Uint16Array(10)),
               (this.pad = new Uint16Array(8)),
               (this.pos = 0),
               (this.finished = !1),
               (e = Ve(e)),
               z(e, 32);
            let n = de(e, 0),
               r = de(e, 2),
               s = de(e, 4),
               i = de(e, 6),
               o = de(e, 8),
               a = de(e, 10),
               c = de(e, 12),
               l = de(e, 14);
            (this.r[0] = n & 8191),
               (this.r[1] = ((n >>> 13) | (r << 3)) & 8191),
               (this.r[2] = ((r >>> 10) | (s << 6)) & 7939),
               (this.r[3] = ((s >>> 7) | (i << 9)) & 8191),
               (this.r[4] = ((i >>> 4) | (o << 12)) & 255),
               (this.r[5] = (o >>> 1) & 8190),
               (this.r[6] = ((o >>> 14) | (a << 2)) & 8191),
               (this.r[7] = ((a >>> 11) | (c << 5)) & 8065),
               (this.r[8] = ((c >>> 8) | (l << 8)) & 8191),
               (this.r[9] = (l >>> 5) & 127);
            for (let u = 0; u < 8; u++) this.pad[u] = de(e, 16 + 2 * u);
         }
         process(e, n, r = !1) {
            let s = r ? 0 : 2048,
               { h: i, r: o } = this,
               a = o[0],
               c = o[1],
               l = o[2],
               u = o[3],
               h = o[4],
               f = o[5],
               g = o[6],
               y = o[7],
               d = o[8],
               p = o[9],
               b = de(e, n + 0),
               w = de(e, n + 2),
               E = de(e, n + 4),
               k = de(e, n + 6),
               B = de(e, n + 8),
               A = de(e, n + 10),
               T = de(e, n + 12),
               _ = de(e, n + 14),
               I = i[0] + (b & 8191),
               P = i[1] + (((b >>> 13) | (w << 3)) & 8191),
               U = i[2] + (((w >>> 10) | (E << 6)) & 8191),
               O = i[3] + (((E >>> 7) | (k << 9)) & 8191),
               q = i[4] + (((k >>> 4) | (B << 12)) & 8191),
               C = i[5] + ((B >>> 1) & 8191),
               v = i[6] + (((B >>> 14) | (A << 2)) & 8191),
               x = i[7] + (((A >>> 11) | (T << 5)) & 8191),
               R = i[8] + (((T >>> 8) | (_ << 8)) & 8191),
               L = i[9] + ((_ >>> 5) | s),
               S = 0,
               H =
                  S +
                  I * a +
                  P * (5 * p) +
                  U * (5 * d) +
                  O * (5 * y) +
                  q * (5 * g);
            (S = H >>> 13),
               (H &= 8191),
               (H +=
                  C * (5 * f) +
                  v * (5 * h) +
                  x * (5 * u) +
                  R * (5 * l) +
                  L * (5 * c)),
               (S += H >>> 13),
               (H &= 8191);
            let $ = S + I * c + P * a + U * (5 * p) + O * (5 * d) + q * (5 * y);
            (S = $ >>> 13),
               ($ &= 8191),
               ($ +=
                  C * (5 * g) +
                  v * (5 * f) +
                  x * (5 * h) +
                  R * (5 * u) +
                  L * (5 * l)),
               (S += $ >>> 13),
               ($ &= 8191);
            let V = S + I * l + P * c + U * a + O * (5 * p) + q * (5 * d);
            (S = V >>> 13),
               (V &= 8191),
               (V +=
                  C * (5 * y) +
                  v * (5 * g) +
                  x * (5 * f) +
                  R * (5 * h) +
                  L * (5 * u)),
               (S += V >>> 13),
               (V &= 8191);
            let F = S + I * u + P * l + U * c + O * a + q * (5 * p);
            (S = F >>> 13),
               (F &= 8191),
               (F +=
                  C * (5 * d) +
                  v * (5 * y) +
                  x * (5 * g) +
                  R * (5 * f) +
                  L * (5 * h)),
               (S += F >>> 13),
               (F &= 8191);
            let D = S + I * h + P * u + U * l + O * c + q * a;
            (S = D >>> 13),
               (D &= 8191),
               (D +=
                  C * (5 * p) +
                  v * (5 * d) +
                  x * (5 * y) +
                  R * (5 * g) +
                  L * (5 * f)),
               (S += D >>> 13),
               (D &= 8191);
            let X = S + I * f + P * h + U * u + O * l + q * c;
            (S = X >>> 13),
               (X &= 8191),
               (X +=
                  C * a +
                  v * (5 * p) +
                  x * (5 * d) +
                  R * (5 * y) +
                  L * (5 * g)),
               (S += X >>> 13),
               (X &= 8191);
            let J = S + I * g + P * f + U * h + O * u + q * l;
            (S = J >>> 13),
               (J &= 8191),
               (J += C * c + v * a + x * (5 * p) + R * (5 * d) + L * (5 * y)),
               (S += J >>> 13),
               (J &= 8191);
            let ee = S + I * y + P * g + U * f + O * h + q * u;
            (S = ee >>> 13),
               (ee &= 8191),
               (ee += C * l + v * c + x * a + R * (5 * p) + L * (5 * d)),
               (S += ee >>> 13),
               (ee &= 8191);
            let ae = S + I * d + P * y + U * g + O * f + q * h;
            (S = ae >>> 13),
               (ae &= 8191),
               (ae += C * u + v * l + x * c + R * a + L * (5 * p)),
               (S += ae >>> 13),
               (ae &= 8191);
            let Q = S + I * p + P * d + U * y + O * g + q * f;
            (S = Q >>> 13),
               (Q &= 8191),
               (Q += C * h + v * u + x * l + R * c + L * a),
               (S += Q >>> 13),
               (Q &= 8191),
               (S = ((S << 2) + S) | 0),
               (S = (S + H) | 0),
               (H = S & 8191),
               (S = S >>> 13),
               ($ += S),
               (i[0] = H),
               (i[1] = $),
               (i[2] = V),
               (i[3] = F),
               (i[4] = D),
               (i[5] = X),
               (i[6] = J),
               (i[7] = ee),
               (i[8] = ae),
               (i[9] = Q);
         }
         finalize() {
            let { h: e, pad: n } = this,
               r = new Uint16Array(10),
               s = e[1] >>> 13;
            e[1] &= 8191;
            for (let a = 2; a < 10; a++)
               (e[a] += s), (s = e[a] >>> 13), (e[a] &= 8191);
            (e[0] += s * 5),
               (s = e[0] >>> 13),
               (e[0] &= 8191),
               (e[1] += s),
               (s = e[1] >>> 13),
               (e[1] &= 8191),
               (e[2] += s),
               (r[0] = e[0] + 5),
               (s = r[0] >>> 13),
               (r[0] &= 8191);
            for (let a = 1; a < 10; a++)
               (r[a] = e[a] + s), (s = r[a] >>> 13), (r[a] &= 8191);
            r[9] -= 8192;
            let i = (s ^ 1) - 1;
            for (let a = 0; a < 10; a++) r[a] &= i;
            i = ~i;
            for (let a = 0; a < 10; a++) e[a] = (e[a] & i) | r[a];
            (e[0] = (e[0] | (e[1] << 13)) & 65535),
               (e[1] = ((e[1] >>> 3) | (e[2] << 10)) & 65535),
               (e[2] = ((e[2] >>> 6) | (e[3] << 7)) & 65535),
               (e[3] = ((e[3] >>> 9) | (e[4] << 4)) & 65535),
               (e[4] = ((e[4] >>> 12) | (e[5] << 1) | (e[6] << 14)) & 65535),
               (e[5] = ((e[6] >>> 2) | (e[7] << 11)) & 65535),
               (e[6] = ((e[7] >>> 5) | (e[8] << 8)) & 65535),
               (e[7] = ((e[8] >>> 8) | (e[9] << 5)) & 65535);
            let o = e[0] + n[0];
            e[0] = o & 65535;
            for (let a = 1; a < 8; a++)
               (o = (((e[a] + n[a]) | 0) + (o >>> 16)) | 0), (e[a] = o & 65535);
         }
         update(e) {
            kt(this);
            let { buffer: n, blockLen: r } = this;
            e = Ve(e);
            let s = e.length;
            for (let i = 0; i < s; ) {
               let o = Math.min(r - this.pos, s - i);
               if (o === r) {
                  for (; r <= s - i; i += r) this.process(e, i);
                  continue;
               }
               n.set(e.subarray(i, i + o), this.pos),
                  (this.pos += o),
                  (i += o),
                  this.pos === r && (this.process(n, 0, !1), (this.pos = 0));
            }
            return this;
         }
         destroy() {
            this.h.fill(0),
               this.r.fill(0),
               this.buffer.fill(0),
               this.pad.fill(0);
         }
         digestInto(e) {
            kt(this), Un(e, this), (this.finished = !0);
            let { buffer: n, h: r } = this,
               { pos: s } = this;
            if (s) {
               for (n[s++] = 1; s < 16; s++) n[s] = 0;
               this.process(n, 0, !0);
            }
            this.finalize();
            let i = 0;
            for (let o = 0; o < 8; o++)
               (e[i++] = r[o] >>> 0), (e[i++] = r[o] >>> 8);
            return e;
         }
         digest() {
            let { buffer: e, outputLen: n } = this;
            this.digestInto(e);
            let r = e.slice(0, n);
            return this.destroy(), r;
         }
      };
   function Ff(t) {
      let e = (r, s) => t(s).update(Ve(r)).digest(),
         n = t(new Uint8Array(32));
      return (
         (e.outputLen = n.outputLen),
         (e.blockLen = n.blockLen),
         (e.create = (r) => t(r)),
         e
      );
   }
   var Sa = Ff((t) => new Js(t));
   var Ca = (t) => Uint8Array.from(t.split("").map((e) => e.charCodeAt(0))),
      Df = Ca("expand 16-byte k"),
      Wf = Ca("expand 32-byte k"),
      Gf = j(Df),
      Ba = j(Wf),
      iw = Ba.slice();
   function M(t, e) {
      return (t << e) | (t >>> (32 - e));
   }
   function Qs(t) {
      return t.byteOffset % 4 === 0;
   }
   var _r = 64,
      Zf = 16,
      Ia = 2 ** 32 - 1,
      Ra = new Uint32Array();
   function Yf(t, e, n, r, s, i, o, a) {
      let c = s.length,
         l = new Uint8Array(_r),
         u = j(l),
         h = Qs(s) && Qs(i),
         f = h ? j(s) : Ra,
         g = h ? j(i) : Ra;
      for (let y = 0; y < c; o++) {
         if ((t(e, n, r, u, o, a), o >= Ia))
            throw new Error("arx: counter overflow");
         let d = Math.min(_r, c - y);
         if (h && d === _r) {
            let p = y / 4;
            if (y % 4 !== 0) throw new Error("arx: invalid block position");
            for (let b = 0, w; b < Zf; b++) (w = p + b), (g[w] = f[w] ^ u[b]);
            y += _r;
            continue;
         }
         for (let p = 0, b; p < d; p++) (b = y + p), (i[b] = s[b] ^ l[p]);
         y += d;
      }
   }
   function Xs(t, e) {
      let {
         allowShortKeys: n,
         extendNonceFn: r,
         counterLength: s,
         counterRight: i,
         rounds: o,
      } = pa(
         { allowShortKeys: !1, counterLength: 8, counterRight: !1, rounds: 20 },
         e,
      );
      if (typeof t != "function") throw new Error("core must be a function");
      return (
         xr(s),
         xr(o),
         qs(i),
         qs(n),
         (a, c, l, u, h = 0) => {
            z(a), z(c), z(l);
            let f = l.length;
            if ((u || (u = new Uint8Array(f)), z(u), xr(h), h < 0 || h >= Ia))
               throw new Error("arx: counter overflow");
            if (u.length < f)
               throw new Error(
                  `arx: output (${u.length}) is shorter than data (${f})`,
               );
            let g = [],
               y = a.length,
               d,
               p;
            if (y === 32) (d = a.slice()), g.push(d), (p = Ba);
            else if (y === 16 && n)
               (d = new Uint8Array(32)),
                  d.set(a),
                  d.set(a, 16),
                  (p = Gf),
                  g.push(d);
            else throw new Error(`arx: invalid 32-byte key, got length=${y}`);
            Qs(c) || ((c = c.slice()), g.push(c));
            let b = j(d);
            if (r) {
               if (c.length !== 24)
                  throw new Error("arx: extended nonce must be 24 bytes");
               r(p, b, j(c.subarray(0, 16)), b), (c = c.subarray(16));
            }
            let w = 16 - s;
            if (w !== c.length)
               throw new Error(`arx: nonce must be ${w} or 16 bytes`);
            if (w !== 12) {
               let k = new Uint8Array(12);
               k.set(c, i ? 0 : 12 - c.length), (c = k), g.push(c);
            }
            let E = j(c);
            for (Yf(t, p, b, E, l, u, h, o); g.length > 0; ) g.pop().fill(0);
            return u;
         }
      );
   }
   function Pa(t, e, n, r, s, i = 20) {
      let o = t[0],
         a = t[1],
         c = t[2],
         l = t[3],
         u = e[0],
         h = e[1],
         f = e[2],
         g = e[3],
         y = e[4],
         d = e[5],
         p = e[6],
         b = e[7],
         w = s,
         E = n[0],
         k = n[1],
         B = n[2],
         A = o,
         T = a,
         _ = c,
         I = l,
         P = u,
         U = h,
         O = f,
         q = g,
         C = y,
         v = d,
         x = p,
         R = b,
         L = w,
         S = E,
         H = k,
         $ = B;
      for (let F = 0; F < i; F += 2)
         (A = (A + P) | 0),
            (L = M(L ^ A, 16)),
            (C = (C + L) | 0),
            (P = M(P ^ C, 12)),
            (A = (A + P) | 0),
            (L = M(L ^ A, 8)),
            (C = (C + L) | 0),
            (P = M(P ^ C, 7)),
            (T = (T + U) | 0),
            (S = M(S ^ T, 16)),
            (v = (v + S) | 0),
            (U = M(U ^ v, 12)),
            (T = (T + U) | 0),
            (S = M(S ^ T, 8)),
            (v = (v + S) | 0),
            (U = M(U ^ v, 7)),
            (_ = (_ + O) | 0),
            (H = M(H ^ _, 16)),
            (x = (x + H) | 0),
            (O = M(O ^ x, 12)),
            (_ = (_ + O) | 0),
            (H = M(H ^ _, 8)),
            (x = (x + H) | 0),
            (O = M(O ^ x, 7)),
            (I = (I + q) | 0),
            ($ = M($ ^ I, 16)),
            (R = (R + $) | 0),
            (q = M(q ^ R, 12)),
            (I = (I + q) | 0),
            ($ = M($ ^ I, 8)),
            (R = (R + $) | 0),
            (q = M(q ^ R, 7)),
            (A = (A + U) | 0),
            ($ = M($ ^ A, 16)),
            (x = (x + $) | 0),
            (U = M(U ^ x, 12)),
            (A = (A + U) | 0),
            ($ = M($ ^ A, 8)),
            (x = (x + $) | 0),
            (U = M(U ^ x, 7)),
            (T = (T + O) | 0),
            (L = M(L ^ T, 16)),
            (R = (R + L) | 0),
            (O = M(O ^ R, 12)),
            (T = (T + O) | 0),
            (L = M(L ^ T, 8)),
            (R = (R + L) | 0),
            (O = M(O ^ R, 7)),
            (_ = (_ + q) | 0),
            (S = M(S ^ _, 16)),
            (C = (C + S) | 0),
            (q = M(q ^ C, 12)),
            (_ = (_ + q) | 0),
            (S = M(S ^ _, 8)),
            (C = (C + S) | 0),
            (q = M(q ^ C, 7)),
            (I = (I + P) | 0),
            (H = M(H ^ I, 16)),
            (v = (v + H) | 0),
            (P = M(P ^ v, 12)),
            (I = (I + P) | 0),
            (H = M(H ^ I, 8)),
            (v = (v + H) | 0),
            (P = M(P ^ v, 7));
      let V = 0;
      (r[V++] = (o + A) | 0),
         (r[V++] = (a + T) | 0),
         (r[V++] = (c + _) | 0),
         (r[V++] = (l + I) | 0),
         (r[V++] = (u + P) | 0),
         (r[V++] = (h + U) | 0),
         (r[V++] = (f + O) | 0),
         (r[V++] = (g + q) | 0),
         (r[V++] = (y + C) | 0),
         (r[V++] = (d + v) | 0),
         (r[V++] = (p + x) | 0),
         (r[V++] = (b + R) | 0),
         (r[V++] = (w + L) | 0),
         (r[V++] = (E + S) | 0),
         (r[V++] = (k + H) | 0),
         (r[V++] = (B + $) | 0);
   }
   function Jf(t, e, n, r) {
      let s = t[0],
         i = t[1],
         o = t[2],
         a = t[3],
         c = e[0],
         l = e[1],
         u = e[2],
         h = e[3],
         f = e[4],
         g = e[5],
         y = e[6],
         d = e[7],
         p = n[0],
         b = n[1],
         w = n[2],
         E = n[3];
      for (let B = 0; B < 20; B += 2)
         (s = (s + c) | 0),
            (p = M(p ^ s, 16)),
            (f = (f + p) | 0),
            (c = M(c ^ f, 12)),
            (s = (s + c) | 0),
            (p = M(p ^ s, 8)),
            (f = (f + p) | 0),
            (c = M(c ^ f, 7)),
            (i = (i + l) | 0),
            (b = M(b ^ i, 16)),
            (g = (g + b) | 0),
            (l = M(l ^ g, 12)),
            (i = (i + l) | 0),
            (b = M(b ^ i, 8)),
            (g = (g + b) | 0),
            (l = M(l ^ g, 7)),
            (o = (o + u) | 0),
            (w = M(w ^ o, 16)),
            (y = (y + w) | 0),
            (u = M(u ^ y, 12)),
            (o = (o + u) | 0),
            (w = M(w ^ o, 8)),
            (y = (y + w) | 0),
            (u = M(u ^ y, 7)),
            (a = (a + h) | 0),
            (E = M(E ^ a, 16)),
            (d = (d + E) | 0),
            (h = M(h ^ d, 12)),
            (a = (a + h) | 0),
            (E = M(E ^ a, 8)),
            (d = (d + E) | 0),
            (h = M(h ^ d, 7)),
            (s = (s + l) | 0),
            (E = M(E ^ s, 16)),
            (y = (y + E) | 0),
            (l = M(l ^ y, 12)),
            (s = (s + l) | 0),
            (E = M(E ^ s, 8)),
            (y = (y + E) | 0),
            (l = M(l ^ y, 7)),
            (i = (i + u) | 0),
            (p = M(p ^ i, 16)),
            (d = (d + p) | 0),
            (u = M(u ^ d, 12)),
            (i = (i + u) | 0),
            (p = M(p ^ i, 8)),
            (d = (d + p) | 0),
            (u = M(u ^ d, 7)),
            (o = (o + h) | 0),
            (b = M(b ^ o, 16)),
            (f = (f + b) | 0),
            (h = M(h ^ f, 12)),
            (o = (o + h) | 0),
            (b = M(b ^ o, 8)),
            (f = (f + b) | 0),
            (h = M(h ^ f, 7)),
            (a = (a + c) | 0),
            (w = M(w ^ a, 16)),
            (g = (g + w) | 0),
            (c = M(c ^ g, 12)),
            (a = (a + c) | 0),
            (w = M(w ^ a, 8)),
            (g = (g + w) | 0),
            (c = M(c ^ g, 7));
      let k = 0;
      (r[k++] = s),
         (r[k++] = i),
         (r[k++] = o),
         (r[k++] = a),
         (r[k++] = p),
         (r[k++] = b),
         (r[k++] = w),
         (r[k++] = E);
   }
   var Sr = Xs(Pa, { counterRight: !1, counterLength: 4, allowShortKeys: !1 }),
      Qf = Xs(Pa, {
         counterRight: !1,
         counterLength: 8,
         extendNonceFn: Jf,
         allowShortKeys: !1,
      });
   var Xf = new Uint8Array(16),
      La = (t, e) => {
         t.update(e);
         let n = e.length % 16;
         n && t.update(Xf.subarray(n));
      },
      Kf = new Uint8Array(32);
   function Ua(t, e, n, r, s) {
      let i = t(e, n, Kf),
         o = Sa.create(i);
      s && La(o, s), La(o, r);
      let a = new Uint8Array(16),
         c = At(a);
      Zt(c, 0, BigInt(s ? s.length : 0), !0),
         Zt(c, 8, BigInt(r.length), !0),
         o.update(a);
      let l = o.digest();
      return i.fill(0), l;
   }
   var Oa = (t) => (e, n, r) => (
         z(e, 32),
         z(n),
         {
            encrypt: (i, o) => {
               let a = i.length,
                  c = a + 16;
               o ? z(o, c) : (o = new Uint8Array(c)), t(e, n, i, o, 1);
               let l = Ua(t, e, n, o.subarray(0, -16), r);
               return o.set(l, a), o;
            },
            decrypt: (i, o) => {
               let a = i.length,
                  c = a - 16;
               if (a < 16)
                  throw new Error("encrypted data must be at least 16 bytes");
               o ? z(o, c) : (o = new Uint8Array(c));
               let l = i.subarray(0, -16),
                  u = i.subarray(-16),
                  h = Ua(t, e, n, l, r);
               if (!Gt(u, h)) throw new Error("invalid tag");
               return t(e, n, l, o, 1), o;
            },
         }
      ),
      hw = ze({ blockSize: 64, nonceLength: 12, tagLength: 16 }, Oa(Sr)),
      fw = ze({ blockSize: 64, nonceLength: 24, tagLength: 16 }, Oa(Qf));
   var Rr = class extends pn {
         constructor(e, n) {
            super(), (this.finished = !1), (this.destroyed = !1), we.hash(e);
            let r = mt(n);
            if (
               ((this.iHash = e.create()),
               typeof this.iHash.update != "function")
            )
               throw new Error(
                  "Expected instance of class which extends utils.Hash",
               );
            (this.blockLen = this.iHash.blockLen),
               (this.outputLen = this.iHash.outputLen);
            let s = this.blockLen,
               i = new Uint8Array(s);
            i.set(r.length > s ? e.create().update(r).digest() : r);
            for (let o = 0; o < i.length; o++) i[o] ^= 54;
            this.iHash.update(i), (this.oHash = e.create());
            for (let o = 0; o < i.length; o++) i[o] ^= 106;
            this.oHash.update(i), i.fill(0);
         }
         update(e) {
            return we.exists(this), this.iHash.update(e), this;
         }
         digestInto(e) {
            we.exists(this),
               we.bytes(e, this.outputLen),
               (this.finished = !0),
               this.iHash.digestInto(e),
               this.oHash.update(e),
               this.oHash.digestInto(e),
               this.destroy();
         }
         digest() {
            let e = new Uint8Array(this.oHash.outputLen);
            return this.digestInto(e), e;
         }
         _cloneInto(e) {
            e || (e = Object.create(Object.getPrototypeOf(this), {}));
            let {
               oHash: n,
               iHash: r,
               finished: s,
               destroyed: i,
               blockLen: o,
               outputLen: a,
            } = this;
            return (
               (e = e),
               (e.finished = s),
               (e.destroyed = i),
               (e.blockLen = o),
               (e.outputLen = a),
               (e.oHash = n._cloneInto(e.oHash)),
               (e.iHash = r._cloneInto(e.iHash)),
               e
            );
         }
         destroy() {
            (this.destroyed = !0), this.oHash.destroy(), this.iHash.destroy();
         }
      },
      wn = (t, e, n) => new Rr(t, e).update(n).digest();
   wn.create = (t, e) => new Rr(t, e);
   function Na(t, e, n) {
      return (
         we.hash(t),
         n === void 0 && (n = new Uint8Array(t.outputLen)),
         wn(t, mt(n), mt(e))
      );
   }
   var Ks = new Uint8Array([0]),
      Ma = new Uint8Array();
   function Ha(t, e, n, r = 32) {
      if ((we.hash(t), we.number(r), r > 255 * t.outputLen))
         throw new Error("Length should be <= 255*HashLen");
      let s = Math.ceil(r / t.outputLen);
      n === void 0 && (n = Ma);
      let i = new Uint8Array(s * t.outputLen),
         o = wn.create(t, e),
         a = o._cloneInto(),
         c = new Uint8Array(o.outputLen);
      for (let l = 0; l < s; l++)
         (Ks[0] = l + 1),
            a
               .update(l === 0 ? Ma : c)
               .update(n)
               .update(Ks)
               .digestInto(c),
            i.set(c, t.outputLen * l),
            o._cloneInto(a);
      return o.destroy(), a.destroy(), c.fill(0), Ks.fill(0), i.slice(0, r);
   }
   var ed = Object.defineProperty,
      Z = (t, e) => {
         for (var n in e) ed(t, n, { get: e[n], enumerable: !0 });
      },
      vn = Symbol("verified"),
      td = (t) => t instanceof Object;
   function ni(t) {
      if (
         !td(t) ||
         typeof t.kind != "number" ||
         typeof t.content != "string" ||
         typeof t.created_at != "number" ||
         typeof t.pubkey != "string" ||
         !t.pubkey.match(/^[a-f0-9]{64}$/) ||
         !Array.isArray(t.tags)
      )
         return !1;
      for (let e = 0; e < t.tags.length; e++) {
         let n = t.tags[e];
         if (!Array.isArray(n)) return !1;
         for (let r = 0; r < n.length; r++)
            if (typeof n[r] == "object") return !1;
      }
      return !0;
   }
   var nd = {};
   Z(nd, {
      Queue: () => od,
      QueueNode: () => $a,
      binarySearch: () => ri,
      insertEventIntoAscendingList: () => id,
      insertEventIntoDescendingList: () => sd,
      normalizeURL: () => rd,
      utf8Decoder: () => st,
      utf8Encoder: () => Le,
   });
   var st = new TextDecoder("utf-8"),
      Le = new TextEncoder();
   function rd(t) {
      t.indexOf("://") === -1 && (t = "wss://" + t);
      let e = new URL(t);
      return (
         (e.pathname = e.pathname.replace(/\/+/g, "/")),
         e.pathname.endsWith("/") && (e.pathname = e.pathname.slice(0, -1)),
         ((e.port === "80" && e.protocol === "ws:") ||
            (e.port === "443" && e.protocol === "wss:")) &&
            (e.port = ""),
         e.searchParams.sort(),
         (e.hash = ""),
         e.toString()
      );
   }
   function sd(t, e) {
      let [n, r] = ri(t, (s) =>
         e.id === s.id
            ? 0
            : e.created_at === s.created_at
              ? -1
              : s.created_at - e.created_at,
      );
      return r || t.splice(n, 0, e), t;
   }
   function id(t, e) {
      let [n, r] = ri(t, (s) =>
         e.id === s.id
            ? 0
            : e.created_at === s.created_at
              ? -1
              : e.created_at - s.created_at,
      );
      return r || t.splice(n, 0, e), t;
   }
   function ri(t, e) {
      let n = 0,
         r = t.length - 1;
      for (; n <= r; ) {
         let s = Math.floor((n + r) / 2),
            i = e(t[s]);
         if (i === 0) return [s, !0];
         i < 0 ? (r = s - 1) : (n = s + 1);
      }
      return [n, !1];
   }
   var $a = class {
         constructor(t) {
            m(this, "value");
            m(this, "next", null);
            m(this, "prev", null);
            this.value = t;
         }
      },
      od = class {
         constructor() {
            m(this, "first");
            m(this, "last");
            (this.first = null), (this.last = null);
         }
         enqueue(t) {
            let e = new $a(t);
            return (
               this.last
                  ? this.last === this.first
                     ? ((this.last = e),
                       (this.last.prev = this.first),
                       (this.first.next = e))
                     : ((e.prev = this.last),
                       (this.last.next = e),
                       (this.last = e))
                  : ((this.first = e), (this.last = e)),
               !0
            );
         }
         dequeue() {
            if (!this.first) return null;
            if (this.first === this.last) {
               let e = this.first;
               return (this.first = null), (this.last = null), e.value;
            }
            let t = this.first;
            return (this.first = t.next), t.value;
         }
      },
      ad = class {
         generateSecretKey() {
            return dn.utils.randomPrivateKey();
         }
         getPublicKey(t) {
            return K(dn.getPublicKey(t));
         }
         finalizeEvent(t, e) {
            let n = t;
            return (
               (n.pubkey = K(dn.getPublicKey(e))),
               (n.id = Cr(n)),
               (n.sig = K(dn.sign(Cr(n), e))),
               (n[vn] = !0),
               n
            );
         }
         verifyEvent(t) {
            if (typeof t[vn] == "boolean") return t[vn];
            let e = Cr(t);
            if (e !== t.id) return (t[vn] = !1), !1;
            try {
               let n = dn.verify(t.sig, e, t.pubkey);
               return (t[vn] = n), n;
            } catch {
               return (t[vn] = !1), !1;
            }
         }
      };
   function cd(t) {
      if (!ni(t))
         throw new Error(
            "can't serialize event with wrong or missing properties",
         );
      return JSON.stringify([
         0,
         t.pubkey,
         t.created_at,
         t.kind,
         t.tags,
         t.content,
      ]);
   }
   function Cr(t) {
      let e = $e(Le.encode(cd(t)));
      return K(e);
   }
   var Br = new ad(),
      ud = Br.generateSecretKey,
      Va = Br.getPublicKey,
      De = Br.finalizeEvent,
      si = Br.verifyEvent,
      ld = {};
   Z(ld, {
      Application: () => fp,
      BadgeAward: () => wd,
      BadgeDefinition: () => ip,
      BlockedRelaysList: () => jd,
      BookmarkList: () => $d,
      Bookmarksets: () => np,
      Calendar: () => wp,
      CalendarEventRSVP: () => vp,
      ChannelCreation: () => Ga,
      ChannelHideMessage: () => Ja,
      ChannelMessage: () => Ya,
      ChannelMetadata: () => Za,
      ChannelMuteUser: () => Qa,
      ClassifiedListing: () => gp,
      ClientAuth: () => Ka,
      CommunitiesList: () => Vd,
      CommunityDefinition: () => kp,
      CommunityPostApproval: () => Cd,
      Contacts: () => yd,
      CreateOrUpdateProduct: () => cp,
      CreateOrUpdateStall: () => ap,
      Curationsets: () => rp,
      Date: () => bp,
      DirectMessageRelaysList: () => Gd,
      DraftClassifiedListing: () => yp,
      DraftLong: () => lp,
      Emojisets: () => hp,
      EncryptedDirectMessage: () => bd,
      EventDeletion: () => md,
      FileMetadata: () => kd,
      FileServerPreference: () => Zd,
      Followsets: () => Kd,
      GenericRepost: () => xd,
      Genericlists: () => ep,
      GiftWrap: () => Xa,
      HTTPAuth: () => ai,
      Handlerinformation: () => Ep,
      Handlerrecommendation: () => xp,
      Highlights: () => Md,
      InterestsList: () => Dd,
      Interestsets: () => op,
      JobFeedback: () => Ld,
      JobRequest: () => Bd,
      JobResult: () => Id,
      Label: () => Rd,
      LightningPubRPC: () => Jd,
      LiveChatMessage: () => Ad,
      LiveEvent: () => dp,
      LongFormArticle: () => up,
      Metadata: () => dd,
      Mutelist: () => Nd,
      NWCWalletInfo: () => Yd,
      NWCWalletRequest: () => ec,
      NWCWalletResponse: () => Qd,
      NostrConnect: () => Xd,
      OpenTimestamps: () => Ed,
      Pinlist: () => Hd,
      PrivateDirectMessage: () => vd,
      ProblemTracker: () => Td,
      ProfileBadges: () => sp,
      PublicChatsList: () => zd,
      Reaction: () => oi,
      RecommendRelay: () => gd,
      RelayList: () => qd,
      Relaysets: () => tp,
      Report: () => _d,
      Reporting: () => Sd,
      Repost: () => ii,
      Seal: () => Wa,
      SearchRelaysList: () => Fd,
      ShortTextNote: () => pd,
      Time: () => mp,
      UserEmojiList: () => Wd,
      UserStatuses: () => pp,
      Zap: () => Od,
      ZapGoal: () => Ud,
      ZapRequest: () => Pd,
      classifyKind: () => hd,
      isEphemeralKind: () => Fa,
      isKind: () => fd,
      isParameterizedReplaceableKind: () => Da,
      isRegularKind: () => za,
      isReplaceableKind: () => ja,
   });
   function za(t) {
      return (
         (1e3 <= t && t < 1e4) ||
         [1, 2, 4, 5, 6, 7, 8, 16, 40, 41, 42, 43, 44].includes(t)
      );
   }
   function ja(t) {
      return [0, 3].includes(t) || (1e4 <= t && t < 2e4);
   }
   function Fa(t) {
      return 2e4 <= t && t < 3e4;
   }
   function Da(t) {
      return 3e4 <= t && t < 4e4;
   }
   function hd(t) {
      return za(t)
         ? "regular"
         : ja(t)
           ? "replaceable"
           : Fa(t)
             ? "ephemeral"
             : Da(t)
               ? "parameterized"
               : "unknown";
   }
   function fd(t, e) {
      let n = e instanceof Array ? e : [e];
      return (ni(t) && n.includes(t.kind)) || !1;
   }
   var dd = 0,
      pd = 1,
      gd = 2,
      yd = 3,
      bd = 4,
      md = 5,
      ii = 6,
      oi = 7,
      wd = 8,
      Wa = 13,
      vd = 14,
      xd = 16,
      Ga = 40,
      Za = 41,
      Ya = 42,
      Ja = 43,
      Qa = 44,
      Ed = 1040,
      Xa = 1059,
      kd = 1063,
      Ad = 1311,
      Td = 1971,
      _d = 1984,
      Sd = 1984,
      Rd = 1985,
      Cd = 4550,
      Bd = 5999,
      Id = 6999,
      Ld = 7e3,
      Ud = 9041,
      Pd = 9734,
      Od = 9735,
      Md = 9802,
      Nd = 1e4,
      Hd = 10001,
      qd = 10002,
      $d = 10003,
      Vd = 10004,
      zd = 10005,
      jd = 10006,
      Fd = 10007,
      Dd = 10015,
      Wd = 10030,
      Gd = 10050,
      Zd = 10096,
      Yd = 13194,
      Jd = 21e3,
      Ka = 22242,
      ec = 23194,
      Qd = 23195,
      Xd = 24133,
      ai = 27235,
      Kd = 3e4,
      ep = 30001,
      tp = 30002,
      np = 30003,
      rp = 30004,
      sp = 30008,
      ip = 30009,
      op = 30015,
      ap = 30017,
      cp = 30018,
      up = 30023,
      lp = 30024,
      hp = 30030,
      fp = 30078,
      dp = 30311,
      pp = 30315,
      gp = 30402,
      yp = 30403,
      bp = 31922,
      mp = 31923,
      wp = 31924,
      vp = 31925,
      xp = 31989,
      Ep = 31990,
      kp = 34550;
   function Ap(t, e) {
      if (
         (t.ids && t.ids.indexOf(e.id) === -1) ||
         (t.kinds && t.kinds.indexOf(e.kind) === -1) ||
         (t.authors && t.authors.indexOf(e.pubkey) === -1)
      )
         return !1;
      for (let n in t)
         if (n[0] === "#") {
            let r = n.slice(1),
               s = t[`#${r}`];
            if (
               s &&
               !e.tags.find(([i, o]) => i === n.slice(1) && s.indexOf(o) !== -1)
            )
               return !1;
         }
      return !(
         (t.since && e.created_at < t.since) ||
         (t.until && e.created_at > t.until)
      );
   }
   function tc(t, e) {
      for (let n = 0; n < t.length; n++) if (Ap(t[n], e)) return !0;
      return !1;
   }
   var Tp = {};
   Z(Tp, {
      getHex64: () => ci,
      getInt: () => nc,
      getSubscriptionId: () => _p,
      matchEventId: () => Sp,
      matchEventKind: () => Cp,
      matchEventPubkey: () => Rp,
   });
   function ci(t, e) {
      let n = e.length + 3,
         r = t.indexOf(`"${e}":`) + n,
         s = t.slice(r).indexOf('"') + r + 1;
      return t.slice(s, s + 64);
   }
   function nc(t, e) {
      let n = e.length,
         r = t.indexOf(`"${e}":`) + n + 3,
         s = t.slice(r),
         i = Math.min(s.indexOf(","), s.indexOf("}"));
      return parseInt(s.slice(0, i), 10);
   }
   function _p(t) {
      let e = t.slice(0, 22).indexOf('"EVENT"');
      if (e === -1) return null;
      let n = t.slice(e + 7 + 1).indexOf('"');
      if (n === -1) return null;
      let r = e + 7 + 1 + n,
         s = t.slice(r + 1, 80).indexOf('"');
      if (s === -1) return null;
      let i = r + 1 + s;
      return t.slice(r + 1, i);
   }
   function Sp(t, e) {
      return e === ci(t, "id");
   }
   function Rp(t, e) {
      return e === ci(t, "pubkey");
   }
   function Cp(t, e) {
      return e === nc(t, "kind");
   }
   var Bp = {};
   Z(Bp, { makeAuthEvent: () => Ip });
   function Ip(t, e) {
      return {
         kind: Ka,
         created_at: Math.floor(Date.now() / 1e3),
         tags: [
            ["relay", t],
            ["challenge", e],
         ],
         content: "",
      };
   }
   var Lp;
   try {
      Lp = WebSocket;
   } catch {}
   var Up;
   try {
      Up = WebSocket;
   } catch {}
   var ye = {};
   Z(ye, {
      BECH32_REGEX: () => rc,
      Bech32MaxSize: () => ui,
      NostrTypeGuard: () => Pp,
      decode: () => Ir,
      encodeBytes: () => Ur,
      naddrEncode: () => Vp,
      neventEncode: () => $p,
      noteEncode: () => Hp,
      nprofileEncode: () => qp,
      npubEncode: () => Np,
      nsecEncode: () => Mp,
   });
   var Pp = {
         isNProfile: (t) => /^nprofile1[a-z\d]+$/.test(t || ""),
         isNEvent: (t) => /^nevent1[a-z\d]+$/.test(t || ""),
         isNAddr: (t) => /^naddr1[a-z\d]+$/.test(t || ""),
         isNSec: (t) => /^nsec1[a-z\d]{58}$/.test(t || ""),
         isNPub: (t) => /^npub1[a-z\d]{58}$/.test(t || ""),
         isNote: (t) => /^note1[a-z\d]+$/.test(t || ""),
         isNcryptsec: (t) => /^ncryptsec1[a-z\d]+$/.test(t || ""),
      },
      ui = 5e3,
      rc = /[\x21-\x7E]{1,83}1[023456789acdefghjklmnpqrstuvwxyz]{6,}/;
   function Op(t) {
      let e = new Uint8Array(4);
      return (
         (e[0] = (t >> 24) & 255),
         (e[1] = (t >> 16) & 255),
         (e[2] = (t >> 8) & 255),
         (e[3] = t & 255),
         e
      );
   }
   function Ir(t) {
      let { prefix: e, words: n } = Et.decode(t, ui),
         r = new Uint8Array(Et.fromWords(n));
      switch (e) {
         case "nprofile": {
            let s = ei(r);
            if (!s[0]?.[0]) throw new Error("missing TLV 0 for nprofile");
            if (s[0][0].length !== 32)
               throw new Error("TLV 0 should be 32 bytes");
            return {
               type: "nprofile",
               data: {
                  pubkey: K(s[0][0]),
                  relays: s[1] ? s[1].map((i) => st.decode(i)) : [],
               },
            };
         }
         case "nevent": {
            let s = ei(r);
            if (!s[0]?.[0]) throw new Error("missing TLV 0 for nevent");
            if (s[0][0].length !== 32)
               throw new Error("TLV 0 should be 32 bytes");
            if (s[2] && s[2][0].length !== 32)
               throw new Error("TLV 2 should be 32 bytes");
            if (s[3] && s[3][0].length !== 4)
               throw new Error("TLV 3 should be 4 bytes");
            return {
               type: "nevent",
               data: {
                  id: K(s[0][0]),
                  relays: s[1] ? s[1].map((i) => st.decode(i)) : [],
                  author: s[2]?.[0] ? K(s[2][0]) : void 0,
                  kind: s[3]?.[0] ? parseInt(K(s[3][0]), 16) : void 0,
               },
            };
         }
         case "naddr": {
            let s = ei(r);
            if (!s[0]?.[0]) throw new Error("missing TLV 0 for naddr");
            if (!s[2]?.[0]) throw new Error("missing TLV 2 for naddr");
            if (s[2][0].length !== 32)
               throw new Error("TLV 2 should be 32 bytes");
            if (!s[3]?.[0]) throw new Error("missing TLV 3 for naddr");
            if (s[3][0].length !== 4)
               throw new Error("TLV 3 should be 4 bytes");
            return {
               type: "naddr",
               data: {
                  identifier: st.decode(s[0][0]),
                  pubkey: K(s[2][0]),
                  kind: parseInt(K(s[3][0]), 16),
                  relays: s[1] ? s[1].map((i) => st.decode(i)) : [],
               },
            };
         }
         case "nsec":
            return { type: e, data: r };
         case "npub":
         case "note":
            return { type: e, data: K(r) };
         default:
            throw new Error(`unknown prefix ${e}`);
      }
   }
   function ei(t) {
      let e = {},
         n = t;
      for (; n.length > 0; ) {
         let r = n[0],
            s = n[1],
            i = n.slice(2, 2 + s);
         if (((n = n.slice(2 + s)), i.length < s))
            throw new Error(`not enough data to read on TLV ${r}`);
         (e[r] = e[r] || []), e[r].push(i);
      }
      return e;
   }
   function Mp(t) {
      return Ur("nsec", t);
   }
   function Np(t) {
      return Ur("npub", Wt(t));
   }
   function Hp(t) {
      return Ur("note", Wt(t));
   }
   function Lr(t, e) {
      let n = Et.toWords(e);
      return Et.encode(t, n, ui);
   }
   function Ur(t, e) {
      return Lr(t, e);
   }
   function qp(t) {
      let e = li({
         0: [Wt(t.pubkey)],
         1: (t.relays || []).map((n) => Le.encode(n)),
      });
      return Lr("nprofile", e);
   }
   function $p(t) {
      let e;
      t.kind !== void 0 && (e = Op(t.kind));
      let n = li({
         0: [Wt(t.id)],
         1: (t.relays || []).map((r) => Le.encode(r)),
         2: t.author ? [Wt(t.author)] : [],
         3: e ? [new Uint8Array(e)] : [],
      });
      return Lr("nevent", n);
   }
   function Vp(t) {
      let e = new ArrayBuffer(4);
      new DataView(e).setUint32(0, t.kind, !1);
      let n = li({
         0: [Le.encode(t.identifier)],
         1: (t.relays || []).map((r) => Le.encode(r)),
         2: [Wt(t.pubkey)],
         3: [new Uint8Array(e)],
      });
      return Lr("naddr", n);
   }
   function li(t) {
      let e = [];
      return (
         Object.entries(t)
            .reverse()
            .forEach(([n, r]) => {
               r.forEach((s) => {
                  let i = new Uint8Array(s.length + 2);
                  i.set([parseInt(n)], 0),
                     i.set([s.length], 1),
                     i.set(s, 2),
                     e.push(i);
               });
            }),
         gn(...e)
      );
   }
   var zp = {};
   Z(zp, { decrypt: () => jp, encrypt: () => sc });
   async function sc(t, e, n) {
      let r = t instanceof Uint8Array ? K(t) : t,
         s = bt.getSharedSecret(r, "02" + e),
         i = ic(s),
         o = Uint8Array.from(yr(16)),
         a = Le.encode(n),
         c = Ys(i, o).encrypt(a),
         l = Ae.encode(new Uint8Array(c)),
         u = Ae.encode(new Uint8Array(o.buffer));
      return `${l}?iv=${u}`;
   }
   async function jp(t, e, n) {
      let r = t instanceof Uint8Array ? K(t) : t,
         [s, i] = n.split("?iv="),
         o = bt.getSharedSecret(r, "02" + e),
         a = ic(o),
         c = Ae.decode(i),
         l = Ae.decode(s),
         u = Ys(a, c).decrypt(l);
      return st.decode(u);
   }
   function ic(t) {
      return t.slice(1, 33);
   }
   var Fp = {};
   Z(Fp, {
      NIP05_REGEX: () => hi,
      isNip05: () => Dp,
      isValid: () => Zp,
      queryProfile: () => oc,
      searchDomain: () => Gp,
      useFetchImplementation: () => Wp,
   });
   var hi = /^(?:([\w.+-]+)@)?([\w_-]+(\.[\w_-]+)+)$/,
      Dp = (t) => hi.test(t || ""),
      Pr;
   try {
      Pr = fetch;
   } catch {}
   function Wp(t) {
      Pr = t;
   }
   async function Gp(t, e = "") {
      try {
         let n = `https://${t}/.well-known/nostr.json?name=${e}`,
            r = await Pr(n, { redirect: "manual" });
         if (r.status !== 200) throw Error("Wrong response code");
         return (await r.json()).names;
      } catch {
         return {};
      }
   }
   async function oc(t) {
      let e = t.match(hi);
      if (!e) return null;
      let [, n = "_", r] = e;
      try {
         let s = `https://${r}/.well-known/nostr.json?name=${n}`,
            i = await Pr(s, { redirect: "manual" });
         if (i.status !== 200) throw Error("Wrong response code");
         let o = await i.json(),
            a = o.names[n];
         return a ? { pubkey: a, relays: o.relays?.[a] } : null;
      } catch {
         return null;
      }
   }
   async function Zp(t, e) {
      let n = await oc(e);
      return n ? n.pubkey === t : !1;
   }
   var Yp = {};
   Z(Yp, { parse: () => Jp });
   function Jp(t) {
      let e = {
            reply: void 0,
            root: void 0,
            mentions: [],
            profiles: [],
            quotes: [],
         },
         n,
         r;
      for (let s = t.tags.length - 1; s >= 0; s--) {
         let i = t.tags[s];
         if (i[0] === "e" && i[1]) {
            let [o, a, c, l, u] = i,
               h = { id: a, relays: c ? [c] : [], author: u };
            if (l === "root") {
               e.root = h;
               continue;
            }
            if (l === "reply") {
               e.reply = h;
               continue;
            }
            if (l === "mention") {
               e.mentions.push(h);
               continue;
            }
            n ? (r = h) : (n = h), e.mentions.push(h);
            continue;
         }
         if (i[0] === "q" && i[1]) {
            let [o, a, c] = i;
            e.quotes.push({ id: a, relays: c ? [c] : [] });
         }
         if (i[0] === "p" && i[1]) {
            e.profiles.push({ pubkey: i[1], relays: i[2] ? [i[2]] : [] });
            continue;
         }
      }
      return (
         e.root || (e.root = r || n || e.reply),
         e.reply || (e.reply = n || e.root),
         [e.reply, e.root].forEach((s) => {
            if (!s) return;
            let i = e.mentions.indexOf(s);
            if ((i !== -1 && e.mentions.splice(i, 1), s.author)) {
               let o = e.profiles.find((a) => a.pubkey === s.author);
               o &&
                  o.relays &&
                  (s.relays || (s.relays = []),
                  o.relays.forEach((a) => {
                     s.relays?.indexOf(a) === -1 && s.relays.push(a);
                  }),
                  (o.relays = s.relays));
            }
         }),
         e.mentions.forEach((s) => {
            if (s.author) {
               let i = e.profiles.find((o) => o.pubkey === s.author);
               i &&
                  i.relays &&
                  (s.relays || (s.relays = []),
                  i.relays.forEach((o) => {
                     s.relays.indexOf(o) === -1 && s.relays.push(o);
                  }),
                  (i.relays = s.relays));
            }
         }),
         e
      );
   }
   var Qp = {};
   Z(Qp, { fetchRelayInformation: () => Kp, useFetchImplementation: () => Xp });
   var ac;
   try {
      ac = fetch;
   } catch {}
   function Xp(t) {
      ac = t;
   }
   async function Kp(t) {
      return await (
         await fetch(
            t.replace("ws://", "http://").replace("wss://", "https://"),
            { headers: { Accept: "application/nostr+json" } },
         )
      ).json();
   }
   var eg = {};
   Z(eg, { fastEventHash: () => uc, getPow: () => cc, minePow: () => tg });
   function cc(t) {
      let e = 0;
      for (let n = 0; n < 64; n += 8) {
         let r = parseInt(t.substring(n, n + 8), 16);
         if (r === 0) e += 32;
         else {
            e += Math.clz32(r);
            break;
         }
      }
      return e;
   }
   function tg(t, e) {
      let n = 0,
         r = t,
         s = ["nonce", n.toString(), e.toString()];
      for (r.tags.push(s); ; ) {
         let i = Math.floor(new Date().getTime() / 1e3);
         if (
            (i !== r.created_at && ((n = 0), (r.created_at = i)),
            (s[1] = (++n).toString()),
            (r.id = uc(r)),
            cc(r.id) >= e)
         )
            break;
      }
      return r;
   }
   function uc(t) {
      return K(
         $e(
            Le.encode(
               JSON.stringify([
                  0,
                  t.pubkey,
                  t.created_at,
                  t.kind,
                  t.tags,
                  t.content,
               ]),
            ),
         ),
      );
   }
   var ng = {};
   Z(ng, {
      finishRepostEvent: () => rg,
      getRepostedEvent: () => sg,
      getRepostedEventPointer: () => lc,
   });
   function rg(t, e, n, r) {
      return De(
         {
            kind: ii,
            tags: [...(t.tags ?? []), ["e", e.id, n], ["p", e.pubkey]],
            content: t.content === "" ? "" : JSON.stringify(e),
            created_at: t.created_at,
         },
         r,
      );
   }
   function lc(t) {
      if (t.kind !== ii) return;
      let e, n;
      for (
         let r = t.tags.length - 1;
         r >= 0 && (e === void 0 || n === void 0);
         r--
      ) {
         let s = t.tags[r];
         s.length >= 2 &&
            (s[0] === "e" && e === void 0
               ? (e = s)
               : s[0] === "p" && n === void 0 && (n = s));
      }
      if (e !== void 0)
         return {
            id: e[1],
            relays: [e[2], n?.[2]].filter((r) => typeof r == "string"),
            author: n?.[1],
         };
   }
   function sg(t, { skipVerification: e } = {}) {
      let n = lc(t);
      if (n === void 0 || t.content === "") return;
      let r;
      try {
         r = JSON.parse(t.content);
      } catch {
         return;
      }
      if (r.id === n.id && !(!e && !si(r))) return r;
   }
   var ig = {};
   Z(ig, { NOSTR_URI_REGEX: () => Or, parse: () => ag, test: () => og });
   var Or = new RegExp(`nostr:(${rc.source})`);
   function og(t) {
      return typeof t == "string" && new RegExp(`^${Or.source}$`).test(t);
   }
   function ag(t) {
      let e = t.match(new RegExp(`^${Or.source}$`));
      if (!e) throw new Error(`Invalid Nostr URI: ${t}`);
      return { uri: e[0], value: e[1], decoded: Ir(e[1]) };
   }
   var cg = {};
   Z(cg, { finishReactionEvent: () => ug, getReactedEventPointer: () => lg });
   function ug(t, e, n) {
      let r = e.tags.filter(
         (s) => s.length >= 2 && (s[0] === "e" || s[0] === "p"),
      );
      return De(
         {
            ...t,
            kind: oi,
            tags: [...(t.tags ?? []), ...r, ["e", e.id], ["p", e.pubkey]],
            content: t.content ?? "+",
         },
         n,
      );
   }
   function lg(t) {
      if (t.kind !== oi) return;
      let e, n;
      for (
         let r = t.tags.length - 1;
         r >= 0 && (e === void 0 || n === void 0);
         r--
      ) {
         let s = t.tags[r];
         s.length >= 2 &&
            (s[0] === "e" && e === void 0
               ? (e = s)
               : s[0] === "p" && n === void 0 && (n = s));
      }
      if (!(e === void 0 || n === void 0))
         return {
            id: e[1],
            relays: [e[2], n[2]].filter((r) => r !== void 0),
            author: n[1],
         };
   }
   var hg = {};
   Z(hg, { matchAll: () => fg, regex: () => fi, replaceAll: () => dg });
   var fi = () => new RegExp(`\\b${Or.source}\\b`, "g");
   function* fg(t) {
      let e = t.matchAll(fi());
      for (let n of e)
         try {
            let [r, s] = n;
            yield {
               uri: r,
               value: s,
               decoded: Ir(s),
               start: n.index,
               end: n.index + r.length,
            };
         } catch {}
   }
   function dg(t, e) {
      return t.replaceAll(fi(), (n, r) =>
         e({ uri: n, value: r, decoded: Ir(r) }),
      );
   }
   var pg = {};
   Z(pg, {
      channelCreateEvent: () => gg,
      channelHideMessageEvent: () => mg,
      channelMessageEvent: () => bg,
      channelMetadataEvent: () => yg,
      channelMuteUserEvent: () => wg,
   });
   var gg = (t, e) => {
         let n;
         if (typeof t.content == "object") n = JSON.stringify(t.content);
         else if (typeof t.content == "string") n = t.content;
         else return;
         return De(
            {
               kind: Ga,
               tags: [...(t.tags ?? [])],
               content: n,
               created_at: t.created_at,
            },
            e,
         );
      },
      yg = (t, e) => {
         let n;
         if (typeof t.content == "object") n = JSON.stringify(t.content);
         else if (typeof t.content == "string") n = t.content;
         else return;
         return De(
            {
               kind: Za,
               tags: [["e", t.channel_create_event_id], ...(t.tags ?? [])],
               content: n,
               created_at: t.created_at,
            },
            e,
         );
      },
      bg = (t, e) => {
         let n = [["e", t.channel_create_event_id, t.relay_url, "root"]];
         return (
            t.reply_to_channel_message_event_id &&
               n.push([
                  "e",
                  t.reply_to_channel_message_event_id,
                  t.relay_url,
                  "reply",
               ]),
            De(
               {
                  kind: Ya,
                  tags: [...n, ...(t.tags ?? [])],
                  content: t.content,
                  created_at: t.created_at,
               },
               e,
            )
         );
      },
      mg = (t, e) => {
         let n;
         if (typeof t.content == "object") n = JSON.stringify(t.content);
         else if (typeof t.content == "string") n = t.content;
         else return;
         return De(
            {
               kind: Ja,
               tags: [["e", t.channel_message_event_id], ...(t.tags ?? [])],
               content: n,
               created_at: t.created_at,
            },
            e,
         );
      },
      wg = (t, e) => {
         let n;
         if (typeof t.content == "object") n = JSON.stringify(t.content);
         else if (typeof t.content == "string") n = t.content;
         else return;
         return De(
            {
               kind: Qa,
               tags: [["p", t.pubkey_to_mute], ...(t.tags ?? [])],
               content: n,
               created_at: t.created_at,
            },
            e,
         );
      },
      vg = {};
   Z(vg, {
      EMOJI_SHORTCODE_REGEX: () => hc,
      matchAll: () => xg,
      regex: () => di,
      replaceAll: () => Eg,
   });
   var hc = /:(\w+):/,
      di = () => new RegExp(`\\B${hc.source}\\B`, "g");
   function* xg(t) {
      let e = t.matchAll(di());
      for (let n of e)
         try {
            let [r, s] = n;
            yield {
               shortcode: r,
               name: s,
               start: n.index,
               end: n.index + r.length,
            };
         } catch {}
   }
   function Eg(t, e) {
      return t.replaceAll(di(), (n, r) => e({ shortcode: n, name: r }));
   }
   var kg = {};
   Z(kg, { useFetchImplementation: () => Ag, validateGithub: () => Tg });
   var pi;
   try {
      pi = fetch;
   } catch {}
   function Ag(t) {
      pi = t;
   }
   async function Tg(t, e, n) {
      try {
         return (
            (await (
               await pi(`https://gist.github.com/${e}/${n}/raw`)
            ).text()) ===
            `Verifying that I control the following Nostr public key: ${t}`
         );
      } catch {
         return !1;
      }
   }
   var _g = {};
   Z(_g, {
      decrypt: () => mi,
      encrypt: () => bi,
      getConversationKey: () => gi,
      v2: () => Ig,
   });
   var fc = 1,
      dc = 65535;
   function gi(t, e) {
      let n = bt.getSharedSecret(t, "02" + e).subarray(1, 33);
      return Na($e, n, "nip44-v2");
   }
   function pc(t, e) {
      let n = Ha($e, t, e, 76);
      return {
         chacha_key: n.subarray(0, 32),
         chacha_nonce: n.subarray(32, 44),
         hmac_key: n.subarray(44, 76),
      };
   }
   function yi(t) {
      if (!Number.isSafeInteger(t) || t < 1)
         throw new Error("expected positive integer");
      if (t <= 32) return 32;
      let e = 1 << (Math.floor(Math.log2(t - 1)) + 1),
         n = e <= 256 ? 32 : e / 8;
      return n * (Math.floor((t - 1) / n) + 1);
   }
   function Sg(t) {
      if (!Number.isSafeInteger(t) || t < fc || t > dc)
         throw new Error(
            "invalid plaintext size: must be between 1 and 65535 bytes",
         );
      let e = new Uint8Array(2);
      return new DataView(e.buffer).setUint16(0, t, !1), e;
   }
   function Rg(t) {
      let e = Le.encode(t),
         n = e.length,
         r = Sg(n),
         s = new Uint8Array(yi(n) - n);
      return gn(r, e, s);
   }
   function Cg(t) {
      let e = new DataView(t.buffer).getUint16(0),
         n = t.subarray(2, 2 + e);
      if (e < fc || e > dc || n.length !== e || t.length !== 2 + yi(e))
         throw new Error("invalid padding");
      return st.decode(n);
   }
   function gc(t, e, n) {
      if (n.length !== 32)
         throw new Error("AAD associated data must be 32 bytes");
      let r = gn(n, e);
      return wn($e, t, r);
   }
   function Bg(t) {
      if (typeof t != "string")
         throw new Error("payload must be a valid string");
      let e = t.length;
      if (e < 132 || e > 87472) throw new Error("invalid payload length: " + e);
      if (t[0] === "#") throw new Error("unknown encryption version");
      let n;
      try {
         n = Ae.decode(t);
      } catch (i) {
         throw new Error("invalid base64: " + i.message);
      }
      let r = n.length;
      if (r < 99 || r > 65603) throw new Error("invalid data length: " + r);
      let s = n[0];
      if (s !== 2) throw new Error("unknown encryption version " + s);
      return {
         nonce: n.subarray(1, 33),
         ciphertext: n.subarray(33, -32),
         mac: n.subarray(-32),
      };
   }
   function bi(t, e, n = yr(32)) {
      let { chacha_key: r, chacha_nonce: s, hmac_key: i } = pc(e, n),
         o = Rg(t),
         a = Sr(r, s, o),
         c = gc(i, a, n);
      return Ae.encode(gn(new Uint8Array([2]), n, a, c));
   }
   function mi(t, e) {
      let { nonce: n, ciphertext: r, mac: s } = Bg(t),
         { chacha_key: i, chacha_nonce: o, hmac_key: a } = pc(e, n),
         c = gc(a, r, n);
      if (!Gt(c, s)) throw new Error("invalid MAC");
      let l = Sr(i, o, r);
      return Cg(l);
   }
   var Ig = {
         utils: { getConversationKey: gi, calcPaddedLen: yi },
         encrypt: bi,
         decrypt: mi,
      },
      Lg = {};
   Z(Lg, { makeNwcRequestEvent: () => Pg, parseConnectionString: () => Ug });
   function Ug(t) {
      let { pathname: e, searchParams: n } = new URL(t),
         r = e,
         s = n.get("relay"),
         i = n.get("secret");
      if (!r || !s || !i) throw new Error("invalid connection string");
      return { pubkey: r, relay: s, secret: i };
   }
   async function Pg(t, e, n) {
      let s = await sc(
            e,
            t,
            JSON.stringify({ method: "pay_invoice", params: { invoice: n } }),
         ),
         i = {
            kind: ec,
            created_at: Math.round(Date.now() / 1e3),
            content: s,
            tags: [["p", t]],
         };
      return De(i, e);
   }
   var Og = {};
   Z(Og, {
      getZapEndpoint: () => Ng,
      makeZapReceipt: () => $g,
      makeZapRequest: () => Hg,
      useFetchImplementation: () => Mg,
      validateZapRequest: () => qg,
   });
   var wi;
   try {
      wi = fetch;
   } catch {}
   function Mg(t) {
      wi = t;
   }
   async function Ng(t) {
      try {
         let e = "",
            { lud06: n, lud16: r } = JSON.parse(t.content);
         if (n) {
            let { words: o } = Et.decode(n, 1e3),
               a = Et.fromWords(o);
            e = st.decode(a);
         } else if (r) {
            let [o, a] = r.split("@");
            e = new URL(`/.well-known/lnurlp/${o}`, `https://${a}`).toString();
         } else return null;
         let i = await (await wi(e)).json();
         if (i.allowsNostr && i.nostrPubkey) return i.callback;
      } catch {}
      return null;
   }
   function Hg({
      profile: t,
      event: e,
      amount: n,
      relays: r,
      comment: s = "",
   }) {
      if (!n) throw new Error("amount not given");
      if (!t) throw new Error("profile not given");
      let i = {
         kind: 9734,
         created_at: Math.round(Date.now() / 1e3),
         content: s,
         tags: [
            ["p", t],
            ["amount", n.toString()],
            ["relays", ...r],
         ],
      };
      return e && i.tags.push(["e", e]), i;
   }
   function qg(t) {
      let e;
      try {
         e = JSON.parse(t);
      } catch {
         return "Invalid zap request JSON.";
      }
      if (!ni(e)) return "Zap request is not a valid Nostr event.";
      if (!si(e)) return "Invalid signature on zap request.";
      let n = e.tags.find(([i, o]) => i === "p" && o);
      if (!n) return "Zap request doesn't have a 'p' tag.";
      if (!n[1].match(/^[a-f0-9]{64}$/))
         return "Zap request 'p' tag is not valid hex.";
      let r = e.tags.find(([i, o]) => i === "e" && o);
      return r && !r[1].match(/^[a-f0-9]{64}$/)
         ? "Zap request 'e' tag is not valid hex."
         : e.tags.find(([i, o]) => i === "relays" && o)
           ? null
           : "Zap request doesn't have a 'relays' tag.";
   }
   function $g({ zapRequest: t, preimage: e, bolt11: n, paidAt: r }) {
      let s = JSON.parse(t),
         i = s.tags.filter(([a]) => a === "e" || a === "p" || a === "a"),
         o = {
            kind: 9735,
            created_at: Math.round(r.getTime() / 1e3),
            content: "",
            tags: [...i, ["P", s.pubkey], ["bolt11", n], ["description", t]],
         };
      return e && o.tags.push(["preimage", e]), o;
   }
   var Vg = {};
   Z(Vg, {
      createRumor: () => vc,
      createSeal: () => xc,
      createWrap: () => Ec,
      unwrapEvent: () => kc,
      unwrapManyEvents: () => Fg,
      wrapEvent: () => ti,
      wrapManyEvents: () => jg,
   });
   var zg = 2 * 24 * 60 * 60,
      yc = () => Math.round(Date.now() / 1e3),
      bc = () => Math.round(yc() - Math.random() * zg),
      mc = (t, e) => gi(t, e),
      wc = (t, e, n) => bi(JSON.stringify(t), mc(e, n)),
      qa = (t, e) => JSON.parse(mi(t.content, mc(e, t.pubkey)));
   function vc(t, e) {
      let n = { created_at: yc(), content: "", tags: [], ...t, pubkey: Va(e) };
      return (n.id = Cr(n)), n;
   }
   function xc(t, e, n) {
      return De(
         { kind: Wa, content: wc(t, e, n), created_at: bc(), tags: [] },
         e,
      );
   }
   function Ec(t, e) {
      let n = ud();
      return De(
         { kind: Xa, content: wc(t, n, e), created_at: bc(), tags: [["p", e]] },
         n,
      );
   }
   function ti(t, e, n) {
      let r = vc(t, e),
         s = xc(r, e, n);
      return Ec(s, n);
   }
   function jg(t, e, n) {
      if (!n || n.length === 0)
         throw new Error("At least one recipient is required.");
      let r = Va(e),
         s = [ti(t, e, r)];
      return (
         n.forEach((i) => {
            s.push(ti(t, e, i));
         }),
         s
      );
   }
   function kc(t, e) {
      let n = qa(t, e);
      return qa(n, e);
   }
   function Fg(t, e) {
      let n = [];
      return (
         t.forEach((r) => {
            n.push(kc(r, e));
         }),
         n.sort((r, s) => r.created_at - s.created_at),
         n
      );
   }
   var Dg = {};
   Z(Dg, {
      getToken: () => Wg,
      hashPayload: () => vi,
      unpackEventFromToken: () => Tc,
      validateEvent: () => Ic,
      validateEventKind: () => Sc,
      validateEventMethodTag: () => Cc,
      validateEventPayloadTag: () => Bc,
      validateEventTimestamp: () => _c,
      validateEventUrlTag: () => Rc,
      validateToken: () => Gg,
   });
   var Ac = "Nostr ";
   async function Wg(t, e, n, r = !1, s) {
      let i = {
         kind: ai,
         tags: [
            ["u", t],
            ["method", e],
         ],
         created_at: Math.round(new Date().getTime() / 1e3),
         content: "",
      };
      s && i.tags.push(["payload", vi(s)]);
      let o = await n(i);
      return (r ? Ac : "") + Ae.encode(Le.encode(JSON.stringify(o)));
   }
   async function Gg(t, e, n) {
      let r = await Tc(t).catch((i) => {
         throw i;
      });
      return await Ic(r, e, n).catch((i) => {
         throw i;
      });
   }
   async function Tc(t) {
      if (!t) throw new Error("Missing token");
      t = t.replace(Ac, "");
      let e = st.decode(Ae.decode(t));
      if (!e || e.length === 0 || !e.startsWith("{"))
         throw new Error("Invalid token");
      return JSON.parse(e);
   }
   function _c(t) {
      return t.created_at
         ? Math.round(new Date().getTime() / 1e3) - t.created_at < 60
         : !1;
   }
   function Sc(t) {
      return t.kind === ai;
   }
   function Rc(t, e) {
      let n = t.tags.find((r) => r[0] === "u");
      return n ? n.length > 0 && n[1] === e : !1;
   }
   function Cc(t, e) {
      let n = t.tags.find((r) => r[0] === "method");
      return n ? n.length > 0 && n[1].toLowerCase() === e.toLowerCase() : !1;
   }
   function vi(t) {
      let e = $e(Le.encode(JSON.stringify(t)));
      return K(e);
   }
   function Bc(t, e) {
      let n = t.tags.find((s) => s[0] === "payload");
      if (!n) return !1;
      let r = vi(e);
      return n.length > 0 && n[1] === r;
   }
   async function Ic(t, e, n, r) {
      if (!si(t)) throw new Error("Invalid nostr event, signature invalid");
      if (!Sc(t)) throw new Error("Invalid nostr event, kind invalid");
      if (!_c(t))
         throw new Error("Invalid nostr event, created_at timestamp invalid");
      if (!Rc(t, e)) throw new Error("Invalid nostr event, url tag invalid");
      if (!Cc(t, n)) throw new Error("Invalid nostr event, method tag invalid");
      if (r && typeof r == "object" && Object.keys(r).length > 0 && !Bc(t, r))
         throw new Error(
            "Invalid nostr event, payload tag does not match request body hash",
         );
      return !0;
   }
   function Zg(t) {
      return (
         t instanceof Uint8Array ||
         (ArrayBuffer.isView(t) && t.constructor.name === "Uint8Array")
      );
   }
   function Mr(t, ...e) {
      if (!Zg(t)) throw new Error("Uint8Array expected");
      if (e.length > 0 && !e.includes(t.length))
         throw new Error(
            "Uint8Array expected of length " + e + ", got length=" + t.length,
         );
   }
   function xi(t, e = !0) {
      if (t.destroyed) throw new Error("Hash instance has been destroyed");
      if (e && t.finished)
         throw new Error("Hash#digest() has already been called");
   }
   function Lc(t, e) {
      Mr(t);
      let n = e.outputLen;
      if (t.length < n)
         throw new Error(
            "digestInto() expects output buffer of length at least " + n,
         );
   }
   var Hr = (t) => new DataView(t.buffer, t.byteOffset, t.byteLength),
      Ue = (t, e) => (t << (32 - e)) | (t >>> e);
   var Yg = Array.from({ length: 256 }, (t, e) =>
      e.toString(16).padStart(2, "0"),
   );
   function Uc(t) {
      Mr(t);
      let e = "";
      for (let n = 0; n < t.length; n++) e += Yg[t[n]];
      return e;
   }
   function Jg(t) {
      if (typeof t != "string")
         throw new Error("utf8ToBytes expected string, got " + typeof t);
      return new Uint8Array(new TextEncoder().encode(t));
   }
   function Ei(t) {
      return typeof t == "string" && (t = Jg(t)), Mr(t), t;
   }
   var Nr = class {
      clone() {
         return this._cloneInto();
      }
   };
   function Pc(t) {
      let e = (r) => t().update(Ei(r)).digest(),
         n = t();
      return (
         (e.outputLen = n.outputLen),
         (e.blockLen = n.blockLen),
         (e.create = () => t()),
         e
      );
   }
   function Qg(t, e, n, r) {
      if (typeof t.setBigUint64 == "function") return t.setBigUint64(e, n, r);
      let s = BigInt(32),
         i = BigInt(4294967295),
         o = Number((n >> s) & i),
         a = Number(n & i),
         c = r ? 4 : 0,
         l = r ? 0 : 4;
      t.setUint32(e + c, o, r), t.setUint32(e + l, a, r);
   }
   var Oc = (t, e, n) => (t & e) ^ (~t & n),
      Mc = (t, e, n) => (t & e) ^ (t & n) ^ (e & n),
      qr = class extends Nr {
         constructor(e, n, r, s) {
            super(),
               (this.blockLen = e),
               (this.outputLen = n),
               (this.padOffset = r),
               (this.isLE = s),
               (this.finished = !1),
               (this.length = 0),
               (this.pos = 0),
               (this.destroyed = !1),
               (this.buffer = new Uint8Array(e)),
               (this.view = Hr(this.buffer));
         }
         update(e) {
            xi(this);
            let { view: n, buffer: r, blockLen: s } = this;
            e = Ei(e);
            let i = e.length;
            for (let o = 0; o < i; ) {
               let a = Math.min(s - this.pos, i - o);
               if (a === s) {
                  let c = Hr(e);
                  for (; s <= i - o; o += s) this.process(c, o);
                  continue;
               }
               r.set(e.subarray(o, o + a), this.pos),
                  (this.pos += a),
                  (o += a),
                  this.pos === s && (this.process(n, 0), (this.pos = 0));
            }
            return (this.length += e.length), this.roundClean(), this;
         }
         digestInto(e) {
            xi(this), Lc(e, this), (this.finished = !0);
            let { buffer: n, view: r, blockLen: s, isLE: i } = this,
               { pos: o } = this;
            (n[o++] = 128),
               this.buffer.subarray(o).fill(0),
               this.padOffset > s - o && (this.process(r, 0), (o = 0));
            for (let h = o; h < s; h++) n[h] = 0;
            Qg(r, s - 8, BigInt(this.length * 8), i), this.process(r, 0);
            let a = Hr(e),
               c = this.outputLen;
            if (c % 4)
               throw new Error("_sha2: outputLen should be aligned to 32bit");
            let l = c / 4,
               u = this.get();
            if (l > u.length)
               throw new Error("_sha2: outputLen bigger than state");
            for (let h = 0; h < l; h++) a.setUint32(4 * h, u[h], i);
         }
         digest() {
            let { buffer: e, outputLen: n } = this;
            this.digestInto(e);
            let r = e.slice(0, n);
            return this.destroy(), r;
         }
         _cloneInto(e) {
            e || (e = new this.constructor()), e.set(...this.get());
            let {
               blockLen: n,
               buffer: r,
               length: s,
               finished: i,
               destroyed: o,
               pos: a,
            } = this;
            return (
               (e.length = s),
               (e.pos = a),
               (e.finished = i),
               (e.destroyed = o),
               s % n && e.buffer.set(r),
               e
            );
         }
      };
   var Xg = new Uint32Array([
         1116352408, 1899447441, 3049323471, 3921009573, 961987163, 1508970993,
         2453635748, 2870763221, 3624381080, 310598401, 607225278, 1426881987,
         1925078388, 2162078206, 2614888103, 3248222580, 3835390401, 4022224774,
         264347078, 604807628, 770255983, 1249150122, 1555081692, 1996064986,
         2554220882, 2821834349, 2952996808, 3210313671, 3336571891, 3584528711,
         113926993, 338241895, 666307205, 773529912, 1294757372, 1396182291,
         1695183700, 1986661051, 2177026350, 2456956037, 2730485921, 2820302411,
         3259730800, 3345764771, 3516065817, 3600352804, 4094571909, 275423344,
         430227734, 506948616, 659060556, 883997877, 958139571, 1322822218,
         1537002063, 1747873779, 1955562222, 2024104815, 2227730452, 2361852424,
         2428436474, 2756734187, 3204031479, 3329325298,
      ]),
      St = new Uint32Array([
         1779033703, 3144134277, 1013904242, 2773480762, 1359893119, 2600822924,
         528734635, 1541459225,
      ]),
      Rt = new Uint32Array(64),
      ki = class extends qr {
         constructor() {
            super(64, 32, 8, !1),
               (this.A = St[0] | 0),
               (this.B = St[1] | 0),
               (this.C = St[2] | 0),
               (this.D = St[3] | 0),
               (this.E = St[4] | 0),
               (this.F = St[5] | 0),
               (this.G = St[6] | 0),
               (this.H = St[7] | 0);
         }
         get() {
            let { A: e, B: n, C: r, D: s, E: i, F: o, G: a, H: c } = this;
            return [e, n, r, s, i, o, a, c];
         }
         set(e, n, r, s, i, o, a, c) {
            (this.A = e | 0),
               (this.B = n | 0),
               (this.C = r | 0),
               (this.D = s | 0),
               (this.E = i | 0),
               (this.F = o | 0),
               (this.G = a | 0),
               (this.H = c | 0);
         }
         process(e, n) {
            for (let h = 0; h < 16; h++, n += 4) Rt[h] = e.getUint32(n, !1);
            for (let h = 16; h < 64; h++) {
               let f = Rt[h - 15],
                  g = Rt[h - 2],
                  y = Ue(f, 7) ^ Ue(f, 18) ^ (f >>> 3),
                  d = Ue(g, 17) ^ Ue(g, 19) ^ (g >>> 10);
               Rt[h] = (d + Rt[h - 7] + y + Rt[h - 16]) | 0;
            }
            let { A: r, B: s, C: i, D: o, E: a, F: c, G: l, H: u } = this;
            for (let h = 0; h < 64; h++) {
               let f = Ue(a, 6) ^ Ue(a, 11) ^ Ue(a, 25),
                  g = (u + f + Oc(a, c, l) + Xg[h] + Rt[h]) | 0,
                  d = ((Ue(r, 2) ^ Ue(r, 13) ^ Ue(r, 22)) + Mc(r, s, i)) | 0;
               (u = l),
                  (l = c),
                  (c = a),
                  (a = (o + g) | 0),
                  (o = i),
                  (i = s),
                  (s = r),
                  (r = (g + d) | 0);
            }
            (r = (r + this.A) | 0),
               (s = (s + this.B) | 0),
               (i = (i + this.C) | 0),
               (o = (o + this.D) | 0),
               (a = (a + this.E) | 0),
               (c = (c + this.F) | 0),
               (l = (l + this.G) | 0),
               (u = (u + this.H) | 0),
               this.set(r, s, i, o, a, c, l, u);
         }
         roundClean() {
            Rt.fill(0);
         }
         destroy() {
            this.set(0, 0, 0, 0, 0, 0, 0, 0), this.buffer.fill(0);
         }
      };
   var Ai = Pc(() => new ki());
   function Nc(t) {
      if (!Number.isSafeInteger(t) || t < 0)
         throw new Error("positive integer expected, got " + t);
   }
   function Kg(t) {
      return (
         t instanceof Uint8Array ||
         (ArrayBuffer.isView(t) && t.constructor.name === "Uint8Array")
      );
   }
   function xn(t, ...e) {
      if (!Kg(t)) throw new Error("Uint8Array expected");
      if (e.length > 0 && !e.includes(t.length))
         throw new Error(
            "Uint8Array expected of length " + e + ", got length=" + t.length,
         );
   }
   function Hc(t) {
      if (typeof t != "function" || typeof t.create != "function")
         throw new Error("Hash should be wrapped by utils.wrapConstructor");
      Nc(t.outputLen), Nc(t.blockLen);
   }
   function En(t, e = !0) {
      if (t.destroyed) throw new Error("Hash instance has been destroyed");
      if (e && t.finished)
         throw new Error("Hash#digest() has already been called");
   }
   function qc(t, e) {
      xn(t);
      let n = e.outputLen;
      if (t.length < n)
         throw new Error(
            "digestInto() expects output buffer of length at least " + n,
         );
   }
   var Yt =
      typeof globalThis == "object" && "crypto" in globalThis
         ? globalThis.crypto
         : void 0;
   var $r = (t) => new DataView(t.buffer, t.byteOffset, t.byteLength),
      Pe = (t, e) => (t << (32 - e)) | (t >>> e);
   function e0(t) {
      if (typeof t != "string")
         throw new Error("utf8ToBytes expected string, got " + typeof t);
      return new Uint8Array(new TextEncoder().encode(t));
   }
   function On(t) {
      return typeof t == "string" && (t = e0(t)), xn(t), t;
   }
   function $c(...t) {
      let e = 0;
      for (let r = 0; r < t.length; r++) {
         let s = t[r];
         xn(s), (e += s.length);
      }
      let n = new Uint8Array(e);
      for (let r = 0, s = 0; r < t.length; r++) {
         let i = t[r];
         n.set(i, s), (s += i.length);
      }
      return n;
   }
   var kn = class {
      clone() {
         return this._cloneInto();
      }
   };
   function Vc(t) {
      let e = (r) => t().update(On(r)).digest(),
         n = t();
      return (
         (e.outputLen = n.outputLen),
         (e.blockLen = n.blockLen),
         (e.create = () => t()),
         e
      );
   }
   function Vr(t = 32) {
      if (Yt && typeof Yt.getRandomValues == "function")
         return Yt.getRandomValues(new Uint8Array(t));
      if (Yt && typeof Yt.randomBytes == "function") return Yt.randomBytes(t);
      throw new Error("crypto.getRandomValues must be defined");
   }
   function t0(t, e, n, r) {
      if (typeof t.setBigUint64 == "function") return t.setBigUint64(e, n, r);
      let s = BigInt(32),
         i = BigInt(4294967295),
         o = Number((n >> s) & i),
         a = Number(n & i),
         c = r ? 4 : 0,
         l = r ? 0 : 4;
      t.setUint32(e + c, o, r), t.setUint32(e + l, a, r);
   }
   var zc = (t, e, n) => (t & e) ^ (~t & n),
      jc = (t, e, n) => (t & e) ^ (t & n) ^ (e & n),
      zr = class extends kn {
         constructor(e, n, r, s) {
            super(),
               (this.blockLen = e),
               (this.outputLen = n),
               (this.padOffset = r),
               (this.isLE = s),
               (this.finished = !1),
               (this.length = 0),
               (this.pos = 0),
               (this.destroyed = !1),
               (this.buffer = new Uint8Array(e)),
               (this.view = $r(this.buffer));
         }
         update(e) {
            En(this);
            let { view: n, buffer: r, blockLen: s } = this;
            e = On(e);
            let i = e.length;
            for (let o = 0; o < i; ) {
               let a = Math.min(s - this.pos, i - o);
               if (a === s) {
                  let c = $r(e);
                  for (; s <= i - o; o += s) this.process(c, o);
                  continue;
               }
               r.set(e.subarray(o, o + a), this.pos),
                  (this.pos += a),
                  (o += a),
                  this.pos === s && (this.process(n, 0), (this.pos = 0));
            }
            return (this.length += e.length), this.roundClean(), this;
         }
         digestInto(e) {
            En(this), qc(e, this), (this.finished = !0);
            let { buffer: n, view: r, blockLen: s, isLE: i } = this,
               { pos: o } = this;
            (n[o++] = 128),
               this.buffer.subarray(o).fill(0),
               this.padOffset > s - o && (this.process(r, 0), (o = 0));
            for (let h = o; h < s; h++) n[h] = 0;
            t0(r, s - 8, BigInt(this.length * 8), i), this.process(r, 0);
            let a = $r(e),
               c = this.outputLen;
            if (c % 4)
               throw new Error("_sha2: outputLen should be aligned to 32bit");
            let l = c / 4,
               u = this.get();
            if (l > u.length)
               throw new Error("_sha2: outputLen bigger than state");
            for (let h = 0; h < l; h++) a.setUint32(4 * h, u[h], i);
         }
         digest() {
            let { buffer: e, outputLen: n } = this;
            this.digestInto(e);
            let r = e.slice(0, n);
            return this.destroy(), r;
         }
         _cloneInto(e) {
            e || (e = new this.constructor()), e.set(...this.get());
            let {
               blockLen: n,
               buffer: r,
               length: s,
               finished: i,
               destroyed: o,
               pos: a,
            } = this;
            return (
               (e.length = s),
               (e.pos = a),
               (e.finished = i),
               (e.destroyed = o),
               s % n && e.buffer.set(r),
               e
            );
         }
      };
   var n0 = new Uint32Array([
         1116352408, 1899447441, 3049323471, 3921009573, 961987163, 1508970993,
         2453635748, 2870763221, 3624381080, 310598401, 607225278, 1426881987,
         1925078388, 2162078206, 2614888103, 3248222580, 3835390401, 4022224774,
         264347078, 604807628, 770255983, 1249150122, 1555081692, 1996064986,
         2554220882, 2821834349, 2952996808, 3210313671, 3336571891, 3584528711,
         113926993, 338241895, 666307205, 773529912, 1294757372, 1396182291,
         1695183700, 1986661051, 2177026350, 2456956037, 2730485921, 2820302411,
         3259730800, 3345764771, 3516065817, 3600352804, 4094571909, 275423344,
         430227734, 506948616, 659060556, 883997877, 958139571, 1322822218,
         1537002063, 1747873779, 1955562222, 2024104815, 2227730452, 2361852424,
         2428436474, 2756734187, 3204031479, 3329325298,
      ]),
      Ct = new Uint32Array([
         1779033703, 3144134277, 1013904242, 2773480762, 1359893119, 2600822924,
         528734635, 1541459225,
      ]),
      Bt = new Uint32Array(64),
      Ti = class extends zr {
         constructor() {
            super(64, 32, 8, !1),
               (this.A = Ct[0] | 0),
               (this.B = Ct[1] | 0),
               (this.C = Ct[2] | 0),
               (this.D = Ct[3] | 0),
               (this.E = Ct[4] | 0),
               (this.F = Ct[5] | 0),
               (this.G = Ct[6] | 0),
               (this.H = Ct[7] | 0);
         }
         get() {
            let { A: e, B: n, C: r, D: s, E: i, F: o, G: a, H: c } = this;
            return [e, n, r, s, i, o, a, c];
         }
         set(e, n, r, s, i, o, a, c) {
            (this.A = e | 0),
               (this.B = n | 0),
               (this.C = r | 0),
               (this.D = s | 0),
               (this.E = i | 0),
               (this.F = o | 0),
               (this.G = a | 0),
               (this.H = c | 0);
         }
         process(e, n) {
            for (let h = 0; h < 16; h++, n += 4) Bt[h] = e.getUint32(n, !1);
            for (let h = 16; h < 64; h++) {
               let f = Bt[h - 15],
                  g = Bt[h - 2],
                  y = Pe(f, 7) ^ Pe(f, 18) ^ (f >>> 3),
                  d = Pe(g, 17) ^ Pe(g, 19) ^ (g >>> 10);
               Bt[h] = (d + Bt[h - 7] + y + Bt[h - 16]) | 0;
            }
            let { A: r, B: s, C: i, D: o, E: a, F: c, G: l, H: u } = this;
            for (let h = 0; h < 64; h++) {
               let f = Pe(a, 6) ^ Pe(a, 11) ^ Pe(a, 25),
                  g = (u + f + zc(a, c, l) + n0[h] + Bt[h]) | 0,
                  d = ((Pe(r, 2) ^ Pe(r, 13) ^ Pe(r, 22)) + jc(r, s, i)) | 0;
               (u = l),
                  (l = c),
                  (c = a),
                  (a = (o + g) | 0),
                  (o = i),
                  (i = s),
                  (s = r),
                  (r = (g + d) | 0);
            }
            (r = (r + this.A) | 0),
               (s = (s + this.B) | 0),
               (i = (i + this.C) | 0),
               (o = (o + this.D) | 0),
               (a = (a + this.E) | 0),
               (c = (c + this.F) | 0),
               (l = (l + this.G) | 0),
               (u = (u + this.H) | 0),
               this.set(r, s, i, o, a, c, l, u);
         }
         roundClean() {
            Bt.fill(0);
         }
         destroy() {
            this.set(0, 0, 0, 0, 0, 0, 0, 0), this.buffer.fill(0);
         }
      };
   var jr = Vc(() => new Ti());
   var Fr = class extends kn {
         constructor(e, n) {
            super(), (this.finished = !1), (this.destroyed = !1), Hc(e);
            let r = On(n);
            if (
               ((this.iHash = e.create()),
               typeof this.iHash.update != "function")
            )
               throw new Error(
                  "Expected instance of class which extends utils.Hash",
               );
            (this.blockLen = this.iHash.blockLen),
               (this.outputLen = this.iHash.outputLen);
            let s = this.blockLen,
               i = new Uint8Array(s);
            i.set(r.length > s ? e.create().update(r).digest() : r);
            for (let o = 0; o < i.length; o++) i[o] ^= 54;
            this.iHash.update(i), (this.oHash = e.create());
            for (let o = 0; o < i.length; o++) i[o] ^= 106;
            this.oHash.update(i), i.fill(0);
         }
         update(e) {
            return En(this), this.iHash.update(e), this;
         }
         digestInto(e) {
            En(this),
               xn(e, this.outputLen),
               (this.finished = !0),
               this.iHash.digestInto(e),
               this.oHash.update(e),
               this.oHash.digestInto(e),
               this.destroy();
         }
         digest() {
            let e = new Uint8Array(this.oHash.outputLen);
            return this.digestInto(e), e;
         }
         _cloneInto(e) {
            e || (e = Object.create(Object.getPrototypeOf(this), {}));
            let {
               oHash: n,
               iHash: r,
               finished: s,
               destroyed: i,
               blockLen: o,
               outputLen: a,
            } = this;
            return (
               (e = e),
               (e.finished = s),
               (e.destroyed = i),
               (e.blockLen = o),
               (e.outputLen = a),
               (e.oHash = n._cloneInto(e.oHash)),
               (e.iHash = r._cloneInto(e.iHash)),
               e
            );
         }
         destroy() {
            (this.destroyed = !0), this.oHash.destroy(), this.iHash.destroy();
         }
      },
      _i = (t, e, n) => new Fr(t, e).update(n).digest();
   _i.create = (t, e) => new Fr(t, e);
   var Li = {};
   go(Li, {
      aInRange: () => We,
      abool: () => Jt,
      abytes: () => An,
      bitGet: () => c0,
      bitLen: () => Bi,
      bitMask: () => Mn,
      bitSet: () => u0,
      bytesToHex: () => Qt,
      bytesToNumberBE: () => Se,
      bytesToNumberLE: () => Gr,
      concatBytes: () => ot,
      createHmacDrbg: () => Ii,
      ensureBytes: () => se,
      equalBytes: () => o0,
      hexToBytes: () => Kt,
      hexToNumber: () => Ci,
      inRange: () => Lt,
      isBytes: () => It,
      memoized: () => Yr,
      notImplemented: () => h0,
      numberToBytesBE: () => Oe,
      numberToBytesLE: () => Zr,
      numberToHexUnpadded: () => Xt,
      numberToVarBytesBE: () => i0,
      utf8ToBytes: () => a0,
      validateObject: () => Ut,
   });
   var Dr = BigInt(0),
      Wr = BigInt(1),
      r0 = BigInt(2);
   function It(t) {
      return (
         t instanceof Uint8Array ||
         (ArrayBuffer.isView(t) && t.constructor.name === "Uint8Array")
      );
   }
   function An(t) {
      if (!It(t)) throw new Error("Uint8Array expected");
   }
   function Jt(t, e) {
      if (typeof e != "boolean")
         throw new Error(t + " boolean expected, got " + e);
   }
   var s0 = Array.from({ length: 256 }, (t, e) =>
      e.toString(16).padStart(2, "0"),
   );
   function Qt(t) {
      An(t);
      let e = "";
      for (let n = 0; n < t.length; n++) e += s0[t[n]];
      return e;
   }
   function Xt(t) {
      let e = t.toString(16);
      return e.length & 1 ? "0" + e : e;
   }
   function Ci(t) {
      if (typeof t != "string")
         throw new Error("hex string expected, got " + typeof t);
      return t === "" ? Dr : BigInt("0x" + t);
   }
   var it = { _0: 48, _9: 57, A: 65, F: 70, a: 97, f: 102 };
   function Fc(t) {
      if (t >= it._0 && t <= it._9) return t - it._0;
      if (t >= it.A && t <= it.F) return t - (it.A - 10);
      if (t >= it.a && t <= it.f) return t - (it.a - 10);
   }
   function Kt(t) {
      if (typeof t != "string")
         throw new Error("hex string expected, got " + typeof t);
      let e = t.length,
         n = e / 2;
      if (e % 2)
         throw new Error(
            "hex string expected, got unpadded hex of length " + e,
         );
      let r = new Uint8Array(n);
      for (let s = 0, i = 0; s < n; s++, i += 2) {
         let o = Fc(t.charCodeAt(i)),
            a = Fc(t.charCodeAt(i + 1));
         if (o === void 0 || a === void 0) {
            let c = t[i] + t[i + 1];
            throw new Error(
               'hex string expected, got non-hex character "' +
                  c +
                  '" at index ' +
                  i,
            );
         }
         r[s] = o * 16 + a;
      }
      return r;
   }
   function Se(t) {
      return Ci(Qt(t));
   }
   function Gr(t) {
      return An(t), Ci(Qt(Uint8Array.from(t).reverse()));
   }
   function Oe(t, e) {
      return Kt(t.toString(16).padStart(e * 2, "0"));
   }
   function Zr(t, e) {
      return Oe(t, e).reverse();
   }
   function i0(t) {
      return Kt(Xt(t));
   }
   function se(t, e, n) {
      let r;
      if (typeof e == "string")
         try {
            r = Kt(e);
         } catch (i) {
            throw new Error(
               t + " must be hex string or Uint8Array, cause: " + i,
            );
         }
      else if (It(e)) r = Uint8Array.from(e);
      else throw new Error(t + " must be hex string or Uint8Array");
      let s = r.length;
      if (typeof n == "number" && s !== n)
         throw new Error(t + " of length " + n + " expected, got " + s);
      return r;
   }
   function ot(...t) {
      let e = 0;
      for (let r = 0; r < t.length; r++) {
         let s = t[r];
         An(s), (e += s.length);
      }
      let n = new Uint8Array(e);
      for (let r = 0, s = 0; r < t.length; r++) {
         let i = t[r];
         n.set(i, s), (s += i.length);
      }
      return n;
   }
   function o0(t, e) {
      if (t.length !== e.length) return !1;
      let n = 0;
      for (let r = 0; r < t.length; r++) n |= t[r] ^ e[r];
      return n === 0;
   }
   function a0(t) {
      if (typeof t != "string") throw new Error("string expected");
      return new Uint8Array(new TextEncoder().encode(t));
   }
   var Si = (t) => typeof t == "bigint" && Dr <= t;
   function Lt(t, e, n) {
      return Si(t) && Si(e) && Si(n) && e <= t && t < n;
   }
   function We(t, e, n, r) {
      if (!Lt(e, n, r))
         throw new Error(
            "expected valid " + t + ": " + n + " <= n < " + r + ", got " + e,
         );
   }
   function Bi(t) {
      let e;
      for (e = 0; t > Dr; t >>= Wr, e += 1);
      return e;
   }
   function c0(t, e) {
      return (t >> BigInt(e)) & Wr;
   }
   function u0(t, e, n) {
      return t | ((n ? Wr : Dr) << BigInt(e));
   }
   var Mn = (t) => (r0 << BigInt(t - 1)) - Wr,
      Ri = (t) => new Uint8Array(t),
      Dc = (t) => Uint8Array.from(t);
   function Ii(t, e, n) {
      if (typeof t != "number" || t < 2)
         throw new Error("hashLen must be a number");
      if (typeof e != "number" || e < 2)
         throw new Error("qByteLen must be a number");
      if (typeof n != "function") throw new Error("hmacFn must be a function");
      let r = Ri(t),
         s = Ri(t),
         i = 0,
         o = () => {
            r.fill(1), s.fill(0), (i = 0);
         },
         a = (...h) => n(s, r, ...h),
         c = (h = Ri()) => {
            (s = a(Dc([0]), h)),
               (r = a()),
               h.length !== 0 && ((s = a(Dc([1]), h)), (r = a()));
         },
         l = () => {
            if (i++ >= 1e3) throw new Error("drbg: tried 1000 values");
            let h = 0,
               f = [];
            for (; h < e; ) {
               r = a();
               let g = r.slice();
               f.push(g), (h += r.length);
            }
            return ot(...f);
         };
      return (h, f) => {
         o(), c(h);
         let g;
         for (; !(g = f(l())); ) c();
         return o(), g;
      };
   }
   var l0 = {
      bigint: (t) => typeof t == "bigint",
      function: (t) => typeof t == "function",
      boolean: (t) => typeof t == "boolean",
      string: (t) => typeof t == "string",
      stringOrUint8Array: (t) => typeof t == "string" || It(t),
      isSafeInteger: (t) => Number.isSafeInteger(t),
      array: (t) => Array.isArray(t),
      field: (t, e) => e.Fp.isValid(t),
      hash: (t) => typeof t == "function" && Number.isSafeInteger(t.outputLen),
   };
   function Ut(t, e, n = {}) {
      let r = (s, i, o) => {
         let a = l0[i];
         if (typeof a != "function")
            throw new Error("invalid validator function");
         let c = t[s];
         if (!(o && c === void 0) && !a(c, t))
            throw new Error(
               "param " +
                  String(s) +
                  " is invalid. Expected " +
                  i +
                  ", got " +
                  c,
            );
      };
      for (let [s, i] of Object.entries(e)) r(s, i, !1);
      for (let [s, i] of Object.entries(n)) r(s, i, !0);
      return t;
   }
   var h0 = () => {
      throw new Error("not implemented");
   };
   function Yr(t) {
      let e = new WeakMap();
      return (n, ...r) => {
         let s = e.get(n);
         if (s !== void 0) return s;
         let i = t(n, ...r);
         return e.set(n, i), i;
      };
   }
   var oe = BigInt(0),
      Y = BigInt(1),
      en = BigInt(2),
      f0 = BigInt(3),
      Ui = BigInt(4),
      Wc = BigInt(5),
      Gc = BigInt(8),
      d0 = BigInt(9),
      p0 = BigInt(16);
   function ie(t, e) {
      let n = t % e;
      return n >= oe ? n : e + n;
   }
   function g0(t, e, n) {
      if (e < oe) throw new Error("invalid exponent, negatives unsupported");
      if (n <= oe) throw new Error("invalid modulus");
      if (n === Y) return oe;
      let r = Y;
      for (; e > oe; ) e & Y && (r = (r * t) % n), (t = (t * t) % n), (e >>= Y);
      return r;
   }
   function ve(t, e, n) {
      let r = t;
      for (; e-- > oe; ) (r *= r), (r %= n);
      return r;
   }
   function Jr(t, e) {
      if (t === oe) throw new Error("invert: expected non-zero number");
      if (e <= oe)
         throw new Error("invert: expected positive modulus, got " + e);
      let n = ie(t, e),
         r = e,
         s = oe,
         i = Y,
         o = Y,
         a = oe;
      for (; n !== oe; ) {
         let l = r / n,
            u = r % n,
            h = s - o * l,
            f = i - a * l;
         (r = n), (n = u), (s = o), (i = a), (o = h), (a = f);
      }
      if (r !== Y) throw new Error("invert: does not exist");
      return ie(s, e);
   }
   function y0(t) {
      let e = (t - Y) / en,
         n,
         r,
         s;
      for (n = t - Y, r = 0; n % en === oe; n /= en, r++);
      for (s = en; s < t && g0(s, e, t) !== t - Y; s++)
         if (s > 1e3)
            throw new Error("Cannot find square root: likely non-prime P");
      if (r === 1) {
         let o = (t + Y) / Ui;
         return function (c, l) {
            let u = c.pow(l, o);
            if (!c.eql(c.sqr(u), l)) throw new Error("Cannot find square root");
            return u;
         };
      }
      let i = (n + Y) / en;
      return function (a, c) {
         if (a.pow(c, e) === a.neg(a.ONE))
            throw new Error("Cannot find square root");
         let l = r,
            u = a.pow(a.mul(a.ONE, s), n),
            h = a.pow(c, i),
            f = a.pow(c, n);
         for (; !a.eql(f, a.ONE); ) {
            if (a.eql(f, a.ZERO)) return a.ZERO;
            let g = 1;
            for (let d = a.sqr(f); g < l && !a.eql(d, a.ONE); g++) d = a.sqr(d);
            let y = a.pow(u, Y << BigInt(l - g - 1));
            (u = a.sqr(y)), (h = a.mul(h, y)), (f = a.mul(f, u)), (l = g);
         }
         return h;
      };
   }
   function b0(t) {
      if (t % Ui === f0) {
         let e = (t + Y) / Ui;
         return function (r, s) {
            let i = r.pow(s, e);
            if (!r.eql(r.sqr(i), s)) throw new Error("Cannot find square root");
            return i;
         };
      }
      if (t % Gc === Wc) {
         let e = (t - Wc) / Gc;
         return function (r, s) {
            let i = r.mul(s, en),
               o = r.pow(i, e),
               a = r.mul(s, o),
               c = r.mul(r.mul(a, en), o),
               l = r.mul(a, r.sub(c, r.ONE));
            if (!r.eql(r.sqr(l), s)) throw new Error("Cannot find square root");
            return l;
         };
      }
      return t % p0, y0(t);
   }
   var m0 = [
      "create",
      "isValid",
      "is0",
      "neg",
      "inv",
      "sqrt",
      "sqr",
      "eql",
      "add",
      "sub",
      "mul",
      "pow",
      "div",
      "addN",
      "subN",
      "mulN",
      "sqrN",
   ];
   function Pi(t) {
      let e = {
            ORDER: "bigint",
            MASK: "bigint",
            BYTES: "isSafeInteger",
            BITS: "isSafeInteger",
         },
         n = m0.reduce((r, s) => ((r[s] = "function"), r), e);
      return Ut(t, n);
   }
   function w0(t, e, n) {
      if (n < oe) throw new Error("invalid exponent, negatives unsupported");
      if (n === oe) return t.ONE;
      if (n === Y) return e;
      let r = t.ONE,
         s = e;
      for (; n > oe; ) n & Y && (r = t.mul(r, s)), (s = t.sqr(s)), (n >>= Y);
      return r;
   }
   function v0(t, e) {
      let n = new Array(e.length),
         r = e.reduce(
            (i, o, a) => (t.is0(o) ? i : ((n[a] = i), t.mul(i, o))),
            t.ONE,
         ),
         s = t.inv(r);
      return (
         e.reduceRight(
            (i, o, a) =>
               t.is0(o) ? i : ((n[a] = t.mul(i, n[a])), t.mul(i, o)),
            s,
         ),
         n
      );
   }
   function Oi(t, e) {
      let n = e !== void 0 ? e : t.toString(2).length,
         r = Math.ceil(n / 8);
      return { nBitLength: n, nByteLength: r };
   }
   function Qr(t, e, n = !1, r = {}) {
      if (t <= oe)
         throw new Error("invalid field: expected ORDER > 0, got " + t);
      let { nBitLength: s, nByteLength: i } = Oi(t, e);
      if (i > 2048)
         throw new Error("invalid field: expected ORDER of <= 2048 bytes");
      let o,
         a = Object.freeze({
            ORDER: t,
            BITS: s,
            BYTES: i,
            MASK: Mn(s),
            ZERO: oe,
            ONE: Y,
            create: (c) => ie(c, t),
            isValid: (c) => {
               if (typeof c != "bigint")
                  throw new Error(
                     "invalid field element: expected bigint, got " + typeof c,
                  );
               return oe <= c && c < t;
            },
            is0: (c) => c === oe,
            isOdd: (c) => (c & Y) === Y,
            neg: (c) => ie(-c, t),
            eql: (c, l) => c === l,
            sqr: (c) => ie(c * c, t),
            add: (c, l) => ie(c + l, t),
            sub: (c, l) => ie(c - l, t),
            mul: (c, l) => ie(c * l, t),
            pow: (c, l) => w0(a, c, l),
            div: (c, l) => ie(c * Jr(l, t), t),
            sqrN: (c) => c * c,
            addN: (c, l) => c + l,
            subN: (c, l) => c - l,
            mulN: (c, l) => c * l,
            inv: (c) => Jr(c, t),
            sqrt: r.sqrt || ((c) => (o || (o = b0(t)), o(a, c))),
            invertBatch: (c) => v0(a, c),
            cmov: (c, l, u) => (u ? l : c),
            toBytes: (c) => (n ? Zr(c, i) : Oe(c, i)),
            fromBytes: (c) => {
               if (c.length !== i)
                  throw new Error(
                     "Field.fromBytes: expected " +
                        i +
                        " bytes, got " +
                        c.length,
                  );
               return n ? Gr(c) : Se(c);
            },
         });
      return Object.freeze(a);
   }
   function Zc(t) {
      if (typeof t != "bigint") throw new Error("field order must be bigint");
      let e = t.toString(2).length;
      return Math.ceil(e / 8);
   }
   function Mi(t) {
      let e = Zc(t);
      return e + Math.ceil(e / 2);
   }
   function Yc(t, e, n = !1) {
      let r = t.length,
         s = Zc(e),
         i = Mi(e);
      if (r < 16 || r < i || r > 1024)
         throw new Error("expected " + i + "-1024 bytes of input, got " + r);
      let o = n ? Se(t) : Gr(t),
         a = ie(o, e - Y) + Y;
      return n ? Zr(a, s) : Oe(a, s);
   }
   var Jc = BigInt(0),
      Xr = BigInt(1);
   function Ni(t, e) {
      let n = e.negate();
      return t ? n : e;
   }
   function Qc(t, e) {
      if (!Number.isSafeInteger(t) || t <= 0 || t > e)
         throw new Error(
            "invalid window size, expected [1.." + e + "], got W=" + t,
         );
   }
   function Hi(t, e) {
      Qc(t, e);
      let n = Math.ceil(e / t) + 1,
         r = 2 ** (t - 1);
      return { windows: n, windowSize: r };
   }
   function E0(t, e) {
      if (!Array.isArray(t)) throw new Error("array expected");
      t.forEach((n, r) => {
         if (!(n instanceof e)) throw new Error("invalid point at index " + r);
      });
   }
   function k0(t, e) {
      if (!Array.isArray(t)) throw new Error("array of scalars expected");
      t.forEach((n, r) => {
         if (!e.isValid(n)) throw new Error("invalid scalar at index " + r);
      });
   }
   var qi = new WeakMap(),
      Xc = new WeakMap();
   function $i(t) {
      return Xc.get(t) || 1;
   }
   function Kc(t, e) {
      return {
         constTimeNegate: Ni,
         hasPrecomputes(n) {
            return $i(n) !== 1;
         },
         unsafeLadder(n, r, s = t.ZERO) {
            let i = n;
            for (; r > Jc; )
               r & Xr && (s = s.add(i)), (i = i.double()), (r >>= Xr);
            return s;
         },
         precomputeWindow(n, r) {
            let { windows: s, windowSize: i } = Hi(r, e),
               o = [],
               a = n,
               c = a;
            for (let l = 0; l < s; l++) {
               (c = a), o.push(c);
               for (let u = 1; u < i; u++) (c = c.add(a)), o.push(c);
               a = c.double();
            }
            return o;
         },
         wNAF(n, r, s) {
            let { windows: i, windowSize: o } = Hi(n, e),
               a = t.ZERO,
               c = t.BASE,
               l = BigInt(2 ** n - 1),
               u = 2 ** n,
               h = BigInt(n);
            for (let f = 0; f < i; f++) {
               let g = f * o,
                  y = Number(s & l);
               (s >>= h), y > o && ((y -= u), (s += Xr));
               let d = g,
                  p = g + Math.abs(y) - 1,
                  b = f % 2 !== 0,
                  w = y < 0;
               y === 0 ? (c = c.add(Ni(b, r[d]))) : (a = a.add(Ni(w, r[p])));
            }
            return { p: a, f: c };
         },
         wNAFUnsafe(n, r, s, i = t.ZERO) {
            let { windows: o, windowSize: a } = Hi(n, e),
               c = BigInt(2 ** n - 1),
               l = 2 ** n,
               u = BigInt(n);
            for (let h = 0; h < o; h++) {
               let f = h * a;
               if (s === Jc) break;
               let g = Number(s & c);
               if (((s >>= u), g > a && ((g -= l), (s += Xr)), g === 0))
                  continue;
               let y = r[f + Math.abs(g) - 1];
               g < 0 && (y = y.negate()), (i = i.add(y));
            }
            return i;
         },
         getPrecomputes(n, r, s) {
            let i = qi.get(r);
            return (
               i ||
                  ((i = this.precomputeWindow(r, n)),
                  n !== 1 && qi.set(r, s(i))),
               i
            );
         },
         wNAFCached(n, r, s) {
            let i = $i(n);
            return this.wNAF(i, this.getPrecomputes(i, n, s), r);
         },
         wNAFCachedUnsafe(n, r, s, i) {
            let o = $i(n);
            return o === 1
               ? this.unsafeLadder(n, r, i)
               : this.wNAFUnsafe(o, this.getPrecomputes(o, n, s), r, i);
         },
         setWindowSize(n, r) {
            Qc(r, e), Xc.set(n, r), qi.delete(n);
         },
      };
   }
   function eu(t, e, n, r) {
      if ((E0(n, t), k0(r, e), n.length !== r.length))
         throw new Error("arrays of points and scalars must have equal length");
      let s = t.ZERO,
         i = Bi(BigInt(n.length)),
         o = i > 12 ? i - 3 : i > 4 ? i - 2 : i ? 2 : 1,
         a = (1 << o) - 1,
         c = new Array(a + 1).fill(s),
         l = Math.floor((e.BITS - 1) / o) * o,
         u = s;
      for (let h = l; h >= 0; h -= o) {
         c.fill(s);
         for (let g = 0; g < r.length; g++) {
            let y = r[g],
               d = Number((y >> BigInt(h)) & BigInt(a));
            c[d] = c[d].add(n[g]);
         }
         let f = s;
         for (let g = c.length - 1, y = s; g > 0; g--)
            (y = y.add(c[g])), (f = f.add(y));
         if (((u = u.add(f)), h !== 0))
            for (let g = 0; g < o; g++) u = u.double();
      }
      return u;
   }
   function Vi(t) {
      return (
         Pi(t.Fp),
         Ut(
            t,
            { n: "bigint", h: "bigint", Gx: "field", Gy: "field" },
            { nBitLength: "isSafeInteger", nByteLength: "isSafeInteger" },
         ),
         Object.freeze({ ...Oi(t.n, t.nBitLength), ...t, p: t.Fp.ORDER })
      );
   }
   function tu(t) {
      t.lowS !== void 0 && Jt("lowS", t.lowS),
         t.prehash !== void 0 && Jt("prehash", t.prehash);
   }
   function A0(t) {
      let e = Vi(t);
      Ut(
         e,
         { a: "field", b: "field" },
         {
            allowedPrivateKeyLengths: "array",
            wrapPrivateKey: "boolean",
            isTorsionFree: "function",
            clearCofactor: "function",
            allowInfinityPoint: "boolean",
            fromBytes: "function",
            toBytes: "function",
         },
      );
      let { endo: n, Fp: r, a: s } = e;
      if (n) {
         if (!r.eql(s, r.ZERO))
            throw new Error(
               "invalid endomorphism, can only be defined for Koblitz curves that have a=0",
            );
         if (
            typeof n != "object" ||
            typeof n.beta != "bigint" ||
            typeof n.splitScalar != "function"
         )
            throw new Error(
               "invalid endomorphism, expected beta: bigint and splitScalar: function",
            );
      }
      return Object.freeze({ ...e });
   }
   var { bytesToNumberBE: T0, hexToBytes: _0 } = Li,
      at = {
         Err: class extends Error {
            constructor(e = "") {
               super(e);
            }
         },
         _tlv: {
            encode: (t, e) => {
               let { Err: n } = at;
               if (t < 0 || t > 256) throw new n("tlv.encode: wrong tag");
               if (e.length & 1) throw new n("tlv.encode: unpadded data");
               let r = e.length / 2,
                  s = Xt(r);
               if ((s.length / 2) & 128)
                  throw new n("tlv.encode: long form length too big");
               let i = r > 127 ? Xt((s.length / 2) | 128) : "";
               return Xt(t) + i + s + e;
            },
            decode(t, e) {
               let { Err: n } = at,
                  r = 0;
               if (t < 0 || t > 256) throw new n("tlv.encode: wrong tag");
               if (e.length < 2 || e[r++] !== t)
                  throw new n("tlv.decode: wrong tlv");
               let s = e[r++],
                  i = !!(s & 128),
                  o = 0;
               if (!i) o = s;
               else {
                  let c = s & 127;
                  if (!c)
                     throw new n(
                        "tlv.decode(long): indefinite length not supported",
                     );
                  if (c > 4)
                     throw new n("tlv.decode(long): byte length is too big");
                  let l = e.subarray(r, r + c);
                  if (l.length !== c)
                     throw new n("tlv.decode: length bytes not complete");
                  if (l[0] === 0)
                     throw new n("tlv.decode(long): zero leftmost byte");
                  for (let u of l) o = (o << 8) | u;
                  if (((r += c), o < 128))
                     throw new n("tlv.decode(long): not minimal encoding");
               }
               let a = e.subarray(r, r + o);
               if (a.length !== o)
                  throw new n("tlv.decode: wrong value length");
               return { v: a, l: e.subarray(r + o) };
            },
         },
         _int: {
            encode(t) {
               let { Err: e } = at;
               if (t < ct)
                  throw new e("integer: negative integers are not allowed");
               let n = Xt(t);
               if (
                  (Number.parseInt(n[0], 16) & 8 && (n = "00" + n),
                  n.length & 1)
               )
                  throw new e("unexpected DER parsing assertion: unpadded hex");
               return n;
            },
            decode(t) {
               let { Err: e } = at;
               if (t[0] & 128)
                  throw new e("invalid signature integer: negative");
               if (t[0] === 0 && !(t[1] & 128))
                  throw new e(
                     "invalid signature integer: unnecessary leading zero",
                  );
               return T0(t);
            },
         },
         toSig(t) {
            let { Err: e, _int: n, _tlv: r } = at,
               s = typeof t == "string" ? _0(t) : t;
            An(s);
            let { v: i, l: o } = r.decode(48, s);
            if (o.length)
               throw new e("invalid signature: left bytes after parsing");
            let { v: a, l: c } = r.decode(2, i),
               { v: l, l: u } = r.decode(2, c);
            if (u.length)
               throw new e("invalid signature: left bytes after parsing");
            return { r: n.decode(a), s: n.decode(l) };
         },
         hexFromSig(t) {
            let { _tlv: e, _int: n } = at,
               r = e.encode(2, n.encode(t.r)),
               s = e.encode(2, n.encode(t.s)),
               i = r + s;
            return e.encode(48, i);
         },
      },
      ct = BigInt(0),
      le = BigInt(1),
      Tv = BigInt(2),
      nu = BigInt(3),
      _v = BigInt(4);
   function S0(t) {
      let e = A0(t),
         { Fp: n } = e,
         r = Qr(e.n, e.nBitLength),
         s =
            e.toBytes ||
            ((d, p, b) => {
               let w = p.toAffine();
               return ot(Uint8Array.from([4]), n.toBytes(w.x), n.toBytes(w.y));
            }),
         i =
            e.fromBytes ||
            ((d) => {
               let p = d.subarray(1),
                  b = n.fromBytes(p.subarray(0, n.BYTES)),
                  w = n.fromBytes(p.subarray(n.BYTES, 2 * n.BYTES));
               return { x: b, y: w };
            });
      function o(d) {
         let { a: p, b } = e,
            w = n.sqr(d),
            E = n.mul(w, d);
         return n.add(n.add(E, n.mul(d, p)), b);
      }
      if (!n.eql(n.sqr(e.Gy), o(e.Gx)))
         throw new Error("bad generator point: equation left != right");
      function a(d) {
         return Lt(d, le, e.n);
      }
      function c(d) {
         let {
            allowedPrivateKeyLengths: p,
            nByteLength: b,
            wrapPrivateKey: w,
            n: E,
         } = e;
         if (p && typeof d != "bigint") {
            if (
               (It(d) && (d = Qt(d)),
               typeof d != "string" || !p.includes(d.length))
            )
               throw new Error("invalid private key");
            d = d.padStart(b * 2, "0");
         }
         let k;
         try {
            k = typeof d == "bigint" ? d : Se(se("private key", d, b));
         } catch {
            throw new Error(
               "invalid private key, expected hex or " +
                  b +
                  " bytes, got " +
                  typeof d,
            );
         }
         return w && (k = ie(k, E)), We("private key", k, le, E), k;
      }
      function l(d) {
         if (!(d instanceof f)) throw new Error("ProjectivePoint expected");
      }
      let u = Yr((d, p) => {
            let { px: b, py: w, pz: E } = d;
            if (n.eql(E, n.ONE)) return { x: b, y: w };
            let k = d.is0();
            p == null && (p = k ? n.ONE : n.inv(E));
            let B = n.mul(b, p),
               A = n.mul(w, p),
               T = n.mul(E, p);
            if (k) return { x: n.ZERO, y: n.ZERO };
            if (!n.eql(T, n.ONE)) throw new Error("invZ was invalid");
            return { x: B, y: A };
         }),
         h = Yr((d) => {
            if (d.is0()) {
               if (e.allowInfinityPoint && !n.is0(d.py)) return;
               throw new Error("bad point: ZERO");
            }
            let { x: p, y: b } = d.toAffine();
            if (!n.isValid(p) || !n.isValid(b))
               throw new Error("bad point: x or y not FE");
            let w = n.sqr(b),
               E = o(p);
            if (!n.eql(w, E))
               throw new Error("bad point: equation left != right");
            if (!d.isTorsionFree())
               throw new Error("bad point: not in prime-order subgroup");
            return !0;
         });
      class f {
         constructor(p, b, w) {
            if (
               ((this.px = p),
               (this.py = b),
               (this.pz = w),
               p == null || !n.isValid(p))
            )
               throw new Error("x required");
            if (b == null || !n.isValid(b)) throw new Error("y required");
            if (w == null || !n.isValid(w)) throw new Error("z required");
            Object.freeze(this);
         }
         static fromAffine(p) {
            let { x: b, y: w } = p || {};
            if (!p || !n.isValid(b) || !n.isValid(w))
               throw new Error("invalid affine point");
            if (p instanceof f) throw new Error("projective point not allowed");
            let E = (k) => n.eql(k, n.ZERO);
            return E(b) && E(w) ? f.ZERO : new f(b, w, n.ONE);
         }
         get x() {
            return this.toAffine().x;
         }
         get y() {
            return this.toAffine().y;
         }
         static normalizeZ(p) {
            let b = n.invertBatch(p.map((w) => w.pz));
            return p.map((w, E) => w.toAffine(b[E])).map(f.fromAffine);
         }
         static fromHex(p) {
            let b = f.fromAffine(i(se("pointHex", p)));
            return b.assertValidity(), b;
         }
         static fromPrivateKey(p) {
            return f.BASE.multiply(c(p));
         }
         static msm(p, b) {
            return eu(f, r, p, b);
         }
         _setWindowSize(p) {
            y.setWindowSize(this, p);
         }
         assertValidity() {
            h(this);
         }
         hasEvenY() {
            let { y: p } = this.toAffine();
            if (n.isOdd) return !n.isOdd(p);
            throw new Error("Field doesn't support isOdd");
         }
         equals(p) {
            l(p);
            let { px: b, py: w, pz: E } = this,
               { px: k, py: B, pz: A } = p,
               T = n.eql(n.mul(b, A), n.mul(k, E)),
               _ = n.eql(n.mul(w, A), n.mul(B, E));
            return T && _;
         }
         negate() {
            return new f(this.px, n.neg(this.py), this.pz);
         }
         double() {
            let { a: p, b } = e,
               w = n.mul(b, nu),
               { px: E, py: k, pz: B } = this,
               A = n.ZERO,
               T = n.ZERO,
               _ = n.ZERO,
               I = n.mul(E, E),
               P = n.mul(k, k),
               U = n.mul(B, B),
               O = n.mul(E, k);
            return (
               (O = n.add(O, O)),
               (_ = n.mul(E, B)),
               (_ = n.add(_, _)),
               (A = n.mul(p, _)),
               (T = n.mul(w, U)),
               (T = n.add(A, T)),
               (A = n.sub(P, T)),
               (T = n.add(P, T)),
               (T = n.mul(A, T)),
               (A = n.mul(O, A)),
               (_ = n.mul(w, _)),
               (U = n.mul(p, U)),
               (O = n.sub(I, U)),
               (O = n.mul(p, O)),
               (O = n.add(O, _)),
               (_ = n.add(I, I)),
               (I = n.add(_, I)),
               (I = n.add(I, U)),
               (I = n.mul(I, O)),
               (T = n.add(T, I)),
               (U = n.mul(k, B)),
               (U = n.add(U, U)),
               (I = n.mul(U, O)),
               (A = n.sub(A, I)),
               (_ = n.mul(U, P)),
               (_ = n.add(_, _)),
               (_ = n.add(_, _)),
               new f(A, T, _)
            );
         }
         add(p) {
            l(p);
            let { px: b, py: w, pz: E } = this,
               { px: k, py: B, pz: A } = p,
               T = n.ZERO,
               _ = n.ZERO,
               I = n.ZERO,
               P = e.a,
               U = n.mul(e.b, nu),
               O = n.mul(b, k),
               q = n.mul(w, B),
               C = n.mul(E, A),
               v = n.add(b, w),
               x = n.add(k, B);
            (v = n.mul(v, x)),
               (x = n.add(O, q)),
               (v = n.sub(v, x)),
               (x = n.add(b, E));
            let R = n.add(k, A);
            return (
               (x = n.mul(x, R)),
               (R = n.add(O, C)),
               (x = n.sub(x, R)),
               (R = n.add(w, E)),
               (T = n.add(B, A)),
               (R = n.mul(R, T)),
               (T = n.add(q, C)),
               (R = n.sub(R, T)),
               (I = n.mul(P, x)),
               (T = n.mul(U, C)),
               (I = n.add(T, I)),
               (T = n.sub(q, I)),
               (I = n.add(q, I)),
               (_ = n.mul(T, I)),
               (q = n.add(O, O)),
               (q = n.add(q, O)),
               (C = n.mul(P, C)),
               (x = n.mul(U, x)),
               (q = n.add(q, C)),
               (C = n.sub(O, C)),
               (C = n.mul(P, C)),
               (x = n.add(x, C)),
               (O = n.mul(q, x)),
               (_ = n.add(_, O)),
               (O = n.mul(R, x)),
               (T = n.mul(v, T)),
               (T = n.sub(T, O)),
               (O = n.mul(v, q)),
               (I = n.mul(R, I)),
               (I = n.add(I, O)),
               new f(T, _, I)
            );
         }
         subtract(p) {
            return this.add(p.negate());
         }
         is0() {
            return this.equals(f.ZERO);
         }
         wNAF(p) {
            return y.wNAFCached(this, p, f.normalizeZ);
         }
         multiplyUnsafe(p) {
            let { endo: b, n: w } = e;
            We("scalar", p, ct, w);
            let E = f.ZERO;
            if (p === ct) return E;
            if (this.is0() || p === le) return this;
            if (!b || y.hasPrecomputes(this))
               return y.wNAFCachedUnsafe(this, p, f.normalizeZ);
            let { k1neg: k, k1: B, k2neg: A, k2: T } = b.splitScalar(p),
               _ = E,
               I = E,
               P = this;
            for (; B > ct || T > ct; )
               B & le && (_ = _.add(P)),
                  T & le && (I = I.add(P)),
                  (P = P.double()),
                  (B >>= le),
                  (T >>= le);
            return (
               k && (_ = _.negate()),
               A && (I = I.negate()),
               (I = new f(n.mul(I.px, b.beta), I.py, I.pz)),
               _.add(I)
            );
         }
         multiply(p) {
            let { endo: b, n: w } = e;
            We("scalar", p, le, w);
            let E, k;
            if (b) {
               let { k1neg: B, k1: A, k2neg: T, k2: _ } = b.splitScalar(p),
                  { p: I, f: P } = this.wNAF(A),
                  { p: U, f: O } = this.wNAF(_);
               (I = y.constTimeNegate(B, I)),
                  (U = y.constTimeNegate(T, U)),
                  (U = new f(n.mul(U.px, b.beta), U.py, U.pz)),
                  (E = I.add(U)),
                  (k = P.add(O));
            } else {
               let { p: B, f: A } = this.wNAF(p);
               (E = B), (k = A);
            }
            return f.normalizeZ([E, k])[0];
         }
         multiplyAndAddUnsafe(p, b, w) {
            let E = f.BASE,
               k = (A, T) =>
                  T === ct || T === le || !A.equals(E)
                     ? A.multiplyUnsafe(T)
                     : A.multiply(T),
               B = k(this, b).add(k(p, w));
            return B.is0() ? void 0 : B;
         }
         toAffine(p) {
            return u(this, p);
         }
         isTorsionFree() {
            let { h: p, isTorsionFree: b } = e;
            if (p === le) return !0;
            if (b) return b(f, this);
            throw new Error(
               "isTorsionFree() has not been declared for the elliptic curve",
            );
         }
         clearCofactor() {
            let { h: p, clearCofactor: b } = e;
            return p === le ? this : b ? b(f, this) : this.multiplyUnsafe(e.h);
         }
         toRawBytes(p = !0) {
            return Jt("isCompressed", p), this.assertValidity(), s(f, this, p);
         }
         toHex(p = !0) {
            return Jt("isCompressed", p), Qt(this.toRawBytes(p));
         }
      }
      (f.BASE = new f(e.Gx, e.Gy, n.ONE)),
         (f.ZERO = new f(n.ZERO, n.ONE, n.ZERO));
      let g = e.nBitLength,
         y = Kc(f, e.endo ? Math.ceil(g / 2) : g);
      return {
         CURVE: e,
         ProjectivePoint: f,
         normPrivateKeyToScalar: c,
         weierstrassEquation: o,
         isWithinCurveOrder: a,
      };
   }
   function R0(t) {
      let e = Vi(t);
      return (
         Ut(
            e,
            { hash: "hash", hmac: "function", randomBytes: "function" },
            {
               bits2int: "function",
               bits2int_modN: "function",
               lowS: "boolean",
            },
         ),
         Object.freeze({ lowS: !0, ...e })
      );
   }
   function ru(t) {
      let e = R0(t),
         { Fp: n, n: r } = e,
         s = n.BYTES + 1,
         i = 2 * n.BYTES + 1;
      function o(C) {
         return ie(C, r);
      }
      function a(C) {
         return Jr(C, r);
      }
      let {
            ProjectivePoint: c,
            normPrivateKeyToScalar: l,
            weierstrassEquation: u,
            isWithinCurveOrder: h,
         } = S0({
            ...e,
            toBytes(C, v, x) {
               let R = v.toAffine(),
                  L = n.toBytes(R.x),
                  S = ot;
               return (
                  Jt("isCompressed", x),
                  x
                     ? S(Uint8Array.from([v.hasEvenY() ? 2 : 3]), L)
                     : S(Uint8Array.from([4]), L, n.toBytes(R.y))
               );
            },
            fromBytes(C) {
               let v = C.length,
                  x = C[0],
                  R = C.subarray(1);
               if (v === s && (x === 2 || x === 3)) {
                  let L = Se(R);
                  if (!Lt(L, le, n.ORDER))
                     throw new Error("Point is not on curve");
                  let S = u(L),
                     H;
                  try {
                     H = n.sqrt(S);
                  } catch (F) {
                     let D = F instanceof Error ? ": " + F.message : "";
                     throw new Error("Point is not on curve" + D);
                  }
                  let $ = (H & le) === le;
                  return (
                     ((x & 1) === 1) !== $ && (H = n.neg(H)), { x: L, y: H }
                  );
               } else if (v === i && x === 4) {
                  let L = n.fromBytes(R.subarray(0, n.BYTES)),
                     S = n.fromBytes(R.subarray(n.BYTES, 2 * n.BYTES));
                  return { x: L, y: S };
               } else {
                  let L = s,
                     S = i;
                  throw new Error(
                     "invalid Point, expected length of " +
                        L +
                        ", or uncompressed " +
                        S +
                        ", got " +
                        v,
                  );
               }
            },
         }),
         f = (C) => Qt(Oe(C, e.nByteLength));
      function g(C) {
         let v = r >> le;
         return C > v;
      }
      function y(C) {
         return g(C) ? o(-C) : C;
      }
      let d = (C, v, x) => Se(C.slice(v, x));
      class p {
         constructor(v, x, R) {
            (this.r = v),
               (this.s = x),
               (this.recovery = R),
               this.assertValidity();
         }
         static fromCompact(v) {
            let x = e.nByteLength;
            return (
               (v = se("compactSignature", v, x * 2)),
               new p(d(v, 0, x), d(v, x, 2 * x))
            );
         }
         static fromDER(v) {
            let { r: x, s: R } = at.toSig(se("DER", v));
            return new p(x, R);
         }
         assertValidity() {
            We("r", this.r, le, r), We("s", this.s, le, r);
         }
         addRecoveryBit(v) {
            return new p(this.r, this.s, v);
         }
         recoverPublicKey(v) {
            let { r: x, s: R, recovery: L } = this,
               S = A(se("msgHash", v));
            if (L == null || ![0, 1, 2, 3].includes(L))
               throw new Error("recovery id invalid");
            let H = L === 2 || L === 3 ? x + e.n : x;
            if (H >= n.ORDER) throw new Error("recovery id 2 or 3 invalid");
            let $ = L & 1 ? "03" : "02",
               V = c.fromHex($ + f(H)),
               F = a(H),
               D = o(-S * F),
               X = o(R * F),
               J = c.BASE.multiplyAndAddUnsafe(V, D, X);
            if (!J) throw new Error("point at infinify");
            return J.assertValidity(), J;
         }
         hasHighS() {
            return g(this.s);
         }
         normalizeS() {
            return this.hasHighS()
               ? new p(this.r, o(-this.s), this.recovery)
               : this;
         }
         toDERRawBytes() {
            return Kt(this.toDERHex());
         }
         toDERHex() {
            return at.hexFromSig({ r: this.r, s: this.s });
         }
         toCompactRawBytes() {
            return Kt(this.toCompactHex());
         }
         toCompactHex() {
            return f(this.r) + f(this.s);
         }
      }
      let b = {
         isValidPrivateKey(C) {
            try {
               return l(C), !0;
            } catch {
               return !1;
            }
         },
         normPrivateKeyToScalar: l,
         randomPrivateKey: () => {
            let C = Mi(e.n);
            return Yc(e.randomBytes(C), e.n);
         },
         precompute(C = 8, v = c.BASE) {
            return v._setWindowSize(C), v.multiply(BigInt(3)), v;
         },
      };
      function w(C, v = !0) {
         return c.fromPrivateKey(C).toRawBytes(v);
      }
      function E(C) {
         let v = It(C),
            x = typeof C == "string",
            R = (v || x) && C.length;
         return v
            ? R === s || R === i
            : x
              ? R === 2 * s || R === 2 * i
              : C instanceof c;
      }
      function k(C, v, x = !0) {
         if (E(C)) throw new Error("first arg must be private key");
         if (!E(v)) throw new Error("second arg must be public key");
         return c.fromHex(v).multiply(l(C)).toRawBytes(x);
      }
      let B =
            e.bits2int ||
            function (C) {
               if (C.length > 8192) throw new Error("input is too large");
               let v = Se(C),
                  x = C.length * 8 - e.nBitLength;
               return x > 0 ? v >> BigInt(x) : v;
            },
         A =
            e.bits2int_modN ||
            function (C) {
               return o(B(C));
            },
         T = Mn(e.nBitLength);
      function _(C) {
         return We("num < 2^" + e.nBitLength, C, ct, T), Oe(C, e.nByteLength);
      }
      function I(C, v, x = P) {
         if (["recovered", "canonical"].some((ae) => ae in x))
            throw new Error("sign() legacy options not supported");
         let { hash: R, randomBytes: L } = e,
            { lowS: S, prehash: H, extraEntropy: $ } = x;
         S == null && (S = !0),
            (C = se("msgHash", C)),
            tu(x),
            H && (C = se("prehashed msgHash", R(C)));
         let V = A(C),
            F = l(v),
            D = [_(F), _(V)];
         if ($ != null && $ !== !1) {
            let ae = $ === !0 ? L(n.BYTES) : $;
            D.push(se("extraEntropy", ae));
         }
         let X = ot(...D),
            J = V;
         function ee(ae) {
            let Q = B(ae);
            if (!h(Q)) return;
            let He = a(Q),
               ft = c.BASE.multiply(Q).toAffine(),
               ce = o(ft.x);
            if (ce === ct) return;
            let xe = o(He * o(J + ce * F));
            if (xe === ct) return;
            let Re = (ft.x === ce ? 0 : 2) | Number(ft.y & le),
               Sn = xe;
            return S && g(xe) && ((Sn = y(xe)), (Re ^= 1)), new p(ce, Sn, Re);
         }
         return { seed: X, k2sig: ee };
      }
      let P = { lowS: e.lowS, prehash: !1 },
         U = { lowS: e.lowS, prehash: !1 };
      function O(C, v, x = P) {
         let { seed: R, k2sig: L } = I(C, v, x),
            S = e;
         return Ii(S.hash.outputLen, S.nByteLength, S.hmac)(R, L);
      }
      c.BASE._setWindowSize(8);
      function q(C, v, x, R = U) {
         let L = C;
         (v = se("msgHash", v)), (x = se("publicKey", x));
         let { lowS: S, prehash: H, format: $ } = R;
         if ((tu(R), "strict" in R))
            throw new Error("options.strict was renamed to lowS");
         if ($ !== void 0 && $ !== "compact" && $ !== "der")
            throw new Error("format must be compact or der");
         let V = typeof L == "string" || It(L),
            F =
               !V &&
               !$ &&
               typeof L == "object" &&
               L !== null &&
               typeof L.r == "bigint" &&
               typeof L.s == "bigint";
         if (!V && !F)
            throw new Error(
               "invalid signature, expected Uint8Array, hex string or Signature instance",
            );
         let D, X;
         try {
            if ((F && (D = new p(L.r, L.s)), V)) {
               try {
                  $ !== "compact" && (D = p.fromDER(L));
               } catch (Re) {
                  if (!(Re instanceof at.Err)) throw Re;
               }
               !D && $ !== "der" && (D = p.fromCompact(L));
            }
            X = c.fromHex(x);
         } catch {
            return !1;
         }
         if (!D || (S && D.hasHighS())) return !1;
         H && (v = e.hash(v));
         let { r: J, s: ee } = D,
            ae = A(v),
            Q = a(ee),
            He = o(ae * Q),
            ft = o(J * Q),
            ce = c.BASE.multiplyAndAddUnsafe(X, He, ft)?.toAffine();
         return ce ? o(ce.x) === J : !1;
      }
      return {
         CURVE: e,
         getPublicKey: w,
         getSharedSecret: k,
         sign: O,
         verify: q,
         ProjectivePoint: c,
         Signature: p,
         utils: b,
      };
   }
   function C0(t) {
      return {
         hash: t,
         hmac: (e, ...n) => _i(t, e, $c(...n)),
         randomBytes: Vr,
      };
   }
   function su(t, e) {
      let n = (r) => ru({ ...t, ...C0(r) });
      return Object.freeze({ ...n(e), create: n });
   }
   var qn = BigInt(
         "0xfffffffffffffffffffffffffffffffffffffffffffffffffffffffefffffc2f",
      ),
      Kr = BigInt(
         "0xfffffffffffffffffffffffffffffffebaaedce6af48a03bbfd25e8cd0364141",
      ),
      Nn = BigInt(1),
      es = BigInt(2),
      iu = (t, e) => (t + e / es) / e;
   function au(t) {
      let e = qn,
         n = BigInt(3),
         r = BigInt(6),
         s = BigInt(11),
         i = BigInt(22),
         o = BigInt(23),
         a = BigInt(44),
         c = BigInt(88),
         l = (t * t * t) % e,
         u = (l * l * t) % e,
         h = (ve(u, n, e) * u) % e,
         f = (ve(h, n, e) * u) % e,
         g = (ve(f, es, e) * l) % e,
         y = (ve(g, s, e) * g) % e,
         d = (ve(y, i, e) * y) % e,
         p = (ve(d, a, e) * d) % e,
         b = (ve(p, c, e) * p) % e,
         w = (ve(b, a, e) * d) % e,
         E = (ve(w, n, e) * u) % e,
         k = (ve(E, o, e) * y) % e,
         B = (ve(k, r, e) * l) % e,
         A = ve(B, es, e);
      if (!ji.eql(ji.sqr(A), t)) throw new Error("Cannot find square root");
      return A;
   }
   var ji = Qr(qn, void 0, void 0, { sqrt: au }),
      Wi = su(
         {
            a: BigInt(0),
            b: BigInt(7),
            Fp: ji,
            n: Kr,
            Gx: BigInt(
               "55066263022277343669578718895168534326250603453777594175500187360389116729240",
            ),
            Gy: BigInt(
               "32670510020758816978083085130507043184471273380659243275938904335757337482424",
            ),
            h: BigInt(1),
            lowS: !0,
            endo: {
               beta: BigInt(
                  "0x7ae96a2b657c07106e64479eac3434e99cf0497512f58995c1396c28719501ee",
               ),
               splitScalar: (t) => {
                  let e = Kr,
                     n = BigInt("0x3086d221a7d46bcde86c90e49284eb15"),
                     r = -Nn * BigInt("0xe4437ed6010e88286f547fa90abfe4c3"),
                     s = BigInt("0x114ca50f7a8e2f3f657c1108d9d44cfd8"),
                     i = n,
                     o = BigInt("0x100000000000000000000000000000000"),
                     a = iu(i * t, e),
                     c = iu(-r * t, e),
                     l = ie(t - a * n - c * s, e),
                     u = ie(-a * r - c * i, e),
                     h = l > o,
                     f = u > o;
                  if ((h && (l = e - l), f && (u = e - u), l > o || u > o))
                     throw new Error(
                        "splitScalar: Endomorphism failed, k=" + t,
                     );
                  return { k1neg: h, k1: l, k2neg: f, k2: u };
               },
            },
         },
         jr,
      ),
      cu = BigInt(0),
      ou = {};
   function ts(t, ...e) {
      let n = ou[t];
      if (n === void 0) {
         let r = jr(Uint8Array.from(t, (s) => s.charCodeAt(0)));
         (n = ot(r, r)), (ou[t] = n);
      }
      return jr(ot(n, ...e));
   }
   var Gi = (t) => t.toRawBytes(!0).slice(1),
      Fi = (t) => Oe(t, 32),
      zi = (t) => ie(t, qn),
      Hn = (t) => ie(t, Kr),
      Zi = Wi.ProjectivePoint,
      B0 = (t, e, n) => Zi.BASE.multiplyAndAddUnsafe(t, e, n);
   function Di(t) {
      let e = Wi.utils.normPrivateKeyToScalar(t),
         n = Zi.fromPrivateKey(e);
      return { scalar: n.hasEvenY() ? e : Hn(-e), bytes: Gi(n) };
   }
   function uu(t) {
      We("x", t, Nn, qn);
      let e = zi(t * t),
         n = zi(e * t + BigInt(7)),
         r = au(n);
      r % es !== cu && (r = zi(-r));
      let s = new Zi(t, r, Nn);
      return s.assertValidity(), s;
   }
   var Tn = Se;
   function lu(...t) {
      return Hn(Tn(ts("BIP0340/challenge", ...t)));
   }
   function I0(t) {
      return Di(t).bytes;
   }
   function L0(t, e, n = Vr(32)) {
      let r = se("message", t),
         { bytes: s, scalar: i } = Di(e),
         o = se("auxRand", n, 32),
         a = Fi(i ^ Tn(ts("BIP0340/aux", o))),
         c = ts("BIP0340/nonce", a, s, r),
         l = Hn(Tn(c));
      if (l === cu) throw new Error("sign failed: k is zero");
      let { bytes: u, scalar: h } = Di(l),
         f = lu(u, s, r),
         g = new Uint8Array(64);
      if ((g.set(u, 0), g.set(Fi(Hn(h + f * i)), 32), !hu(g, r, s)))
         throw new Error("sign: Invalid signature produced");
      return g;
   }
   function hu(t, e, n) {
      let r = se("signature", t, 64),
         s = se("message", e),
         i = se("publicKey", n, 32);
      try {
         let o = uu(Tn(i)),
            a = Tn(r.subarray(0, 32));
         if (!Lt(a, Nn, qn)) return !1;
         let c = Tn(r.subarray(32, 64));
         if (!Lt(c, Nn, Kr)) return !1;
         let l = lu(Fi(a), Gi(o), s),
            u = B0(o, c, Hn(-l));
         return !(!u || !u.hasEvenY() || u.toAffine().x !== a);
      } catch {
         return !1;
      }
   }
   var fu = {
      getPublicKey: I0,
      sign: L0,
      verify: hu,
      utils: {
         randomPrivateKey: Wi.utils.randomPrivateKey,
         lift_x: uu,
         pointToBytes: Gi,
         numberToBytesBE: Oe,
         bytesToNumberBE: Se,
         taggedHash: ts,
         mod: ie,
      },
   };
   var gl = W(Qi(), 1);
   var yl = W(qe(), 1);
   function mu(t) {
      return (
         t instanceof Uint8Array ||
         (ArrayBuffer.isView(t) && t.constructor.name === "Uint8Array")
      );
   }
   function wu(t, e) {
      return Array.isArray(e)
         ? e.length === 0
            ? !0
            : t
              ? e.every((n) => typeof n == "string")
              : e.every((n) => Number.isSafeInteger(n))
         : !1;
   }
   function M0(t) {
      if (typeof t != "function") throw new Error("function expected");
      return !0;
   }
   function Vn(t, e) {
      if (typeof e != "string") throw new Error(`${t}: string expected`);
      return !0;
   }
   function vu(t) {
      if (!Number.isSafeInteger(t)) throw new Error(`invalid integer: ${t}`);
   }
   function Xi(t) {
      if (!Array.isArray(t)) throw new Error("array expected");
   }
   function xu(t, e) {
      if (!wu(!0, e)) throw new Error(`${t}: array of strings expected`);
   }
   function Eu(t, e) {
      if (!wu(!1, e)) throw new Error(`${t}: array of numbers expected`);
   }
   function N0(...t) {
      let e = (i) => i,
         n = (i, o) => (a) => i(o(a)),
         r = t.map((i) => i.encode).reduceRight(n, e),
         s = t.map((i) => i.decode).reduce(n, e);
      return { encode: r, decode: s };
   }
   function H0(t) {
      let e = typeof t == "string" ? t.split("") : t,
         n = e.length;
      xu("alphabet", e);
      let r = new Map(e.map((s, i) => [s, i]));
      return {
         encode: (s) => (
            Xi(s),
            s.map((i) => {
               if (!Number.isSafeInteger(i) || i < 0 || i >= n)
                  throw new Error(
                     `alphabet.encode: digit index outside alphabet "${i}". Allowed: ${t}`,
                  );
               return e[i];
            })
         ),
         decode: (s) => (
            Xi(s),
            s.map((i) => {
               Vn("alphabet.decode", i);
               let o = r.get(i);
               if (o === void 0)
                  throw new Error(`Unknown letter: "${i}". Allowed: ${t}`);
               return o;
            })
         ),
      };
   }
   function q0(t = "") {
      return (
         Vn("join", t),
         {
            encode: (e) => (xu("join.decode", e), e.join(t)),
            decode: (e) => (Vn("join.decode", e), e.split(t)),
         }
      );
   }
   var ku = (t, e) => (e === 0 ? t : ku(e, t % e)),
      is = (t, e) => t + (e - ku(t, e)),
      ss = (() => {
         let t = [];
         for (let e = 0; e < 40; e++) t.push(2 ** e);
         return t;
      })();
   function Ki(t, e, n, r) {
      if ((Xi(t), e <= 0 || e > 32))
         throw new Error(`convertRadix2: wrong from=${e}`);
      if (n <= 0 || n > 32) throw new Error(`convertRadix2: wrong to=${n}`);
      if (is(e, n) > 32)
         throw new Error(
            `convertRadix2: carry overflow from=${e} to=${n} carryBits=${is(e, n)}`,
         );
      let s = 0,
         i = 0,
         o = ss[e],
         a = ss[n] - 1,
         c = [];
      for (let l of t) {
         if ((vu(l), l >= o))
            throw new Error(`convertRadix2: invalid data word=${l} from=${e}`);
         if (((s = (s << e) | l), i + e > 32))
            throw new Error(`convertRadix2: carry overflow pos=${i} from=${e}`);
         for (i += e; i >= n; i -= n) c.push(((s >> (i - n)) & a) >>> 0);
         let u = ss[i];
         if (u === void 0) throw new Error("invalid carry");
         s &= u - 1;
      }
      if (((s = (s << (n - i)) & a), !r && i >= e))
         throw new Error("Excess padding");
      if (!r && s > 0) throw new Error(`Non-zero padding: ${s}`);
      return r && i > 0 && c.push(s >>> 0), c;
   }
   function $0(t, e = !1) {
      if ((vu(t), t <= 0 || t > 32))
         throw new Error("radix2: bits should be in (0..32]");
      if (is(8, t) > 32 || is(t, 8) > 32)
         throw new Error("radix2: carry overflow");
      return {
         encode: (n) => {
            if (!mu(n))
               throw new Error("radix2.encode input should be Uint8Array");
            return Ki(Array.from(n), 8, t, !e);
         },
         decode: (n) => (
            Eu("radix2.decode", n), Uint8Array.from(Ki(n, t, 8, e))
         ),
      };
   }
   function gu(t) {
      return (
         M0(t),
         function (...e) {
            try {
               return t.apply(null, e);
            } catch {}
         }
      );
   }
   var eo = N0(H0("qpzry9x8gf2tvdw0s3jn54khce6mua7l"), q0("")),
      yu = [996825010, 642813549, 513874426, 1027748829, 705979059];
   function $n(t) {
      let e = t >> 25,
         n = (t & 33554431) << 5;
      for (let r = 0; r < yu.length; r++) ((e >> r) & 1) === 1 && (n ^= yu[r]);
      return n;
   }
   function bu(t, e, n = 1) {
      let r = t.length,
         s = 1;
      for (let i = 0; i < r; i++) {
         let o = t.charCodeAt(i);
         if (o < 33 || o > 126) throw new Error(`Invalid prefix (${t})`);
         s = $n(s) ^ (o >> 5);
      }
      s = $n(s);
      for (let i = 0; i < r; i++) s = $n(s) ^ (t.charCodeAt(i) & 31);
      for (let i of e) s = $n(s) ^ i;
      for (let i = 0; i < 6; i++) s = $n(s);
      return (s ^= n), eo.encode(Ki([s % ss[30]], 30, 5, !1));
   }
   function V0(t) {
      let e = t === "bech32" ? 1 : 734539939,
         n = $0(5),
         r = n.decode,
         s = n.encode,
         i = gu(r);
      function o(h, f, g = 90) {
         Vn("bech32.encode prefix", h),
            mu(f) && (f = Array.from(f)),
            Eu("bech32.encode", f);
         let y = h.length;
         if (y === 0) throw new TypeError(`Invalid prefix length ${y}`);
         let d = y + 7 + f.length;
         if (g !== !1 && d > g)
            throw new TypeError(`Length ${d} exceeds limit ${g}`);
         let p = h.toLowerCase(),
            b = bu(p, f, e);
         return `${p}1${eo.encode(f)}${b}`;
      }
      function a(h, f = 90) {
         Vn("bech32.decode input", h);
         let g = h.length;
         if (g < 8 || (f !== !1 && g > f))
            throw new TypeError(
               `invalid string length: ${g} (${h}). Expected (8..${f})`,
            );
         let y = h.toLowerCase();
         if (h !== y && h !== h.toUpperCase())
            throw new Error("String must be lowercase or uppercase");
         let d = y.lastIndexOf("1");
         if (d === 0 || d === -1)
            throw new Error(
               'Letter "1" must be present between prefix and data only',
            );
         let p = y.slice(0, d),
            b = y.slice(d + 1);
         if (b.length < 6)
            throw new Error("Data must be at least 6 characters long");
         let w = eo.decode(b).slice(0, -6),
            E = bu(p, w, e);
         if (!b.endsWith(E))
            throw new Error(`Invalid checksum in ${h}: expected "${E}"`);
         return { prefix: p, words: w };
      }
      let c = gu(a);
      function l(h) {
         let { prefix: f, words: g } = a(h, !1);
         return { prefix: f, words: g, bytes: r(g) };
      }
      function u(h, f) {
         return o(h, s(f));
      }
      return {
         encode: o,
         decode: a,
         encodeFromBytes: u,
         decodeToBytes: l,
         decodeUnsafe: c,
         fromWords: r,
         fromWordsUnsafe: i,
         toWords: s,
      };
   }
   var to = V0("bech32");
   var ml = W(Ee(), 1);
   var cb = W(Ee(), 1),
      ub = W(Ee(), 1),
      kl = W(Ee(), 1),
      lb = W(Ee(), 1),
      hb = W(Ee(), 1);
   var fb = W(qe(), 1);
   var db = W(qe(), 1),
      Al = W(Ee(), 1),
      Tl = W(qe(), 1),
      _l = W(qe(), 1),
      Sl = W(Qi(), 1);
   var Cl = W(Ee(), 1),
      Cb = W(Du(), 1),
      Ll = W(Ee(), 1),
      Bb = W(qe(), 1);
   function us(t, e, n = "write") {
      if (!t.outboxTracker) return;
      let r = t.outboxTracker.data.get(e);
      if (r) return n === "write" ? r.writeRelays : r.readRelays;
   }
   async function K0(t, e, n = "write") {
      if (t.outboxTracker)
         return (
            t.outboxTracker.data.has(e) ||
               (await t.outboxTracker.trackUsers([e])),
            us(t, e, n)
         );
   }
   function ey(t, e) {
      let n = new Map();
      return (
         e.forEach((s) => {
            let i = us(t, s);
            i &&
               i.forEach((o) => {
                  let a = n.get(o) || 0;
                  n.set(o, a + 1);
               });
         }),
         Array.from(n.entries())
            .sort((s, i) => i[1] - s[1])
            .map((s) => s[0])
      );
   }
   function ty(t, e, n = "read") {
      let r = new Map(),
         s = new Set();
      return (
         e.forEach((i) => {
            let o = us(t, i, n);
            o && o.size > 0
               ? (o.forEach((a) => {
                    (r.get(a) || new Set()).add(i);
                 }),
                 r.set(i, o))
               : s.add(i);
         }),
         { pubkeysToRelays: r, authorsMissingRelays: s }
      );
   }
   function hl(t, e, n, { count: r, preferredRelays: s } = {}) {
      r ?? (r = 2), s ?? (s = new Set());
      let i = t.pool,
         o = i.connectedRelays();
      o.forEach((f) => {
         s.add(f.url);
      });
      let a = new Map(),
         { pubkeysToRelays: c, authorsMissingRelays: l } = ty(t, e, n),
         u = ey(t, e),
         h = (f, g) => {
            let y = a.get(g) || [];
            y.push(f), a.set(g, y);
         };
      for (let [f, g] of c.entries()) {
         let y = r;
         for (let d of o) g.has(d.url) && (h(f, d.url), y--);
         for (let d of g) a.has(d) && (h(f, d), y--);
         if (!(y <= 0))
            for (let d of u) {
               if (y <= 0) break;
               g.has(d) && (h(f, d), y--);
            }
      }
      for (let f of l)
         i.permanentAndConnectedRelays().forEach((g) => {
            let y = a.get(g.url) || [];
            y.push(f), a.set(g.url, y);
         });
      return a;
   }
   function ny(t, e, n = 2) {
      return hl(t, e, "write", { count: n });
   }
   function Wu(t) {
      try {
         return Je(t);
      } catch {
         return;
      }
   }
   function Je(t) {
      let e = cy(t.toLowerCase(), {
         stripAuthentication: !1,
         stripWWW: !1,
         stripHash: !0,
      });
      return e.endsWith("/") || (e += "/"), e;
   }
   function Gu(t) {
      let e = new Set();
      for (let n of t)
         try {
            e.add(Je(n));
         } catch {}
      return Array.from(e);
   }
   var ry = "text/plain",
      sy = "us-ascii",
      oo = (t, e) => e.some((n) => (n instanceof RegExp ? n.test(t) : n === t)),
      iy = new Set(["https:", "http:", "file:"]),
      oy = (t) => {
         try {
            let { protocol: e } = new URL(t);
            return e.endsWith(":") && !e.includes(".") && !iy.has(e);
         } catch {
            return !1;
         }
      },
      ay = (t, { stripHash: e }) => {
         let n = /^data:(?<type>[^,]*?),(?<data>[^#]*?)(?:#(?<hash>.*))?$/.exec(
            t,
         );
         if (!n) throw new Error(`Invalid URL: ${t}`);
         let r = n.groups?.type ?? "",
            s = n.groups?.data ?? "",
            i = n.groups?.hash ?? "",
            o = r.split(";");
         i = e ? "" : i;
         let a = !1;
         o[o.length - 1] === "base64" && (o.pop(), (a = !0));
         let c = o.shift()?.toLowerCase() ?? "",
            u = [
               ...o
                  .map((h) => {
                     let [f, g = ""] = h.split("=").map((y) => y.trim());
                     return f === "charset" && ((g = g.toLowerCase()), g === sy)
                        ? ""
                        : `${f}${g ? `=${g}` : ""}`;
                  })
                  .filter(Boolean),
            ];
         return (
            a && u.push("base64"),
            (u.length > 0 || (c && c !== ry)) && u.unshift(c),
            `data:${u.join(";")},${a ? s.trim() : s}${i ? `#${i}` : ""}`
         );
      };
   function cy(t, e = {}) {
      if (
         ((e = {
            defaultProtocol: "http",
            normalizeProtocol: !0,
            forceHttp: !1,
            forceHttps: !1,
            stripAuthentication: !0,
            stripHash: !1,
            stripTextFragment: !0,
            stripWWW: !0,
            removeQueryParameters: [/^utm_\w+/i],
            removeTrailingSlash: !0,
            removeSingleSlash: !0,
            removeDirectoryIndex: !1,
            removeExplicitPort: !1,
            sortQueryParameters: !0,
            ...e,
         }),
         typeof e.defaultProtocol == "string" &&
            !e.defaultProtocol.endsWith(":") &&
            (e.defaultProtocol = `${e.defaultProtocol}:`),
         (t = t.trim()),
         /^data:/i.test(t))
      )
         return ay(t, e);
      if (oy(t)) return t;
      let n = t.startsWith("//");
      (!n && /^\.*\//.test(t)) ||
         (t = t.replace(/^(?!(?:\w+:)?\/\/)|^\/\//, e.defaultProtocol));
      let s = new URL(t);
      if (e.forceHttp && e.forceHttps)
         throw new Error(
            "The `forceHttp` and `forceHttps` options cannot be used together",
         );
      if (
         (e.forceHttp && s.protocol === "https:" && (s.protocol = "http:"),
         e.forceHttps && s.protocol === "http:" && (s.protocol = "https:"),
         e.stripAuthentication && ((s.username = ""), (s.password = "")),
         e.stripHash
            ? (s.hash = "")
            : e.stripTextFragment &&
              (s.hash = s.hash.replace(/#?:~:text.*?$/i, "")),
         s.pathname)
      ) {
         let o = /\b[a-z][a-z\d+\-.]{1,50}:\/\//g,
            a = 0,
            c = "";
         for (;;) {
            let u = o.exec(s.pathname);
            if (!u) break;
            let h = u[0],
               f = u.index,
               g = s.pathname.slice(a, f);
            (c += g.replace(/\/{2,}/g, "/")), (c += h), (a = f + h.length);
         }
         let l = s.pathname.slice(a, s.pathname.length);
         (c += l.replace(/\/{2,}/g, "/")), (s.pathname = c);
      }
      if (s.pathname)
         try {
            s.pathname = decodeURI(s.pathname);
         } catch {}
      if (
         (e.removeDirectoryIndex === !0 &&
            (e.removeDirectoryIndex = [/^index\.[a-z]+$/]),
         Array.isArray(e.removeDirectoryIndex) &&
            e.removeDirectoryIndex.length > 0)
      ) {
         let o = s.pathname.split("/"),
            a = o[o.length - 1];
         oo(a, e.removeDirectoryIndex) &&
            ((o = o.slice(0, -1)), (s.pathname = o.slice(1).join("/") + "/"));
      }
      if (
         (s.hostname &&
            ((s.hostname = s.hostname.replace(/\.$/, "")),
            e.stripWWW &&
               /^www\.(?!www\.)[a-z\-\d]{1,63}\.[a-z.\-\d]{2,63}$/.test(
                  s.hostname,
               ) &&
               (s.hostname = s.hostname.replace(/^www\./, ""))),
         Array.isArray(e.removeQueryParameters))
      )
         for (let o of [...s.searchParams.keys()])
            oo(o, e.removeQueryParameters) && s.searchParams.delete(o);
      if (
         (!Array.isArray(e.keepQueryParameters) &&
            e.removeQueryParameters === !0 &&
            (s.search = ""),
         Array.isArray(e.keepQueryParameters) &&
            e.keepQueryParameters.length > 0)
      )
         for (let o of [...s.searchParams.keys()])
            oo(o, e.keepQueryParameters) || s.searchParams.delete(o);
      if (e.sortQueryParameters) {
         s.searchParams.sort();
         try {
            s.search = decodeURIComponent(s.search);
         } catch {}
      }
      e.removeTrailingSlash && (s.pathname = s.pathname.replace(/\/$/, "")),
         e.removeExplicitPort && s.port && (s.port = "");
      let i = t;
      return (
         (t = s.toString()),
         !e.removeSingleSlash &&
            s.pathname === "/" &&
            !i.endsWith("/") &&
            s.hash === "" &&
            (t = t.replace(/\/$/, "")),
         (e.removeTrailingSlash || s.pathname === "/") &&
            s.hash === "" &&
            e.removeSingleSlash &&
            (t = t.replace(/\/$/, "")),
         n && !e.normalizeProtocol && (t = t.replace(/^http:\/\//, "//")),
         e.stripProtocol && (t = t.replace(/^(?:https?:)?\/\//, "")),
         t
      );
   }
   var uy = class extends Error {
         constructor(e, n, r, s) {
            super(e);
            m(this, "errors");
            m(this, "publishedToRelays");
            m(this, "intendedRelaySet");
            (this.errors = n),
               (this.publishedToRelays = r),
               (this.intendedRelaySet = s);
         }
         get relayErrors() {
            let e = [];
            for (let [n, r] of this.errors) e.push(`${n.url}: ${r}`);
            return e.join(`
`);
         }
      },
      lt = class fl {
         constructor(e, n, r) {
            m(this, "relays");
            m(this, "debug");
            m(this, "ndk");
            m(this, "pool");
            (this.relays = e),
               (this.ndk = n),
               (this.pool = r ?? n.pool),
               (this.debug = n.debug.extend("relayset"));
         }
         addRelay(e) {
            this.relays.add(e);
         }
         get relayUrls() {
            return Array.from(this.relays).map((e) => e.url);
         }
         static fromRelayUrls(e, n, r = !0, s) {
            if (((s = s ?? n.pool), !s)) throw new Error("No pool provided");
            let i = new Set();
            for (let o of e) {
               let a = s.relays.get(Je(o));
               if (a) a.status < 5 && r && a.connect(), i.add(a);
               else {
                  let c = new Nt(Je(o), n?.relayAuthDefaultPolicy, n);
                  s.useTemporaryRelay(
                     c,
                     void 0,
                     "requested from fromRelayUrls " + e,
                  ),
                     i.add(c);
               }
            }
            return new fl(new Set(i), n, s);
         }
         async publish(e, n, r = 1) {
            let s = new Set(),
               i = new Map(),
               o = e.isEphemeral();
            e.publishStatus = "pending";
            let a = Array.from(this.relays).map(
               (c) =>
                  new Promise((l) => {
                     c.publish(e, n)
                        .then((u) => {
                           s.add(c), l();
                        })
                        .catch((u) => {
                           o || i.set(c, u), l();
                        });
                  }),
            );
            if ((await Promise.all(a), s.size < r)) {
               if (!o) {
                  let c = new uy(
                     "Not enough relays received the event",
                     i,
                     s,
                     this,
                  );
                  throw (
                     ((e.publishStatus = "error"),
                     (e.publishError = c),
                     this.ndk.emit(
                        "event:publish-failed",
                        e,
                        c,
                        this.relayUrls,
                     ),
                     c)
                  );
               }
            } else
               e.emit("published", { relaySet: this, publishedToRelays: s });
            return s;
         }
         get size() {
            return this.relays.size;
         }
      },
      ao = (0, dl.default)("ndk:outbox:calculate");
   async function ly(t, e) {
      let n = new Set(),
         r = await K0(t, e.pubkey);
      r &&
         r.forEach((o) => {
            let a = t.pool?.getRelay(o);
            a && n.add(a);
         });
      let s = e.tags
         .filter((o) => ["a", "e"].includes(o[0]))
         .map((o) => o[2])
         .filter((o) => o && o.startsWith("wss://"))
         .filter((o) => {
            try {
               return new URL(o), !0;
            } catch {
               return !1;
            }
         })
         .map((o) => Je(o));
      (s = Array.from(new Set(s)).slice(0, 5)),
         s.forEach((o) => {
            let a = t.pool?.getRelay(o, !0, !0);
            a && (ao("Adding relay hint %s", o), n.add(a));
         });
      let i = e.getMatchingTags("p").map((o) => o[1]);
      return (
         i.length < 5
            ? Array.from(
                 hl(t, i, "read", { preferredRelays: new Set(r) }).keys(),
              ).forEach((a) => {
                 let c = t.pool?.getRelay(a, !1, !0);
                 c && (ao("Adding p-tagged relay %s", a), n.add(c));
              })
            : ao("Too many p-tags to consider %d", i.length),
         t.pool?.permanentAndConnectedRelays().forEach((o) => n.add(o)),
         new lt(n, t)
      );
   }
   function hy(t, e, n) {
      let r = new Map(),
         s = new Set();
      if (
         (e.forEach((i) => {
            i.authors && i.authors.forEach((o) => s.add(o));
         }),
         s.size > 0)
      ) {
         let i = ny(t, Array.from(s));
         for (let o of i.keys()) r.set(o, []);
         for (let o of e)
            if (o.authors)
               for (let [a, c] of i.entries()) {
                  let l = o.authors.filter((u) => c.includes(u));
                  r.set(a, [...r.get(a), { ...o, authors: l }]);
               }
            else for (let a of i.keys()) r.set(a, [...r.get(a), o]);
      } else
         t.explicitRelayUrls &&
            t.explicitRelayUrls.forEach((i) => {
               r.set(i, e);
            });
      return (
         r.size === 0 &&
            n
               .permanentAndConnectedRelays()
               .slice(0, 5)
               .forEach((i) => {
                  r.set(i.url, e);
               }),
         r
      );
   }
   function lo(t, e, n) {
      return hy(t, e, n);
   }
   function fy(t, e) {
      let n = new Map(),
         r = (o) => o.join(","),
         s = (o, a) => o.every((c, l) => c === a[l]),
         i = (o) => {
            for (let [a, c] of n)
               if (s(c, o) || s(o, c)) {
                  o.length >= c.length && n.set(a, o);
                  return;
               }
            n.set(r(o), o);
         };
      return t.concat(e).forEach(i), Array.from(n.values());
   }
   async function dy(t, e = []) {
      let n = /(@|nostr:)(npub|nprofile|note|nevent|naddr)[a-zA-Z0-9]+/g,
         r = /(?<=\s|^)(#[^\s!@#$%^&*()=+./,[{\]};:'"?><]+)/g,
         s = [],
         i = (o) => {
            e.find((a) => ["q", o[0]].includes(a[0]) && a[1] === o[1]) ||
               e.push(o);
         };
      return (
         (t = t.replace(n, (o) => {
            try {
               let a = o.split(/(@|nostr:)/)[2],
                  { type: c, data: l } = ye.decode(a),
                  u;
               switch (c) {
                  case "npub":
                     u = ["p", l];
                     break;
                  case "nprofile":
                     u = ["p", l.pubkey];
                     break;
                  case "note":
                     s.push(
                        new Promise(async (h) => {
                           i(["e", l, await co(a), "mention"]), h();
                        }),
                     );
                     break;
                  case "nevent":
                     s.push(
                        new Promise(async (h) => {
                           let { id: f, author: g } = l,
                              { relays: y } = l;
                           (!y || y.length === 0) && (y = [await co(a)]),
                              i(["e", f, y[0], "mention"]),
                              g && i(["p", g]),
                              h();
                        }),
                     );
                     break;
                  case "naddr":
                     s.push(
                        new Promise(async (h) => {
                           let f = [l.kind, l.pubkey, l.identifier].join(":"),
                              g = l.relays ?? [];
                           g.length === 0 && (g = [await co(a)]),
                              i(["a", f, g[0], "mention"]),
                              i(["p", l.pubkey]),
                              h();
                        }),
                     );
                     break;
                  default:
                     return o;
               }
               return u && i(u), `nostr:${a}`;
            } catch {
               return o;
            }
         })),
         await Promise.all(s),
         (t = t.replace(r, (o, a) => {
            let c = ["t", a.slice(1)];
            return (
               e.find((l) => l[0] === c[0] && l[1] === c[1]) || e.push(c), o
            );
         })),
         { content: t, tags: e }
      );
   }
   async function co(t) {
      return "";
   }
   function py() {
      if (this.kind === void 0) throw new Error("Kind not set");
      return (
         [0, 3].includes(this.kind) ||
         (this.kind >= 1e4 && this.kind < 2e4) ||
         (this.kind >= 3e4 && this.kind < 4e4)
      );
   }
   function gy() {
      if (this.kind === void 0) throw new Error("Kind not set");
      return this.kind >= 2e4 && this.kind < 3e4;
   }
   function yy() {
      if (this.kind === void 0) throw new Error("Kind not set");
      return this.kind >= 3e4 && this.kind < 4e4;
   }
   var by = "nip44";
   async function my(t, e, n = by) {
      if (!this.ndk) throw new Error("No NDK instance found!");
      if ((e || (await this.ndk.assertSigner(), (e = this.ndk.signer)), !t)) {
         let r = this.getMatchingTags("p");
         if (r.length !== 1)
            throw new Error(
               "No recipient could be determined and no explicit recipient was provided",
            );
         t = this.ndk.getUser({ pubkey: r[0][1] });
      }
      this.content = await e?.encrypt(t, this.content, n);
   }
   async function wy(t, e, n) {
      if (!this.ndk) throw new Error("No NDK instance found!");
      e || (await this.ndk.assertSigner(), (e = this.ndk.signer)),
         t || (t = this.author),
         n || (n = this.content.match(/\?iv=/) ? "nip04" : "nip44"),
         (this.content = await e?.decrypt(t, this.content, n));
   }
   var vy = 2;
   function xy(t = vy) {
      let e = [];
      return (
         this.onRelays.length > 0
            ? (e = this.onRelays.map((n) => n.url))
            : this.relay && (e = [this.relay.url]),
         e.length > t && (e = e.slice(0, t)),
         this.isParamReplaceable()
            ? ye.naddrEncode({
                 kind: this.kind,
                 pubkey: this.pubkey,
                 identifier: this.replaceableDTag(),
                 relays: e,
              })
            : e.length > 0
              ? ye.neventEncode({
                   id: this.tagId(),
                   relays: e,
                   author: this.pubkey,
                })
              : ye.noteEncode(this.tagId())
      );
   }
   async function Ey(t = !0, e) {
      if (!e && t) {
         if (!this.ndk) throw new Error("No NDK instance found");
         this.ndk.assertSigner(), (e = this.ndk.signer);
      }
      let n = new he(this.ndk, { kind: ky(this) });
      return (
         (n.content = JSON.stringify(this.rawEvent())),
         n.tag(this),
         this.kind !== 1 && n.tags.push(["k", `${this.kind}`]),
         e && (await n.sign(e)),
         t && (await n.publish()),
         n
      );
   }
   function ky(t) {
      return t.kind === 1 ? 6 : 16;
   }
   function pl(t) {
      return t.getMatchingTags("e").some((e) => e[3]);
   }
   function Ay(t, e) {
      e ?? (e = t.tagType());
      let n = t.tags.find((r) => r[3] === "root");
      if (!n) {
         if (pl(t)) return;
         let r = t.getMatchingTags(e);
         if (r.length < 3) return r[0];
      }
      return n;
   }
   function Ty(t, e) {
      e ?? (e = t.tagType());
      let n = t.tags.find((r) => r[3] === "reply");
      if (n) return n;
      if ((n || (n = t.tags.find((r) => r[3] === "root")), !n)) {
         if (pl(t)) return;
         let r = t.getMatchingTags(e);
         if (r.length === 1) return r[0];
         if (r.length === 2) return r[1];
      }
   }
   async function _y(t, e) {
      if (!this.ndk) throw new Error("NDK instance not found");
      let n = this.getMatchingTags(t, e);
      if (n.length === 0) return;
      let [r, s, i] = n[0];
      return await this.ndk.fetchEvent(s, {}, void 0);
   }
   async function Sy(t) {
      if (!this.ndk) throw new Error("NDK instance not found");
      let e = Ay(this);
      if (e) return this.ndk.fetchEventFromTag(e, this, t);
   }
   async function Ry(t) {
      if (!this.ndk) throw new Error("NDK instance not found");
      let e = Ty(this);
      if (e) return this.ndk.fetchEventFromTag(e, this, t);
   }
   function Cy(t = !1, e = !1) {
      let n = [
         0,
         this.pubkey,
         this.created_at,
         this.kind,
         this.tags,
         this.content,
      ];
      return t && n.push(this.sig), e && n.push(this.id), JSON.stringify(n);
   }
   function By(t) {
      let e = JSON.parse(t),
         n = {
            pubkey: e[1],
            created_at: e[2],
            kind: e[3],
            tags: e[4],
            content: e[5],
         };
      return e.length >= 7 && (n.sig = e[6]), e.length >= 8 && (n.id = e[7]), n;
   }
   var ho,
      Dn = {};
   function Iy(t) {
      (ho = t),
         (ho.onmessage = (e) => {
            let [n, r] = e.data,
               s = Dn[n];
            if (!s) {
               console.error("No record found for event", n);
               return;
            }
            delete Dn[n];
            for (let i of s.resolves) i(r);
         });
   }
   async function Ly(t, e) {
      return new Promise((r) => {
         let s = t.serialize(),
            i = !1;
         Dn[t.id] || ((Dn[t.id] = { event: t, resolves: [] }), (i = !0)),
            Dn[t.id].resolves.push(r),
            i &&
               ho.postMessage({
                  serialized: s,
                  id: t.id,
                  sig: t.sig,
                  pubkey: t.pubkey,
               });
      });
   }
   var Uy = /^[a-f0-9]{64}$/;
   function Py() {
      if (
         typeof this.kind != "number" ||
         typeof this.content != "string" ||
         typeof this.created_at != "number" ||
         typeof this.pubkey != "string" ||
         !this.pubkey.match(Uy) ||
         !Array.isArray(this.tags)
      )
         return !1;
      for (let t = 0; t < this.tags.length; t++) {
         let e = this.tags[t];
         if (!Array.isArray(e)) return !1;
         for (let n = 0; n < e.length; n++)
            if (typeof e[n] == "object") return !1;
      }
      return !0;
   }
   var _n = new gl.LRUCache({ maxSize: 1e3, entryExpirationTimeInMS: 6e4 });
   function Oy(t) {
      if (typeof this.signatureVerified == "boolean")
         return this.signatureVerified;
      let e = _n.get(this.id);
      if (e !== null) return (this.signatureVerified = !!e);
      try {
         if (this.ndk?.asyncSigVerification)
            Ly(this, t).then((n) => {
               t &&
                  ((this.signatureVerified = n),
                  n && _n.set(this.id, this.sig)),
                  n ||
                     (this.ndk.emit("event:invalid-sig", this),
                     _n.set(this.id, !1));
            });
         else {
            let n = Ai(new TextEncoder().encode(this.serialize())),
               r = fu.verify(this.sig, n, this.pubkey);
            return (
               r ? _n.set(this.id, this.sig) : _n.set(this.id, !1),
               (this.signatureVerified = r)
            );
         }
      } catch {
         return (this.signatureVerified = !1);
      }
   }
   function My() {
      return Ny(this.serialize());
   }
   function Ny(t) {
      let e = Ai(new TextEncoder().encode(t));
      return Uc(e);
   }
   var Hy = [3],
      he = class nn extends ll.EventEmitter {
         constructor(n, r) {
            super();
            m(this, "ndk");
            m(this, "created_at");
            m(this, "content", "");
            m(this, "tags", []);
            m(this, "kind");
            m(this, "id", "");
            m(this, "sig");
            m(this, "pubkey", "");
            m(this, "signatureVerified");
            m(this, "_author");
            m(this, "relay");
            m(this, "publishStatus", "success");
            m(this, "publishError");
            m(this, "serialize", Cy.bind(this));
            m(this, "getEventHash", My.bind(this));
            m(this, "validate", Py.bind(this));
            m(this, "verifySignature", Oy.bind(this));
            m(this, "isReplaceable", py.bind(this));
            m(this, "isEphemeral", gy.bind(this));
            m(this, "isParamReplaceable", yy.bind(this));
            m(this, "encode", xy.bind(this));
            m(this, "encrypt", my.bind(this));
            m(this, "decrypt", wy.bind(this));
            m(this, "fetchTaggedEvent", _y.bind(this));
            m(this, "fetchRootEvent", Sy.bind(this));
            m(this, "fetchReplyEvent", Ry.bind(this));
            m(this, "repost", Ey.bind(this));
            (this.ndk = n),
               (this.created_at = r?.created_at),
               (this.content = r?.content || ""),
               (this.tags = r?.tags || []),
               (this.id = r?.id || ""),
               (this.sig = r?.sig),
               (this.pubkey = r?.pubkey || ""),
               (this.kind = r?.kind),
               r instanceof nn &&
                  (this.relay &&
                     ((this.relay = r.relay),
                     this.ndk?.subManager.seenEvent(r.id, this.relay)),
                  (this.publishStatus = r.publishStatus),
                  (this.publishError = r.publishError));
         }
         get onRelays() {
            let n = [];
            return (
               this.ndk
                  ? (n = this.ndk.subManager.seenEvents.get(this.id) || [])
                  : this.relay && n.push(this.relay),
               n
            );
         }
         static deserialize(n, r) {
            return new nn(n, By(r));
         }
         rawEvent() {
            return {
               created_at: this.created_at,
               content: this.content,
               tags: this.tags,
               kind: this.kind,
               pubkey: this.pubkey,
               id: this.id,
               sig: this.sig,
            };
         }
         set author(n) {
            var r;
            (this.pubkey = n.pubkey),
               (this._author = n),
               (r = this._author).ndk ?? (r.ndk = this.ndk);
         }
         get author() {
            if (this._author) return this._author;
            if (!this.ndk) throw new Error("No NDK instance found");
            let n = this.ndk.getUser({ pubkey: this.pubkey });
            return (this._author = n), n;
         }
         tagExternal(n, r, s) {
            let i = ["i"],
               o = ["k"];
            switch (r) {
               case "url":
                  let a = new URL(n);
                  (a.hash = ""),
                     i.push(a.toString()),
                     o.push(`${a.protocol}//${a.host}`);
                  break;
               case "hashtag":
                  i.push(`#${n.toLowerCase()}`), o.push("#");
                  break;
               case "geohash":
                  i.push(`geo:${n.toLowerCase()}`), o.push("geo");
                  break;
               case "isbn":
                  i.push(`isbn:${n.replace(/-/g, "")}`), o.push("isbn");
                  break;
               case "podcast:guid":
                  i.push(`podcast:guid:${n}`), o.push("podcast:guid");
                  break;
               case "podcast:item:guid":
                  i.push(`podcast:item:guid:${n}`), o.push("podcast:item:guid");
                  break;
               case "podcast:publisher:guid":
                  i.push(`podcast:publisher:guid:${n}`),
                     o.push("podcast:publisher:guid");
                  break;
               case "isan":
                  i.push(`isan:${n.split("-").slice(0, 4).join("-")}`),
                     o.push("isan");
                  break;
               case "doi":
                  i.push(`doi:${n.toLowerCase()}`), o.push("doi");
                  break;
               default:
                  throw new Error(`Unsupported NIP-73 entity type: ${r}`);
            }
            s && i.push(s), this.tags.push(i), this.tags.push(o);
         }
         tag(n, r, s, i) {
            let o = [];
            if (n.fetchProfile !== void 0) {
               i ?? (i = "p");
               let c = [i, n.pubkey];
               r && c.push("", r), o.push(c);
            } else if (n instanceof nn) {
               let c = n;
               s ?? (s = c?.pubkey === this.pubkey),
                  (o = c.referenceTags(r, s, i));
               for (let l of c.getMatchingTags("p"))
                  l[1] !== this.pubkey &&
                     (this.tags.find((u) => u[0] === "p" && u[1] === l[1]) ||
                        this.tags.push(["p", l[1]]));
            } else if (Array.isArray(n)) o = [n];
            else throw new Error("Invalid argument", n);
            this.tags = fy(this.tags, o);
         }
         async toNostrEvent(n) {
            if (!n && this.pubkey === "") {
               let i = await this.ndk?.signer?.user();
               this.pubkey = i?.pubkey || "";
            }
            this.created_at || (this.created_at = Math.floor(Date.now() / 1e3));
            let { content: r, tags: s } = await this.generateTags();
            (this.content = r || ""), (this.tags = s);
            try {
               this.id = this.getEventHash();
            } catch {}
            return this.rawEvent();
         }
         getMatchingTags(n, r) {
            let s = this.tags.filter((i) => i[0] === n);
            return r === void 0 ? s : s.filter((i) => i[3] === r);
         }
         hasTag(n, r) {
            return this.tags.some((s) => s[0] === n && (!r || s[3] === r));
         }
         tagValue(n) {
            let r = this.getMatchingTags(n);
            if (r.length !== 0) return r[0][1];
         }
         get alt() {
            return this.tagValue("alt");
         }
         set alt(n) {
            this.removeTag("alt"), n && this.tags.push(["alt", n]);
         }
         get dTag() {
            return this.tagValue("d");
         }
         set dTag(n) {
            this.removeTag("d"), n && this.tags.push(["d", n]);
         }
         removeTag(n) {
            let r = Array.isArray(n) ? n : [n];
            this.tags = this.tags.filter((s) => !r.includes(s[0]));
         }
         async sign(n) {
            n
               ? (this.author = await n.user())
               : (this.ndk?.assertSigner(), (n = this.ndk.signer));
            let r = await this.toNostrEvent();
            return (this.sig = await n.sign(r)), this.sig;
         }
         async publishReplaceable(n, r, s) {
            return (
               (this.id = ""),
               (this.created_at = Math.floor(Date.now() / 1e3)),
               (this.sig = ""),
               this.publish(n, r, s)
            );
         }
         async publish(n, r, s) {
            if ((this.sig || (await this.sign()), !this.ndk))
               throw new Error(
                  "NDKEvent must be associated with an NDK instance to publish",
               );
            if (
               (n ||
                  (n = this.ndk.devWriteRelaySet || (await ly(this.ndk, this))),
               this.kind === 5 && this.ndk.cacheAdapter?.deleteEventIds)
            ) {
               let a = this.getMatchingTags("e").map((c) => c[1]);
               this.ndk.cacheAdapter.deleteEventIds(a);
            }
            let i = this.rawEvent();
            if (this.ndk.cacheAdapter?.addUnpublishedEvent)
               try {
                  this.ndk.cacheAdapter.addUnpublishedEvent(this, n.relayUrls);
               } catch (a) {
                  console.error("Error adding unpublished event to cache", a);
               }
            this.kind === 5 &&
               this.ndk.cacheAdapter?.deleteEventIds &&
               this.ndk.cacheAdapter.deleteEventIds(
                  this.getMatchingTags("e").map((a) => a[1]),
               ),
               this.ndk.subManager.dispatchEvent(i, void 0, !0);
            let o = await n.publish(this, r, s);
            return (
               o.forEach((a) => this.ndk?.subManager.seenEvent(this.id, a)), o
            );
         }
         async generateTags() {
            let n = [],
               r = await dy(this.content, this.tags),
               s = r.content;
            if (
               ((n = r.tags),
               this.kind &&
                  this.isParamReplaceable() &&
                  !this.getMatchingTags("d")[0])
            ) {
               let o = this.tagValue("title"),
                  c = [...Array(o ? 6 : 16)]
                     .map(() => Math.random().toString(36)[2])
                     .join("");
               o &&
                  o.length > 0 &&
                  (c =
                     o.replace(/[^a-z0-9]+/gi, "-").replace(/^-|-$/g, "") +
                     "-" +
                     c),
                  n.push(["d", c]);
            }
            if (this.shouldAddClientTag) {
               let i = ["client", this.ndk.clientName ?? ""];
               this.ndk.clientNip89 && i.push(this.ndk.clientNip89), n.push(i);
            } else n = n.filter((i) => i[0] !== "client");
            return { content: s || "", tags: n };
         }
         get shouldAddClientTag() {
            return !(
               (!this.ndk?.clientName && !this.ndk?.clientNip89) ||
               Hy.includes(this.kind) ||
               this.isEphemeral() ||
               this.hasTag("client")
            );
         }
         muted() {
            let n = this.ndk?.mutedIds.get(this.pubkey);
            if (n && n === "p") return "author";
            let r = this.tagReference(),
               s = this.ndk?.mutedIds.get(r[1]);
            return s && s === r[0] ? "event" : null;
         }
         replaceableDTag() {
            if (this.kind && this.kind >= 3e4 && this.kind <= 4e4) {
               let n = this.getMatchingTags("d")[0];
               return n ? n[1] : "";
            }
            throw new Error("Event is not a parameterized replaceable event");
         }
         deduplicationKey() {
            return this.kind === 0 ||
               this.kind === 3 ||
               (this.kind && this.kind >= 1e4 && this.kind < 2e4)
               ? `${this.kind}:${this.pubkey}`
               : this.tagId();
         }
         tagId() {
            return this.isParamReplaceable() ? this.tagAddress() : this.id;
         }
         tagAddress() {
            if (!this.isParamReplaceable())
               throw new Error(
                  "This must only be called on replaceable events",
               );
            let n = this.replaceableDTag();
            return `${this.kind}:${this.pubkey}:${n}`;
         }
         tagType() {
            return this.isParamReplaceable() ? "a" : "e";
         }
         tagReference(n) {
            let r;
            return (
               this.isParamReplaceable()
                  ? (r = ["a", this.tagAddress()])
                  : (r = ["e", this.tagId()]),
               this.relay ? r.push(this.relay.url) : r.push(""),
               r.push(n ?? ""),
               this.isParamReplaceable() || r.push(this.pubkey),
               r
            );
         }
         referenceTags(n, r, s) {
            let i = [];
            return (
               this.isParamReplaceable()
                  ? (i = [
                       [s ?? "a", this.tagAddress()],
                       [s ?? "e", this.id],
                    ])
                  : (i = [[s ?? "e", this.id]]),
               (i = i.map(
                  (o) => (
                     o[0] === "e" || n
                        ? o.push(this.relay?.url ?? "")
                        : this.relay?.url && o.push(this.relay?.url),
                     o
                  ),
               )),
               i.forEach((o) => {
                  o[0] === "e"
                     ? (o.push(n ?? ""), o.push(this.pubkey))
                     : n && o.push(n);
               }),
               (i = [...i, ...this.getMatchingTags("h")]),
               r || i.push(...this.author.referenceTags()),
               i
            );
         }
         filter() {
            return this.isParamReplaceable()
               ? { "#a": [this.tagId()] }
               : { "#e": [this.tagId()] };
         }
         async delete(n, r = !0) {
            if (!this.ndk) throw new Error("No NDK instance found");
            this.ndk.assertSigner();
            let s = new nn(this.ndk, { kind: 5, content: n || "" });
            return (
               s.tag(this, void 0, !0),
               s.tags.push(["k", this.kind.toString()]),
               r && (this.emit("deleted"), await s.publish()),
               s
            );
         }
         async react(n, r = !0) {
            if (!this.ndk) throw new Error("No NDK instance found");
            this.ndk.assertSigner();
            let s = new nn(this.ndk, { kind: 7, content: n });
            return s.tag(this), r ? await s.publish() : await s.sign(), s;
         }
         get isValid() {
            return this.validate();
         }
         reply() {
            let n = new nn(this.ndk);
            if (this.kind === 1)
               (n.kind = 1),
                  this.hasTag("e")
                     ? (n.tags = [
                          ...n.tags,
                          ...this.getMatchingTags("e"),
                          ...this.getMatchingTags("p"),
                          ...this.getMatchingTags("a"),
                          ...this.referenceTags("reply"),
                       ])
                     : n.tag(this, "root");
            else {
               n.kind = 1111;
               let r = ["A", "E", "I", "P"],
                  s = this.tags.filter((i) => r.includes(i[0]));
               if (s.length > 0) {
                  let i = this.tagValue("K");
                  n.tags.push(...s), i && n.tags.push(["K", i]);
                  let [o, a, c, ...l] = this.tagReference(),
                     u = [o, a, ...l];
                  n.tags.push(u);
               } else {
                  let [i, o, a, c] = this.tagReference(),
                     l = [i, o, c ?? ""];
                  i === "e" && l.push(this.pubkey), n.tags.push(l);
                  let u = [...l];
                  (u[0] = u[0].toUpperCase()),
                     n.tags.push(u),
                     n.tags.push(["K", this.kind.toString()]),
                     n.tags.push(["P", this.pubkey]);
               }
               n.tags.push(["k", this.kind.toString()]),
                  n.tags.push(...this.getMatchingTags("p")),
                  n.tags.push(["p", this.pubkey]);
            }
            return n;
         }
      },
      qy = 5,
      $y = 1e3,
      Vy = class {
         constructor(t, e) {
            m(this, "ndkRelay");
            m(this, "ws");
            m(this, "_status");
            m(this, "timeoutMs");
            m(this, "connectedAt");
            m(this, "_connectionStats", {
               attempts: 0,
               success: 0,
               durations: [],
            });
            m(this, "debug");
            m(this, "netDebug");
            m(this, "connectTimeout");
            m(this, "reconnectTimeout");
            m(this, "ndk");
            m(this, "openSubs", new Map());
            m(this, "openCountRequests", new Map());
            m(this, "openEventPublishes", new Map());
            m(this, "serial", 0);
            m(this, "baseEoseTimeout", 4400);
            m(this, "updateConnectionStats", {
               connected: () => {
                  this._connectionStats.success++,
                     (this._connectionStats.connectedAt = Date.now());
               },
               disconnected: () => {
                  this._connectionStats.connectedAt &&
                     (this._connectionStats.durations.push(
                        Date.now() - this._connectionStats.connectedAt,
                     ),
                     this._connectionStats.durations.length > 100 &&
                        this._connectionStats.durations.shift()),
                     (this._connectionStats.connectedAt = void 0);
               },
               attempt: () => {
                  this._connectionStats.attempts++,
                     (this._connectionStats.connectedAt = Date.now());
               },
            });
            (this.ndkRelay = t), (this._status = 1);
            let n = Math.floor(Math.random() * 1e3);
            (this.debug = this.ndkRelay.debug.extend("connectivity" + n)),
               (this.ndk = e);
         }
         async connect(t, e = !0) {
            if (
               (this._status !== 2 && this._status !== 1) ||
               this.reconnectTimeout
            ) {
               this.debug(
                  "Relay requested to be connected but was in state %s or it had a reconnect timeout",
                  this._status,
               );
               return;
            }
            this.reconnectTimeout &&
               (clearTimeout(this.reconnectTimeout),
               (this.reconnectTimeout = void 0)),
               this.connectTimeout &&
                  (clearTimeout(this.connectTimeout),
                  (this.connectTimeout = void 0)),
               t ?? (t = this.timeoutMs),
               !this.timeoutMs && t && (this.timeoutMs = t),
               this.timeoutMs &&
                  (this.connectTimeout = setTimeout(
                     () => this.onConnectionError(e),
                     this.timeoutMs,
                  ));
            try {
               this.updateConnectionStats.attempt(),
                  this._status === 1 ? (this._status = 4) : (this._status = 2),
                  (this.ws = new WebSocket(this.ndkRelay.url)),
                  (this.ws.onopen = this.onConnect.bind(this)),
                  (this.ws.onclose = this.onDisconnect.bind(this)),
                  (this.ws.onmessage = this.onMessage.bind(this)),
                  (this.ws.onerror = this.onError.bind(this));
            } catch (n) {
               throw (
                  (this.debug(`Failed to connect to ${this.ndkRelay.url}`, n),
                  (this._status = 1),
                  e
                     ? this.handleReconnection()
                     : this.ndkRelay.emit(
                          "delayed-connect",
                          2 * 24 * 60 * 60 * 1e3,
                       ),
                  n)
               );
            }
         }
         disconnect() {
            this._status = 0;
            try {
               this.ws?.close();
            } catch (t) {
               this.debug("Failed to disconnect", t), (this._status = 1);
            }
         }
         onConnectionError(t) {
            this.debug(
               `Error connecting to ${this.ndkRelay.url}`,
               this.timeoutMs,
            ),
               t && !this.reconnectTimeout && this.handleReconnection();
         }
         onConnect() {
            this.netDebug?.("connected", this.ndkRelay),
               this.reconnectTimeout &&
                  (clearTimeout(this.reconnectTimeout),
                  (this.reconnectTimeout = void 0)),
               this.connectTimeout &&
                  (clearTimeout(this.connectTimeout),
                  (this.connectTimeout = void 0)),
               this.updateConnectionStats.connected(),
               (this._status = 5),
               this.ndkRelay.emit("connect"),
               this.ndkRelay.emit("ready");
         }
         onDisconnect() {
            this.netDebug?.("disconnected", this.ndkRelay),
               this.updateConnectionStats.disconnected(),
               this._status === 5 && this.handleReconnection(),
               (this._status = 1),
               this.ndkRelay.emit("disconnect");
         }
         onMessage(t) {
            this.netDebug?.(t.data, this.ndkRelay, "recv");
            try {
               let e = JSON.parse(t.data),
                  [n, r, ...s] = e;
               switch (n) {
                  case "EVENT": {
                     let i = this.openSubs.get(r),
                        o = e[2];
                     if (!i) {
                        this.debug(
                           `Received event for unknown subscription ${r}`,
                        );
                        return;
                     }
                     i.onevent(o);
                     return;
                  }
                  case "COUNT": {
                     let i = e[2],
                        o = this.openCountRequests.get(r);
                     o &&
                        (o.resolve(i.count), this.openCountRequests.delete(r));
                     return;
                  }
                  case "EOSE": {
                     let i = this.openSubs.get(r);
                     if (!i) return;
                     i.oneose(r);
                     return;
                  }
                  case "OK": {
                     let i = e[2],
                        o = e[3],
                        a = this.openEventPublishes.get(r),
                        c = a?.pop();
                     if (!a || !c) {
                        this.debug("Received OK for unknown event publish", r);
                        return;
                     }
                     i ? c.resolve(o) : c.reject(new Error(o)),
                        a.length === 0
                           ? this.openEventPublishes.delete(r)
                           : this.openEventPublishes.set(r, a);
                     return;
                  }
                  case "CLOSED": {
                     let i = this.openSubs.get(r);
                     if (!i) return;
                     i.onclosed(e[2]);
                     return;
                  }
                  case "NOTICE":
                     this.onNotice(e[1]);
                     return;
                  case "AUTH": {
                     this.onAuthRequested(e[1]);
                     return;
                  }
               }
            } catch (e) {
               this.debug(
                  `Error parsing message from ${this.ndkRelay.url}: ${e.message}`,
                  e?.stack,
               );
               return;
            }
         }
         async onAuthRequested(t) {
            let e =
               this.ndkRelay.authPolicy ?? this.ndk?.relayAuthDefaultPolicy;
            if (
               (this.debug("Relay requested authentication", {
                  havePolicy: !!e,
               }),
               this._status === 7)
            ) {
               this.debug("Already authenticating, ignoring");
               return;
            }
            if (((this._status = 6), e)) {
               if (this._status >= 5) {
                  this._status = 7;
                  let n;
                  try {
                     n = await e(this.ndkRelay, t);
                  } catch (r) {
                     this.debug("Authentication policy threw an error", r),
                        (n = !1);
                  }
                  if (
                     (this.debug("Authentication policy returned", !!n),
                     n instanceof he || n === !0)
                  ) {
                     n instanceof he && (await this.auth(n));
                     let r = async () => {
                        if (this._status >= 5 && this._status < 8) {
                           let s = new he(this.ndk);
                           (s.kind = 22242),
                              (s.tags = [
                                 ["relay", this.ndkRelay.url],
                                 ["challenge", t],
                              ]),
                              await s.sign(),
                              this.auth(s)
                                 .then(() => {
                                    (this._status = 8),
                                       this.ndkRelay.emit("authed"),
                                       this.debug("Authentication successful");
                                 })
                                 .catch((i) => {
                                    (this._status = 6),
                                       this.ndkRelay.emit("auth:failed", i),
                                       this.debug("Authentication failed", i);
                                 });
                        } else
                           this.debug(
                              "Authentication failed, it changed status, status is %d",
                              this._status,
                           );
                     };
                     n === !0 &&
                        (this.ndk?.signer
                           ? r().catch((s) => {
                                console.error("Error authenticating", s);
                             })
                           : (this.debug(
                                "No signer available for authentication localhost",
                             ),
                             this.ndk?.once("signer:ready", r))),
                        (this._status = 5),
                        this.ndkRelay.emit("authed");
                  }
               }
            } else this.ndkRelay.emit("auth", t);
         }
         onError(t) {
            this.debug(`WebSocket error on ${this.ndkRelay.url}:`, t);
         }
         get status() {
            return this._status;
         }
         isAvailable() {
            return this._status === 5;
         }
         isFlapping() {
            let t = this._connectionStats.durations;
            if (t.length % 3 !== 0) return !1;
            let n = t.reduce((o, a) => o + a, 0) / t.length,
               r =
                  t.map((o) => Math.pow(o - n, 2)).reduce((o, a) => o + a, 0) /
                  t.length;
            return Math.sqrt(r) < $y;
         }
         async onNotice(t) {
            this.ndkRelay.emit("notice", t);
         }
         handleReconnection(t = 0) {
            if (this.reconnectTimeout) return;
            if (this.isFlapping()) {
               this.ndkRelay.emit("flapping", this._connectionStats),
                  (this._status = 3);
               return;
            }
            let e = this.connectedAt
               ? Math.max(0, 6e4 - (Date.now() - this.connectedAt))
               : 5e3 * (this._connectionStats.attempts + 1);
            (this.reconnectTimeout = setTimeout(() => {
               (this.reconnectTimeout = void 0),
                  (this._status = 2),
                  this.connect().catch((n) => {
                     t < qy
                        ? setTimeout(
                             () => {
                                this.handleReconnection(t + 1);
                             },
                             (1e3 * (t + 1)) ^ 4,
                          )
                        : this.debug("Reconnect failed");
                  });
            }, e)),
               this.ndkRelay.emit("delayed-connect", e),
               this.debug("Reconnecting in", e),
               (this._connectionStats.nextReconnectAt = Date.now() + e);
         }
         async send(t) {
            this._status >= 5 && this.ws?.readyState === WebSocket.OPEN
               ? (this.ws?.send(t), this.netDebug?.(t, this.ndkRelay, "send"))
               : this.debug(
                    `Not connected to ${this.ndkRelay.url} (%d), not sending message ${t}`,
                    this._status,
                 );
         }
         async auth(t) {
            let e = new Promise((n, r) => {
               let s = this.openEventPublishes.get(t.id) ?? [];
               s.push({ resolve: n, reject: r }),
                  this.openEventPublishes.set(t.id, s);
            });
            return (
               this.send('["AUTH",' + JSON.stringify(t.rawEvent()) + "]"), e
            );
         }
         async publish(t) {
            let e = new Promise((n, r) => {
               let s = this.openEventPublishes.get(t.id) ?? [];
               s.length > 0 &&
                  console.warn(
                     "Duplicate event publishing detected, you are publishing event " +
                        t.id +
                        " twice",
                  ),
                  s.push({ resolve: n, reject: r }),
                  this.openEventPublishes.set(t.id, s);
            });
            return this.send('["EVENT",' + JSON.stringify(t) + "]"), e;
         }
         async count(t, e) {
            this.serial++;
            let n = e?.id || "count:" + this.serial,
               r = new Promise((s, i) => {
                  this.openCountRequests.set(n, { resolve: s, reject: i });
               });
            return (
               this.send(
                  '["COUNT","' + n + '",' + JSON.stringify(t).substring(1),
               ),
               r
            );
         }
         close(t, e) {
            this.send('["CLOSE","' + t + '"]');
            let n = this.openSubs.get(t);
            this.openSubs.delete(t), n && n.onclose(e);
         }
         req(t) {
            this.send(
               '["REQ","' +
                  t.subId +
                  '",' +
                  JSON.stringify(t.executeFilters).substring(1),
            ) + "",
               this.openSubs.set(t.subId, t);
         }
         get connectionStats() {
            return this._connectionStats;
         }
         get url() {
            return this.ndkRelay.url;
         }
         get connected() {
            return this._status >= 5 && this.ws?.readyState === WebSocket.OPEN;
         }
      },
      zy = class {
         constructor(t) {
            m(this, "ndkRelay");
            m(this, "debug");
            (this.ndkRelay = t), (this.debug = t.debug.extend("publisher"));
         }
         async publish(t, e = 2500) {
            let n,
               r = () =>
                  new Promise((u, h) => {
                     try {
                        this.publishEvent(t)
                           .then((f) => {
                              this.ndkRelay.emit("published", t),
                                 t.emit("relay:published", this.ndkRelay),
                                 u(!0);
                           })
                           .catch(h);
                     } catch (f) {
                        h(f);
                     }
                  }),
               s = new Promise((u, h) => {
                  n = setTimeout(() => {
                     (n = void 0), h(new Error("Timeout: " + e + "ms"));
                  }, e);
               }),
               i = () => {
                  r()
                     .then((u) => o(u))
                     .catch((u) => a(u));
               },
               o,
               a,
               c = (u) => {
                  throw (
                     (this.ndkRelay.debug("Publish failed", u, t.id),
                     this.ndkRelay.emit("publish:failed", t, u),
                     t.emit("relay:publish:failed", this.ndkRelay, u),
                     u)
                  );
               },
               l = () => {
                  n && clearTimeout(n),
                     this.ndkRelay.removeListener("connect", i);
               };
            return this.ndkRelay.status >= 5
               ? Promise.race([r(), s]).catch(c).finally(l)
               : (this.ndkRelay.status <= 1
                    ? (console.warn(
                         "Relay is disconnected, trying to connect to publish an event",
                         this.ndkRelay.url,
                      ),
                      this.ndkRelay.connect())
                    : console.warn(
                         "Relay not connected, waiting for connection to publish an event",
                         this.ndkRelay.url,
                      ),
                 Promise.race([
                    new Promise((u, h) => {
                       (o = u), (a = h), this.ndkRelay.once("connect", i);
                    }),
                    s,
                 ])
                    .catch(c)
                    .finally(l));
         }
         async publishEvent(t) {
            return this.ndkRelay.connectivity.publish(t.rawEvent());
         }
      };
   function jy(t, e) {
      let n = [];
      for (let s of t) {
         let i = Object.entries(s || {})
            .map(([o, a]) => (["since", "until"].includes(o) ? o + ":" + a : o))
            .sort()
            .join("-");
         n.push(i);
      }
      let r = e ? "+" : "";
      return (r += n.join("|")), r;
   }
   function Fy(t) {
      let e = [],
         n = {};
      return (
         t.filter((r) => !!r.limit).forEach((r) => e.push(r)),
         (t = t.filter((r) => !r.limit)),
         t.length === 0
            ? e
            : (t.forEach((r) => {
                 Object.entries(r).forEach(([s, i]) => {
                    Array.isArray(i)
                       ? n[s] === void 0
                          ? (n[s] = [...i])
                          : (n[s] = Array.from(new Set([...n[s], ...i])))
                       : (n[s] = i);
                 });
              }),
              [...e, n])
      );
   }
   var Dy = class {
         constructor(t, e, n) {
            m(this, "fingerprint");
            m(this, "items", new Map());
            m(this, "topSubManager");
            m(this, "debug");
            m(this, "status", 0);
            m(this, "onClose");
            m(this, "relay");
            m(this, "eosed", !1);
            m(this, "executionTimer");
            m(this, "fireTime");
            m(this, "delayType");
            m(this, "executeFilters");
            m(this, "id", Math.random().toString(36).substring(7));
            m(this, "_subId");
            m(this, "subIdParts", new Set());
            m(this, "executeOnRelayReady", () => {
               if (this.status === 2) {
                  if (this.items.size === 0) {
                     this.debug(
                        "No items to execute; this relay was probably too slow to respond and the caller gave up",
                        {
                           status: this.status,
                           fingerprint: this.fingerprint,
                           items: this.items,
                           itemsSize: this.items.size,
                           id: this.id,
                           subId: this.subId,
                        },
                     ),
                        this.cleanup();
                     return;
                  }
                  this.debug("Executing on relay ready", {
                     status: this.status,
                     fingerprint: this.fingerprint,
                     items: this.items,
                     itemsSize: this.items.size,
                  }),
                     (this.status = 1),
                     this.execute();
               }
            });
            m(
               this,
               "reExecuteAfterAuth",
               (() => {
                  let t = this.subId;
                  this.debug("Re-executing after auth", this.items.size),
                     this.eosed
                        ? this.relay.close(this.subId)
                        : this.debug(
                             "We are abandoning an opened subscription, once it EOSE's, the handler will close it",
                             { oldSubId: t },
                          ),
                     (this._subId = void 0),
                     (this.status = 1),
                     this.execute(),
                     this.debug(
                        "Re-executed after auth %s \u{1F449} %s",
                        t,
                        this.subId,
                     );
               }).bind(this),
            );
            (this.relay = t),
               (this.topSubManager = n),
               (this.debug = t.debug.extend("subscription-" + this.id)),
               (this.fingerprint =
                  e || Math.random().toString(36).substring(7));
         }
         get subId() {
            return this._subId
               ? this._subId
               : ((this._subId = this.fingerprint.slice(0, 15)), this._subId);
         }
         addSubIdPart(t) {
            this.subIdParts.add(t);
         }
         addItem(t, e) {
            if (
               (this.debug("Adding item", {
                  filters: e,
                  internalId: t.internalId,
                  status: this.status,
                  fingerprint: this.fingerprint,
                  id: this.subId,
                  items: this.items,
                  itemsSize: this.items.size,
               }),
               !this.items.has(t.internalId))
            )
               switch (
                  (t.on("close", this.removeItem.bind(this, t)),
                  this.items.set(t.internalId, { subscription: t, filters: e }),
                  this.status !== 3 &&
                     t.subId &&
                     (!this._subId || this._subId.length < 48) &&
                     (this.status === 0 || this.status === 1) &&
                     this.addSubIdPart(t.subId),
                  this.status)
               ) {
                  case 0:
                     this.evaluateExecutionPlan(t);
                     break;
                  case 3:
                     console.log(
                        "BUG: This should not happen: This subscription needs to catch up with a subscription that was already running",
                        e,
                     );
                     break;
                  case 1:
                     this.evaluateExecutionPlan(t);
                     break;
                  case 4:
                     throw (
                        (this.debug(
                           "Subscription is closed, cannot add new items %o (%o)",
                           t,
                           e,
                        ),
                        new Error(
                           "Cannot add new items to a closed subscription",
                        ))
                     );
               }
         }
         removeItem(t) {
            if ((this.items.delete(t.internalId), this.items.size === 0)) {
               if (!this.eosed) return;
               this.close(), this.cleanup();
            }
         }
         close() {
            if (this.status === 4) return;
            let t = this.status;
            if (((this.status = 4), t === 3))
               try {
                  this.relay.close(this.subId);
               } catch (e) {
                  this.debug("Error closing subscription", e, this);
               }
            else
               this.debug(
                  "Subscription wanted to close but it wasn't running, this is probably ok",
                  { subId: this.subId, prevStatus: t, sub: this },
               );
            this.cleanup();
         }
         cleanup() {
            this.executionTimer && clearTimeout(this.executionTimer),
               this.relay.off("ready", this.executeOnRelayReady),
               this.relay.off("authed", this.reExecuteAfterAuth),
               this.onClose && this.onClose(this);
         }
         evaluateExecutionPlan(t) {
            if (!t.isGroupable()) {
               (this.status = 1), this.execute();
               return;
            }
            if (
               t.filters.find((r) => !!r.limit) &&
               ((this.executeFilters = this.compileFilters()),
               this.executeFilters.length >= 10)
            ) {
               (this.status = 1), this.execute();
               return;
            }
            let e = t.groupableDelay,
               n = t.groupableDelayType;
            if (!e)
               throw new Error("Cannot group a subscription without a delay");
            if (this.status === 0) this.schedule(e, n);
            else {
               let r = this.delayType,
                  s = this.fireTime - Date.now();
               if (r === "at-least" && n === "at-least")
                  s < e &&
                     (this.executionTimer && clearTimeout(this.executionTimer),
                     this.schedule(e, n));
               else if (r === "at-least" && n === "at-most")
                  s > e &&
                     (this.executionTimer && clearTimeout(this.executionTimer),
                     this.schedule(e, n));
               else if (r === "at-most" && n === "at-most")
                  s > e &&
                     (this.executionTimer && clearTimeout(this.executionTimer),
                     this.schedule(e, n));
               else if (r === "at-most" && n === "at-least")
                  s > e &&
                     (this.executionTimer && clearTimeout(this.executionTimer),
                     this.schedule(e, n));
               else
                  throw new Error(
                     "Unknown delay type combination " + r + " " + n,
                  );
            }
         }
         schedule(t, e) {
            this.status = 1;
            let n = Date.now();
            (this.fireTime = n + t), (this.delayType = e);
            let r = setTimeout(this.execute.bind(this), t);
            e === "at-least" && (this.executionTimer = r);
         }
         finalizeSubId() {
            this.subIdParts.size > 0
               ? (this._subId = Array.from(this.subIdParts).join("-"))
               : (this._subId = this.fingerprint.slice(0, 15)),
               (this._subId +=
                  "-" + Math.random().toString(36).substring(2, 7));
         }
         execute() {
            if (this.status === 1) {
               if (this.relay.connected)
                  this.relay.status < 8 &&
                     this.relay.once("authed", this.reExecuteAfterAuth);
               else {
                  (this.status = 2),
                     this.debug("Waiting for relay to be ready", {
                        status: this.status,
                        id: this.subId,
                        fingerprint: this.fingerprint,
                        items: this.items,
                        itemsSize: this.items.size,
                     }),
                     this.relay.once("ready", this.executeOnRelayReady);
                  return;
               }
               (this.status = 3),
                  this.finalizeSubId(),
                  (this.executeFilters = this.compileFilters()),
                  this.relay.req(this);
            }
         }
         onstart() {}
         onevent(t) {
            this.topSubManager.dispatchEvent(t, this.relay);
         }
         oneose(t) {
            if (((this.eosed = !0), t !== this.subId)) {
               this.debug(
                  "Received EOSE for an abandoned subscription",
                  t,
                  this.subId,
               ),
                  this.relay.close(t);
               return;
            }
            this.items.size === 0 && this.close();
            for (let { subscription: e } of this.items.values())
               e.eoseReceived(this.relay),
                  e.closeOnEose &&
                     (this.debug("Removing item because of EOSE", {
                        filters: e.filters,
                        internalId: e.internalId,
                        status: this.status,
                        fingerprint: this.fingerprint,
                        items: this.items,
                        itemsSize: this.items.size,
                     }),
                     this.removeItem(e));
         }
         onclose(t) {
            this.status = 4;
         }
         onclosed(t) {
            if (t)
               for (let { subscription: e } of this.items.values())
                  e.closedReceived(this.relay, t);
         }
         compileFilters() {
            let t = [],
               e = Array.from(this.items.values()).map((r) => r.filters);
            if (!e[0])
               return (
                  this.debug("\u{1F440} No filters to merge", this.items),
                  console.error("BUG: No filters to merge!", this.items),
                  []
               );
            let n = e[0].length;
            for (let r = 0; r < n; r++) {
               let s = e.map((i) => i[r]);
               t.push(...Fy(s));
            }
            return t;
         }
      },
      Wy = class {
         constructor(t, e) {
            m(this, "relay");
            m(this, "subscriptions");
            m(this, "generalSubManager");
            (this.relay = t),
               (this.subscriptions = new Map()),
               (this.generalSubManager = e);
         }
         addSubscription(t, e) {
            let n;
            if (!t.isGroupable()) n = this.createSubscription(t, e);
            else {
               let r = jy(e, t.closeOnEose);
               r &&
                  (n = (this.subscriptions.get(r) || []).find(
                     (i) => i.status < 3,
                  )),
                  n ?? (n = this.createSubscription(t, e, r));
            }
            n.addItem(t, e);
         }
         createSubscription(t, e, n) {
            let r = new Dy(this.relay, n || null, this.generalSubManager);
            r.onClose = this.onRelaySubscriptionClose.bind(this);
            let s = this.subscriptions.get(r.fingerprint) ?? [];
            return this.subscriptions.set(r.fingerprint, [...s, r]), r;
         }
         onRelaySubscriptionClose(t) {
            let e = this.subscriptions.get(t.fingerprint) ?? [];
            e
               ? e.length === 1
                  ? this.subscriptions.delete(t.fingerprint)
                  : ((e = e.filter((n) => n.id !== t.id)),
                    this.subscriptions.set(t.fingerprint, e))
               : console.warn(
                    "Unexpectedly did not find a subscription with fingerprint",
                    t.fingerprint,
                 );
         }
      };
   var rn,
      Nt =
         ((rn = class extends ul.EventEmitter {
            constructor(n, r, s) {
               super();
               m(this, "url");
               m(this, "scores");
               m(this, "connectivity");
               m(this, "subs");
               m(this, "publisher");
               m(this, "authPolicy");
               m(this, "lowestValidationRatio");
               m(this, "targetValidationRatio");
               m(this, "validationRatioFn");
               m(this, "validatedEventCount", 0);
               m(this, "nonValidatedEventCount", 0);
               m(this, "trusted", !1);
               m(this, "complaining", !1);
               m(this, "debug");
               m(this, "req");
               m(this, "close");
               (this.url = Je(n)),
                  (this.scores = new Map()),
                  (this.debug = (0, cl.default)(`ndk:relay:${n}`)),
                  (this.connectivity = new Vy(this, s)),
                  (this.connectivity.netDebug = s?.netDebug),
                  (this.req = this.connectivity.req.bind(this.connectivity)),
                  (this.close = this.connectivity.close.bind(
                     this.connectivity,
                  )),
                  (this.subs = new Wy(this, s.subManager)),
                  (this.publisher = new zy(this)),
                  (this.authPolicy = r),
                  (this.targetValidationRatio = s?.initialValidationRatio),
                  (this.lowestValidationRatio = s?.lowestValidationRatio),
                  (this.validationRatioFn = (
                     s?.validationRatioFn ?? rn.defaultValidationRatioUpdateFn
                  ).bind(this)),
                  this.updateValidationRatio(),
                  s || console.trace("relay created without ndk");
            }
            updateValidationRatio() {
               setTimeout(() => {
                  this.updateValidationRatio();
               }, 3e4);
            }
            get status() {
               return this.connectivity.status;
            }
            get connectionStats() {
               return this.connectivity.connectionStats;
            }
            async connect(n, r = !0) {
               return this.connectivity.connect(n, r);
            }
            disconnect() {
               this.status !== 1 && this.connectivity.disconnect();
            }
            subscribe(n, r) {
               this.subs.addSubscription(n, r);
            }
            async publish(n, r = 2500) {
               return this.publisher.publish(n, r);
            }
            referenceTags() {
               return [["r", this.url]];
            }
            addValidatedEvent() {
               this.validatedEventCount++;
            }
            addNonValidatedEvent() {
               this.nonValidatedEventCount++;
            }
            get validationRatio() {
               return this.nonValidatedEventCount === 0
                  ? 1
                  : this.validatedEventCount /
                       (this.validatedEventCount + this.nonValidatedEventCount);
            }
            shouldValidateEvent() {
               return this.trusted
                  ? !1
                  : this.targetValidationRatio === void 0
                    ? !0
                    : this.validationRatio < this.targetValidationRatio;
            }
            get connected() {
               return this.connectivity.connected;
            }
         }),
         m(rn, "defaultValidationRatioUpdateFn", (n, r, s) => {
            if (
               n.lowestValidationRatio === void 0 ||
               n.targetValidationRatio === void 0
            )
               return 1;
            let i = n.validationRatio;
            if (n.validationRatio > n.targetValidationRatio) {
               let o = r / 100;
               i = Math.max(n.lowestValidationRatio, n.validationRatio - o);
            }
            return i < n.validationRatio ? i : n.validationRatio;
         }),
         rn),
      Zu = class extends al.EventEmitter {
         constructor(e = [], n = [], r, s) {
            super();
            m(this, "_relays", new Map());
            m(this, "autoConnectRelays", new Set());
            m(this, "blacklistRelayUrls");
            m(this, "debug");
            m(this, "temporaryRelayTimers", new Map());
            m(this, "flappingRelays", new Set());
            m(this, "backoffTimes", new Map());
            m(this, "ndk");
            (this.debug = s ?? r.debug.extend("pool")),
               (this.ndk = r),
               (this.relayUrls = e),
               (this.blacklistRelayUrls = new Set(n));
         }
         get relays() {
            return this._relays;
         }
         set relayUrls(e) {
            this._relays.clear();
            for (let n of e) {
               let r = new Nt(n, void 0, this.ndk);
               (r.connectivity.netDebug = this.ndk.netDebug),
                  this.addRelay(r, !1);
            }
         }
         set name(e) {
            this.debug = this.debug.extend(e);
         }
         useTemporaryRelay(e, n = 3e4, r) {
            let s = this.relays.has(e.url);
            s ||
               (this.addRelay(e),
               this.debug(
                  "Adding temporary relay %s for filters %o",
                  e.url,
                  r,
               ));
            let i = this.temporaryRelayTimers.get(e.url);
            if ((i && clearTimeout(i), !s || i)) {
               let o = setTimeout(() => {
                  this.ndk.explicitRelayUrls?.includes(e.url) ||
                     this.removeRelay(e.url);
               }, n);
               this.temporaryRelayTimers.set(e.url, o);
            }
         }
         addRelay(e, n = !0) {
            let r = this.relays.has(e.url),
               s = this.blacklistRelayUrls?.has(e.url),
               i = e.url.includes("/npub1"),
               o = !0,
               a = e.url;
            if (r) return;
            if (s) {
               this.debug(`Refusing to add relay ${a}: blacklisted`);
               return;
            }
            if (i) {
               this.debug(`Refusing to add relay ${a}: is a filter relay`);
               return;
            }
            if (this.ndk.cacheAdapter?.getRelayStatus) {
               let d = this.ndk.cacheAdapter.getRelayStatus(a);
               if (d && d.dontConnectBefore)
                  if (d.dontConnectBefore > Date.now()) {
                     let p = d.dontConnectBefore - Date.now();
                     this.debug(
                        `Refusing to add relay ${a}: delayed connect for ${p}ms`,
                     ),
                        setTimeout(() => {
                           this.addRelay(e, n);
                        }, p);
                     return;
                  } else o = !1;
            }
            let c = (d) => this.emit("notice", e, d),
               l = () => this.handleRelayConnect(a),
               u = () => this.handleRelayReady(e),
               h = () => this.emit("relay:disconnect", e),
               f = () => this.handleFlapping(e),
               g = (d) => this.emit("relay:auth", e, d),
               y = () => this.emit("relay:authed", e);
            e.off("notice", c),
               e.off("connect", l),
               e.off("ready", u),
               e.off("disconnect", h),
               e.off("flapping", f),
               e.off("auth", g),
               e.off("authed", y),
               e.on("notice", c),
               e.on("connect", l),
               e.on("ready", u),
               e.on("disconnect", h),
               e.on("flapping", f),
               e.on("auth", g),
               e.on("authed", y),
               e.on("delayed-connect", (d) => {
                  this.ndk.cacheAdapter?.updateRelayStatus &&
                     this.ndk.cacheAdapter.updateRelayStatus(e.url, {
                        dontConnectBefore: Date.now() + d,
                     });
               }),
               this.relays.set(a, e),
               n && this.autoConnectRelays.add(a),
               n &&
                  (this.emit("relay:connecting", e),
                  e.connect(void 0, o).catch((d) => {
                     this.debug(`Failed to connect to relay ${a}`, d);
                  }));
         }
         removeRelay(e) {
            let n = this.relays.get(e);
            if (n)
               return (
                  n.disconnect(),
                  this.relays.delete(e),
                  this.autoConnectRelays.delete(e),
                  this.emit("relay:disconnect", n),
                  !0
               );
            let r = this.temporaryRelayTimers.get(e);
            return (
               r && (clearTimeout(r), this.temporaryRelayTimers.delete(e)), !1
            );
         }
         isRelayConnected(e) {
            let n = Je(e),
               r = this.relays.get(n);
            return r ? r.status === 5 : !1;
         }
         getRelay(e, n = !0, r = !1, s) {
            let i = this.relays.get(Je(e));
            return (
               i ||
                  ((i = new Nt(e, void 0, this.ndk)),
                  (i.connectivity.netDebug = this.ndk.netDebug),
                  r ? this.useTemporaryRelay(i, 3e4, s) : this.addRelay(i, n)),
               i
            );
         }
         handleRelayConnect(e) {
            let n = this.relays.get(e);
            if (!n) {
               console.error("NDK BUG: relay not found in pool", {
                  relayUrl: e,
               });
               return;
            }
            this.emit("relay:connect", n),
               this.stats().connected === this.relays.size &&
                  this.emit("connect");
         }
         handleRelayReady(e) {
            this.emit("relay:ready", e);
         }
         async connect(e) {
            let n = [];
            this.debug(
               `Connecting to ${this.relays.size} relays${e ? `, timeout ${e}...` : ""}`,
            );
            let r = new Set(this.autoConnectRelays.keys());
            this.ndk.explicitRelayUrls?.forEach((s) => {
               let i = Je(s);
               r.add(i);
            });
            for (let s of r) {
               let i = this.relays.get(s);
               if (!i) continue;
               let o = new Promise(
                  (a, c) => (
                     this.emit("relay:connecting", i),
                     i.connect(e).then(a).catch(c)
                  ),
               );
               if (e) {
                  let a = new Promise((c, l) => {
                     setTimeout(() => l(`Timed out after ${e}ms`), e);
                  });
                  n.push(
                     Promise.race([o, a]).catch((c) => {
                        this.debug(
                           `Failed to connect to relay ${i.url}: ${c ?? "No reason specified"}`,
                        );
                     }),
                  );
               } else n.push(o);
            }
            e &&
               setTimeout(() => {
                  let s = this.stats().connected === this.relays.size,
                     i = this.stats().connected > 0;
                  !s && i && this.emit("connect");
               }, e),
               await Promise.all(n);
         }
         checkOnFlappingRelays() {
            let e = this.flappingRelays.size,
               n = this.relays.size;
            if (e / n >= 0.8)
               for (let r of this.flappingRelays) this.backoffTimes.set(r, 0);
         }
         handleFlapping(e) {
            this.debug(`Relay ${e.url} is flapping`);
            let n = this.backoffTimes.get(e.url) || 5e3;
            (n = n * 2),
               this.backoffTimes.set(e.url, n),
               this.debug(`Backoff time for ${e.url} is ${n}ms`),
               setTimeout(() => {
                  this.debug(`Attempting to reconnect to ${e.url}`),
                     this.emit("relay:connecting", e),
                     e.connect(),
                     this.checkOnFlappingRelays();
               }, n),
               e.disconnect(),
               this.emit("flapping", e);
         }
         size() {
            return this.relays.size;
         }
         stats() {
            let e = { total: 0, connected: 0, disconnected: 0, connecting: 0 };
            for (let n of this.relays.values())
               e.total++,
                  n.status === 5
                     ? e.connected++
                     : n.status === 1
                       ? e.disconnected++
                       : n.status === 4 && e.connecting++;
            return e;
         }
         connectedRelays() {
            return Array.from(this.relays.values()).filter(
               (e) => e.status >= 5,
            );
         }
         permanentAndConnectedRelays() {
            return Array.from(this.relays.values()).filter(
               (e) => e.status >= 5 && !this.temporaryRelayTimers.has(e.url),
            );
         }
         urls() {
            return Array.from(this.relays.keys());
         }
      };
   function Yu(t) {
      return !!(Gy(t.filter) && Zy(t));
   }
   function Gy(t) {
      return !!t.ids;
   }
   function Zy(t) {
      let e = t.filter.ids;
      return !!e && e.length === t.eventFirstSeen.size;
   }
   function Yy(t) {
      let e;
      if (t.match(bl)) {
         let [n, r, s] = t.split(":"),
            i = { authors: [r], kinds: [parseInt(n)] };
         return s && (i["#d"] = [s]), i;
      }
      if (t.match(Qy))
         try {
            switch (((e = ye.decode(t)), e.type)) {
               case "nevent": {
                  let r = { ids: [e.data.id] };
                  return (
                     e.data.author && (r.authors = [e.data.author]),
                     e.data.kind && (r.kinds = [e.data.kind]),
                     r
                  );
               }
               case "note":
                  return { ids: [e.data] };
               case "naddr":
                  let n = { authors: [e.data.pubkey], kinds: [e.data.kind] };
                  return (
                     e.data.identifier && (n["#d"] = [e.data.identifier]), n
                  );
            }
         } catch (n) {
            console.error("Error decoding", t, n);
         }
      return { ids: [t] };
   }
   function Jy(t) {
      return t.match(bl) !== null;
   }
   var bl = /^(\d+):([0-9A-Fa-f]+)(?::(.*))?$/,
      Qy = /^n(event|ote|profile|pub|addr)1[\d\w]+$/;
   function Xy(t, e) {
      try {
         let n = ye.decode(t);
         if (["naddr", "nevent"].includes(n?.type)) {
            let r = n.data;
            if (r?.relays)
               return r.relays.map(
                  (s) => new Nt(s, e.relayAuthDefaultPolicy, e),
               );
         }
      } catch {}
      return [];
   }
   var Ky = {
         closeOnEose: !1,
         cacheUsage: "CACHE_FIRST",
         groupable: !0,
         groupableDelay: 100,
         groupableDelayType: "at-most",
      },
      eb = class extends yl.EventEmitter {
         constructor(e, n, r, s, i) {
            super();
            m(this, "subId");
            m(this, "filters");
            m(this, "opts");
            m(this, "pool");
            m(this, "skipVerification", !1);
            m(this, "skipValidation", !1);
            m(this, "relayFilters");
            m(this, "relaySet");
            m(this, "ndk");
            m(this, "debug");
            m(this, "eventFirstSeen", new Map());
            m(this, "eosesSeen", new Set());
            m(this, "lastEventReceivedAt");
            m(this, "internalId");
            m(this, "closeOnEose");
            m(this, "poolMonitor");
            m(this, "skipOptimisticPublishEvent", !1);
            m(this, "onStopped");
            m(this, "eoseTimeout");
            m(this, "eosed", !1);
            if (
               ((this.ndk = e),
               (this.pool = r?.pool || e.pool),
               (this.opts = { ...Ky, ...(r || {}) }),
               (this.filters = n instanceof Array ? n : [n]),
               (this.subId = i || r?.subId),
               (this.internalId = Math.random().toString(36).substring(7)),
               (this.relaySet = s),
               (this.debug = e.debug.extend(
                  `subscription[${r?.subId ?? this.internalId}]`,
               )),
               (this.skipVerification = r?.skipVerification || !1),
               (this.skipValidation = r?.skipValidation || !1),
               (this.closeOnEose = r?.closeOnEose || !1),
               (this.skipOptimisticPublishEvent =
                  r?.skipOptimisticPublishEvent || !1),
               this.opts.cacheUsage === "ONLY_CACHE" && !this.opts.closeOnEose)
            )
               throw new Error(
                  "Cannot use cache-only options with a persistent subscription",
               );
         }
         relaysMissingEose() {
            return this.relayFilters
               ? Array.from(this.relayFilters.keys()).filter(
                    (n) => !this.eosesSeen.has(this.pool.getRelay(n, !1, !1)),
                 )
               : [];
         }
         get filter() {
            return this.filters[0];
         }
         get groupableDelay() {
            if (this.isGroupable()) return this.opts?.groupableDelay;
         }
         get groupableDelayType() {
            return this.opts?.groupableDelayType || "at-most";
         }
         isGroupable() {
            return this.opts?.groupable || !1;
         }
         shouldQueryCache() {
            return this.opts?.cacheUsage !== "ONLY_RELAY";
         }
         shouldQueryRelays() {
            return this.opts?.cacheUsage !== "ONLY_CACHE";
         }
         shouldWaitForCache() {
            return (
               this.opts.closeOnEose &&
               !!this.ndk.cacheAdapter?.locking &&
               this.opts.cacheUsage !== "PARALLEL"
            );
         }
         async start() {
            let e;
            if (
               this.shouldQueryCache() &&
               ((e = this.startWithCache()),
               this.shouldWaitForCache() && (await e, Yu(this)))
            ) {
               this.emit("eose", this);
               return;
            }
            this.shouldQueryRelays()
               ? (this.startWithRelays(), this.startPoolMonitor())
               : this.emit("eose", this);
         }
         startPoolMonitor() {
            let e = this.debug.extend("pool-monitor");
            (this.poolMonitor = (n) => {
               if (this.relayFilters?.has(n.url)) return;
               lo(this.ndk, this.filters, this.pool).get(n.url) &&
                  (this.relayFilters?.set(n.url, this.filters),
                  n.subscribe(this, this.filters));
            }),
               this.pool.on("relay:connect", this.poolMonitor);
         }
         stop() {
            this.emit("close", this),
               this.poolMonitor &&
                  this.pool.off("relay:connect", this.poolMonitor),
               this.removeAllListeners(),
               this.onStopped?.();
         }
         hasAuthorsFilter() {
            return this.filters.some((e) => e.authors?.length);
         }
         async startWithCache() {
            if (this.ndk.cacheAdapter?.query) {
               let e = this.ndk.cacheAdapter.query(this);
               this.ndk.cacheAdapter.locking && (await e);
            }
         }
         startWithRelays() {
            if (!this.relaySet || this.relaySet.relays.size === 0)
               this.relayFilters = lo(this.ndk, this.filters, this.pool);
            else {
               this.relayFilters = new Map();
               for (let e of this.relaySet.relays)
                  this.relayFilters.set(e.url, this.filters);
            }
            if (!(!this.relayFilters || this.relayFilters.size === 0))
               for (let [e, n] of this.relayFilters)
                  this.pool.getRelay(e, !0, !0, n).subscribe(this, n);
         }
         eventReceived(e, n, r = !1, s = !1) {
            let i = e.id,
               o = this.eventFirstSeen.has(i),
               a;
            if ((e instanceof he && (a = e), o)) {
               let c = Date.now() - (this.eventFirstSeen.get(i) || 0);
               if ((this.emit("event:dup", i, n, c, this), n)) {
                  let l = _n.get(i);
                  l &&
                     typeof l == "string" &&
                     e.sig === l &&
                     n.addValidatedEvent();
               }
            } else {
               if (
                  (a ?? (a = new he(this.ndk, e)),
                  (a.ndk = this.ndk),
                  (a.relay = n),
                  !r && !s)
               ) {
                  if (!this.skipValidation && !a.isValid) {
                     this.debug(
                        "Event failed validation %s from relay %s",
                        i,
                        n?.url,
                     );
                     return;
                  }
                  if (n)
                     if (n?.shouldValidateEvent() !== !1) {
                        if (!this.skipVerification)
                           if (
                              !a.verifySignature(!0) &&
                              !this.ndk.asyncSigVerification
                           ) {
                              this.debug(
                                 "Event failed signature validation",
                                 e,
                              );
                              return;
                           } else n && n.addValidatedEvent();
                     } else n.addNonValidatedEvent();
                  this.ndk.cacheAdapter &&
                     this.ndk.cacheAdapter.setEvent(a, this.filters, n);
               }
               !r && n && this.ndk.emit("event", a, n),
                  (!s || this.skipOptimisticPublishEvent !== !0) &&
                     (this.emit("event", a, n, this),
                     this.eventFirstSeen.set(i, Date.now()));
            }
            this.lastEventReceivedAt = Date.now();
         }
         closedReceived(e, n) {
            this.emit("closed", e, n);
         }
         eoseReceived(e) {
            this.debug("EOSE received from %s", e.url), this.eosesSeen.add(e);
            let n = this.lastEventReceivedAt
                  ? Date.now() - this.lastEventReceivedAt
                  : void 0,
               r = this.eosesSeen.size === this.relayFilters?.size,
               s = Yu(this),
               i = (o) => {
                  this.debug("Performing EOSE: %s %d", o, this.eosed),
                     !this.eosed &&
                        (this.eoseTimeout && clearTimeout(this.eoseTimeout),
                        this.emit("eose", this),
                        (this.eosed = !0));
               };
            if (s || r) i("query filled or seen all");
            else if (this.relayFilters) {
               let o = 1e3,
                  a = new Set(this.pool.connectedRelays().map((u) => u.url)),
                  c = Array.from(this.relayFilters.keys()).filter((u) =>
                     a.has(u),
                  );
               if (c.length === 0) return;
               let l = this.eosesSeen.size / c.length;
               if (
                  (this.debug("Percentage of relays that have sent EOSE", {
                     subId: this.subId,
                     percentageOfRelaysThatHaveSentEose: l,
                     seen: this.eosesSeen.size,
                     total: c.length,
                  }),
                  this.eosesSeen.size >= 2 && l >= 0.5)
               ) {
                  if (((o = o * (1 - l)), o === 0)) {
                     i("time to wait was 0");
                     return;
                  }
                  this.eoseTimeout && clearTimeout(this.eoseTimeout);
                  let u = () => {
                     (n = this.lastEventReceivedAt
                        ? Date.now() - this.lastEventReceivedAt
                        : void 0),
                        n !== void 0 && n < 20
                           ? (this.eoseTimeout = setTimeout(u, o))
                           : i("send eose timeout: " + o);
                  };
                  this.eoseTimeout = setTimeout(u, o);
               }
            }
         }
      };
   async function tb(t, e, n = 3) {
      if (!this.ndk) throw new Error("NDK not set");
      let r = await this.ndk.fetchEvent(
         { kinds: [n], authors: [this.pubkey] },
         t || { groupable: !1 },
      );
      if (r) {
         let s = new Set();
         return (
            r.tags.forEach((i) => {
               i[0] === "p" && s.add(i[1]);
            }),
            e && this.ndk?.outboxTracker?.trackUsers(Array.from(s)),
            [...s].reduce((i, o) => {
               let a = new ht({ pubkey: o });
               return (a.ndk = this.ndk), i.add(a), i;
            }, new Set())
         );
      }
      return new Set();
   }
   function Ju(t) {
      let e = {},
         n;
      try {
         n = JSON.parse(t.content);
      } catch (r) {
         throw new Error(`Failed to parse profile event: ${r}`);
      }
      return (
         (e.created_at = t.created_at),
         (e.profileEvent = JSON.stringify(t.rawEvent())),
         Object.keys(n).forEach((r) => {
            switch (r) {
               case "name":
                  e.name = n.name;
                  break;
               case "display_name":
                  e.displayName = n.display_name;
                  break;
               case "image":
               case "picture":
                  e.image = n.picture || n.image;
                  break;
               case "banner":
                  e.banner = n.banner;
                  break;
               case "bio":
                  e.bio = n.bio;
                  break;
               case "nip05":
                  e.nip05 = n.nip05;
                  break;
               case "lud06":
                  e.lud06 = n.lud06;
                  break;
               case "lud16":
                  e.lud16 = n.lud16;
                  break;
               case "about":
                  e.about = n.about;
                  break;
               case "zapService":
                  e.zapService = n.zapService;
                  break;
               case "website":
                  e.website = n.website;
                  break;
               default:
                  e[r] = n[r];
                  break;
            }
         }),
         e
      );
   }
   function nb(t) {
      let e = {};
      for (let [n, r] of Object.entries(t))
         switch (n) {
            case "username":
            case "name":
               e.name = r;
               break;
            case "displayName":
               e.display_name = r;
               break;
            case "image":
            case "picture":
               e.picture = r;
               break;
            case "bio":
            case "about":
               e.about = r;
               break;
            default:
               e[n] = r;
               break;
         }
      return JSON.stringify(e);
   }
   var rb = /^(?:([\w.+-]+)@)?([\w.-]+)$/;
   async function Qu(t, e, n = fetch, r = {}) {
      return await t.queuesNip05.add({
         id: e,
         func: async () => {
            if (t.cacheAdapter && t.cacheAdapter.loadNip05) {
               let c = await t.cacheAdapter.loadNip05(e);
               if (c !== "missing") {
                  if (c) {
                     let l = new ht({
                        pubkey: c.pubkey,
                        relayUrls: c.relays,
                        nip46Urls: c.nip46,
                     });
                     return (l.ndk = t), l;
                  } else if (r.cache !== "no-cache") return null;
               }
            }
            let s = e.match(rb);
            if (!s) return null;
            let [i, o = "_", a] = s;
            try {
               let c = await n(
                     `https://${a}/.well-known/nostr.json?name=${o}`,
                     r,
                  ),
                  { names: l, relays: u, nip46: h } = sb(await c.json()),
                  f = l[o.toLowerCase()],
                  g = null;
               return (
                  f && (g = { pubkey: f, relays: u?.[f], nip46: h?.[f] }),
                  t?.cacheAdapter &&
                     t.cacheAdapter.saveNip05 &&
                     t.cacheAdapter.saveNip05(e, g),
                  g
               );
            } catch (c) {
               return (
                  t?.cacheAdapter &&
                     t.cacheAdapter.saveNip05 &&
                     t?.cacheAdapter.saveNip05(e, null),
                  console.error("Failed to fetch NIP05 for", e, c),
                  null
               );
            }
         },
      });
   }
   function sb(t) {
      let e = { names: {} };
      for (let [n, r] of Object.entries(t.names))
         typeof n == "string" &&
            typeof r == "string" &&
            (e.names[n.toLowerCase()] = r);
      if (t.relays) {
         e.relays = {};
         for (let [n, r] of Object.entries(t.relays))
            typeof n == "string" &&
               Array.isArray(r) &&
               (e.relays[n] = r.filter((s) => typeof s == "string"));
      }
      if (t.nip46) {
         e.nip46 = {};
         for (let [n, r] of Object.entries(t.nip46))
            typeof n == "string" &&
               Array.isArray(r) &&
               (e.nip46[n] = r.filter((s) => typeof s == "string"));
      }
      return e;
   }
   var Ot,
      ib =
         ((Ot = class extends he {
            constructor(n, r) {
               super(n, r);
               m(this, "_p2pk");
               this.kind ?? (this.kind = 10019);
            }
            static from(n) {
               return new Ot(n.ndk, n);
            }
            set relays(n) {
               this.tags = this.tags.filter((r) => r[0] !== "relay");
               for (let r of n) this.tags.push(["relay", r]);
            }
            get relays() {
               let n = [];
               for (let r of this.tags) r[0] === "relay" && n.push(r[1]);
               return n;
            }
            set mints(n) {
               this.tags = this.tags.filter((r) => r[0] !== "mint");
               for (let r of n) this.tags.push(["mint", r]);
            }
            get mints() {
               let n = [];
               for (let r of this.tags) r[0] === "mint" && n.push(r[1]);
               return Array.from(new Set(n));
            }
            get p2pk() {
               return this._p2pk
                  ? this._p2pk
                  : ((this._p2pk = this.tagValue("pubkey") ?? this.pubkey),
                    this._p2pk);
            }
            set p2pk(n) {
               (this._p2pk = n),
                  this.removeTag("pubkey"),
                  n && this.tags.push(["pubkey", n]);
            }
            get relaySet() {
               return lt.fromRelayUrls(this.relays, this.ndk);
            }
         }),
         m(Ot, "kind", 10019),
         m(Ot, "kinds", [10019]),
         Ot),
      ob = (0, ml.default)("ndk:zapper:ln");
   async function Xu({ lud06: t, lud16: e }, n) {
      let r;
      if (e && !e.startsWith("LNURL")) {
         let [s, i] = e.split("@");
         r = `https://${i}/.well-known/lnurlp/${s}`;
      } else if (t) {
         let { words: s } = to.decode(t, 1e3),
            i = to.fromWords(s);
         r = new TextDecoder("utf-8").decode(i);
      }
      if (!r)
         throw (
            (ob("No zap endpoint found %o", { lud06: t, lud16: e }),
            new Error("No zap endpoint found"))
         );
      try {
         let i = await (n.httpFetch || fetch)(r);
         if (i.status !== 200) {
            let o = await i.text();
            throw new Error(`Unable to fetch zap endpoint ${r}: ${o}`);
         }
         return await i.json();
      } catch (s) {
         throw new Error(`Unable to fetch zap endpoint ${r}: ${s}`);
      }
   }
   var ht = class wl {
         constructor(e) {
            m(this, "ndk");
            m(this, "profile");
            m(this, "_npub");
            m(this, "_pubkey");
            m(this, "relayUrls", []);
            m(this, "nip46Urls", []);
            m(this, "follows", tb.bind(this));
            e.npub && (this._npub = e.npub),
               e.hexpubkey && (this._pubkey = e.hexpubkey),
               e.pubkey && (this._pubkey = e.pubkey),
               e.relayUrls && (this.relayUrls = e.relayUrls),
               e.nip46Urls && (this.nip46Urls = e.nip46Urls);
         }
         get npub() {
            if (!this._npub) {
               if (!this._pubkey) throw new Error("pubkey not set");
               this._npub = ye.npubEncode(this.pubkey);
            }
            return this._npub;
         }
         get nprofile() {
            return (
               console.log("encoding with pubkey", this.pubkey),
               ye.nprofileEncode({ pubkey: this.pubkey })
            );
         }
         set npub(e) {
            this._npub = e;
         }
         get hexpubkey() {
            return this.pubkey;
         }
         set hexpubkey(e) {
            this._pubkey = e;
         }
         get pubkey() {
            if (!this._pubkey) {
               if (!this._npub) throw new Error("npub not set");
               this._pubkey = ye.decode(this.npub).data;
            }
            return this._pubkey;
         }
         set pubkey(e) {
            this._pubkey = e;
         }
         async getZapInfo(e = !0, n = ["nip61", "nip57"]) {
            if (!this.ndk) throw new Error("No NDK instance found");
            let r = [];
            if (
               (n.includes("nip61") && r.push(10019),
               n.includes("nip57") && r.push(0),
               r.length === 0)
            )
               return [];
            let s = await this.ndk.fetchEvents(
               { kinds: r, authors: [this.pubkey] },
               { cacheUsage: "ONLY_CACHE", groupable: !1 },
            );
            s.size < n.length &&
               (s = await this.ndk.fetchEvents(
                  { kinds: r, authors: [this.pubkey] },
                  { cacheUsage: "ONLY_RELAY" },
               ));
            let i = [],
               o = Array.from(s).find((c) => c.kind === 10019),
               a = Array.from(s).find((c) => c.kind === 0);
            if (o) {
               let c = ib.from(o);
               if (
                  (c.mints.length > 0 &&
                     i.push({
                        type: "nip61",
                        data: {
                           mints: c.mints,
                           relays: c.relays,
                           p2pk: c.p2pk,
                        },
                     }),
                  !e)
               )
                  return i;
            }
            if (a) {
               let c = Ju(a),
                  { lud06: l, lud16: u } = c;
               try {
                  let h = await Xu({ lud06: l, lud16: u }, this.ndk);
                  h && i.push({ type: "nip57", data: h });
               } catch (h) {
                  console.error("Error getting NIP-57 zap spec", h);
               }
            }
            return i;
         }
         async getZapConfiguration(e) {
            if ((e ?? (e = this.ndk), !e))
               throw new Error("No NDK instance found");
            let n = async () => {
               if (this.ndk?.cacheAdapter?.loadUsersLNURLDoc) {
                  let s = await this.ndk.cacheAdapter.loadUsersLNURLDoc(
                     this.pubkey,
                  );
                  if (s !== "missing") {
                     if (s === null) return;
                     if (s) return s;
                  }
               }
               let r;
               try {
                  if (
                     (await this.fetchProfile({ groupable: !1 }), this.profile)
                  ) {
                     let { lud06: s, lud16: i } = this.profile;
                     r = await Xu({ lud06: s, lud16: i }, e);
                  }
               } catch {}
               if (
                  (this.ndk?.cacheAdapter?.saveUsersLNURLDoc &&
                     this.ndk.cacheAdapter.saveUsersLNURLDoc(
                        this.pubkey,
                        r || null,
                     ),
                  !!r)
               )
                  return r;
            };
            return await e.queuesZapConfig.add({ id: this.pubkey, func: n });
         }
         async getZapperPubkey() {
            return (await this.getZapConfiguration())?.nostrPubkey;
         }
         static async fromNip05(e, n, r = !1) {
            if (!n) throw new Error("No NDK instance found");
            let s = {};
            r && (s.cache = "no-cache");
            let i = await Qu(n, e, n?.httpFetch, s);
            if (i) {
               let o = new wl({
                  pubkey: i.pubkey,
                  relayUrls: i.relays,
                  nip46Urls: i.nip46,
               });
               return (o.ndk = n), o;
            }
         }
         async fetchProfile(e, n = !1) {
            if (!this.ndk) throw new Error("NDK not set");
            this.profile || (this.profile = {});
            let r = null;
            if (
               this.ndk.cacheAdapter &&
               this.ndk.cacheAdapter.fetchProfile &&
               e?.cacheUsage !== "ONLY_RELAY"
            ) {
               let o = await this.ndk.cacheAdapter.fetchProfile(this.pubkey);
               if (o) return (this.profile = o), o;
            }
            !e &&
               this.ndk.cacheAdapter &&
               this.ndk.cacheAdapter.locking &&
               ((r = await this.ndk.fetchEvents(
                  { kinds: [0], authors: [this.pubkey] },
                  { cacheUsage: "ONLY_CACHE", closeOnEose: !0, groupable: !1 },
               )),
               (e = {
                  cacheUsage: "ONLY_RELAY",
                  closeOnEose: !0,
                  groupable: !0,
                  groupableDelay: 250,
               })),
               (!r || r.size === 0) &&
                  (r = await this.ndk.fetchEvents(
                     { kinds: [0], authors: [this.pubkey] },
                     e,
                  ));
            let s = Array.from(r).sort((o, a) => o.created_at - a.created_at);
            if (s.length === 0) return null;
            let i = s[0];
            return (
               (this.profile = Ju(i)),
               n && (this.profile.profileEvent = JSON.stringify(i)),
               this.profile &&
                  this.ndk.cacheAdapter &&
                  this.ndk.cacheAdapter.saveProfile &&
                  this.ndk.cacheAdapter.saveProfile(this.pubkey, this.profile),
               this.profile
            );
         }
         async followSet(e, n, r = 3) {
            let s = await this.follows(e, n, r);
            return new Set(Array.from(s).map((i) => i.pubkey));
         }
         tagReference() {
            return ["p", this.pubkey];
         }
         referenceTags(e) {
            let n = [["p", this.pubkey]];
            return e && n[0].push("", e), n;
         }
         async publish() {
            if (!this.ndk) throw new Error("No NDK instance found");
            if (!this.profile) throw new Error("No profile available");
            this.ndk.assertSigner(),
               await new he(this.ndk, {
                  kind: 0,
                  content: nb(this.profile),
               }).publish();
         }
         async follow(e, n, r = 3) {
            if (!this.ndk) throw new Error("No NDK instance found");
            if (
               (this.ndk.assertSigner(),
               n || (n = await this.follows(void 0, void 0, r)),
               n.has(e))
            )
               return !1;
            n.add(e);
            let s = new he(this.ndk, { kind: r });
            for (let i of n) s.tag(i);
            return await s.publish(), !0;
         }
         async unfollow(e, n, r = 3) {
            if (!this.ndk) throw new Error("No NDK instance found");
            this.ndk.assertSigner(),
               n || (n = await this.follows(void 0, void 0, r));
            let s = new Set(),
               i = !1;
            for (let a of n) a.pubkey !== e.pubkey ? s.add(a) : (i = !0);
            if (!i) return !1;
            let o = new he(this.ndk, { kind: r });
            for (let a of s) o.tag(a);
            return await o.publish();
         }
         async validateNip05(e) {
            if (!this.ndk) throw new Error("No NDK instance found");
            let n = await Qu(this.ndk, e);
            return n === null ? null : n.pubkey === this.pubkey;
         }
      },
      ab = class vl extends he {
         constructor(n, r) {
            super(n, r);
            m(this, "_encryptedTags");
            m(this, "encryptedTagsLength");
            this.kind ?? (this.kind = 30001);
         }
         static from(n) {
            return new vl(n.ndk, n);
         }
         get title() {
            let n = this.tagValue("title") || this.tagValue("name");
            return (
               n ||
               (this.kind === 3
                  ? "Contacts"
                  : this.kind === 1e4
                    ? "Mute"
                    : this.kind === 10001
                      ? "Pinned Notes"
                      : this.kind === 10002
                        ? "Relay Metadata"
                        : this.kind === 10003
                          ? "Bookmarks"
                          : this.kind === 10004
                            ? "Communities"
                            : this.kind === 10005
                              ? "Public Chats"
                              : this.kind === 10006
                                ? "Blocked Relays"
                                : this.kind === 10007
                                  ? "Search Relays"
                                  : this.kind === 10050
                                    ? "Direct Message Receive Relays"
                                    : this.kind === 10015
                                      ? "Interests"
                                      : this.kind === 10030
                                        ? "Emojis"
                                        : this.tagValue("d"))
            );
         }
         set title(n) {
            this.removeTag(["title", "name"]),
               n && this.tags.push(["title", n]);
         }
         get name() {
            return this.title;
         }
         set name(n) {
            this.title = n;
         }
         get description() {
            return this.tagValue("description");
         }
         set description(n) {
            this.removeTag("description"),
               n && this.tags.push(["description", n]);
         }
         get image() {
            return this.tagValue("image");
         }
         set image(n) {
            this.removeTag("image"), n && this.tags.push(["image", n]);
         }
         isEncryptedTagsCacheValid() {
            return !!(
               this._encryptedTags &&
               this.encryptedTagsLength === this.content.length
            );
         }
         async encryptedTags(n = !0) {
            if (n && this.isEncryptedTagsCacheValid())
               return this._encryptedTags;
            if (!this.ndk) throw new Error("NDK instance not set");
            if (!this.ndk.signer) throw new Error("NDK signer not set");
            let r = await this.ndk.signer.user();
            try {
               if (this.content.length > 0)
                  try {
                     let s = await this.ndk.signer.decrypt(r, this.content),
                        i = JSON.parse(s);
                     return i && i[0]
                        ? ((this.encryptedTagsLength = this.content.length),
                          (this._encryptedTags = i))
                        : ((this.encryptedTagsLength = this.content.length),
                          (this._encryptedTags = []));
                  } catch {
                     console.log(`error decrypting ${this.content}`);
                  }
            } catch {}
            return [];
         }
         validateTag(n) {
            return !0;
         }
         getItems(n) {
            return this.tags.filter((r) => r[0] === n);
         }
         get items() {
            return this.tags.filter(
               (n) =>
                  ![
                     "d",
                     "L",
                     "l",
                     "title",
                     "name",
                     "description",
                     "published_at",
                     "summary",
                     "image",
                     "thumb",
                     "alt",
                     "expiration",
                     "subject",
                     "client",
                  ].includes(n[0]),
            );
         }
         async addItem(n, r = void 0, s = !1, i = "bottom") {
            if (!this.ndk) throw new Error("NDK instance not set");
            if (!this.ndk.signer) throw new Error("NDK signer not set");
            let o;
            if (n instanceof he) o = [n.tagReference(r)];
            else if (n instanceof ht) o = n.referenceTags();
            else if (n instanceof Nt) o = n.referenceTags();
            else if (Array.isArray(n)) o = [n];
            else throw new Error("Invalid object type");
            if ((r && o[0].push(r), s)) {
               let a = await this.ndk.signer.user(),
                  c = await this.encryptedTags();
               i === "top" ? c.unshift(...o) : c.push(...o),
                  (this._encryptedTags = c),
                  (this.encryptedTagsLength = this.content.length),
                  (this.content = JSON.stringify(c)),
                  await this.encrypt(a);
            } else i === "top" ? this.tags.unshift(...o) : this.tags.push(...o);
            (this.created_at = Math.floor(Date.now() / 1e3)),
               this.emit("change");
         }
         async removeItemByValue(n, r = !0) {
            if (!this.ndk) throw new Error("NDK instance not set");
            if (!this.ndk.signer) throw new Error("NDK signer not set");
            let s = this.tags.findIndex((c) => c[1] === n);
            s >= 0 && this.tags.splice(s, 1);
            let i = await this.ndk.signer.user(),
               o = await this.encryptedTags(),
               a = o.findIndex((c) => c[1] === n);
            if (
               (a >= 0 &&
                  (o.splice(a, 1),
                  (this._encryptedTags = o),
                  (this.encryptedTagsLength = this.content.length),
                  (this.content = JSON.stringify(o)),
                  await this.encrypt(i)),
               r)
            )
               return this.publishReplaceable();
            (this.created_at = Math.floor(Date.now() / 1e3)),
               this.emit("change");
         }
         async removeItem(n, r) {
            if (!this.ndk) throw new Error("NDK instance not set");
            if (!this.ndk.signer) throw new Error("NDK signer not set");
            if (r) {
               let s = await this.ndk.signer.user(),
                  i = await this.encryptedTags();
               i.splice(n, 1),
                  (this._encryptedTags = i),
                  (this.encryptedTagsLength = this.content.length),
                  (this.content = JSON.stringify(i)),
                  await this.encrypt(s);
            } else this.tags.splice(n, 1);
            return (
               (this.created_at = Math.floor(Date.now() / 1e3)),
               this.emit("change"),
               this
            );
         }
         has(n) {
            return this.items.some((r) => r[1] === n);
         }
         filterForItems() {
            let n = new Set(),
               r = new Map(),
               s = [];
            for (let i of this.items)
               if (i[0] === "e" && i[1]) n.add(i[1]);
               else if (i[0] === "a" && i[1]) {
                  let [o, a, c] = i[1].split(":");
                  if (!o || !a) continue;
                  let l = `${o}:${a}`,
                     u = r.get(l) || [];
                  u.push(c || ""), r.set(l, u);
               }
            if ((n.size > 0 && s.push({ ids: Array.from(n) }), r.size > 0))
               for (let [i, o] of r.entries()) {
                  let [a, c] = i.split(":");
                  s.push({ "kinds": [parseInt(a)], "authors": [c], "#d": o });
               }
            return s;
         }
      },
      xl = ab;
   var Ku = "read",
      el = "write",
      fo = class El extends he {
         constructor(e, n) {
            super(e, n), this.kind ?? (this.kind = 10002);
         }
         static from(e) {
            return new El(e.ndk, e.rawEvent());
         }
         get readRelayUrls() {
            return this.tags
               .filter((e) => e[0] === "r" || e[0] === "relay")
               .filter((e) => !e[2] || (e[2] && e[2] === Ku))
               .map((e) => Wu(e[1]))
               .filter((e) => !!e);
         }
         set readRelayUrls(e) {
            for (let n of e) this.tags.push(["r", n, Ku]);
         }
         get writeRelayUrls() {
            return this.tags
               .filter((e) => e[0] === "r" || e[0] === "relay")
               .filter((e) => !e[2] || (e[2] && e[2] === el))
               .map((e) => Wu(e[1]))
               .filter((e) => !!e);
         }
         set writeRelayUrls(e) {
            for (let n of e) this.tags.push(["r", n, el]);
         }
         get bothRelayUrls() {
            return this.tags
               .filter((e) => e[0] === "r" || e[0] === "relay")
               .filter((e) => !e[2])
               .map((e) => e[1]);
         }
         set bothRelayUrls(e) {
            for (let n of e) this.tags.push(["r", n]);
         }
         get relays() {
            return this.tags
               .filter((e) => e[0] === "r" || e[0] === "relay")
               .map((e) => e[1]);
         }
         get relaySet() {
            if (!this.ndk) throw new Error("NDKRelayList has no NDK instance");
            return new lt(
               new Set(this.relays.map((e) => this.ndk.pool.getRelay(e))),
               this.ndk,
            );
         }
      };
   function tl(t, e) {
      try {
         let n = JSON.parse(e.content),
            r = new fo(t),
            s = new Set(),
            i = new Set();
         for (let [o, a] of Object.entries(n)) {
            try {
               o = Je(o);
            } catch {
               continue;
            }
            if (!a) s.add(o), i.add(o);
            else {
               let c = a;
               c.write && i.add(o), c.read && s.add(o);
            }
         }
         return (
            (r.readRelayUrls = Array.from(s)),
            (r.writeRelayUrls = Array.from(i)),
            r
         );
      } catch {}
   }
   var Mt,
      Xv =
         ((Mt = class extends he {
            constructor(n, r) {
               super(n, r);
               m(this, "debug");
               m(this, "_proofs", []);
               m(this, "sender", this.author);
               this.kind ?? (this.kind = 9321),
                  (this.debug =
                     n?.debug.extend("nutzap") ??
                     (0, kl.default)("ndk:nutzap")),
                  this.alt || (this.alt = "This is a nutzap");
            }
            static from(n) {
               let r = new this(n.ndk, n);
               try {
                  let s = r.getMatchingTags("proof");
                  s.length
                     ? (r._proofs = s.map((i) => JSON.parse(i[1])))
                     : (r._proofs = JSON.parse(r.content));
               } catch {
                  return;
               }
               if (!(!r._proofs || !r._proofs.length)) return r;
            }
            set comment(n) {
               this.content = n ?? "";
            }
            get comment() {
               let n = this.tagValue("comment");
               return n || this.content;
            }
            set proofs(n) {
               (this._proofs = n),
                  (this.tags = this.tags.filter((r) => r[0] !== "proof"));
               for (let r of n) this.tags.push(["proof", JSON.stringify(r)]);
               this.removeTag("amount"),
                  this.tags.push(["amount", this.amount.toString()]);
            }
            get proofs() {
               return this._proofs;
            }
            get p2pk() {
               let n = this.proofs[0];
               try {
                  let r = JSON.parse(n.secret),
                     s = {};
                  if (
                     (typeof r == "string"
                        ? ((s = JSON.parse(r)),
                          this.debug("stringified payload", n.secret))
                        : typeof r == "object" && (s = r),
                     s[0] === "P2PK" && s[1]?.data)
                  ) {
                     let a = s[1].data.slice(2, -1);
                     if (a) return a;
                  }
               } catch (r) {
                  this.debug("error parsing p2pk pubkey", r, this.proofs[0]);
               }
            }
            get mint() {
               return this.tagValue("u");
            }
            set mint(n) {
               this.removeTag("u"), this.tag(["u", n]);
            }
            get unit() {
               return this.tagValue("unit") ?? "msat";
            }
            set unit(n) {
               this.removeTag("unit"), n && this.tag(["unit", n]);
            }
            get amount() {
               return this.proofs.reduce((r, s) => r + s.amount, 0) * 1e3;
            }
            set target(n) {
               (this.tags = this.tags.filter((r) => r[0] !== "p")),
                  n instanceof he && this.tags.push(n.tagReference());
            }
            set recipientPubkey(n) {
               this.removeTag("p"), this.tag(["p", n]);
            }
            get recipientPubkey() {
               return this.tagValue("p");
            }
            get recipient() {
               let n = this.recipientPubkey;
               return this.ndk
                  ? this.ndk.getUser({ pubkey: n })
                  : new ht({ pubkey: n });
            }
            get isValid() {
               let n = 0,
                  r = 0;
               for (let s of this.tags)
                  s[0] === "p" && n++, s[0] === "u" && r++;
               return n === 1 && r === 1 && this.proofs.length > 0;
            }
         }),
         m(Mt, "kind", 9321),
         m(Mt, "kinds", [Mt.kind]),
         Mt);
   function pb(t, e) {
      return t.created_at > e.created_at ? t : e;
   }
   async function gb(t, e) {
      return (await Rl([t], e)).get(t);
   }
   async function Rl(t, e, n = !1) {
      let r = e.outboxPool || e.pool,
         s = new Set();
      for (let u of r.relays.values()) s.add(u);
      let i = new Map(),
         o = new Map(),
         a = new lt(s, e);
      if (e.cacheAdapter?.locking && !n) {
         let u = await e.fetchEvents(
            { kinds: [3, 10002], authors: t },
            { cacheUsage: "ONLY_CACHE" },
         );
         for (let h of u) h.kind === 10002 && i.set(h.pubkey, fo.from(h));
         for (let h of u)
            if (h.kind === 3) {
               if (i.has(h.pubkey)) continue;
               let f = tl(e, h);
               f && o.set(h.pubkey, f);
            }
         t = t.filter((h) => !i.has(h) && !o.has(h));
      }
      if (t.length === 0) return i;
      let c = new Map(),
         l = new Map();
      return new Promise(async (u) => {
         let h = e.subscribe(
            { kinds: [3, 10002], authors: t },
            {
               closeOnEose: !0,
               pool: r,
               groupable: !0,
               cacheUsage: "ONLY_RELAY",
               subId: "ndk-relay-list-fetch",
            },
            a,
            !1,
         );
         h.on("event", (f) => {
            if (f.kind === 10002) {
               let g = c.get(f.pubkey);
               if (g && g.created_at > f.created_at) return;
               c.set(f.pubkey, f);
            } else if (f.kind === 3) {
               let g = l.get(f.pubkey);
               if (g && g.created_at > f.created_at) return;
               l.set(f.pubkey, f);
            }
         }),
            h.on("eose", () => {
               for (let f of c.values()) i.set(f.pubkey, fo.from(f));
               for (let f of t) {
                  if (i.has(f)) continue;
                  let g = l.get(f);
                  if (!g) continue;
                  let y = tl(e, g);
                  y && i.set(f, y);
               }
               u(i);
            }),
            h.start();
      });
   }
   var uo = class {
         constructor(t) {
            m(this, "type");
            m(this, "relayUrlScores");
            m(this, "readRelays");
            m(this, "writeRelays");
            (this.type = t),
               (this.relayUrlScores = new Map()),
               (this.readRelays = new Set()),
               (this.writeRelays = new Set());
         }
      },
      yb = class extends _l.EventEmitter {
         constructor(e) {
            super();
            m(this, "data");
            m(this, "ndk");
            m(this, "debug");
            (this.ndk = e),
               (this.debug = e.debug.extend("outbox-tracker")),
               (this.data = new Sl.LRUCache({
                  maxSize: 1e5,
                  entryExpirationTimeInMS: 2 * 60 * 1e3,
               }));
         }
         async trackUsers(e, n = !1) {
            let r = [];
            for (let s = 0; s < e.length; s += 400) {
               let o = e
                  .slice(s, s + 400)
                  .map((a) => nl(a))
                  .filter((a) => !this.data.has(a));
               if (o.length !== 0) {
                  for (let a of o) this.data.set(a, new uo("user"));
                  r.push(
                     new Promise((a) => {
                        Rl(o, this.ndk, n)
                           .then((c) => {
                              for (let [l, u] of c) {
                                 let h = this.data.get(l);
                                 if ((h ?? (h = new uo("user")), u)) {
                                    (h.readRelays = new Set(
                                       Gu(u.readRelayUrls),
                                    )),
                                       (h.writeRelays = new Set(
                                          Gu(u.writeRelayUrls),
                                       ));
                                    for (let f of h.readRelays)
                                       this.ndk.pool.blacklistRelayUrls.has(
                                          f,
                                       ) && h.readRelays.delete(f);
                                    for (let f of h.writeRelays)
                                       this.ndk.pool.blacklistRelayUrls.has(
                                          f,
                                       ) && h.writeRelays.delete(f);
                                    this.data.set(l, h);
                                 }
                              }
                           })
                           .finally(a);
                     }),
                  );
               }
            }
            return Promise.all(r);
         }
         track(e, n, r = !0) {
            let s = nl(e);
            n ?? (n = bb(e));
            let i = this.data.get(s);
            return (
               i || ((i = new uo(n)), e instanceof ht && this.trackUsers([e])),
               i
            );
         }
      };
   function nl(t) {
      return t instanceof ht ? t.pubkey : t;
   }
   function bb(t) {
      return t instanceof ht ? "user" : "kind";
   }
   function mb(t, e) {
      let n = e.connectedRelays();
      if (
         !Array.from(t.relays).some((s) => n.map((i) => i.url).includes(s.url))
      )
         for (let s of n) t.addRelay(s);
      if (n.length === 0) for (let s of e.relays.values()) t.addRelay(s);
      return t;
   }
   function rl(t) {
      if (!t || t === "") return !1;
      try {
         return new URL(t), !0;
      } catch {
         return !1;
      }
   }
   async function wb(t, e, n, r = { type: "timeout" }) {
      let s = this.debug.extend("fetch-event-from-tag"),
         [i, o, a] = t;
      (n = {}), s("fetching event from tag", t, n, r);
      let c = us(this, e.pubkey);
      if (c && c.size > 0) {
         s("fetching event from author relays %o", Array.from(c));
         let d = lt.fromRelayUrls(Array.from(c), this),
            p = await this.fetchEvent(o, n, d);
         if (p) return p;
      } else s("no author relays found for %s", e.pubkey, e);
      let l = lo(this, [{ ids: [o] }], this.pool);
      s("fetching event without relay hint", l);
      let u = await this.fetchEvent(o, n);
      if (u) return u;
      if (a && a !== "") {
         let d = await this.fetchEvent(
            o,
            n,
            this.pool.getRelay(a, !0, !0, [{ ids: [o] }]),
         );
         if (d) return d;
      }
      let h,
         f = rl(a) ? this.pool.getRelay(a, !1, !0, [{ ids: [o] }]) : void 0,
         g = new Promise((d) => {
            this.fetchEvent(o, n, f).then(d);
         });
      if (!rl(a) || r.type === "none") return g;
      let y = new Promise(async (d) => {
         let p = r.relaySet,
            b = r.timeout ?? 1500,
            w = new Promise((E) => setTimeout(E, b));
         if ((r.type === "timeout" && (await w), h)) d(h);
         else {
            s("fallback fetch triggered");
            let E = await this.fetchEvent(o, n, p);
            d(E);
         }
      });
      switch (r.type) {
         case "timeout":
            return Promise.race([g, y]);
         case "eose":
            return (h = await g), h || y;
      }
   }
   var vb = "/.well-known/nostr/nip96.json",
      xb = class {
         constructor(t, e) {
            m(this, "ndk");
            m(this, "spec");
            m(this, "url");
            m(this, "nip98Required", !1);
            (this.url = `https://${t}${vb}`), (this.ndk = e);
         }
         async prepareUpload(t, e = "POST") {
            if (
               (this.validateHttpFetch(),
               this.spec || (await this.fetchSpec()),
               !this.spec)
            )
               throw new Error("Failed to fetch NIP96 spec");
            let n = {};
            return (
               this.nip98Required &&
                  (n = {
                     Authorization: await this.generateNip98Header(
                        this.spec.api_url,
                        e,
                        t,
                     ),
                  }),
               { url: this.spec.api_url, headers: n }
            );
         }
         async xhrUpload(t, e) {
            let n = "POST",
               { url: r, headers: s } = await this.prepareUpload(e, n);
            t.open(n, r, !0),
               s.Authorization &&
                  t.setRequestHeader("Authorization", s.Authorization);
            let i = new FormData();
            return (
               i.append("file", e),
               new Promise((o, a) => {
                  (t.onload = function () {
                     t.status >= 200 && t.status < 300
                        ? o(JSON.parse(t.responseText))
                        : a(new Error(t.statusText));
                  }),
                     (t.onerror = function () {
                        a(new Error("Network Error"));
                     }),
                     t.send(i);
               })
            );
         }
         async upload(t) {
            let e = "POST",
               { url: n, headers: r } = await this.prepareUpload(t, e),
               s = new FormData();
            s.append("file", t);
            let i = await this.ndk.httpFetch(this.spec.api_url, {
               method: e,
               headers: r,
               body: s,
            });
            if (i.status !== 200)
               throw new Error(`Failed to upload file to ${n}`);
            let o = await i.json();
            if (o.status !== "success") throw new Error(o.message);
            return o;
         }
         validateHttpFetch() {
            if (!this.ndk)
               throw new Error("NDK is required to fetch NIP96 spec");
            if (!this.ndk.httpFetch)
               throw new Error(
                  "NDK must have an httpFetch method to fetch NIP96 spec",
               );
         }
         async fetchSpec() {
            this.validateHttpFetch();
            let t = await this.ndk.httpFetch(this.url);
            if (t.status !== 200)
               throw new Error(`Failed to fetch NIP96 spec from ${this.url}`);
            let e = await t.json();
            if (!e)
               throw new Error(`Failed to parse NIP96 spec from ${this.url}`);
            (this.spec = e),
               (this.nip98Required = this.spec.plans.free.is_nip98_required);
         }
         async generateNip98Header(t, e, n) {
            let r = new he(this.ndk, {
               kind: 27235,
               tags: [
                  ["u", t],
                  ["method", e],
               ],
            });
            if (["POST", "PUT", "PATCH"].includes(e)) {
               let i = await this.calculateSha256(n);
               r.tags.push(["payload", i]);
            }
            return (
               await r.sign(), `Nostr ${btoa(JSON.stringify(r.rawEvent()))}`
            );
         }
         async calculateSha256(t) {
            let e = await t.arrayBuffer(),
               n = await crypto.subtle.digest("SHA-256", e);
            return Array.from(new Uint8Array(n))
               .map((i) => i.toString(16).padStart(2, "0"))
               .join("");
         }
      },
      sl = class {
         constructor(t, e) {
            m(this, "queue", []);
            m(this, "maxConcurrency");
            m(this, "processing", new Set());
            m(this, "promises", new Map());
            this.maxConcurrency = e;
         }
         add(t) {
            if (this.promises.has(t.id)) return this.promises.get(t.id);
            let e = new Promise((n, r) => {
               this.queue.push({
                  ...t,
                  func: () =>
                     t.func().then(
                        (s) => (n(s), s),
                        (s) => {
                           throw (r(s), s);
                        },
                     ),
               }),
                  this.process();
            });
            return (
               this.promises.set(t.id, e),
               e.finally(() => {
                  this.promises.delete(t.id),
                     this.processing.delete(t.id),
                     this.process();
               }),
               e
            );
         }
         process() {
            if (
               this.processing.size >= this.maxConcurrency ||
               this.queue.length === 0
            )
               return;
            let t = this.queue.shift();
            !t ||
               this.processing.has(t.id) ||
               (this.processing.add(t.id), t.func());
         }
         clear() {
            this.queue = [];
         }
         clearProcessing() {
            this.processing.clear();
         }
         clearAll() {
            this.clear(), this.clearProcessing();
         }
         length() {
            return this.queue.length;
         }
      },
      Eb = class {
         constructor(t) {
            m(this, "subscriptions");
            m(this, "seenEvents", new Map());
            m(this, "debug");
            m(this, "filterMatchingTime", 0);
            m(this, "filterMatchingCount", 0);
            (this.subscriptions = new Map()),
               (this.debug = t.extend("sub-manager"));
         }
         add(t) {
            this.subscriptions.set(t.internalId, t),
               t.onStopped &&
                  console.log(
                     "SUB-MANAGER BUG: Subscription already had onStopped! \u{1F914}",
                     t.internalId,
                  ),
               (t.onStopped = () => {
                  this.subscriptions.delete(t.internalId);
               }),
               t.on("close", () => {
                  this.subscriptions.delete(t.internalId);
               });
         }
         seenEvent(t, e) {
            let n = this.seenEvents.get(t) || [];
            n.push(e), this.seenEvents.set(t, n);
         }
         dispatchEvent(t, e, n = !1) {
            e && this.seenEvent(t.id, e);
            let r = this.subscriptions.values(),
               s = [],
               i = Date.now();
            for (let o of r) tc(o.filters, t) && s.push(o);
            (this.filterMatchingTime += Date.now() - i),
               (this.filterMatchingCount += s.length);
            for (let o of s) o.eventReceived(t, void 0, !1, n);
         }
      },
      Bl = (0, Cl.default)("ndk:active-user");
   async function kb(t) {
      if (!this.autoConnectUserRelays) return;
      let e = await gb(t.pubkey, this);
      if (e) {
         for (let n of e.relays) {
            let r = this.pool.relays.get(n);
            r ||
               ((r = new Nt(n, this.relayAuthDefaultPolicy, this)),
               this.pool.addRelay(r));
         }
         return e;
      }
   }
   async function Ab(t) {
      let e = this.outboxPool || this.pool;
      e.connectedRelays.length > 0
         ? il.call(this, t)
         : e.once("connect", () => {
              il.call(this, t);
           });
   }
   async function il(t) {
      let e = await kb.call(this, t),
         n = [{ kinds: [10006], authors: [t.pubkey] }];
      this.autoFetchUserMutelist && n[0].kinds.push(1e4);
      let r = e ? e.relaySet : void 0,
         s = this.subscribe(
            n,
            { subId: "active-user-settings", closeOnEose: !0 },
            r,
            !1,
         ),
         i = new Map();
      s.on("event", (o) => {
         let a = i.get(o.kind);
         (a && a.created_at >= o.created_at) || i.set(o.kind, o);
      }),
         s.on("eose", () => {
            for (let o of i.values()) Tb.call(this, o);
         }),
         s.start();
   }
   async function Tb(t) {
      t.kind === 10006 ? _b.call(this, t) : t.kind === 1e4 && Sb.call(this, t);
   }
   function _b(t) {
      let e = xl.from(t);
      for (let n of e.items) this.pool.blacklistRelayUrls.add(n[0]);
      Bl("Added %d relays to relay blacklist", e.items.length);
   }
   function Sb(t) {
      let e = xl.from(t);
      for (let n of e.items) this.mutedIds.set(n[1], n[0]);
      Bl("Added %d users to mute list", e.items.length);
   }
   var Rb = ["wss://purplepag.es/", "wss://nos.lol/"],
      ol = ["wss://brb.io/", "wss://nostr.mutinywallet.com/"],
      Il = class extends Tl.EventEmitter {
         constructor(e = {}) {
            super();
            m(this, "_explicitRelayUrls");
            m(this, "pool");
            m(this, "outboxPool");
            m(this, "_signer");
            m(this, "_activeUser");
            m(this, "cacheAdapter");
            m(this, "debug");
            m(this, "devWriteRelaySet");
            m(this, "outboxTracker");
            m(this, "mutedIds");
            m(this, "clientName");
            m(this, "clientNip89");
            m(this, "queuesZapConfig");
            m(this, "queuesNip05");
            m(this, "asyncSigVerification", !1);
            m(this, "initialValidationRatio", 1);
            m(this, "lowestValidationRatio", 1);
            m(this, "validationRatioFn");
            m(this, "subManager");
            m(this, "publishingFailureHandled", !1);
            m(this, "relayAuthDefaultPolicy");
            m(this, "httpFetch");
            m(this, "netDebug");
            m(this, "autoConnectUserRelays", !0);
            m(this, "autoFetchUserMutelist", !0);
            m(this, "walletConfig");
            m(this, "fetchEventFromTag", wb.bind(this));
            (this.debug = e.debug || (0, Al.default)("ndk")),
               (this.netDebug = e.netDebug),
               (this._explicitRelayUrls = e.explicitRelayUrls || []),
               (this.subManager = new Eb(this.debug)),
               (this.pool = new Zu(
                  e.explicitRelayUrls || [],
                  e.blacklistRelayUrls || ol,
                  this,
               )),
               (this.pool.name = "main"),
               this.pool.on("relay:auth", async (n, r) => {
                  this.relayAuthDefaultPolicy &&
                     (await this.relayAuthDefaultPolicy(n, r));
               }),
               (this.autoConnectUserRelays = e.autoConnectUserRelays ?? !0),
               (this.autoFetchUserMutelist = e.autoFetchUserMutelist ?? !0),
               (this.clientName = e.clientName),
               (this.clientNip89 = e.clientNip89),
               (this.relayAuthDefaultPolicy = e.relayAuthDefaultPolicy),
               e.enableOutboxModel &&
                  ((this.outboxPool = new Zu(
                     e.outboxRelayUrls || Rb,
                     e.blacklistRelayUrls || ol,
                     this,
                     this.debug.extend("outbox-pool"),
                  )),
                  (this.outboxPool.name = "outbox"),
                  (this.outboxTracker = new yb(this))),
               (this.signer = e.signer),
               (this.cacheAdapter = e.cacheAdapter),
               (this.mutedIds = e.mutedIds || new Map()),
               e.devWriteRelayUrls &&
                  (this.devWriteRelaySet = lt.fromRelayUrls(
                     e.devWriteRelayUrls,
                     this,
                  )),
               (this.queuesZapConfig = new sl("zaps", 3)),
               (this.queuesNip05 = new sl("nip05", 10)),
               (this.signatureVerificationWorker =
                  e.signatureVerificationWorker),
               (this.initialValidationRatio = e.initialValidationRatio || 1),
               (this.lowestValidationRatio = e.lowestValidationRatio || 1);
            try {
               this.httpFetch = fetch;
            } catch {}
         }
         set explicitRelayUrls(e) {
            (this._explicitRelayUrls = e), (this.pool.relayUrls = e);
         }
         get explicitRelayUrls() {
            return this._explicitRelayUrls || [];
         }
         set signatureVerificationWorker(e) {
            (this.asyncSigVerification = !!e), e && Iy(e);
         }
         addExplicitRelay(e, n, r = !0) {
            let s;
            return (
               typeof e == "string" ? (s = new Nt(e, n, this)) : (s = e),
               this.pool.addRelay(s, r),
               this.explicitRelayUrls.push(s.url),
               s
            );
         }
         toJSON() {
            return { relayCount: this.pool.relays.size }.toString();
         }
         get activeUser() {
            return this._activeUser;
         }
         set activeUser(e) {
            let n = this._activeUser?.pubkey !== e?.pubkey;
            (this._activeUser = e),
               e && n ? Ab.call(this, e) : e || (this.mutedIds = new Map());
         }
         get signer() {
            return this._signer;
         }
         set signer(e) {
            (this._signer = e),
               e && this.emit("signer:ready", e),
               e?.user().then((n) => {
                  (n.ndk = this), (this.activeUser = n);
               });
         }
         async connect(e) {
            this._signer &&
               this.autoConnectUserRelays &&
               (this.debug(
                  "Attempting to connect to user relays specified by signer %o",
                  await this._signer.relays?.(this),
               ),
               this._signer.relays &&
                  (await this._signer.relays(this)).forEach((s) =>
                     this.pool.addRelay(s),
                  ));
            let n = [this.pool.connect(e)];
            return (
               this.outboxPool && n.push(this.outboxPool.connect(e)),
               this.debug("Connecting to relays %o", { timeoutMs: e }),
               Promise.allSettled(n).then(() => {})
            );
         }
         getUser(e) {
            let n = new ht(e);
            return (n.ndk = this), n;
         }
         async getUserFromNip05(e, n = !1) {
            return ht.fromNip05(e, this, n);
         }
         subscribe(e, n, r, s = !0) {
            let i = new eb(this, e, n, r);
            this.subManager.add(i);
            let o = n?.pool ?? this.pool;
            if (r)
               for (let a of r.relays)
                  o.useTemporaryRelay(a, void 0, i.filters);
            if (this.outboxPool && i.hasAuthorsFilter()) {
               let a = i.filters
                  .filter((c) => c.authors && c.authors?.length > 0)
                  .map((c) => c.authors)
                  .flat();
               this.outboxTracker?.trackUsers(a);
            }
            return s && setTimeout(() => i.start(), 0), i;
         }
         async publish(e, n, r) {
            return (
               this.debug("Deprecated: Use `event.publish()` instead"),
               e.publish(n, r)
            );
         }
         async fetchEvent(e, n, r) {
            let s, i;
            if (
               (r instanceof Nt
                  ? (i = new lt(new Set([r]), this))
                  : r instanceof lt && (i = r),
               !r && typeof e == "string" && !Jy(e))
            ) {
               let o = Xy(e, this);
               o.length > 0 &&
                  ((i = new lt(new Set(o), this)), (i = mb(i, this.pool)));
            }
            if (
               (typeof e == "string"
                  ? (s = [Yy(e)])
                  : Array.isArray(e)
                    ? (s = e)
                    : (s = [e]),
               s.length === 0)
            )
               throw new Error(`Invalid filter: ${JSON.stringify(e)}`);
            return new Promise((o) => {
               let a = null,
                  c = this.subscribe(
                     s,
                     { ...(n || {}), closeOnEose: !0 },
                     i,
                     !1,
                  ),
                  l = setTimeout(() => {
                     c.stop(), o(a);
                  }, 1e4);
               c.on("event", (u) => {
                  (u.ndk = this),
                     u.isReplaceable()
                        ? (!a || a.created_at < u.created_at) && (a = u)
                        : (clearTimeout(l), o(u));
               }),
                  c.on("eose", () => {
                     clearTimeout(l), o(a);
                  }),
                  c.start();
            });
         }
         async fetchEvents(e, n, r) {
            return new Promise((s) => {
               let i = new Map(),
                  o = this.subscribe(
                     e,
                     { ...(n || {}), closeOnEose: !0 },
                     r,
                     !1,
                  ),
                  a = (c) => {
                     c instanceof he || (c = new he(void 0, c));
                     let l = c.deduplicationKey(),
                        u = i.get(l);
                     u && (c = pb(u, c)), (c.ndk = this), i.set(l, c);
                  };
               o.on("event", a),
                  o.on("eose", () => {
                     s(new Set(i.values()));
                  }),
                  o.start();
            });
         }
         assertSigner() {
            if (!this.signer)
               throw (
                  (this.emit("signer:required"), new Error("Signer required"))
               );
         }
         getNip96(e) {
            return new xb(e, this);
         }
         set wallet(e) {
            if (
               (console.log("setting wallet", {
                  lnPay: e?.lnPay,
                  cashuPay: e?.cashuPay,
               }),
               !e)
            ) {
               this.walletConfig = void 0;
               return;
            }
            this.walletConfig ?? (this.walletConfig = {}),
               (this.walletConfig.lnPay = e?.lnPay?.bind(e)),
               (this.walletConfig.cashuPay = e?.cashuPay?.bind(e));
         }
      };
   var e1 = (0, Ll.default)("ndk:zapper");
   self.WebSocket || (self.WebSocket = WebSocket);
   self.NDK = Il;
})();
/*! Bundled license information:

@scure/base/lib/index.js:
  (*! scure-base - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/hashes/esm/utils.js:
  (*! noble-hashes - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/abstract/utils.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/abstract/modular.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/abstract/curve.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/abstract/weierstrass.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/_shortw_utils.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/secp256k1.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/hashes/esm/utils.js:
  (*! noble-hashes - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@scure/base/lib/esm/index.js:
  (*! scure-base - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/ciphers/esm/utils.js:
  (*! noble-ciphers - MIT License (c) 2023 Paul Miller (paulmillr.com) *)

@noble/hashes/esm/utils.js:
  (*! noble-hashes - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/hashes/esm/utils.js:
  (*! noble-hashes - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/abstract/utils.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/abstract/modular.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/abstract/curve.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/abstract/weierstrass.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/_shortw_utils.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@noble/curves/esm/secp256k1.js:
  (*! noble-curves - MIT License (c) 2022 Paul Miller (paulmillr.com) *)

@scure/base/lib/esm/index.js:
  (*! scure-base - MIT License (c) 2022 Paul Miller (paulmillr.com) *)
*/
